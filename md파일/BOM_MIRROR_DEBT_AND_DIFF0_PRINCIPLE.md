# ★BOM 레거시 미러 부채 & diff0 원칙 (핵심 설계 판단 — 필독)

> 작성: 2026-08-15 (세션 634b8f52). MJC 이중계상 사고에서 출발해 규명한 **구조적 진실**과 **클린 전환 원칙**.
> 관련 정본: [[BOM_STRUCTURE_CANON]] · [[unified_bom_schema]] · [[CS_CALC_EXCEPT_HANDOFF]] · [[MIGRATION_ISSUES]]
> 관련 메모: [[newerp-nxbomline-single-bom]] [[newerp-nx-bom-build]] [[newerp-cost-engine-csbom]] [[newerp-migration-issues-registry]]

---

## 0. 한 줄 결론

**우리 신규 ERP의 계산용 BOM(`nx.bom_line`)은 "클린 3축 모델"이 아니라 레거시 CS를 새 테이블에 재현한 "미러"다.** 형태(테이블·품번통합)는 바꿨지만 의미(다중 플래그·변형SUB·죽은행)는 그대로 복제했다. 그래서 **신규 ERP인데도 레거시의 병(이중계상 등)이 그대로 나온다.**
그리고 **diff0은 "결과(원가·계획 수치) 동일"이지 "방식·구조 동일"이 아니다.** → 방식을 바꿔 클린으로 가도 되며, 조건은 오직 **오라클로 결과 동등성 증명**이다.

---

## 1. 발단 — MJC 이중계상 사고

- 증상: 품목BOM관리 조회에서 `MJC62301702`가 **2번** 표시(제품 AJR77224002). 수정(편집) 화면에선 1개.
- 원인: `nx.bom_line`에서 MJC가 **직접자식(seq6)** 이면서 **대원 SUB(AJR77224002-12-1) 안(seq3)** 에도 있음.
  - 레거시 CS_M_ITEM_BOM: 직접행 `CS_CALC_EXCEPT_FLAG=1`(계산제외) + 대원 SUB=0(계산) → **한 번만** 계산.
  - nx: 직접행 `cs_calc_except=0`(미제외) → **직접+SUB 둘 다 계산 = 이중계상**(재료비 +14,304).
- 근본 도구: `_migration/sub_norm/r_add_missing_edges.py` (2026-08-13 실행). **add-only 설계**(누락엣지만 추가, 기존행 수정 안 함). 대원 SUB 엣지는 추가했지만 **대체된 기존 직접행의 `cs_calc_except`를 재동기화하지 않아** 갭 발생.
- 조치: 직접 3행(MJU66570403·MJU66570404·MJC62301702, bom_id=4590 seq4/5/6) `cs_calc_except=1`로 수정. remarks에 `[csexc-fix 2026-08-15 …]` 태그. 백업: 세션 스크래치패드 `nx_bomline_csexc_fix_backup.json`.
- 왜 2→1로 사라졌나: 원가엔진(`_harness/nx_cost_engine.py` `_value_node_nae`/`_expandable_nae`)이 전개 시 `if not cs_calc_except` → **제외행은 노드 생성조차 안 함.** 삭제가 아니라 전개 필터에서 빠진 것.

---

## 2. 구조적 진실 — "1 BOM 테이블 + 다중 독립 플래그"

한 개의 `nx.bom_line` 행이 **프로그램별로 다른 의미**를 갖는다(레거시 CS 그대로 복제):

```
cs_calc_except (원가용 제외)  ·  except_flag (소요/PR용 제외)
set_except (세트용)  ·  kitting (키팅용)  ·  sagub_default (사급용)
```

**실측 — 원가제외(cs_calc_except=1) 414행이 다른 축에선 살아있음:**

| 원가에선 제외지만… | 아직 쓰는 곳 |
|---|---|
| 소요(except_flag=0)엔 포함 | **312행** |
| 키팅 대상(kitting=1) | 189행 |
| 세트 포함(set_except=0) | 402행 |
| 사급(sagub_default=1) | 3행 |

➡️ **한 행이 "원가엔 빠지지만 소요/키팅/세트엔 쓰임" → 함부로 삭제 불가**(그 프로그램들이 깨짐). 레거시가 행을 안 지우고 플래그만 쌓은 이유 = 이 다중 용도. **이 오버로딩이 복잡함·지뢰의 근원.**

---

## 3. 스코프 (실측, 동기화 날짜 260630 기준)

| 항목 | 수치 | 의미 |
|---|---|---|
| nx vs 레거시 플래그 불일치 | **17행** / 전체 36,901페어 | 99.95% 정합 |
| ├ 이중계상(nx0/lg1) | 3행 (AJR77224002) | **원가버그 → 수정완료** |
| ├ 과제외·원가무해 | 13행 | ~0 영향 |
| └ 과제외·과소 | 1행 (AJR74482401-1 −605) | 검토 대기(의도적 guard 가능성) |
| MJC형 "직접+SUB 양쪽 활성" 중복 | 301건 / 195제품 | **대부분 정상 공통부품 재사용**(볼트 3H01582C 등), 버그 아님 |
| 계산제외(죽은) 행 총량 | 414행 / 176 BOM | 클린업 후보(단 다축 확인 필수) |

**중요: "301 중복"은 오해 금지.** 레거시와 대조 시 진짜 버그는 3행뿐. 나머지는 실제로 두 곳에서 쓰는 정상 부품.

---

## 4. ★핵심 원칙 — diff0 = 결과 동일 ≠ 방식 동일

- 원가 오라클(`_harness/cost_oracle.py`, `SP_CS_견적서`)·계획 verify가 비교하는 건 **최종 숫자**(재료비 jae, 실원가 silwon, 계획 수량). **내부 BOM 구조·플래그는 안 본다.**
- 증명: MJC는 **삭제하든 플래그하든 원가 동일**. 방식이 달라도 결과 같으면 diff0.
- **오늘(현재일) 원가가 커 보이는 건 버그 아님** — nx 단가/LME 스냅샷이 당일치로 아직 안 올라온 **드리프트**. 검증은 **동기화된 과거 날짜(예 260630)** 로 해야 함. (검증 예: AJR77224002 260630 diff=0.14, 앵커 AJR75563503 정확일치.)

➡️ **함의: 레거시 플래그 구조를 흉내 낼 필요가 없었다.** 깨끗한 BOM으로 만들되 "결과가 같은지"만 오라클로 증명하면 diff0을 만족한다.

### 왜 그동안 미러로 만들었나
같은 방식 = 같은 결과가 **자동 보장**이라 빠르고 안전했기 때문(검증 부담 회피). 다른 방식은 **전 품목 결과 동등성 증명**이 필요. → 그 부채를 이제 갚으면 됨. (과거 "CS 강제정합→원가훼손→롤백" 이력도 같은 맥락.)

---

## 5. 클린 전환 원칙 (제자리 갈아엎기 금지)

**방식 자유 + 결과 고정**이므로:
1. **깨끗한 BOM을 "따로" 만든다** — 변형SUB→라우팅/조달, 죽은행 제거, 플래그 없음(= `unified_bom_schema.sql` 목표). 현재 미러는 **그대로 두고 병렬로.**
2. **오라클을 전 품목에 돌려 "클린 == 레거시 결과"를 증명.**
3. **전 품목 diff0 확인 후** 원가·계획 엔진을 클린 BOM 읽도록 **전환.** 라이브는 증명 끝날 때까지 안 건드림.
4. **끊는 시점 결정** = "이제 우리 엔진/ BOM이 정본이다"(diff0 게이트 해제)를 언제 선언할지. 그 전엔 diff0 유지.

### 클린 모델에서 다축 문제 해소
원가/소요/키팅/세트를 **축별로 분리**하면 한 행이 상충 플래그를 안 지므로, "안 쓰는 행 = 삭제"가 **깨끗하게 성립**(지금 미러에선 위험한 삭제가 클린에선 안전).

---

## 6. 운영 상태 & 지금 할 일

- 상황: 데이터 마이그레이션 사실상 완료(어제치 제외), **직원들이 레거시+신규 병행 사용 중.**
- 주말 권장 = **재설계가 아니라 검증·정리·감시**:
  1. **전수 정합 대조**(nx 전체 vs 레거시, 동기화 날짜) → 누락/초과엣지·플래그드리프트·원가차 완전 펀치리스트.
  2. **펀치리스트 수정**(MJC 완료, AJR74482401-1, 13행, 신규 발견분).
  3. **상시 드리프트 감시** — sync 직후 자동 대조(제2의 MJC를 사람 눈 아닌 자동으로).
  4. **어제 데이터 적재 + 재검증.**
- 병행운영 중엔 **diff0 = 안전 게이트 유지**(직원이 두 시스템에서 같은 숫자 보게).

---

## 7. 대기 중 결정 (사용자)

1. MJC를 **플래그 유지 vs 삭제 전환** 중 무엇으로.
2. **전 품목 결과-동등성 증명**(클린 vs 레거시 오라클) 실행 시점 — "방식 바꿔도 됨" 실측 확정.
3. 남은 불일치 **14행** 정리(특히 AJR74482401-1 −605, `r_add_missing_edges.py`의 `p.startswith(ch+'-')` guard와 연관).
4. **diff0 게이트 해제 시점**(클린 정본 승격) 결정.

---

## 8. 코드/근거 레퍼런스

- 미러 매핑: `_schema/MIGRATION_ISSUES.md` §A (bom_line←CS_M_ITEM_BOM, 플래그 1:1 복제)
- 근본 도구: `_migration/sub_norm/r_add_missing_edges.py` (add-only, 플래그 재동기화 없음)
- 엔진 제외 로직: `_harness/nx_cost_engine.py` `_expandable_nae`/`_value_node_nae` (`if not cs_calc_except`)
- 결과 오라클: `_harness/cost_oracle.py` (`SP_CS_견적서` 실원가/내부용, 결과 비교기)
- 저장 왕복 계약: `_schema/CS_CALC_EXCEPT_HANDOFF.md` (플래그 통과·전체교체 주의)
- 클린 목표: `_schema/unified_bom_schema.sql` (BOM=구성만·단가마스터 분리·조달프로파일)
