# PNC next-gen web ERP - backend dependencies
#
# NOTE: comments must stay ASCII.
#   pip reads this file with the system codepage (CP949 on Korean Windows),
#   so Korean comments raise UnicodeDecodeError and the whole install fails.
#   (hit on production 184, 2026-08-20)
#
# Install:  python -m pip install -r requirements.txt
# Targets:  production server 184 (port 8010), each dev PC running the backend
#
# DB credentials (db_client.py) live outside this repo, in the sibling
# folder ../New_ERP/ - see CLAUDE.md section 8.

fastapi==0.133.1
uvicorn==0.41.0
pyodbc==5.3.0          # MSSQL (read PARTNER_ERP / write nx)
openpyxl==3.1.5        # excel upload/download
Pillow==12.2.0         # Code128 barcode image (rendered directly, no extra barcode pkg)
qrcode==8.2            # product sticker QR
# 490 silent auto-print: kanban/label PDF generation (routers/printjob.py).
# Without it /api/print/* returns 501 and the screen falls back to the browser
# print dialog (hit on a dev backend on port 8011, 2026-09-04).
PyMuPDF>=1.24
