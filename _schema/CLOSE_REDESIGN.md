# 마감(Close) 재설계 — 잠정 일마감 / 확정 월마감(익월 10일) + 열린 조정 창

> 2026-09-07 대표님 요구 반영 초안. **일마감 순차·잠금 구조**가 "조정이 익월 10일까지 계속 들어오는 현실"과 충돌하는 문제를 해소.
> ★이 문서는 **제안(초안)** 이다. 확정 전 대표 검토·수정 게이트.

---

## 1. 배경 — 실제 업무 타이밍 (대표 확인)

- **전월(M) 마감은 익월(M+1) 10일에 확정**한다. 예: 8월 마감 = 9/10.
- M+1 **1~9일**에 업체들과 **① 매입단가 협상**(→ 재고 원가) + **② 이월 정리**(→ 정산 귀속)를 진행한다.
- **반품 등 물리 실물**도 뒤늦게(일마감 다 된 뒤) 들어온다.
- **이 조정들은 전부 "M월(8월)"에 귀속·반영**되어야 한다(9월 아님).
- 즉 **M월은 M+1월 10일까지 "열려 있어야" 한다.**

## 2. 문제 — 현행 구조가 이 현실과 안 맞는 이유

현행 `close.py` 일마감:
- **순차 체인 강제**: 앞날이 마감돼야 다음날 마감 가능(`_assert` 연쇄 가드).
- **각 일마감 = 스냅샷 확정 + 잠금**(`period_close.close_flag=1`).
- ⟹ **31일까지 가려면 주말 포함 모든 날이 잠긴다.**

그 결과:
1. **조정을 등록할 "열린 날"이 없다** — 가격 수정·반품·이월을 넣을 날짜가 전부 잠겨 있다.
2. 늦게 온 조정을 반영하려면 **그 날부터 체인을 다 되풀어(reopen) 재마감**해야 한다 = churn.
3. **이동평균 특성상 단가 변동은 "그 입고일 이전부터 재생"해야 반영**되는데, 일마감은 직전 스냅샷을 기초로 삼아 옛 입고의 단가 변동을 못 잡는다(직전 스냅샷에 옛 평균이 굳음).

## 3. 새 구조 — 핵심 원칙

| | 현행 | **재설계** |
|---|---|---|
| **일마감** | 스냅샷 확정 + 잠금 + 순차강제 | **잠정 스냅샷**(참고용). **잠금·순차강제 없음**, 언제든 재계산(refresh) |
| **월마감(10일)** | 그냥 그달 스냅샷 잠금 | **유일한 확정·잠금**. 전월말부터 **그 달 전체를 최종 단가·이월·반품으로 재생** |
| **조정 등록** | 잠긴 날이라 불가 | **아무 때나** 전표/override에 등록(막는 잠금 없음) |
| **귀속** | 날짜 잠금 | 전표 날짜 + 이월 override 로 **M월 귀속** 결정 |

**한 줄 요약**: 일마감은 "잠정 미리보기", **월마감(10일)이 진짜 마감**. 그 사이는 계속 열려 있어 조정이 자유롭게 들어오고, 월마감이 전부 흡수해 확정한다.

## 4. 조정 유형별 처리

| 조정 | 등록 위치 | 재고(이동평균) | 정산 |
|---|---|---|---|
| **① 매입단가 협상** | 전표 `PU_T_STOCK_MAINT`(매입마감이 갱신) | **월마감 전체재생이 흡수** → 최종 원가 확정 | 매입 정산 |
| **② 이월 정리** | `nx.magam_carry_ovr`(override) | 재고 원가 무변 · **이월재고/금액(원가) 별도 표시** | 정산 귀속 이동 |
| **③ 반품 등 물리** | 전표(실일자, tag R 등) | qty 변동 → **월마감 흡수**(또는 해당일 잠정 refresh) | 매출/매입 반영 |

- **①의 반영 원리**: 월마감이 **M-1 월마감을 기초로 M월 전체를 재생**하면, M/5 입고든 M/20 입고든 협상으로 바뀐 최종 단가를 **다시 읽어** 이동평균에 반영한다. (직전 일마감 시드로는 불가 — 그래서 전체재생이 필수.)
- **②의 반영**: 이월은 재고 원가를 바꾸지 않는다. 물리적으로 나간 사급 불출이 정산에선 익월로 넘어가므로, **"이월재고/금액"(원가=이월수량×이동평균단가)** 을 수불장·일일현황에 **별도 컬럼으로 가산 표시**(마감장 기준, override 포함, 날짜 무관).

## 5. 일마감의 새 역할 (잠정)

- **목적**: 월중 재고를 잠정으로 보여주기(일일 영업/매입 현황·수불장 조회용).
- **잠금 제거**: `period_close` 잠금을 걸지 않는다(또는 '잠정' 상태로만 기록). 조정 등록을 막지 않는다.
- **순차강제 완화**: 특정일 스냅샷이 필요하면 **그 날짜만 독립적으로 계산**(기초=직전 확정 월마감 + 그 달 1일~그날 재생). 앞날 전부를 미리 마감할 필요 없음.
- **재계산 자유**: 조정이 들어오면 관련 일자 스냅샷을 **언제든 refresh**(멱등). 잠정이라 부담 없음.
- ★대안: 일 스냅샷을 아예 굳히지 않고 **on-demand 계산**(조회 시점에 그날까지 재생)으로 둘 수도 있다 — 저장 안 하니 stale 없음(단 조회가 무거움, 캐시로 완화). → **§9 열린 결정 1**.

## 6. 월마감(익월 10일)의 역할 (확정)

1. **기초 = 전월(M-1) 월마감 확정치**(잠정 일마감 아님).
2. **M월 전체(1일~말일)를 최종 데이터로 재생**: 매입단가 협상분(전표) + 이월(override) + 반품(전표)까지 전부 반영.
3. **스냅샷 확정 + 잠금**(이 시점이 유일한 잠금).
4. 이후 M월은 닫힘. 추가 조정이 있으면 **명시적 재개(reopen)** 로만(감사 로그).

★**안전(diff0)**: 조정이 없으면 이동평균은 결정적이라 "전월말부터 전체재생" = "마지막 일마감부터 재생" **동일 결과**. 무수정 달로 검증.

## 7. 조회 화면 반영

- **월중~10일 확정 전**: 잠정 일마감(또는 on-demand) 값으로 표시하되 **"잠정(미확정)" 표기**.
- **10일 확정 후**: 월마감 확정치.
- **일일 영업/매입 현황**: 재고(기초/기말)에 **이월재고/금액**을 별도 표시·가산(§4-②).
- **폴백 오해 제거**(§1-9-1): 확정 스냅샷이 없으면 조용히 옛값 폴백하지 말고 **잠정/기준일 표기**.

## 8. 구현 단계 (제안)

1. ~~**월마감 전체재생**~~ — ✅**완료**(2026-09-07). `_mv_base(monthly=True)`. §10-1 검증.
2. ~~**일마감 잠금 해제**~~ — ✅**완료**(2026-09-08). §10-2 검증.
3. **이월재고/금액(원가)** — 마감장(override) 기준 이월분을 이동평균단가로 평가해 수불장·일일현황에 별도 컬럼.
4. **조정 등록 UX** — 잠긴 날 없이 전표/이월/반품을 아무 때나 등록(막는 게이트 제거).
5. **잠정/확정 표기** — 조회 화면에 상태 표기.

## 9. 열린 결정 (대표 확인 필요)

1. ~~일마감을 (a) 잠정 스냅샷 refresh 로 남길지, (b) on-demand 계산으로 갈지~~
   → ✅**확정 (a) 잠정 스냅샷** (대표 2026-09-08). **일·월마감 구조는 현행 유지.**

   **실측 근거** (자재 전 스코프 25,176품목, 조회 1회 비용):

   | 방식 | 기초 | 재생 구간 | 비용 |
   |---|---|---|---:|
   | (a) 잠정 스냅샷 | 260831 일마감 | 9/1~9/8 (8일·4,942건) | **0.83s** |
   | (b) on-demand | 2607 월마감 | 8/1~9/8 (39일·20,655건) | **1.84s** |

   재생 구간별 전표 읽기: 1일 0.62s · 8일 0.77s · 39일 1.34s.
   ⟹ 비용의 대부분은 구간 길이가 아니라 **고정 오버헤드 0.6s**(6갈래 전표 쿼리)이고,
   on-demand 추가분은 **조회당 약 +0.7s**. 즉 '너무 느려서 불가'는 아니다(과장하지 않는다).

   **그럼에도 (a)인 이유 — 속도보다 구조:**
   - **월마감(확정)은 어차피 스냅샷이 필요**하다. (b)로 가도 스냅샷 인프라는 남아 **두 방식 병존**이 된다.
   - 조회는 화면마다 반복된다. +0.7s 가 매번 누적되고 캐시가 비면 그대로 문다(캐시 hit 0.15s).
   - 계산식은 둘이 같다(확정 스냅샷 + 이후 전표). 차이는 **기초를 어디서 끊느냐**뿐 = (a)가 (b)의 상위집합.

   **★(a)의 전제조건 = stale 방지 → ✅구현 완료(2026-09-08, §14)**
   잠정 스냅샷이 있는데 **그 일자 이전 전표가 나중에 들어오면 스냅샷이 낡는다.** 2단계에서
   순차강제를 걷어냈으므로 더 쉽게 발생한다. `STOCK_GATING_CLOSE_LOCK_RULES.md` 순차마감 절이
   이미 지목한 항목 — *"그 구간에 전표가 들어오면 이후 스냅샷을 '재마감 필요'로 표시"*.
   **훅은 이미 있다**: 재고 쓰기 33곳이 전부 `common.stock_changed()` 를 부른다 → 거기서
   그 일자 이후의 잠정 일마감을 **'갱신필요' 표시 또는 자동 무효화**하면 닫힌다.
2. **반품이 정산 대상인지**(익월 이월 규칙 적용) **아니면 재고만 조정인지.**
   → 대표 지시(2026-09-08): **"반품을 하면 수불장에 반영되어 재고 수량과 금액이 달라져야 한다"**
     = 재고 반영은 **필수**로 확정. '정산 귀속(익월 이월)'까지 갈지는 아직 열려 있음.
     ★그런데 **지금 코드는 반대로 움직인다** — §11 참조.
3. ~~월마감 잠금 후 재개(reopen) 권한·로그 수준~~ → ✅**확정**: 월(확정) 해제 = 수퍼관리자(시스템관리자)만,
   일(잠정) 해제 = 일반 마감권한. `period_close.reopen_user/reopen_dt` 에 기록. (구현 `45713fc`·`1d69020`)
4. **일마감을 실제로 매일 돌릴지**, 아니면 필요할 때(조회 시)만 계산할지.

---

## 부록. 현행 코드 매핑 (변경 지점)

- `close.py` `_mv_base`(L660) — 월마감 기초를 전월 월마감으로(전체재생).
- `close.py` `close_run`(L1336) — 일마감 잠금·순차가드 완화(잠정 모드).
- `live_api.py` `dailypurissue`/`_snapsum`(L476) — 이월재고/금액 가산 + 잠정/기준일 표기.
- `common.py` `_carry_win_ovr`(L530) — 이월재고 소스(마감장 기준).
- 이월 평가단가 = 스냅샷 `avg_cost`(=stock_amt/stock_qty).

---

## 10. 진행·검증 로그

### 10-1. 1단계 — 월마감 전월말 전체재생 (2026-09-07, `29ff2f7`→리베이스 `e23c310`)
- `close.py _mv_base(cur, target, monthly=False)` 신설. `_snap_mat` 이 `ptype='M'` 일 때 `monthly=True` 전달.
- **검증**: `_schema/close_fullreplay_verify.py` — 2607 월마감 기초에서 260801~260831 을
  (A)하루씩 순차 vs (B)한 번에 전체재생 → **품목별 차이 0건 · 총액 4,112,880,949 완전일치(diff0)**.
  ⟹ 무수정 달에서는 두 방식이 같다 = 변경 안전. 차이는 '수정분'에서만 생긴다(그게 이 변경의 목적).

### 10-2. 2단계 — 일마감 잠금 해제(잠정화) (2026-09-08)

| 무엇 | 어디 | 내용 |
|---|---|---|
| **잠금 판정에서 일마감 제외** | `common.py _lock_msg` | `ptype='D'` 판정 삭제 → **월마감(+구 stock_close)만** 잠근다. 전 쓰기 화면 38곳이 이 함수 하나를 쓰므로 여기만 고치면 전부 반영(§0-★★ '판정은 한 곳') |
| **순차 강제 = 월마감만** | `close.py close_run` | 일마감 연쇄 가드 제거. 중간이 비어도 `_mv_base` 가 '직전 확정 스냅샷'을 찾아 이어 계산 |
| **일마감 재실행 = 갱신** | `close.py close_run` | 일마감은 `이미 마감` 409 대신 refresh(멱등). 월마감만 중복 확정 차단 |
| **해제 권한 분리** | `close.py close_cancel` | 월(확정) 해제 = `_assert_reopen`(수퍼관리자) · 일(잠정) 해제 = `_assert_can_close`(일반 마감권한). 후속기간 연쇄 가드도 월에만 |
| **잠정/확정 표기** | `close_run` 응답·note · `close_status` | `firm`(월=1/일=0)·`refresh` 필드, note 에 `[잠정]`·`·갱신` 기록 → §7 화면 표기의 데이터 준비 |

**★불변**: 규칙B(마감된 기간 수정 불가)는 **월마감 기준으로 그대로 살아 있다.** 일마감은 애초에 '확정'이
아니었어야 할 것을 잠그고 있었던 것이고, 이미 월마감된 과거 달은 여전히 막힌다.

**실측 (라이브 nx, 읽기전용)** — `_lock_msg` 판정:

```
MAT 확정행 — 일마감 31건(최신 260831·260830·260829) · 월마감 2건(2607·2606)
  260715  월마감된 달(2607)          → 잠금: 2607 마감된 월입니다 — 생성/수정/삭제 불가
  260831  일마감(그달 미월마감=잠정)  → ★열림(None)     ← 조정을 넣을 수 있는 날
  260830 / 260829 / 260828           → ★열림(None)
```

**TestBed** (`--port 8097`, 롤백모드·오염 0):

- **[R] 규칙 26건 PASS · FAIL 0** — 잠금 케이스가 이제 월마감(2607)으로 막는다(`마감월(2607) 편집 불가`).
- **[F] 흐름 13 PASS · FAIL 0** — 신규 역케이스 **「자재입고 — 잠정 일마감 일자는 열려 있다」** 가
  260831 에 원장·수불장·재고 **3곳 일치 (+7)** 로 기록됨을 확인.

**같은 커밋에서 고친 TestBed 결함 2건** (하드룰: 규칙을 바꾸면 [R] 케이스도 같이):

1. `closed` 픽스처가 `ORDER BY period DESC` 로 **최신 일마감(260904)** 을 집었다 → 이제 안 잠기므로
   잠금 케이스가 통째로 헛돈다. **`ptype='M'` 한정**으로 교정.
2. 「마감 해제 권한 게이트」 keyword 가 `권한이 없습니다` 였는데, `5c07288`(`_assert_reopen`) 이후
   실제 문구는 `수퍼관리자…` 다. **문구 불일치로 헛통과**하고 있었다 → keyword 교정.

**하네스 확장 1건**: `run_flow` 에 `scope_ymd` 추가. 프로브가 관측일자(기본=오늘)에 묶여 있어
**과거 일자에 쓰는 케이스가 '델타 +0' 거짓 FAIL** 이 났다(실제로 겪음). 케이스가 관측일자를 지정하고
끝나면 원복한다.

### 10-3. 남은 것
- ~~§8-3 이월재고/금액~~ ✅완료(§16) · §8-4 조정 등록 UX · §8-5 화면 잠정/확정 표기
- ~~§9-1 전제조건 = 잠정 스냅샷 stale 방지~~ → ✅**완료**(§14)
- §9 잔여 결정 = 2번(반품이 정산 대상인지) · 4번(일마감을 매일 돌릴지)
- ⚠️**전제 확인 필요**: 9/5 백필(`04a7deb`)이 넣었던 **2608 월마감·9월 일마감이 지금 nx 에 없다**
  (실측: 월마감 2607·2606, 일마감 최신 260831). 9/7 컷오버 롤백(DB 원복)으로 사라진 것으로 보인다.
  → 재컷오버 시 백필 재실행 필요 여부 확인.

---

## 11. ~~결함~~ ✅**해결** — 자재반품이 재고를 반대로 움직이던 문제 (2026-09-08)

대표 지시 *"반품을 하면 수불장에 반영되어 재고 수량과 금액이 달라져야 한다"* 를 확인하다 발견.

### 증상 (샌드박스 실측·롤백)
```
품목 1NHA0801206 · 260907 · 자재반품 25개 등록
  before net = +2,420.0
  after  net = +2,445.0        ← 25 줄어야 하는데 25 늘었다
```

### 원인 사슬
1. 자재반품 화면은 `MAINT_TAG='RT'` 로 쓴다(`stock.py` `screen='return'`, `sign=-1`).
2. 그런데 `nx.PU_T_STOCK_MAINT.MAINT_TAG` 는 **varchar(1)** — `'RT'` 는 저장 자체가 안 된다
   (잘림오류. `STOCK_FLOW_INOUT_VERIFY.md` **F2** 가 지적한 그 문제).
3. F2 대응으로 `stock.py:521` 이 **`'RT' → 'T'`** 로 매핑해 넣는다.
4. 레거시 `'T'` = **생산창고 반납**(자재창고로 되돌아옴 = 입고 성격) 이라,
   `close._mv_moves` 의 T 쿼리가 **`SUM(-MAINT_QTY)`** 로 부호를 반전시킨다.
5. ⟹ 반품행(−25 저장)이 **+25** 가 되어 재고가 증가한다.

**즉 F2 수정이 '수불장에 안 잡힘'을 '수불장에 반대로 잡힘'으로 바꿔 놓았다.**
(태그 전수 점검 결과 그 외 미분류 태그는 0종 — 이 건이 유일한 갭이다.)

### 지금까지의 피해 = 없음
- `nx.PU_T_STOCK_MAINT` 의 `'RT'` 전표 **0건**, 반품 화면 실사용 전.
- 8월 마감 재계산 총액 **4,112,880,949 로 불변**(diff0) — 기존 확정값은 안 건드렸다.

### 고친 방법 — (b)안 채택 (대표 확정)

| 안 | 내용 | 장점 | 걸리는 점 |
|---|---|---|---|
| **(a) 스키마 확장** | `MAINT_TAG` → `varchar(4)`, `'RT'` 그대로 저장 + `TA_OUT_TAGS` 에 `'RT'` 등록 | 의미가 정직(반품은 반품 태그). 화면 표시도 −25 로 맞음 | 미러 테이블이라 `r_delta_sync` 의 `do_full` 이 **스키마 드리프트 감지 시 DROP+SELECT INTO 폴백** → varchar(1) 로 되돌아갈 수 있다 |
| **(b) 1글자 출고태그 신설** | 미사용 1글자를 '자재반품'으로 정의 + `TA_OUT_TAGS` 등록 | 스키마 무변경 = 미러 재복제에 안전 | 레거시 태그 공간을 우리가 점유 — 나중에 레거시가 같은 글자를 쓰면 충돌 |

※ '`'T'` 에 넣되 양수로 저장' 은 엔진은 맞지만 **자재입출고현황 화면에 +25 로 보인다** → 채택 불가.

어느 쪽이든 함께 해야 할 것: `TA_OUT_TAGS` 등록 · TestBed 반품 케이스에 **부호 검증** 추가
(현재 `[F] 자재반품` 케이스는 전표 3곳 기록만 보고 **마감엔진 부호는 안 본다** — 그래서 못 잡았다).

---

## 12. 수불장 전수 재검증 — 3부서 × 입고/출고/반품 (2026-09-08)

대표 지시: *"자재, 생산, 영업 관련 수불장 전체, 3부서의 입고, 출고, 반품 전체를 재검증"*.

**하네스** = `_schema/ledger_signs_verify.py` (주입→측정→롤백, 오염 0).
읽기만으로 판정하지 않는다 — 실제 쓰기 경로와 **같은 테이블·태그·부호**로 전표를 주입하고
수불장 엔진(`_mat_ledger`/`_prd_ledger`/`_sal_ledger`)의 기말 델타를 잰다.

### 결과 — 13건 중 2건 문제

| 도메인 | 유형 | 검증 | 기말Δ | 기대 | 판정 |
|---|---|---|---:|---|---|
| MAT | 입고 | 자재입고관리 tag 9 (+) | +100 | 증가 | PASS |
| MAT | 출고 | 자재출고관리 tag B (−) | −100 | 감소 | PASS |
| MAT | **반품** | 자재반품 RT→'T' (−) | **+100** | 감소 | **★FAIL 부호반대** |
| MAT | 조정 | 자재재고조정 tag 2 (+) | +100 | 증가 | PASS |
| PRD | 입고 | 자재→생산 이동 B/gubun1 (−) | +100 | 증가 | PASS |
| PRD | 출고 | 생산사용 tag 4 (−) | −100 | 감소 | PASS |
| PRD | 반품 | 생산창고 반납 T/gubun3 (−) | −100 | 감소 | PASS |
| PRD | 조정 | 생산재고조정 tag 2 (+) | +100 | 증가 | PASS |
| SAL | 입고 | 생산입고 tag P (+) | +100 | 증가 | PASS |
| SAL | 출고 | 출하등록 tag J (−) | −100 | 감소 | PASS |
| SAL | 반품 | 출하반품 tag R (+) | +100 | 증가 | PASS |
| SAL | 조정 | 제품재고조정 tag 2 (+) | +100 | 증가 | PASS |
| SAL | **조정** | 제품재고조정 **웹 `nx.prod_stock_adjust`** | **+0** | 증가 | **★FAIL 미반영** |

### 확인된 문제 2건

**① MAT 자재반품이 재고를 반대로 움직인다** (§11 상세)
`'RT'`(varchar1 잘림) → `'T'` 매핑인데 레거시 `'T'`=생산창고 반납이라 엔진이 부호를 반전한다.
★생산(PRD)쪽 T 쿼리는 `out_wh_gubun='3'` 로 한정하는데 **자재(MAT)쪽 T 쿼리엔 그 필터가 없다** —
그래서 우리 반품행이 자재 T 버킷에 걸린다.

**② 웹 제품재고조정이 영업 수불장·마감에 안 잡힌다**
`prodstockadj.py` 는 미러 clobber 회피를 위해 **신규 `nx.prod_stock_adjust`** 에 쓰는데,
영업 수불장 엔진 `_sal_moves` 는 `sa_t_stock_maint`·`pu_t_stock_maint` 만 읽는다.
⟹ 화면에서 조정·**반품(R)**·불량(1) 을 등록해도 수불장·마감이 모른다.
실적 **0건**(아직 미사용) 이라 피해는 없다.

### 정직 기록 — 검증 중 낸 오판 1건
처음에 SAL 출하반품을 `−수량 저장`으로 가정해 "부호반대 FAIL" 을 냈다. 그러나
`nx.SA_T_STOCK_MAINT` tag `'R'` 전기간 실적 1건이 **양수 저장**(240115) 이고 엔진이 `outq += −qty`
로 받으므로 **정상**이다. ⟹ 검증 케이스의 부호 가정도 데이터로 확인해야 한다(안 하면 없는 결함을 만든다).

---

## 13. 수정·재검증 완료 (2026-09-08)

### 13-1. 무엇을 고쳤나

| # | 문제 | 고친 내용 |
|---|---|---|
| ① | 자재반품이 재고를 **+로 늘림** | 미러 태그 매핑 `'RT'→'T'` 를 **`'RT'→'U'`** 로. `'U'`=자재반품(레거시 미사용 1글자) 신설 후 **출고 버킷 등록** |
| ② | 웹 제품재고조정이 영업 수불장에 **미반영** | `_sal_moves` 의 영업 원장 소스를 **`sa_t_stock_maint` ∪ `nx.prod_stock_adjust`** 로 |

**(b)안(태그 신설)을 택한 이유**: `MAINT_TAG` 를 varchar(4)로 늘리는 (a)안은 미러 재복제
(`r_delta_sync` `do_full` 의 스키마드리프트 DROP 폴백)에서 되돌아갈 수 있다. 컷오버를 앞두고
스키마를 건드리지 않는 쪽이 안전하다.

**따라 고친 곳(전수)** — 태그를 바꾸면 읽는 곳이 전부 따라가야 한다:
`stock.py`(미러 매핑 2곳) · `close.py`(`TA_OUT_TAGS`) ·
`live_api.py`(자재수불장 LINES/BF 출고 태그목록 + 표시명 `'U'→자재반품`) ·
`core.js`(`TAGN` 에 `'U':'반품'` — 화면·원장은 `RT`, 미러는 `U` 라 둘 다 매핑)

### 13-2. 재검증 — 전부 통과

```
수불장 부호 전수검증 (ledger_signs_verify.py)   14건 · 문제 0건
  MAT 입고/출고/반품/반납/조정 · PRD 입고/출고/반품/조정 · SAL 입고/출고/반품/조정×2
흐름 TestBed [F]  PASS 14 · FAIL 0 · 오염 0
흐름 TestBed [R]  PASS 26 · FAIL 0 · 오염 0
회귀   자재 8월 마감 총액 4,112,880,949 = 변경 전과 diff0
       영업 수불장 260901~260908 불변식 위반 0건
```

★**회귀 가드 추가**: 레거시 `'T'`(생산창고 반납)는 자재창고로 **되돌아오는** 것이라 자재재고
증가가 맞다. 반품을 T 에서 떼어낸 뒤에도 그 의미가 그대로인지 케이스로 못박았다(PASS).

### 13-3. TestBed 편입

- `_schema/ledger_signs_verify.py` — **종료코드**(FAIL 시 1) 추가 → CI 게이트 가능
- `_migration/run_testbed.ps1` — 흐름 TestBed 뒤에 **수불장 부호검증 자동 실행**
  (흐름 TestBed 는 "전표가 3곳에 기록되는가"만 본다. **엔진이 어느 방향으로 세는가**는
   못 잡는다 — 이번 반품 결함이 그래서 안 잡혔다. 두 층을 같이 돌려야 한다.)
- `flow_server.py` — 프로브 **`제품조정`**(`nx.prod_stock_adjust`) 신설 + 그 테이블을 오염 감시 대상에 추가
- `flow_cases.py` — `[F] 제품재고조정 (prodstockadj/save)` 케이스

### 13-4. 하네스 결함 1건 동반 수정 — 거짓 오염 경보

오염 판정 테이블에 `nx.app_session` 이 들어 있었다. 그런데 **하네스가 로그인하면 세션이 반드시
생기므로**, 토큰을 새로 딸 때마다 "★★오염 의심" 이 떴다(실측 +3). 거짓 경보가 반복되면
진짜 오염 신호를 무시하게 된다. ⟹ `SIDE_EFFECT_TABS` 로 분리해 **관측은 하되 판정에서 제외**.
계정 자체(`app_user`)는 변하면 안 되므로 판정에 남겼다.

---

## 14. 잠정 스냅샷 stale 방지 — 구현 (2026-09-08)

§9-1 에서 (a)잠정 스냅샷을 택한 **전제조건**. 일마감이 잠정이 되면서 생긴 유일한 위험 =
**스냅샷 일자 이전에 전표가 나중에 들어오면 기초가 낡는다** → 그 뒤 계산이 전부 틀어진다.

### 왜 훅이 아니라 '읽기 시점 지문 대조'인가

| | 훅 방식 | **지문 대조(채택)** |
|---|---|---|
| 방법 | `stock_changed()` 에서 그 일자 이후 스냅샷을 stale 처리 | 확정 때 지문을 남기고, 기초로 쓰기 직전 다시 재서 대조 |
| 비용 | 코드 **42곳** 수정 | 조회당 0.03~0.07초/테이블 (실측) |
| 위험 | **한 곳만 빠져도 구멍** | 쓰는 쪽이 뭘 하든 **읽을 때 매번 확인 → 빠뜨릴 수 없다** |

실측: `stock_changed()` 호출부 42곳이 **아무도 일자를 안 넘긴다**. 재고게이팅 "예외 없음"이
예외 하나로 무너졌던 것과 같은 함정이라 훅 방식을 버렸다.

### 설계 — 핵심은 "표시"가 아니라 "안 쓰는 것"

```
지문 = (행수, 수량합, 최근 INSERT, 최근 UPDATE)
   · 행수·수량합이 있어야 **삭제**가 잡힌다(삭제는 datetime 으로 안 잡힌다)
   · 창 = 직전 확정 월마감 다음날 ~ 스냅샷일 (월마감 구간은 규칙B로 잠겨 변할 수 없다)

① nx.period_close 에 컬럼 4개 additive : src_rows · src_sum · src_ins · src_upd
② close_run 이 마감과 **같은 트랜잭션**에서 지문 기록 (_fp_store)
③ 기초 해석기가 잠정 스냅샷을 쓰기 직전 대조 → 다르면 **건너뛴다**(_fp_stale)
④ close_status 에 stale 노출 → 화면 '갱신필요' 배지
```

★③이 전부다. **표시만 하고 계속 쓰면 사람이 잊는 순간 틀린 재고가 남는다** —
`STOCK_GATING_CLOSE_LOCK_RULES` 가 순차마감 완화를 보류했던 바로 그 이유다.
건너뛰면 더 이전 확정(월마감)에서 재생되므로 **값은 항상 정확**하고, 다시 마감하면 성능이 회복된다.

### 공용함수로 운영 (대표 확정)

판정은 `close._fp_stale` **하나**만 쓴다. 소비자 4곳:

| 소비자 | 경로 |
|---|---|
| `_mv_base` (자재) · `_prd_base` · `_sal_base` | 기초 후보에서 stale 잠정 스냅샷 제외 |
| **`live_api._snapsum`** (일일 영업/매입 현황) | ★**엔진을 우회해 `nx.stock_snapshot` 을 직독하던 유일한 곳.** 성능 때문에 직독하는 설계라 그대로 두되, 같은 공용 판정을 불러 낡은 일마감은 건너뛰고 다음 확정으로 내려간다 |

수불장 3종·마감 스냅샷·**재고 게이트**(`common._mat_avail_map`)는 전부 위 기초 해석기를 지나므로
자동 적용된다. `rawmatledger`·`sagubledger`·`partledger` 등은 별개 원장이라 무관(전수 grep 확인).

### 안전장치 — 기존 스냅샷은 건드리지 않는다

지문이 **NULL 이면 stale 로 보지 않는다.** 이 기능 이전에 만든 스냅샷을 전부 무효로 돌리면
기존 마감이 통째로 재생돼 위험하다. 그런 스냅샷은 다시 마감할 때 지문이 붙는다.

### 검증 (`_schema/snapshot_stale_verify.py` · 샌드박스·롤백·오염 0)

| 도메인 | 주입 전 | 주입 후 | 기초 이동 |
|---|---|---|---|
| MAT | stale=False | stale=True | 260831(일) → **2607(월)** |
| PRD | stale=False | stale=True | 260906 → **260905** |
| SAL | stale=False | stale=True | 260906 → **260905** |

**회귀 없음**: 자재 8월 마감 총액 `4,112,880,949` diff0 · 수불장 3종 기초 출처 불변 ·
`ledger_signs_verify` 14건 문제 0 · 흐름 TestBed [F] 14 PASS / [R] 26 PASS · 오염 0.

`run_testbed.ps1` 에 편입 — 흐름 TestBed → 수불장 부호검증 → **stale 방지 검증** 순으로 돈다.

### 남은 한계 (정직 기록)
같은 구간에서 삭제와 삽입이 정확히 상쇄되고 시각까지 안 변하는 조합은 이론상 지문을 통과한다.
삽입이 있으면 `INSERT_DATETIME` 이 올라가므로 실제로 만들기는 어렵다. 완전 차단이 필요하면
지문에 이미 들어 있는 `수량합` 외에 품목별 해시를 더할 수 있으나, 비용 대비 실익이 낮아 하지 않았다.

---

## 15. HTTP 레벨 검증 편입 (2026-09-08)

엔진 하네스(`ledger_signs_verify.py`·`snapshot_stale_verify.py`)는 **in-process 로 엔진을 직접**
부른다. 계산은 정확히 잡지만 **라우터 결선·인증·응답형태·캐시**는 안 지난다.
⟹ 화면이 실제로 부르는 경로로도 한 번 통과시킨다(`flow_cases.LEDGER_HTTP_CASES`).

| 케이스 | 확인 |
|---|---|
| `GET /api/close/status` | `firm`·`stale` 노출 · **일=잠정 / 월=확정** 이 ptype 과 일치 |
| `GET /api/close/ledger?domain=MAT` | 3,237행 · 불변식 위반 0 |
| 〃 `PRD` | 2,839행 · 위반 0 |
| 〃 `SAL` | 465행 · 위반 0 |
| **반품 end-to-end** ①→②→③ | 기말수량 확보 → `/api/stock/save` 반품 9 → **6,025,301 → 6,025,292 (Δ−9)** |

★반품 3단 케이스가 이번 결함의 회귀 방지선이다. 종전엔 이 자리에서 **+9 로 늘었다.**
엔진 검증만으로는 화면이 부르는 경로가 같은 값을 주는지 알 수 없어 HTTP 로 한 번 더 못박았다.

`run_testbed.ps1` 최종 구성: 흐름 → **HTTP 수불장** → 수불장 부호검증 → stale 방지.
결과: **PASS 7 · FAIL 0 · 오염 0**.

---

## 16. 이월재고 — 수불장 별도 표현 (2026-09-08, §8-3 완료)

### 대표 정의 (이번에 명확해진 것)

> **"불출했지만(매출) 협력사와 협의해 이월을 하면, 당월 매출로 인식하지 않고
>  협력사에 있는 우리 재고로 인식해야 한다."**

종전 문서는 이월을 *"정산 귀속·표시만"* 으로 적어 뒀는데(`common.py:493`), 그건 **절반**이었다.
정산에서 빼는 것뿐 아니라 **자산으로는 여전히 우리 재고**라는 뜻이다.
⟹ 창고에서는 나갔지만(tag5 출고 = 수불장 기말에서 이미 빠짐) **자산은 우리 것**이므로,
기말에 섞지 않고 **별도 컬럼**으로 드러낸다.

### 구현

| | |
|---|---|
| 대상 | `PU_T_STOCK_MAINT` **tag5**(협력사 매출출고) 중 마감 이월창 |
| 판정 | **`common._carry_win_ovr('SALE')`** — 매출마감 화면과 **완전히 같은 식**(§0-★★). `nx.magam_carry_ovr` override(사람이 옮긴 당월↔이월)도 그대로 반영 |
| 평가 | **원가** = 이월수량 × 그 품목 기말 이동평균단가. 매출액(판가)이 아니다 — 재고로 인식하므로 재고자산 평가원칙을 따른다 |
| 표시 | 수불장 행에 `cq`(이월재고수량)·`ca`(이월재고금액) **별도 열** + `fq`/`fa`(기말재고=창고+이월) |
| 불변식 | 기초+입−출±조정=기말 은 **창고 기준 그대로** 유지. 이월은 그 바깥의 부가 정보 |

★**창고 이동이 0 이어도 숨기지 않는다.** 전량 협력사에 가 있는 품목(기말 0 · 이월만 있음)이
기본 조회에서 가려지면 "우리 재고"를 별도로 표현하는 의미가 없다. 실측으로 **9품목**이 그랬다
(`zero` 필터에 `_cq0` 예외 추가 → 3,681 → 3,690행, 이월 172 → **181품목** 전부 노출).

### 실측 (2608)

```
이월  181품목 · 수량 587,610 · 금액 582,936,582원(원가)   ← tag5 수량의 68%
      (매출액 기준으론 643,375,425원 — 원가평가라 다르다)
기말(창고) 수량 6,759,583 · 금액 4,115,660,953 · 불변식 위반 0
전량 협력사 보관(기말0·이월만) 9품목 — 예: RAB30641901 이월 123,600 · 기말창고 0
```

### 화면·검증

- 자재수불장(`screens.pur.js`)에 **이월수량·이월금액** 2열(자재 탭만) + 총계 + CSV. 캐시버스팅 `?v=260908carry`
- HTTP TestBed `[HTTP] 이월재고(협력사 보관) 별도 표현` — 구조·행합 정합·전량이월 행 노출까지 검사
  → **PASS 8 / FAIL 0** (이월 181품목 · 587,610 · 5.83억 · 전량이월 행 9 · 행합일치)

### 남은 것
- 생산(PRD)·영업(SAL) 수불장은 이월 대상이 아니다(tag5=자재 불출). 필요해지면 같은 헬퍼를 재사용.
- 매입(PUR) 이월은 재고가 이미 우리 창고에 있어 재고 인식 이슈가 없다 — 정산 귀속만.

### 16-1. ★정정 — 기말재고 합계에 **반영한다** (대표 확정 2026-09-08)

처음엔 "별도 표현" 을 *기말과 섞지 않는다* 로만 구현했는데, 대표 확정은 한 걸음 더 나간다:

> **"수불장에 기말재고 합계가 그것을 반영해야 한다."** (매출액은 불필요)

협력사에 있어도 **우리 재고**이므로 기말재고에 들어가야 한다. ⟹ 최종 구조:

```
창고기말(sq)        = 기초 + 입 − 출 ± 조정        ← 불변식은 여기서 검산(그대로 유지)
이월재고수량(cq)     = tag5 이월분(협력사 보관)
이월재고금액(ca)     = cq × 기말 이동평균단가(원가)
기말재고(fq/fa)      = 창고기말 + 이월             ← ★화면 헤드라인 '재고수량/재고금액'
```

**화면**(자재수불장, 자재 탭): 헤드라인 `재고수량/재고금액` = **기말재고(창고+이월)** ·
상세부에 `창고기말` · `이월재고수량` · `이월재고금액` 3열. 총계·CSV·정렬·필터(±재고) 모두 합계 기준.

**실측 2608**
```
창고기말        6,759,583 ·  4,115,660,953
이월재고          587,610 ·    582,936,582   (181품목 · 전량이월 9품목)
기말재고(합계)  7,347,193 ·  4,698,597,535
행 단위 불일치 0 · 불변식(창고 기준) 위반 0
```

**검증**: HTTP TestBed 가 `fq = sq + cq` 를 행·총계 양쪽에서 검사 → **PASS 8 / FAIL 0**
```
이월 181품목 · 587,610 · 582,936,582원(원가) · 전량이월 행 9 · 행합일치
기말재고 7,347,193 = 창고 6,759,583 + 이월 587,610 반영
```
