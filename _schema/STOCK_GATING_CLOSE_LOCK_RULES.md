# 재고 가용성 게이팅 + 마감 일자 잠금 — 전 도메인 공통 규칙 (정본)

> 작성 2026-08-19. 대표 지시: **마이너스 재고가 시스템에서 발생하지 않게** + **마감된 일자는 수정 못하게** — 생산·영업·자재 **전 프로그램에 동일 규칙**으로.
> 이 문서 = 흩어진 기록(주로 [[NX_STOCK_LEDGER_DESIGN]] · 코드 기구현분)을 **하나로 통합·정리**. 신규 구현은 여기 기준.
> 관련 durable: `NX_STOCK_LEDGER_DESIGN.md`(재고점 모델·posting), `TRANSACTION_CUTOVER_DESIGN.md`(컷오버).

---

> **★다른 세션 진입점**: 재고·마감을 건드리기 전에 **`_schema/STOCK_CLOSE_HANDOFF.md`** 를 먼저 읽을 것
> (방향=과도기·지금 지킬 결선·도메인 경계·미결 자재단가 회계방식). 2026-08-27 재고/마감 담당 세션 작성.

## 0-★. 하드룰 — **예외 없음** (대표 지시 2026-08-28, 최우선)

> 원문: **"생산실적도 막으라고 했어.. 우리 시스템은 예외가 없어"**
> **"대신 경고 메시지를 주고 왜 안되는지 알려주라고 했어"**
> **"이것 관련해서 정확한 rule이니깐 기록을 해!! 우리 시스템 규칙이야. 예외는 없어."**

**규칙 A-0 (예외 금지).** 재고 가용성 게이팅에는 **어떤 예외도 두지 않는다.**
품목 종류·창고·추적 여부·부서·화면을 이유로 게이트를 **건너뛰는 코드를 만들지 않는다.**
"이 품목은 관리 대상이 아니라서", "미등록이라 판정할 수 없어서", "오차단이 우려돼서"는
**전부 사유가 되지 않는다.** 판정할 수 없으면 **통과가 아니라 차단**이다.

**규칙 A-0-1 (사유 고지 의무).** 차단할 때는 **왜 안 되는지**를 반드시 사용자에게 보여준다.
최소 포함 항목: ① 어느 품목이 ② 얼마 필요한데 ③ 어디에 얼마 있는지(축별) ④ 그래서 무엇을 해야 하는지.
> 나쁜 예: `재고부족` / 좋은 예: `AJR75563503: 소요 120 > 가용 45 (준비재고 0 + 자재재고 45[자재일마감]) · 생산창고에 300 있으나 백플러시 소비축이 아님`

**규칙 A-0-2 (판정축 일치).** 게이트가 보는 재고축은 **그 프로그램이 실제로 빼가는 축과 같아야** 한다.
다른 축을 보고 통과시키면 게이트가 있으나 마나다(아래 사고 참조).

### ★이 규칙이 생긴 경위 (2026-08-28 실측)
백플러시 게이트에 `_tracked()` 예외가 있었다 — `nx.mat_stock_daily` 에 없는 품목은 **판정 없이 통과**.
주석의 명분은 "비키팅품·사급포함품 오차단 방지". 실측 결과:

| 실측 | 값 |
|---|---|
| `nx.bom` 소비대상 자식 품목 | 9,955 |
| 그중 `mat_stock_daily` 미등록 = **게이트 무효** | **6,988 (70.2%)** |
| 미등록의 최대 집단 | sgroup **130 가공품 4,640** (사내 제작 반제품) |
| 가공품 재고 실소재지 | `PR_T_MAT_STOCK_WH` 3,488 · `PU_T_MAT_STOCK_WH` 4,066 (mat_stock_daily 아님) |

즉 **게이트가 소비 품목의 70%에 대해 작동하지 않았다.** 예외 하나가 규칙 전체를 무력화한 사례.
→ 예외 제거. 판정축을 소비축(RDY → MAT)과 일치시키고, 미등록은 통과가 아니라 **차단 + 사유**로 바꾼다.

### 적용 지점 (전 생산실적 경로 — 빠짐없이)
| 경로 | 화면 | 게이트 |
|---|---|---|
| `/api/procbc/save` | 바코드생산실적 | 원소재·하위자재 축별 검사 + 사유(`재고부족: 원소재 X(필요/보유)`) |
| `/api/backflush/post` · `_backflush_core` | 백플러시(완성공정 소비) | **예외 없이** 전 자식·용접봉 검사 + 축별 사유 |
| `/api/procreg/save` | 공정별생산실적 | 실적 수량만큼 BOM 소요 검사 + 사유 |
| `/api/stock/save` (issue·return·adjust−) | 자재출고/반품/조정 | 기구현 |
| `/api/kitting/cell-confirm` | 준비실적처리 | flag-only(무차감) — **차감이 없으므로 게이트 대상 아님**(예외가 아니라 해당 없음) |

★"차감이 없어서 해당 없음"과 "차감이 있는데 안 막음"은 다르다. **후자는 금지.**

---

## 0-★★★. ★컷오버 필수 — **재고 게이트의 자재정본을 실시간으로 승격해야 한다** (2026-08-28 발견)

> **지금 이 문서 §125 의 전제가 깨졌다.** 반드시 읽고 컷오버 전에 처리할 것.

### 무슨 일이 있었나
§125 는 이렇게 판단했다(2026-08-19):
> 실시간 자재정본 승격 = 지금 불필요 — **게이트는 dev만·미배포·미enforce** 라 리스크 없음.
> 실시간 승격은 **게이트를 실운영 하드 ON 하는 컷오버 시점에 함께** 진행.

그런데 **2026-08-28 PR #97 로 생산실적 게이트를 운영에 배포했다.**
⟹ "미배포라 리스크 없음" 이라는 전제가 사라졌다. 승격을 더 미룰 수 없다.

### 지금 실제로 벌어지고 있는 일
게이트(`common._mat_avail`)는 `nx.mat_stock_daily`(일 스냅샷)를 읽는다.
그 테이블을 채우는 것은 **`_migration/sub_norm/matclose_movavg_build.py`** 인데 —

| 확인 | 결과 |
|---|---|
| 백엔드가 자동 호출하나 | **없음** |
| 배치·PowerShell 스크립트 | **없음** |
| 매일 마이그(`r_delta_sync`)에 포함 | **없음** |
| 윈도우 작업 스케줄러 / SQL Agent | **없음** |

⟹ **사람이 손으로 돌리는 스크립트다. 실행 시점이 설계상 정의된 적이 없다.**
   (스크립트 첫 줄에 "**사라진 원빌더**를 기록스펙대로 재작성" — 이미 한 번 소실된 코드다.)

**실측(2026-08-28)**: 빌더가 **8/25 에서 멈춰** 있다. 8/26·27·28 은 전표가 각각 2,012·1,739·977건
있는데 `mat_stock_daily` 에는 없다. 그 결과 —

| | |
|---|---|
| 8/26~28 이동이 있는 품목 | 1,310 |
| **게이트가 "재고 있음" 으로 보지만 실제로는 음수인 품목** | **5** (아래 정정) |

예: `5210A22409A` — 게이트는 2,241 있다고 보지만 실제 −3,065. **출고를 통과시킨다.**
즉 **"음수재고 절대 차단"(§0-★) 이 조용히 뚫려 있었다.**

> **★정정**: 이 칸에 처음 적힌 수는 **133** 이었다. 그 133 은 **손으로 짠 검산 SQL** 로 센 것인데
> 그 SQL 이 **수입 전표(`PU_T_STOCK_MAINT_C`)를 빠뜨렸다** — 입고를 덜 세니 음수가 과다하게 나왔다.
> **엔진(`_mv_moves`)으로 다시 세면 5건이다.**
> 그리고 이 수는 **누가 빌더를 마지막으로 언제 돌렸느냐에 따라 출렁인다**(측정 당시 최신일 260825 → 지금 260906).
> 건수가 크든 작든, **게이트 정확도가 사람의 수동 실행에 매여 있다**는 것 자체가 결함이다.

### 조치 (컷오버 전 필수) → ✅ **2026-08-28 완료**
**게이트의 자재정본을 `mat_stock_daily` → 실시간(확정 스냅샷 + 그 이후 전표)으로 승격.**
- 마감·수불장이 이미 그렇게 계산한다(§7-1 파생 뷰). **게이트만 옛 소스에 남아 있었다.**
- 승격으로 **빌더 의존이 사라졌다** — 누가 언제 돌리는지가 더 이상 문제가 아니다.
- 컷오버 후 `mat_stock_daily` 는 은퇴 대상이므로(코드 주석 명시) 어차피 해야 할 일이었다.
- **단일 소스 하드룰**(CLAUDE.md §1-9-1)과 같은 방향 — 재고 정본은 하나다.

#### 구현
| 위치 | 내용 |
|---|---|
| `common._mat_avail_map()` | **close 엔진 호출**(`_mv_base`/`_mv_moves`/`_mv_step`) → `{품번: 수량}` |
| `common._mat_avail()` | 맵 조회. 없으면 **0** — 판정 불가를 통과로 바꾸지 않는다(규칙 A-0) |
| `common.stock_changed()` | 재고 쓰기 33곳 → 맵 즉시 무효화 |
| TTL 60초 | 웹 밖(매일 7:30 마이그)이 DB 를 직접 바꾸는 경우 대비 |
| `backflush._avail_axes`/`_avail_bulk` | 생산실적 게이트 MAT 축도 같은 맵 |

#### ★게이트 전용 SQL 을 새로 짜지 않는다
`_mv_moves` 는 **6갈래**(입고tag·수입·수출·출고tag·생산창고반납·재고조정)를 센다.
손으로 다시 짜면 반드시 하나를 빠뜨린다 — 실제로 수입을 빠뜨려 **수불장과 56건이 갈렸고**,
그 잘못된 SQL 이 위의 "133" 을 만들었다.
`AJR30057201`: 기초 376 + 수입 2,000 = **2,376**(정답)인데 손SQL 은 376.

#### 검증 (2026-08-28)
```
[G-4] 게이트 정본 vs 수불장 기말  — 수불장 3,681 · 정본 3,681 · 공통 3,681
      ★수량 불일치 0건            ← 게이트·수불장·마감이 같은 값
      최초 산출 1.17초 · 캐시 재조회 0.0000초 · 음수 6

flow_verify.py     자재 6종 3곳 일치 · 오염 0 PASS
flow_scenarios.py  PASS 39 · FAIL 0 · SKIP 1 · 오염 0 PASS
      차단 사유에 [자재정본] 표기 = 새 소스가 실제로 쓰인다
```

★임시로 빌더를 돌려 메우는 것은 **해결이 아니다.** 또 멈춘다(누가 언제 돌리는지가 없으므로).

---

## 0-★★. 결함수정 — 마감잠금이 **화면마다 다른 잠금원**을 보고 있었다 (2026-08-28)

### 무엇이 문제였나
`_closed(cur, ymd)` 는 **구 전역 월잠금 `nx.stock_close` 만** 봤다.
우리 마감은 마감관리 화면이 **`nx.period_close`** 에 기록한다.
⟹ `_closed` 를 쓰는 화면들은 **마감된 달의 전표를 그대로 저장**했다.

TestBed 확장(케이스 24→35)이 즉시 잡아냈다:

| 화면 | 시도 | 결과(수정 전) |
|---|---|---|
| 생산파트재고조정 `/api/stockmaint/save` | 마감월 2607 로 저장 | **200 통과** — 마감된 달에 조정이 들어감 |
| 발주입고 `/api/matrecv/receive` | 마감월 2607 로 입고 | **200 통과** |

자재입출고(`/api/stock/save`)는 `_lock_msg` 를 함께 보고 있어 막혔다 —
**같은 규칙인데 화면마다 판정이 달랐다.** §0-★ 규칙 A-0-2(판정축 일치) 위반.

### 수정
`common._closed(cur, ymd, domain="MAT")` 를 **공용 게이트 `_lock_msg` 에 위임**시켰다.
`_lock_msg` 는 ① 일마감 ② 월마감 ③ 구 `stock_close`(하위호환) 순으로 보므로
**종전 동작을 포함하면서** 우리 마감까지 잡는다. 사유 메시지도 구체적이 된다(A-0-1).

- `routers/stock.py` 안에 있던 **로컬 `_closed` 사본**(구 잠금원만 보던 것)을 제거 — 공용을 가리고 있었다.
- 호출부 17곳에 도메인을 명시: 생산파트재고조정=`PRD` · 자재/창고간이동/발주입고=`MAT` · 영업·사급=`SAL`.

### 교훈
> **같은 규칙을 두 곳에서 판정하면 반드시 갈린다.**
> 마감잠금은 `_lock_msg` **하나만** 쓴다. 새 쓰기 화면을 만들 때 잠금 판정을 직접 짜지 말 것.
> (§21 "같은 값을 두 곳에서 계산하면 갈린다" 와 같은 뿌리 — 이번엔 *판정*이었다.)

---

## 0. 두 규칙 (한 줄 정의)
- **규칙 A — 재고 가용성 게이팅**: 어떤 차감(출고/소비/키팅/이동)도 **결과 재고가 음수가 되면 거부**. 앞 단계 재고가 없으면 다음 단계 불가.
- **규칙 B — 마감 일자 잠금**: **마감된 기간(≤마감일)의 거래는 생성·수정·삭제 불가**(생산·영업·자재 공통). MASTER 강행옵션은 정책.

---

## 1. 규칙 A — 재고 가용성 게이팅 (마이너스 원천차단)

### 1.1 단계 체인 (대표 지시 + ★생산 2공정 분리)
**생산은 2가지(PR_M_WORK): P2=가공 · P1=용접.** 따라서 체인은 5단계:
```
자재입고 → 키팅(준비) → [P2 가공] → [P1 용접/조립] → 출고
  └ 입고 안 된 품목은 키팅 불가
     └ 키팅 안 되면 가공실적 안 잡힘
        └ 가공(가공품) 없으면 용접/조립 실적 안 잡힘
           └ 용접(완성품) 없으면 출고 불가
역방향(취소/반품)도 동일 — 결과가 음수면 거부
```
재고점(STOCK_POINT, [[NX_STOCK_LEDGER_DESIGN]]): **MAT(자재) → RDY(준비) → PRD_G(가공품, P2·가공창고 P0001/11라인) → PRD_W/ASY(용접·완성, P1·S계열 라인) → 출하**. 사급=SAG.
- **P2 가공**: 자재(동파이프) 절삭/축관 → 가공품(반제품). 소비=자재/준비, 산출=가공품.
- **P1 용접/조립**: 가공품+부품 용접·체결·검사 → 완성품. 소비=가공품/부품, 산출=완성(ASY).
- 게이팅은 **각 공정 진입 시** 앞 재고점(가공은 자재/준비, 용접은 가공품) 가용검증.

### 1.2 공용 함수 (신설·통일)
```
assert_stock(cur, stock_point, key_dims, need_qty)
  현재고 = 원장 SUM(해당 재고점·품목·[파트/cust])   # nx.stock_ledger 파생 = 기초+ΣMAINT
  if need_qty > 현재고 + 허용오차:
      raise HTTPException(400, f"재고부족 ({품목} 가용 {현재고:g} < 필요 {need_qty:g})")
```
- 차감 쓰기의 **트랜잭션 내, 원장 INSERT 직전** 호출.
- 취소/역거래: 되돌린 뒤 재고가 음수면 거부(동일 함수, need=역수량).
- 현재고 정본 = **nx.stock_ledger STOCK_POINT별 SUM**(컷오버 전 미적재 구간은 레거시 대사로 보강 — [[TRANSACTION_CUTOVER_DESIGN]]).

### 1.3 기구현(참고) — 이미 있는 패턴을 표준으로
- `stock.py`(자재조정/출고): `재고부족 (가용 avail < qty)` 가드 有 → **이 패턴을 공용함수로 승격**.
- `prodwrite.py`(생산자재이동): `FROM파트 재고부족` 가드 有.

---

## 2. 규칙 B — 마감 일자 잠금 (전 도메인)

### 2.1 마감 단위 (실측 인프라 기준)
- **월마감(전역)**: `nx.stock_close(ym, close_flag)` — 공용함수 `_closed(cur, ymd)`가 이미 판정. **생산·영업·자재 공통**.
- **업체별 마감일**: `MAGAM`/`CM_M_CUST_MAGAM`(JUN_YYMM·JUN_MAGAM_DAY·MAGAM_DAY) — 매입/매출 정산 기준([[newerp-lgsagub-recvcompare]] 등에서 사용).
- **일마감(자재)**: 자재 일마감 스냅샷([[newerp-matclose-movavg]] nx.mat_stock_daily) — 확정일 이하 잠금(추후 연동).
- 레거시 3가드(월말 pu_t_month_stock·수불 pu_t_month_stock_wh·일마감 CM_M_DAILY_MAGAM) → **nx.stock_close/MAGAM로 통합**([[NX_STOCK_LEDGER_DESIGN]] §E).

### 2.2 공용 함수 (통일)
```
assert_open(cur, ymd, domain=None, cust=None)
  if _closed(cur, ymd):  raise HTTPException(400, f"{_ym(ymd)} 마감된 월입니다 — 수정 불가")
  if cust and 업체마감일 >= ymd: raise ...   # 업체별(매입/매출)
```
- **모든 쓰기(생성/수정/삭제)** 진입부에서 대상 일자 검증. 수정/삭제는 **기존 행 일자와 신규 일자 둘 다** 검증.

### 2.3 기구현(참고)
- `_closed(cur, ymd)` = `nx.stock_close WHERE ym` (common.py). 이미 `prodwrite`(생산·자재)·`sales`(매출)에서 호출.

---

## 3. ★현재 구현상태 인벤토리 (2026-08-19 실측) — 균일 적용 대상

| 프로그램(라우터) | 쓰기 | 마감잠금 | 재고부족가드 | 갭(추가필요) |
|---|---|---|---|---|
| 자재조정/입고 `stock.py` | 6 | **6(전부)** | 8 | ✅ 재점검결과 전부 커버(문서 "1곳"은 오탐) |
| 생산실적/자재출고/조정 `prodwrite.py` | 6 | 6 | 4 | ✅ 양호 |
| 매출/출고 `sales.py` | 17 | 9→**11** | 3 | ★2026-08-19 saleout copy/carryover 마감잠금 추가. 잔여=sagub/output·lgsale(today-dated 저위험) |
| **키팅 `kitting.py`** | 2 | **✅적용(2026-08-19)** | 재고게이트=다음단계 | 재고게이트(입고없으면 키팅불가) 남음 |
| **생산백플러시 `backflush.py`** | 1 | **✅적용(post)** | 재고게이트=다음단계 | 재고게이트(키팅소비+용접봉) 남음 |
| **도입입고 `dopip.py`** | 3 | **✅적용(save/batch)** | — (입고=불필요) | 완료 |
| 매입마감 `purmagam.py` | 2 | **✅ 도메인락(pur_close 업체별)** | 0 | ✅ 이미 마감잠금(문서 "0"은 전역 _closed만 센 오탐) |
| 매출마감 `salemagam.py` | 5 | **✅ 도메인락(sale_close 업체별)** | 0 | ✅ 〃 |

**★2026-08-19 Step2 재점검 결과**: 문서 §3 초안의 마감잠금 "0/갭"은 **전역 `_closed`만 세어 도메인 전용 락을 놓친 오탐**이었음 — 매입/매출마감은 각 `pur_close`/`sale_close`(업체별 close_flag)로, stock.py는 6/6 이미 잠금. **실제 갭 = sales.py의 dated 쓰기 일부**였고, 그중 명백한 2건(saleout **copy**·**carryover**)에 `_sale_close_lookup`(매출마감) 가드 추가(마감기간 복사/이월 차단). 잔여(sagub/output·lgsale)는 today-dated/문서발행이라 저위험 — 다음 보강대상.

---

## 4. 균일 적용 계획 (구현 순서 — 승인 후 하나씩)
1. **공용함수 2개 확정**(common.py): `assert_stock(...)`, `assert_open(...)`. 기존 stock.py 재고부족·`_closed` 패턴을 승격·통일.
2. **키팅**(kitting.py cell-confirm): 자재입고(MAT)/준비 재고 없으면 키팅 거부 + 마감잠금.
3. **생산실적 백플러시**(backflush.py): 준비(RDY)/자재(MAT) 없으면 실적 거부 + 마감잠금.
4. **출고/매출**(sales.py·saleout): 완성(ASY) 없으면 출고 거부(재고가드 전면) + 마감잠금 전면.
5. **도입·매입입고**(dopip): 마감잠금(입고는 재고가드 불필요).
6. **마감처리 자체**(purmagam/salemagam): 마감확정 후 해당기간 재편집 잠금.
7. 각 단계 **왕복검증**(정상/부족거부/마감거부) + 취소·반품 역가드.

## 4-B. ★생산 소비 모델 (대표 지시 2026-08-19 — 다음단계 설계·구현)
재고 게이팅(규칙 A)의 **생산실적 단계 소비 정본**. 아래 3가지를 반영해야 마이너스 차단이 정확해짐.

### (1) 생산실적 = 키팅된 것 기준 소비
- 생산실적을 잡을 때 **키팅확인분(RDY)을 소비**한다. 즉 백플러시 소비원천 = 키팅된 수량.
- posting: **−RDY**(키팅 소진) + **−MAT**(실자재) → **+PRD_G(가공)/+PRD_W·ASY(용접완성)**.
- 게이트: 키팅(RDY) 없으면 생산실적 불가 → 앞서 정의한 체인과 일치.

### ★(1-보강) 생산실적 게이트 = 자재재고로만, 키팅과 무관 (2026-08-19 사용자 확정)
**★★확정 규칙 (두 줄):**
1. **자재가 부족하면 생산실적이 잡히면 안 된다** — 자재 현재고 < BOM소요면 실적 **하드 차단**(물리적으로 생산 불가). 마이너스 원천차단.
2. **자재는 있는데 키팅만 안 됐다고 실적을 막으면 안 된다** — 게이트는 **키팅(RDY)을 아예 안 본다**. 자재만 있으면 실적 잡힘. (키팅=flag-only `kitting_cell_confirm` "자재무차감", 준비됨 플래그일 뿐·"키팅 재고" 관리대상 없음.)

⟹ **생산실적 게이트 = 오직 자재 현재고(`_mat_avail` = 확정스냅샷+이후전표, 2026-08-28 실시간 승격) 기준.** 키팅 상태·RDY원장 무관.

- **구현(backflush.py `_backflush_core`, post)**: BOM 리프별 `소요=cq×prod_qty` vs `_mat_avail`. 부족(관리자재)이면 `return ok:False`("자재부족으로 생산실적 불가"). ★키팅/RDY 조회 없음.
- ~~★**커버리지 인지**: 자재재고 '관리품목'(mat_stock_daily 존재)만 게이트 — 비키팅품·사급포함품은 제외~~
  **★2026-08-28 폐기 — 이 예외가 규칙 §0-★ 을 무효화하고 있었다.** `_tracked()` 예외가
  **소비 대상 자식의 70.2%(6,988/9,955)** 를 판정 없이 통과시켰다(대표 지시: *"우리 시스템은 예외가 없어"*).
  지금은 **전수 판정**하고, 정본 밖이면 자재창고(`PU_T_MAT_STOCK_WH`)를 2순위 축으로 본다.
  어디에도 없으면 **0** — 판정 불가를 통과로 바꾸지 않는다(규칙 A-0).
- ~~★**한계(컷오버 전제)**: mat_stock_daily=레거시 일스냅샷 → 당일 입고분·연속차감 미반영~~ → **★2026-08-28 해소.** 자재정본을 실시간(확정스냅샷+이후전표)으로 승격 완료(§4 step4 실행). 당일 이동이 즉시 반영된다.
- ★**−RDY/−MAT 분할 재검토**: 소비 posting은 여전히 '−RDY 우선, 없으면 −MAT'인데 키팅=flag-only면 자재 전량 −MAT이 정합(RDY는 플래그 상쇄만). 게이트(자재재고 full need 검사)는 이미 kitting-independent라 무영향. 컷오버 정리대상.

**★검증 완료(2026-08-19, 읽기전용·무오염, 실제 `_backflush_bom`+`_mat_avail`+게이트조건 그대로. 하네스 scratchpad/verify_backflush_gate.py)**:
| 검증 | 표본 | 키팅RDY | 결과 |
|---|---|---|---|
| ①자재부족→차단 | 5211A10305F | 0 | BLOCK(부품 3종 재고0) ✓ |
| ②자재있음+**미키팅**→통과 | 5211A10305F-SUB | 0 | PASS(자재충분) ✓ |
| ③자재부족+**키팅됨**→차단 | AEG74589807 | 40 | BLOCK(MEE64065104 재고0) ✓ |
| ④구조증명 | 게이트 소스 | — | RDY/키팅 조회 **없음** → 자재재고로만 판정 ✓ |
결론: **자재재고=유일 판정기준, 키팅 완전무관**을 실데이터+코드로 증명. ②=키팅 안돼도 자재있으면 실적, ③=키팅돼도 자재없으면 차단(자재 우선).

~~**★실시간 자재정본 승격 = 지금 불필요(컷오버 과제로 연기, 2026-08-19 판단)**: 게이트는 dev만·미배포·미enforce라 리스크 없음…~~

**★★2026-08-28 — 이 판단의 전제가 깨졌고, 승격을 완료했다.**
- 전제였던 *"게이트는 dev만·미배포"* 가 **PR#97 로 운영 배포되면서 무너졌다.** 그때 이 문단을 갱신하지 않았다.
- 그리고 `mat_stock_daily` 빌더는 **사람이 손으로 돌린다** — 자동 실행 지점이 설계상 정의된 적이 없다.
  실제로 **8/25 에 멈춰** 있었고, 게이트가 실제 음수 품목을 "재고 있음"으로 통과시켰다.
- ⟹ `_mat_avail` 을 **확정 스냅샷 + 그 이후 전표**(마감·수불장과 같은 엔진)로 승격.
  **빌더 의존 자체가 사라졌다** — 누가 언제 돌리는지가 더는 문제가 아니다.

| 검증 | 결과 |
|---|---|
| 게이트 정본 vs **수불장 기말** | **불일치 0건** (3,681/3,681) |
| 산출 | 최초 1.17초 · 캐시 재조회 0.00초 |
| TestBed `flow_scenarios.py` | **PASS 39 / FAIL 0 / 오염 0** |

> **교훈**: *"지금은 리스크가 없으니 나중에"* 라고 적은 판단은 **전제가 바뀔 때 같이 갱신되지 않는다.**
> 배포로 전제가 깨졌는데 문서는 3주 넘게 옛 판단을 그대로 말하고 있었다.

### (2) 용접봉 등 공정종속 자재 = 공정 소요량으로 별도 차감
- **BOM 소비가 아님**. 용접봉은 **공정 소요량(용접ST × 원단위)**으로 계산해 생산실적 시 **함께 차감**([[newerp-weld-cost-split]]·[[newerp-weld-settlement-roadmap]]).
- 재고점: 용접봉창고(**Q1000**) 또는 자재 − / 협력사정산 연동.
- 게이트: 생산실적 백플러시에 **BOM 자재차감 + 용접봉 공정차감**을 같이 계산. 용접봉 재고 부족도 검증 대상.

### (3) 키팅 안 하는 품목 = 자재→생산창고 직접 이동으로 차감
- **케이블타이·비닐·고무캡** 등 저가/범용 부자재는 **키팅을 안 함**. 자재창고 → 생산창고로 **직접 이동(MV)**하며 재고 차감.
- 키팅(RDY) 단계를 **우회** → 이 품목들은 RDY 게이트 대상 아님, 대신 **자재(MAT) 재고에서 이동출고 시 차감·게이트**.
- 설계필요: 비키팅품 판별 기준(품목분류/플래그?), 이동 시점·수량 산정(생산량×BOM? 수동?), MV posting(−MAT자재/+PRD생산창고).
- → **다음단계에서 상세 설계·구현**(대표 지시).

## 4-C. ★현재고 보강 · 컷오버 안전 (마이그 5단계 — 재고게이트의 전제)
> 재고게이트(규칙 A)는 **정확한 현재고**가 있어야 오차단이 없다. 현재 nx 현재고는 미비 → 아래 순서로 보강·검증 후 게이트 ON.

### 현재 상태 실측(2026-08-19)
- **nx.stock_ledger**: MAT 171,972행(260401~오늘, tag B/S/9/C/5/P/4/2/J/T/1) — **기초(tag3) 없음** → SUM=순변화만(현재고 아님). RDY 12행(웹키팅만), **PRD/ASY 0행**.
- **nx.stock_close_snap**(기초 스냅샷, Phase5): 컬럼 ym·stock_point·item_key·gpc·cust·base/in/out/end_qty — **비어있음**.
- 미러 PU_T_STOCK_MAINT = 라이브 동일 최신(260819) → 델타싱크 가능.
- ★월표기 규칙: **STOCK_YYMM='YYMM' = 그 달의 기말**. 2607=7월기말=8월기초, 2606=6월기말=7월기초. (실측: 2606=4,936M=우리260630, 2607=4,473M=우리260731)

### 정본 현재고 공식 (드리프트 불가)
```
현재고(point,item) = 기초(stock_close_snap.end_qty, 직전마감월) + Σ stock_ledger.MAINT_QTY(마감월+1 이후)
```
- 기초는 **반드시 확정 마감스냅샷(PU_T_MONTH_*)** 에서. ★레거시 잔량테이블(PU_T_MAT_STOCK 등)은 드리프트 있음 → 직복사 금지.

### ★검증으로 발견(2026-08-19) — nx.stock_ledger MAT은 자재 현재고 정본 아님
- 사전검증: `2607기초+원장 8월델타` vs 검증본 mat_stock_daily 260818 → **45% 불일치(3817중 1716)**.
- 원인: **nx.stock_ledger에 최근(8월) 이동이 누락**(미동기화). 예 MBL67877901: 원장 8월=비어있음, 레거시=tag9+100,000·tag5−24,000·tagB−4,352=+71,648. 2607(16,000)+71,648=mat_stock_daily 88,093 ✓.
- ~~**결론: 자재 현재고 정본 = `mat_stock_daily`**~~ → **★2026-08-28 갱신**
  **자재 현재고 정본 = `common._mat_avail()` = 확정 스냅샷 + 그 이후 전표**(마감·수불장과 같은 엔진).
  `nx.stock_ledger` MAT 은 여전히 미완(병행운영중 미동기) → **자재 게이트 source 금지**(이 부분은 유효).
  `mat_stock_daily` 는 수동 빌더 산출물이라 **게이트에서 뺐다** — 조회화면 참조만 남았고 은퇴 대상.
  **★게이트 전용 SQL 을 새로 짜지 말 것.** `_mat_avail()` 을 부른다.
- ★재고게이트 `_stock_short_msg`는 nx.stock_ledger SUM이 아니라 **정본 현재고(자재=mat_stock_daily / 레거시 recipe)**를 읽어야 함. (게이트 켜기 전 이 source 교체 필수 — 안 그러면 45% 오차단.)

### ★키팅 게이트 = BOM 부품 기준 (2026-08-19 검증발견)
- 키팅 품목(RDY 원장)은 전부 **ASSY/SUB(분류 110/120, Tube Assembly·Sub Assy)** — **자재(MAT)가 아님**. `_mat_avail(키팅품목)`=0 → 그대로 쓰면 **전 키팅 오차단**.
- 따라서 키팅 게이트 = **"그 ASSY의 BOM 자재 부품들이 입고(자재재고>0)됐는지"**. nx.bom 전개 → 각 자재부품 소요(use_qty×키팅수량) vs `_mat_avail(부품)`. 부족부품 있으면 키팅 거부.
- 이건 [[§4-B (1) 생산실적=키팅기준 소비]]와 연결 — 키팅=예약이지만 부품 가용성은 자재재고로 검증. (BOM 전개 = backflush `_backflush_bom` 재사용.)
- **★검증발견(2026-08-19, 착수보류)**: 개념검증(AJR30019901 10키팅=21부품)에서 ①**일부 ASSY BOM 전개 0종**(AJR30133601 = nx.bom 커버리지 갭) → 게이트 미작동 ②부품 9/21 자재재고0(진짜부족 vs mat_stock_daily 부품커버리지 갭 불명). **지금 켜면 (a)BOM없는 ASSY 무검증통과 (b)갭부품 오차단** → **BOM 전개 커버리지 + 부품 자재재고 커버리지 검증이 선행필수.** 자재게이트처럼 정본이 확실할 때만 ON.

### 각 재고점 게이트 source (정본)
| 재고점 | 게이트 대상 품목 | 현재고 정본 | 상태 |
|---|---|---|---|
| 자재(MAT) | 자재출고 품목=자재 | `_mat_avail`(mat_stock_daily) | ✅ 적용 |
| 준비/키팅 | ASSY의 **BOM 자재부품** | 부품별 `_mat_avail` | 게이트 설계됨(BOM전개), 커버리지검증 선행 |
| 생산(PRD가공/PRD용접) | 앞공정 산출물(가공품) | **`_prod_avail`**(생산재고조회 480 recipe 단일품목, line=P0001 가공/그외 용접) | ✅ **정본확보**(2026-08-19 레거시 480 diff0, 6품목 일치) |
| 완성(ASY) | 완제품출고=완성품 | **`_finished_avail`**(제품재고조회 040 recipe 단일품목) | ✅ **정본확보**(2026-08-19 레거시 040 diff0, 5품목 일치) |

**★2026-08-19 Step1 완료 — 현재고 정본 공용함수 3점(common.py)**: `_mat_avail`(자재)·`_prod_avail`(생산가공/용접, line 파라미터)·`_finished_avail`(완성/제품) + 각 `_*_short_msg` 게이트래퍼. 모두 레거시 조회화면과 diff0 검증(읽기전용, nx 무오염). ★이로써 §4-C 표의 생산/완성 "미비"가 해소 — 이제 각 재고점 게이트가 정본 현재고를 읽을 수 있음. **주의: 함수는 nx 병행운영중 테스트오염 반영(오늘자) → 컷오버 후 정본. 게이트 ON 판단은 다음 단계(키팅 커버리지·왕복검증 필요).**

### 마이그 5단계 (각 단계 검증 필수) — ★source 수정 반영
1. **자재(MAT) 기초 심기**: nx.stock_close_snap에 **2607(7월기말) MAT 스냅샷**(PU_T_MONTH_STOCK_WH Z99990) 적재. 자기생성 근거키(ym=2607·point=MAT) 재적재 멱등.
2. **MAT diff0 대조**: `현재고=snap(2607)+Σ원장(≥260801)` == 레거시 현재고(2607+8월이동). 품목별 diff0 검증(허용오차0).
3. **RDY/PRD/ASY 마이그**: 준비(PU_T_READY_STOCK/月마감)·생산(PR_T_MAT_STOCK_WH/月)·완성(SA_T_ITEM_STOCK/月) 기초 스냅샷 적재 + 이동원장 미러. 각 재고점 diff0.
4. **재고게이트 ON**: 현재고 정확 확인 후 `_stock_short_msg`를 현재고공식(기초+델타)으로 승격, 전 차감쓰기점 적용.
5. **컷오버 직전 전 재고점 diff0**: `mirror_recon.py`로 nx파생 vs 레거시 잔량 대조. 불일치=레거시 드리프트로 확인 후 컷오버.

### 이중계상 방지 (핵심)
- 기초=마감월 기말 스냅샷, 델타=**마감월+1 이후 이동만**. 원장이 4~7월 이동을 포함하므로, 현재고 델타는 **MAINT_YMD ≥ (마감월+1)01 필터**로 합산(원장 무삭제·무재베이스).
- 즉 원장은 그대로 두고, **기초는 stock_close_snap·델타는 날짜필터**로 분리 → 이중계상 구조적 불가.

## 5. 원칙 (사고 재발방지)
- 게이팅은 **트랜잭션 내**(검증→INSERT 원자성). 원장(stock_ledger) **무삭제**([[feedback-nx-ledger-no-mass-delete]]).
- 현재고 정본 = **nx 단일원장 파생**(드리프트 구조적 불가). 컷오버 전 미적재분은 레거시 대사 보강.
- 규칙은 **공용함수 2개**로만 존재 → 전 프로그램이 같은 함수를 호출 = **동일 규칙 보장**(중복로직 금지).

---

## ★ 완제품 출고(ASY) 게이트 — 현황·측정 (2026-08-29 착수, 컷오버 필수 6번)

### 확인된 결함 — **출하가 음수재고를 통과한다**
`_finished_avail()` · `_finished_short_msg()` 는 `common.py` 에 **구현돼 있으나 아무도 호출하지 않는다**
(실측: 정의 외 참조 0건). 즉 완제품 게이트는 **함수만 있고 꺼져 있다.**

완제품을 차감하는 실제 경로:
| 경로 | 쓰는 곳 | 마감잠금 | **재고게이트** |
|---|---|---|---|
| 출하실적 `/api/lgsale/save` | `sales._lgsale_led_post` → `stock_ledger −ASY(tag J)` + `SA_T_STOCK_MAINT(tag J)` | ✅ `_assert_open` | **☐ 없음** |

⟹ `§0-★`(음수재고 예외 없이 차단)이 **완제품 축에서 뚫려 있다.** 자재·생산은 막고 완성만 안 막는다.

### ★측정 방법 주의 — 1차 측정이 틀렸다
처음엔 *"차단율 42.5% = 위험"* 이라는 결론을 냈다. **잘못된 비교였다.**
이미 일어난 출하가 **재고에 이미 반영된 뒤**의 값과 그 출하량을 비교했기 때문이다.
```
AJR75462802  출하 20  vs 가용 12   ← 출하 전에는 32 였다. 막힐 이유가 없다.
```
⟹ 비교 기준은 반드시 **출하 직전 재고**(`_finished_avail(item, asof=출하일 전날)`)여야 한다.

> **교훈**: 게이트 사전측정은 *"그때 그 시점에 이 게이트가 어떻게 판정했겠나"* 를 재는 것이다.
> 지금 재고로 과거 출하를 재면 **자기 자신을 빼고 비교**하게 되어 차단율이 부풀려진다.
> (`mat_stock_daily` 때 "오판 133건"이 손SQL 오류였던 것과 같은 부류의 실수다.)

### 재측정 결과 (집합 쿼리 · 출하 직전 재고 기준, 2026-08-29)
```
8월 출하 (출하일, 품목) 1,876건 · 산출 73초
   통과 1,509 · 차단 367 · 차단율 19.6%
   그중 직전재고가 **이미 음수**인 건 0
   직전재고가 **0** 인 건 219
```
(품목마다 `_finished_avail()` 을 부르면 3중 서브쿼리가 수백 번 돌아 15분에도 안 끝난다.
 같은 recipe 를 **한 번의 집합 쿼리**로 펴야 한다.)

### ★결론 — **지금 켜면 안 된다. 이유는 커버리지가 아니라 축(軸)이다**

차단 품목을 파고드니 원인이 명확했다.

| 실측 | 값 |
|---|---|
| 8월 출하 품목 | **591종** |
| 그중 **입고(+) 이력이 아예 없는** 품목 | **82종 (13.9%)** |
| 그 82종 중 **직납 전표(`out_wh_gubun='2'`)가 있는** 품목 | **82종 = 전부** |
| 그 82종 중 `P`+파트 입고가 있는 품목 | **0종** |

⟹ **직납품이다.** 협력사가 LG로 바로 보내 **우리 창고를 거치지 않는다.**
입고가 없는 게 정상이고, 따라서 **완성재고라는 개념 자체가 없다.**
여기에 재고 게이트를 걸면 **첫 출하부터 막힌다.**

> **이건 `§0-★`(예외 없음)과 충돌하지 않는다.** 자재 게이트에서
> *"생산창고(`PR_T_MAT_STOCK_WH`)는 백플러시 소비축이 아니다"* 라고 축을 구분한 것과 같다.
> **직납은 ASY 재고축이 아니다.** 예외를 두는 게 아니라, 애초에 그 축을 지나가지 않는다.

`AJJ30041902` 실측 — 출하 830 · `tag J` 차감 830 · 직납 전표 −873.
recipe 는 `− 직납` 이라 부호상 **더해져** 상쇄된다(이중차감은 아니다).
다만 그 보정이 **출하보다 늦게 쌓이면** 그 시점 재고는 0으로 보인다 → 오차단.

### ⟹ 컷오버 6번 항목의 실질 내용이 바뀐다
종전: *"`_finished_avail` 을 적용한다"*
실제: **"직납을 판별하는 규칙을 먼저 정하고, 그 다음 적용한다"**

| 선행 결정 | 왜 |
|---|---|
| **직납 판별 기준** | 품목 속성이 아니다 — `cost_gubun='5'` 는 82종 중 4종뿐이고 나머지는 거래 단위로 결정된다. **거래 기준**(직납 전표 존재/출하 유형)으로 판별해야 한다 |
| 판별 결과의 처리 | 직납 출하는 **−ASY 를 아예 게시하지 않는다** vs **게시하되 게이트만 면제** — 어느 쪽인지 결정 필요 |

### 켜는 순간 같이 지킬 것
1. **수정(update) 경로**: `lgsale_save` 는 기존 −ASY 를 지우고 재게시한다.
   지우기 *전에* 판정하면 **자기 자신 때문에 막힌다** → 삭제 후 판정하거나 `가용 + 기존 출하량` 으로 비교.
2. 차단 시 **왜 안 되는지** 알린다(`§0-★` — 품목·출하량·가용 명시).
3. 켠 뒤 **차단율을 다시 잰다** — 직납 제외 후 0 에 가까워야 정상.

### ★참고 — 완성 재고의 원천은 단일원장이 아니다
```
stock_ledger  ASY   입고(+)      0건            ·  출고(−)     7건 −7
sa_t_stock_maint    입고(+) 136,716건 5,879,148  ·  출고(−) 157,603건 −6,878,053
```
완성 입고는 **단일원장에 전혀 안 들어온다.** 그래서 `_finished_avail` 이
`sa_t_stock_maint`(레거시 미러)를 읽는 것이고, 이는 인계문서 §7-5 의
*"PRD/ASY 는 사실상 비어 있다"* 와 일치한다. **컷오버 후 이 축을 어떻게 세울지가 남은 숙제다.**

---

## ✅ 완제품 출고(ASY) 게이트 — **적용 완료** (2026-08-29)

### 최종 판정 — 켜도 안전하다
```
8월 완성 출고 (품목, 일자) 1,876건
   통과 1,875 · 차단 1 · 차단율 0.05%
   차단 1건 = MJU63357501 8/18 · 출고 94 > 재고 80 = **진짜 음수재고**
```

### ★가는 길에 네 번 틀렸다 — 같은 실수 반복 금지
| # | 잘못한 측정 | 나온 값 | 무엇이 틀렸나 |
|---|---|---|---|
| ① | 출하 **이후** 재고와 그 출하량을 비교 | 42.5% | 자기 자신을 뺀 값과 비교했다 |
| ② | 품목마다 `_finished_avail()` 호출 | (15분에도 미완) | 3중 서브쿼리 수백 번. **집합으로 펴야** 한다 |
| ③ | 직납 입고를 `출하일 이전` 만 누적 | 19.6% | **같은 날 입고**가 통째로 빠졌다 |
| ④ | 조정(tag 2)을 가용에 **더함** | 0.21% | `×(−1)` 로 담기므로 **빼야** 더해진다 |
| ⑤ | 표본을 `2502 기초` 큰 품목으로 선정 | 오차단 오판 | 기초만 크고 지금은 0. **현재 잔량**으로 골라야 한다 |

> 그 과정에서 *"직납이라 우리 창고를 안 거친다"* 고 단정했는데 **틀렸다.**
> **대표 정정**: *"절삭 직납이면 우리 창고에 들어와서 나간다. 협력사 차량이 LG 로 들어갈 수가 없다."*
> 화면 recipe 가 이미 그렇게 짜여 있었다 — `직납 자재출고(pu out_wh='2')` 의 `−qty` 를 **제품 입고**로 잡는다.

### 정본 정렬 — 화면과 같은 계산
`common._finished_avail()` 을 제품재고조회(`live_api.salesstock`) 와 **버킷까지 동일**하게 맞췄다.
```
기말 = 기초 + 입고 − 출고 − 조정
  입고 = 완성 P(in_part='') + (B,V) + **직납 자재출고의 −qty**
  출고 = 완성 (J,8,R) 의 −qty
  조정 = 완성 (2) 의 −qty
```
**전수 대조**: 화면 2,190품목 중 잔량≠0 **267품목 · 불일치 0건** (159초).

### 결선
| 위치 | 내용 |
|---|---|
| `sales._asy_gate()` | `_finished_short_msg` 호출 → 부족하면 400 |
| `/api/lgsale/save` 신규 | `_assert_open`(마감잠금) → **`_asy_gate`** → INSERT |
| `/api/lgsale/save` 수정 | `_assert_open` → **`_lgsale_led_del` 먼저** → `_asy_gate` → 재게시 |

> **수정 경로 함정**: 기존 `−ASY` 를 지우기 **전에** 판정하면 자기 자신이 이미 빠져 있어
> 정상 수정도 막힌다. 반드시 지운 뒤 판정한다.

### 사유 고지 (§0-★)
```
재고부족 — 5006AR4091G: 출하 11100 > 완성재고 10099
          (제품재고조회 기준: 기초+입고−출고−조정. 부족 1001)
```

### 검증 (롤백 서버 · 오염 0)
| 시나리오 | 결과 |
|---|---|
| 재고 0 품목 1개 출하 | ✅ 차단 |
| 재고 10,100 중 1개 출하 | ✅ 통과 (200) |
| 재고 10,100 중 11,100 출하 | ✅ 차단 (부족 1,001) |

**TestBed**: 프로브 `원장ASY` 신설 + 케이스 2종 추가 → **PASS 41 / FAIL 0 / SKIP 1 · 오염 0**
(종전 39 → 41)

---

## 순차 마감 — 완화 검토 후 **현행 유지**(2026-09-01 대표 판단)

**나온 상황** — 8/30 을 안 닫고 8/31 을 닫으려다 막혔다.
`close.py` 연쇄 가드: "직전 기간이 마감되지 않았습니다 — 마감은 순서대로 해야 합니다".
대표 우려: "미마감된 날짜에 새로운 전표와 반품을 발행할 수 있는데, 이럴 때 마감 안 된 날을 남겨놔야 한다."

**기술 확인 (풀어도 되는가)**
`_mv_base` 는 기초를 **"target 직전의 가장 최근 확정 스냅샷"** 으로 잡고 그 뒤 이동을 전부 fold 한다.
⟹ 8/30 을 건너뛰고 8/31 을 닫아도 **그 시점 숫자는 맞다**(8/28 기초 + 8/29~31 이동).

**그런데 그 다음이 문제다**
8/31 마감 후 8/29 에 전표를 추가하면 **8/31 스냅샷은 그 전표를 모른다** → 재고가 조용히 틀려지고,
9/1 이후는 8/31 을 기초로 쓰므로 **오차가 계속 전파**된다. 경고도 없다.
해법 자체는 간단하다 — **8/31 을 해제하고 다시 마감**하면 정확해진다. 다만 사람이 매번 기억해야 한다.

**결론 — 대표 판단으로 현행(순차) 유지.** "그냥 순서대로 마감할게. 나중에 변경사항이 생기면 알려줄게."

### ☐ 재개하게 되면 필요한 것 (순서대로)
1. 순차 가드 해제 — `close.py` 연쇄 가드
2. 미마감 구간이 있는 채로 마감하면 **그 사실을 note 에 기록·화면 표시**
3. **그 구간에 전표가 들어오면 이후 스냅샷을 '재마감 필요' 로 표시** ← 이게 있어야 안전하다
   (없으면 사람이 잊는 순간 틀린 재고가 남는다)
