# LME 과다적용 근본원인 규명 (2026-08-15)

## 결론 (한 줄)
LME 과다적용 ~6품목은 **엔진 버그가 아니라 `nx.bom_line`의 평탄화/중복 구조**(r_add_missing_edges류 손상)가 원인.
base(재료비)는 INNER_PROD 경계로 이 손상을 마스킹해 diff0이지만, `lme_total`의 별도 순회(cg5만 정지)가 손상을 그대로 노출한다.

## 증상 (260630, 실원가 오라클 대비)
| 품번 | 실원가 diff | base | LME(엔진/레거시) | 유형 |
|---|---|---|---|---|
| AJR30012101 | −605 | 정상(53,771 일치) | −1184 / −578 | 순수 LME 과다 |
| AJR30012102/103 | −606 | 정상 | 동일 | 순수 LME 과다 |
| AJR75563702 | −8,913 | 정상(34,644 일치) | −10046 / −1133 | 순수 LME 과다 |
| AJR30133607 | +12,825 | **+13,020 과다** | −1623 / −811 | base+LME 이중계상 |
| AJR30133707 | ~−140 | 근사 | −2319 / −1888 | 혼합 |

- **순수 LME형**: 중복엣지가 INNER_PROD=0(매입/외주완성) 노드 밑 → base는 매입가로 leaf 처리해 마스킹, LME만 관통 전개.
- **이중계상형**: 중복엣지가 INNER_PROD=1(사내) 노드 밑 → base도 이중 계상.

## 근본원인: nx.bom_line 평탄화/중복 (CS_M_ITEM_BOM 대비)
예 `AJR74482401-1`:
- CS(레거시 정본): `AJR74482401-1 → [AJR74482401]` (1자식) → `AJR74482401 → [AJR74482401-4-1, MJU63706901-01, RAC30599301-1]`
- nx(손상): `AJR74482401-1 → [AJR74482401-4-1, MJU63706901-01, AJR74482401]` (손자를 직접자식으로 끌어올림 + 실제자식도 유지)

→ 엔진 lme_total이 직접자식 AJR74482401-4-1을 타고 내부 동(MJU61882012 −538, MJU63706901-01 −67)까지 LME 가산.

## 스코프 (전수, 260814 기준)
- nx.bom_line "손자=직접자식" 중복패턴: **445 부모-자식쌍 / 247 부모품번** (대부분은 CS에도 있는 **정당한 구성** — 건드리면 diff0 깨짐)
- **스퓨리어스(CS에 없음, nx만 있음) = 13엣지 / 7 부모품번**, 그중 cs_calc_except=0 활성버그 = 10
  - 부모: `AJR74482401-1`, `AJR30133607`, `AJR30133707-SUB-1`, `AJR30133707-SUB-2`, (+3)
  - 목록: scratchpad/spurious_edges.json

## 왜 단순 수정이 안 되는가 (검증됨)
1. **스퓨리어스 엣지 단순 삭제 → base 붕괴.** AJR30012101에서 −28,517 소실. nx의 AJR74482401(cg='3')은 base가 leaf로 처리 → 실제 원가는 직접자식 경로가 담당. 직접자식 삭제 시 원가 소실.
2. **레거시 LME_CALC_FLAG 전파(SP line 186: 사내 노드 밑 LME OFF) 규칙을 nx 구조에 이식 → 불일치.** AJR30133607만 정확(d−0.1), 나머지 부분개선, 이미 diff0인 AJR77224002 회귀(d35). nx≠CS 구조라 규칙 이식이 깨끗이 안 됨.

## 정답 패턴 (AJR77224002 선례)
AJR77224002(MJC 이중계상)는 **중복엣지를 구조적으로 정리(삭제)해 nx를 CS와 맞춘 뒤, 엔진 무변경으로 diff0** 달성. 
→ 이 7개 부모도 **nx.bom_line 구조를 CS의 2계층 정본으로 복구**(평탄화 되돌림: 손자를 실제 부모 밑으로, cg/make_type 정합)해야 함. 엔진(lme_total) 로직은 정상이므로 손대지 않는다.

## 권고
- 이 7개 부모의 구조복구는 **sub_norm/R01 재구축(옆 세션)과 동일 성격** → 거기 합류 또는 부모 1개씩 타깃 복구 + 전수 회귀(2826품목 diff0 게이트).
- 절대 하지 말 것: 정당한 445중복(CS에도 있음) 삭제, lme_total 경계 임의 변경(회귀 유발).

## 엔진 LME 규칙 후보 전수검증 (2026-08-15, 결론=단순규칙 없음)
레거시 LME는 #TEMP_BOM→#TEMP_RESULT의 CUM_MAT_CODE 집계 bottom-set에 종속. 단순 순회규칙으로 재현 시도 모두 실패:

| 규칙 | AJR30012101 | AJR30133607 | AJR30133707 | AJR75563702 | 2826 회귀 |
|---|---|---|---|---|---|
| orig(cg5만 정지) | d−605 | d−811 | d−430 | d−8912 | 기준 |
| nested-made 차단 | **d0** | d−811 | d+1888✗ | d+961 | — |
| cg='3' 밑 차단 | **d0**(복구시) | d0(복구시) | d0(복구시) | d+788 | **회귀126/개선12** ✗ |

- **cg='3' 밑 LME 차단 규칙 = 폐기.** 138품목 변경 중 **회귀 126**(통과품목 파괴), 개선 12뿐. 레거시는 대부분 cg='3' 노드 밑 동 LME를 정상 계산함(AJR30012101은 우연 일치).
- **구조복구 효과(복구/orig엔진)**: AJR30133607(+12825→0)·AJR30133707(0) = base 이중계상형만 해결. AJR30012 계열(−538)·AJR75563702(−8912, 구조는 CS와 동일=순수 LME로직)는 **미해결**.
- **AJR75563702는 구조 정상**(CS와 RAC만 차이). 순수 LME-로직 잔차 = #TEMP_RESULT bottom-set 미묘함.

## 최종 결론 & 권고 (갱신)
1. **구조복구(7부모→CS)**: 정당·안전(nx→CS). base 이중계상형(AJR30133607류) 해결. 단 nx.bom_line은 소요/세트/kitting 공유 → 광범위 회귀검증 + sub_norm 세션 조율 필요. **엔진 무변경**.
2. **순수 LME-로직 잔차(~4품목: AJR30012101/102/103·AJR75563702)**: 단순규칙 부재 확정. **레거시 #TEMP_BOM+#TEMP_RESULT LME 로직 충실 재현**만이 정답(엔진 대공사·전수회귀 필수). ~4품목·LME성분만 → 컷오버 후 전담 태스크 권고, 또는 승인후 착수.

## 적용된 수정 (로그)
- **2026-08-16 AJR30133607 [nx이관차] 정리(사용자 승인)**: nx.bom_line bom_id=2410의 스퓨리어스 3엣지(MJC62301702 seq12·MJU00777604 seq6·MJU00777605 seq3, 전부 remarks='드리프트복원') **삭제**. 손자를 직접자식으로 끌어올린 중복(CS엔 없음, 진짜 SUB AJR30133604-12-1=정상). 결과: **재료비 +13,020 이중계상→+1.8, LME −811.7 정상화(d−0.1)**. 회귀검증: SUB d−0.1·AJR30133604 d+1.7, 삭제가 가공 0.0영향(무회귀). 백업 scratchpad/del607_backup.json. 플래그: cs_calc_except=0(원가 이중)·except_flag=1(소요 이미제외)·set_except=0. **잔차 가공비 −169는 삭제 무관 별개(④ 가공비차 도메인)**.

## 엔진-on-CS 전수 스캔 (진행중, ②크기 확정용)
"엔진이 CS_M_ITEM_BOM 직독 시 레거시와 일치하는가" = 순수 로직 갭 격리. 개별검증: AJR30133607/707/77224002=일치(d≈0, [nx이관차]) / AJR30012101계열(−538)·AJR75563702(−8912)=**CS로도 불일치=엔진 로직 갭**. 전수 스캔으로 로직 갭 총 품목수 확정중(scratchpad/engcs_sweep.csv).

## ★두 번째 엔진 갭: 용접 가공비 (2026-08-16 규명)
"6개 LME 품목" 파던 중 **별개의 엔진 갭** 발견 — 가공비(용접).
- **AJR30133707**: 실제 nx 가공 엔진=3,181 vs 레거시=5,471 (**d−2,290**). 노드별로 RAC(용접봉)·용접링 노드가 레거시 가공비를 나름.
- **근본**: 엔진 silwon 가공비(`proc_amt` line 215)는 **nx.routing만** 읽고 **proc_weld의 weld_st(용접표준시간)를 안 읽음**. AJR30133707의 SUB들은 nx.routing이 **비어있고**(공정 0) 용접이 **proc_weld에만** 존재(weld_st: 본품2·SUB-1 3·SUB-2 15). → 용접 가공비 통째로 누락.
- 용접 가공비는 별도 nx.bom 경로(engine line 842 `_weld_parts`)에만 구현됨(내부원가/LG BOM용). **실원가 silwon 경로엔 미반영.**
- **분류**: [[newerp-weld-cost-split]] "용접행위=가공비(용접ST×원단위)" 재설계의 **미완 구현** (proc_weld 데이터는 있으나 silwon 엔진이 안 씀) + 일부 SUB는 routing 이관갭([[newerp-gagong-cost-structure]] "일부SUB 이관갭"의 잔여).
- **드리프트 밴드에이드 상호작용**: AJR30133707은 재료 +4,420(드리프트 스퓨리어스) + 가공 −4,559(용접누락)이 상쇄돼 현재 d−139. 드리프트만 지우면 가공갭 −6,443 노출 → **용접 가공비 먼저 풀어야 diff0.**

## ★최종 분류 — "6개 LME 품목"의 진짜 정체
| 요인 | 품목 | 성격 | 조치 |
|---|---|---|---|
| LME 엔진갭(매입완제품 관통) | AJR30012101/102/103·AJR75563702 등 ~23품목(≥1원, 스캔) | 엔진로직 | lme_total 매입경계 정지 규칙(정밀·회귀검증) |
| 용접 가공비 누락 | AJR30133707·AJR30133607(−169) 등 | 엔진 미완구현(용접재설계) | silwon proc_amt에 weld_st×임율 반영 |
| 드리프트 스퓨리어스(재료 이중) | AJR30133607(완료)·기타 | [nx이관차] 데이터 | CS정렬(용접갭 없는 것만 단독 안전) |

## ★LME 갭 전수 확정 (2026-08-16 밤, engine-on-CS 완주)
**engine-on-CS 전수 스캔 완료: 2,826 중 LME 엔진갭 = 41개(≥1원), 13개(≥100원).** (순수 로직 갭, 데이터 노이즈 없음)

**13개 ≥100원 = 최소 3개 다른 근본원인 (이질적):**
1. **11개 = 정확히 −538.47** = MJU61882012(동) via AJR74482401. AJR74482401=make_type1·매입가22980·in_cust2096(외주완성)인데 그 내부 4-1(INNER=0,매입가28000) 밑 동에 LME 오적용. 레거시는 4-1에서 정지(base도 정지, lme_total만 관통). 품목: AJR30012101/102/103·AJR74462301/302/303·AJR74964701·AJR74985201/202/204·AJR74482401-7-1.
2. **AJR75563702 = −8,912 [nx이관차 데이터버그·확정]**: `-6-1` 변형 동(MJU65030906-6-1 등 8개)이 **nx.item.wt=1.0(플레이스홀더)** — 진짜중량 아님(base MJU65030906=0.054). LME=metal_sub×1.0=−1212.6(정상 ×0.054=−65). **변형 동 중량 오적재.** 고칠수있음(진짜중량=diam/thick/length 계산). ★스코프: `-6-1`류 변형 동 wt=1.0 전수 점검 필요.
3. **AJR77223106 = −236.79(레거시=0) [거래처별·확정]**: 동 MJU66795001·MJU66494401(**in_cust=2067**)에 엔진 LME 매김, 레거시 0. 2067이 유상사급(LME) 대상 아닌 듯. 레거시 line810 CUST_CODE·사급단가 조건 관련. STS 3개(레거시=0)도 유사.

**★규칙 후보 전부 회귀(검증 확정, 적용 안 함):**
| 규칙 | ≥100 해결 | 전수 회귀 |
|---|---|---|
| nested-made(사내밑 차단) | 7/35 | AJR30133707·AJR74524 등 악화 |
| cg='3' 밑 차단 | (복구시) | **회귀 126** |
| INNER=1+매입가 밑 차단 | 11/13 | **회귀 113** (타깃 248노드=대부분 진짜 사내) |

→ **단순 순회규칙으론 불가 확정.** 레거시 LME 경계는 #TEMP_RESULT bottom-set에 종속(context-dependent), 앵커플래그로 안 잡힘. **유일한 확실 해법 = 레거시 #TEMP_BOM+#TEMP_RESULT LME 충실 재현**(대공사). base 원가는 41개 전부 정상, LME 성분만 상이.

**권고**: 41개(mostly tiny·13 실질) 소수 + 이질적 → ①AJR75563702류 데이터 수정([nx이관차], 변형 동 중량) ②AJR74482401류는 충실재현 필요시 착수 ③소액/STS는 잔차 문서화. 컷오버는 base 정상이라 안전, LME 성분 41품목 잔차 등록.

## 재현 도구 (scratchpad)
- `lmetrace.py` LME 가산 항목 추적 / `legtree.py` 레거시 SP 트리 / `csvsnx.py` CS vs nx 엣지대조 / `scope2.py` 스퓨리어스 전수 / `lmecalc.py` 전파규칙 테스트
