# LG사급현황 · 사급부품 월별수불 — 금액축 정본 (2026-08-30)

> **대상**: `GET /api/lgsagub/recvcompare_parts_ledger` · 화면 `SCREEN.lgsagub` 「리시빙비교(부품)」 좌측 표
> **한 줄**: 금액축 = **`nx.price_item`(매입·거래처 LG) 적용일 as-of**. 입고·소요·기말을 **같은 단가**로 평가한다.
> 관련 [[newerp-lgsagub-recvcompare]] · `DO_NOT_USE_FIELDS.md §18` · `00_MASTER_INDEX.md §0`

---

## 1. 무엇이 문제였나 (대표 제보)

> "소요보다 입고가 대부분 많은데 왜 금액이 마이너스로 나오는지 확인을 해줘."

기말**수량**은 +인데 기말**금액**이 −로 나왔다.

| 월 | 기말수량 | 기말금액(당시) |
|---|---:|---:|
| 2602 | 3,098 | **−70,666,529** |
| 2604 | 18,908 | −158,012,778 |
| 2606 | 4,532 | −222,844,911 |

## 2. 원인 — 금액의 축이 둘이었다

```
입고금액 = 그 달 OSP 실제 청구금액          ← 실제단가(월마다 다름)
소요금액 = 소요수량 × 전기간 OSP 평균단가    ← 고정 평균단가
```

**수량은 한 축인데 금액만 두 축**이었다. 월별 실제단가는 전기간 평균과 **−4.0% ~ +8.5%** 벌어진다(실측).
초반 달은 입고금액이 과소 계상되고 그 차이가 **잔액에 누적**돼 마이너스로 눌렸다가,
8월에 뒤집혀 +243,875,726 으로 수렴했다. **전기간 누적은 원래 양수 = 데이터 결함이 아니라 계산 축 결함.**

## 3. 고침 — 매입가격 단일 축 · 대표 확정

> 대표: "이 금액축은 매입 기준으로 가야할 것 같아. **매입장의 마감 기준**으로 계산을 해야할 것 같아."
> "**매입가격**을 해야 해. 그냥 **매입실적**을 기반으로 보는 거라니깐"
> (단가 화면을 짚으며) "이 가격창은 **사급가격이 매월 찍히는 것** 같은데"

```
단가 = nx.price_item (price_type='매입', vendor_code='LG')  적용일 as-of 최신 1건
입고금액 = Σ(월 입고수량 × 그 단가)
소요금액 = Σ(소요수량   × 그 단가)
기말금액 = Σ(품목별 잔량 × 그 단가)
```

### ★소스를 두 번 틀렸다 — 같은 실수 반복 금지
| 시도 | 무엇 | 왜 틀렸나 |
|---|---|---|
| 1차 | OSP 전기간 평균단가 | 입고는 실제금액인데 소요만 평균 → **축이 둘** |
| 2차 | `close._mv_buyprice` (레거시 매입전표 as-of **누적**) | ① 누적이라 **과거 매입까지 섞임**(2608 실측 8,395 vs 당월 9,539, −12%). "OSP vs 매입 4.3% 괴리" 라고 보고했던 것도 실은 이 창 문제였다.<br>② 소스가 레거시 `PU_T_STOCK_MAINT` — `00_MASTER_INDEX §0 규칙1` 이 **직독 신규 금지**(컷오버에 죽는 코드) |
| **확정** | **`nx.price_item`(매입·LG) 적용일 as-of** | `DO_NOT_USE_FIELDS §18` 이 지정한 **단일 소스**. 적용일 시계열이라 창이 정확하고 컷오버에도 산다 |

**교훈**: 단가를 다룰 때는 **§18 유일 소스표부터 본다.** 대표가 화면(품목단가 관리)을 짚어 주기 전까지
레거시 전표를 뒤지고 있었다 — 기록을 먼저 읽었으면 한 번에 갔다.

### 실측 커버리지·정확도 (2026-08-30)
- 사급부품 **243/243종** 전부 `price_item` 에 존재(거래처 `LG`, 625행 시계열)
- OSP 청구단가 대비 **−0.6% ~ +4.8%** (누적 매입전표의 −12% 왜곡이 사라짐)
  2602 8,898→8,843(−0.6%) · 2604 8,595→8,608(+0.2%) · 2608 9,563→9,510(−0.6%)
- **폴백 없음** — 없으면 0 으로 드러낸다(§18). 응답 `px_miss`/`px_miss_items`.
  실측: 2602~2606 각 1종, 2607~2608 0종.

## 4. 품목별로 굴린다 (총계 굴림 폐기)

전에는 월 **총계**(수량합)로만 굴렸다. 품목별 잔량은 +/−가 섞이고 단가가 품목마다 크게 달라
**총수량은 상쇄돼도 금액은 상쇄되지 않는다.**

```
2608 기준
  잔량 +  (131종)   +68,760개   +649,495,265
  잔량 −   (90종)   −41,600개   −379,207,957
  ─────────────────────────────────────────
  기말              +27,160개   +270,287,308
```

⟹ 숨은 마이너스를 `neg_cnt` / `neg_qty` / `neg_amt` 로 **드러낸다**(§1-9-1 — 만들지 않고 드러낸다).
화면에 「잔량−종 / 잔량−수량 / 잔량−금액」 3컬럼 추가.

> **참고**: 품목별 굴림과 총계 굴림은 **수학적으로 동일**하다(고정단가·선형).
> 굴리는 방식이 문제가 아니라 **단가 축**이 문제였다. 품목별 굴림은 `neg_*` 를 뽑기 위한 것이다.

## 5. 검증 (2026-08-30 · dev 8012 실API · §7 시점규칙까지 반영한 최종값)

| 월 | 기말수량 | 기말금액 | 잔량−종 | 잔량−수량 | 잔량−금액 |
|---|---:|---:|---:|---:|---:|
| 2602 | 4,580 | **+31,932,774** | 79 | −14,487 | −96,648,511 |
| 2603 | 4,156 | −36,972,944 | 90 | −22,513 | −186,298,759 |
| 2604 | 21,395 | **+115,845,363** | 74 | −19,012 | −118,202,291 |
| 2605 | 22,913 | **+217,546,516** | 77 | −19,121 | −100,021,735 |
| 2606 | 8,626 | **+69,553,802** | 91 | −28,779 | −209,413,512 |
| 2607 | −9,147 | −97,786,406 | 91 | −53,156 | −457,183,949 |
| 2608 | 31,908 | **+297,251,263** | 80 | −37,362 | −354,445,022 |

- **불변식(기초+입고−소요=기말, 수량) 위반 0건**
- **부호 정합(숨은 마이너스 없으면 수량+ ⇒ 금액+) 위반 0건**
- **단가없음(`px_miss`) 전월 0종**

착수 시점 대비: 2602 −70,666,529 → **+31,932,774** · 2604 −158,012,778 → **+115,845,363**
· 2606 −222,844,911 → **+69,553,802**

### 남은 마이너스 2개는 결함이 아니라 사실이다
- **2607** — 기말**수량 자체가 −9,147개**. 수량이 −면 금액도 −가 맞다.
- **2603** — 수량 +4,156개인데 금액 −36,972,944. 숨은 마이너스 90종 때문이며 `neg_*` 로 드러난다.

## 6. 함께 제거 — 소요엔진 위반 잔재 (CLAUDE.md §1-10)

`_parts_maps` / `_explode_parts` = `CS_M_ITEM_BOM` **ad-hoc 재귀**. EBF64570401 을 2배 계상했던 그 코드다.
`recvcompare_parts` 가 `ch, sg310 = _parts_maps(cur)` 로 부르고는 있었으나 **결과를 전혀 쓰지 않았다**
(CS_M_ITEM_BOM 전체를 모듈캐시에 올리기만 하는 무용 호출). 전부 제거.
소요는 두 엔드포인트 모두 **통일 소요엔진 `nx_soyo_engine.sagub_parts_soyo`** 만 쓴다.

## 7. 사급 전환 시점 (point-in-time) — 대표 확정 2026-08-30

사급부품 판정이 **"전 기간 OSP 에 한 번이라도 등장하면 사급"** 이라 시간 개념이 없었다.
8월에 처음 사급이 된 품목의 **2~7월 소요까지 사급 소비로** 잡혀 마이너스가 났다.

```
MJX62771713 의 OSP 입고
  2602 2603 2604 2605 2606 2607 2608
     0    0    0    0    0    0  190   ← 8월에 처음 사급 전환
  소요 1,812 (2~7월 1,678 포함) → 잔량 −1,622
```
그전엔 우리가 둔안코리아에서 사서 쓰던 것이다(대표매입처·자재재고 45가 증거).

**고침**: `osp_first`(품목별 첫 OSP 입고월)를 만들어 **전환 시점 이전 소요는 계상하지 않는다.**
★원소재(TUBE) 쪽은 이미 원단위 `eff_ym` 으로 같은 point-in-time 을 한다 — **부품만 빠져 있었다.**
★계상만 빼면 정확하다: 실측 **사급부품이 다른 사급부품을 자식으로 갖는 BOM 행 0건**
  (walk 를 계속해도 그 밑에 사급 자손이 없다). stop_set 을 월마다 바꿔 재전개할 필요 없음(성능 유지).

| | 종수 | 수량 | 금액 |
|---|---:|---:|---:|
| 적용 전 | 90종 | −41,601 | −379,207,957 |
| **적용 후** | **80종** | **−37,362** | **−354,445,022** |

부수효과: `px_miss` 가 전월 0 이 됐다(전환 전 기간에 단가를 찾던 것이 사라짐).

---

## 8. 남은 마이너스 80종 = **입고 시차** (BOM 오류 아님 · 규명 완료)

### 8-1. 잘못 짚었던 진단 — 취소 (같은 실수 반복 금지)
처음엔 "같은 Assy 에 대체 품번이 병렬 등록돼 소요가 2배"라고 진단했으나 **틀렸다.**
레거시 개발품목BOM관리 역전개가 반증이다:

| | 우리 `v_pr_bom` 상위 | 레거시 역전개 | 차이 |
|---|---:|---:|---|
| MJX62771704 | 46곳 | **41곳** | 5곳 = `AJR30007102~06` |
| MJX62771713 | 5곳 | **5곳** | 0 |

레거시도 `EXCEPT_FLAG=1` 인 그 5곳을 뺀다. 즉 **그 Assy 의 실사용 자식은 713 이고 우리 소요 엔진이 정확**하다.
플래그 엇갈림(전 6,577 상위 중 188곳, 사급끼리는 11곳)은 **두 플래그의 목적이 원래 다르기 때문**이지 오류가 아니다.
  `EXCEPT_FLAG` = 전개제외(조달 귀속) / `CS_CALC_EXCEPT_FLAG` = 원가 중복 방지
  (감사 스크립트 `_migration/audit_bom_flag_crossover.py` 로 재현 가능)

### 8-2. 실제 원인 = 입고 시차 (실측)
부족 상위 4종(전체 −37,362 중 63%)의 **월별 입고/소요 비율**:

| 품번 | 2602 | 2603 | 2604 | 2605 | 2606 | 2607 | 2608 |
|---|---:|---:|---:|---:|---:|---:|---:|
| MJX65072203 | 108% | 107% | 111% | 119% | 65% | **20%** | 110% |
| AJU32452009 | 123% | 82% | 95% | 105% | 62% | 94% | 123% |
| RAA35197201 | 83% | 101% | 64% | 121% | 91% | **0%** | 143% |
| MJX65072201 | 146% | 83% | 106% | 88% | 68% | 53% | 138% |

BOM 이 틀렸다면 매달 비슷한 비율(예: 90%)로 나와야 한다. 실제로는 **20~146% 로 널뛰고
6~7월 급락 → 8월 110~143% 되받기**가 4종 공통이다. ⟹ **입고 시차.**
`2607 −53,156개 → 2608 −37,362개` 감소도 같은 신호.

### 8-3. 1대당 소요 검증 — 이상 없음
밸브 3종은 **완제품 전부 1대당 1.0** 으로 일관.
레진 `RAA35197201`(KG 단위)만 0.135 / 3.78 / 0.54 로 갈리는데 **같은 뿌리의 단계별 값**이다:
```
RAA35197201 (KG)
  └ 4778A20001A  Leg,Install                 1개당 0.135 kg
       └ AET73671523  Install Part Assembly  28개 → 3.78 kg
            └ PQ061208C41.AKOR (완제품)       → 0.54 kg
```
소요 40,028kg 의 **95% 가 슬림 인버터 설치부품**(PQ0612/PQ0615). 구조·산수 모두 정상.
★레진은 **LG 사급을 받아 협력사에 출고**해 사출시키므로(대표 확인) 구조적으로 시차가 더 길다.

### 8-4. 결론
남은 마이너스는 **손실이 아니라 "아직 안 들어온 사급 재고"**다.
원소재(TUBE) 쪽에서 "매월 3억 차이 = 손실 아니라 동 재고 잔량"으로 결론난 것과 같은 성격
([[newerp-lgsagub-recvcompare]]). 화면 `neg_*` 로 드러내되 **결함으로 취급하지 않는다.**
읽는 법: 다음 달에 줄면 시차, 계속 커지면 그때 파본다.
