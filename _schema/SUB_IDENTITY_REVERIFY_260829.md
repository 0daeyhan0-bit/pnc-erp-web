# SUB 정체성 "287" 재검증 (2026-08-29, feat/sub-name-rnn-snn Step B)

> 목적: SUB 명명 `{ASSY}_R{route}_S{nn}` 정본화 착수 전, 정본설계 §8-5의 **"공용 조립SUB 코드 294 → 정체성 287"** 이 지금 데이터로 재현되는지 독립 재측정.
> 관련: `SUB_CODE_MASKS_REAL_ASSY.md §8-5` · 메모리 [[newerp-sub-name-registry]]. 도구=scratchpad/sub_verify287.py·sub_sens.py(읽기전용).

## 방법론 (§10 준수)
- 스코프 = **납품제품(nx.sale_dtl, sale_ymd>=250101) = 2169품목** 재귀전개.
- edge 소스 = **엔진 `NxCostEngine.lines()`**(정본 BOM 소스·소요량 아님·구조 identity만, ad-hoc CTE 금지 준수). 용접=`_weld_lines`(RAC 분리).
- 조립SUB = BOM 보유 AND 비RAC 직속자식 ≥2. (§8-5: 단일가공 자식≤1·원소재 제외)
- 공용 = 서로 다른 부모(품목) ≥2 에서 참조.
- 정체성(재귀) = 구성(자식키+수량[+자식 make_type]) + 용접[+본인 make_type]. 자식키 = 자식이 조립SUB면 그 재귀 정체성, 아니면 `L:`+코드.

## 실측 결과 (문서 294/289/287과 **불일치**)
| 필터 | 공용 조립SUB 코드 | 정체성(make_type無) | 정체성(+make_type §8-5) |
|---|---|---|---|
| make_type 필터 없음 | **344** | 337 | 341 |
| **매입완성품 제외**(node make_type∈{제작1,외주2}) | **307** | 302 | **304** |
| 문서 §8-5 (2026-08-26) | 294 | 289(레지스트리) | **287** |

- 매입완성품 37 제외 내역: 공용 BOM有+자식≥2 = 344 중 make_type **외주2=229·제작1=78·구매3=28·외주직납5=7·공백2**. → {1,2}만 SUB로 307, {3,5} 37 제외(예 `MJU65517915+용접링`(3)·`AJR33295151`(3)·`AAA31179501`(5)).
- 정본 복제군(코드>1 같은 정체성) = **3군 6코드**만 발견(`5211A22074A/-2`·`AJR30012008/AJR30012011-SOCKET`·`AJR30077802-20-1/AJR30117902-F&T-1`). 문서는 7복제쌍(294→287) → **내 측정이 복제를 덜 잡음**.

## ★결론 — "287"은 지금 데이터로 견고하게 재현 안 됨 (정의 민감)
독립 재측정 = **307코드→304정체성**(문서 294→287과 13~17 차이). 방향도 다름(make_type 추가는 분할=증가, 문서 289→287은 감소 뉘앙스). **이 갭은 "결함"이 아니라 정의 미확정**이며, 정확히 기록이 경고한 현상(§8-5 "측정 4597→517→458→294→289→287 계속 틀림, 검증없는 단정 반복이 잘못").

### 갭의 3대 원인(=착수 전 확정 필요)
1. **납품 스코프 소스**: 나=sale_dtl distinct 25.01~(2169, 사급/vir 포함 가능). 문서=미상(LG 완제품만? 1361 납품Assy?). → **top 정의 확정**.
2. **★구분(make_type) 소스 신뢰불가**: sourcing.py:591 명시 "nx.item make_type 신뢰불가(MJU='2'·부자재='3' 역전)→cost_gubun 사용", `_mk5`는 make_type 없으면 has_bom로 대체. 정체성 축인 "각 노드 구분"을 raw make_type으로 잡으면 SUB 필터·시그니처가 흔들림(외주2=229의 상당수가 실제 부품일 수 있음). → **구분 소스 확정(_mk5 vs cost_gubun vs make_type)**.
3. **정체성 정규화**: 리프=raw코드 vs 자식SUB=재귀. 벤더변형(-F&T/-19-1)을 리프로 두면 구조동일이 갈림. → **리프 코드 정규화 규칙 확정**.

## 다음 (A 착수 전 게이트)
- ⓐ `_sub_signature`에 make_type 추가 / ⓑ 레지스트리 재구축 **하기 전에 위 3정의를 사장님·기록으로 확정.** 안 그러면 재구축이 또 다른 숫자(304? 341?)를 낳음.
- 재구축의 목표는 "287 숫자 맞추기"가 아니라 **정의가 고정된 재현가능 파이프라인**(같은 입력→같은 코드수). 숫자는 정의의 결과.
- 원가 diff0·생산계획 미접촉·옆에짓고·배포 승인후 불변.
