# TestBed 종합 결과 — 2026-09-06

> 전부 **롤백 모드**로 돌렸다 — DB 오염 0. 라이브 `PARTNER_ERP` 는 읽기만 했다.
> 원본 = `_schema/TESTBED_RESULT_260906_1501.txt`(전체 144) · `_schema/TESTBED_RESULT_260906_1457.txt`(전구간 37)

---

## 0. 한 줄 요약

| 실행 | PASS | FAIL | SKIP |
|---|---:|---:|---:|
| 전 구간(E+F+G) 읽기 | 33 | 4 | 0 |
| 전체 스위트 | 105 | 8 | 31 |
| **★쓰기 실행 — 준비등록·바코드실적** | **11** | **0** | 0 |
| **★쓰기 실행 — 사급 루프** | **8** | **0** | 0 |

**대표님이 지시한 "전체적인 흐름"을 실제 API 로 태워서 전부 통과했다.**
읽기 FAIL 8건 중 코드 결함으로 볼 것은 **F2 하나뿐**(내일 대표님 실물 테스트 예정).

---

## 1. ★쓰기 실행 — 생산준비등록 → BOM 재고이동 (PASS 5)

`/api/ready/commit` · 도번 `AJR77144201` · 파트 `P0002` · 1세트

| 재고점 | 변동 | 판정 |
|---|---:|---|
| 자재창고 `PU_T_MAT_STOCK_WH` | **−12.00** | BOM 소요만큼 출고 ✅ |
| 자재수불 `PU_T_STOCK_MAINT` | **−12.00** | tag `B` 음수 ✅ |
| 파트재고 `PR_T_MAT_STOCK_WH` | **+12.00** | 생산파트로 이동 ✅ |
| 준비재고 `PU_T_READY_STOCK` | **+1.00** | 세트 1 ✅ |

→ **BOM 기준 재고이동이 실제로 일어난다.** 자재 5종이 소요량대로 빠져 파트재고로 옮겨갔다.

## 2. ★쓰기 실행 — 생산 바코드 실적 (PASS 6)

`/api/gagong/barcode/register` · BOX_NO `82267` · 양품 1

```
생산재고  MJU65533802      +1
가공창고  MJU65533802      +1
원소재    MJU66885911-3M   -0.7923   ← 중량 기준 차감
+ PU_T_CUT_DTL 이력 1행 · PR_T_INDI_CUTTING PROD_FLAG='1'
```

→ **재고 차감(−)과 완제품 증가(+)가 동시에 일어난다.**

## 3. ★쓰기 실행 — 사급 루프 (PASS 8)

`setin/issue`(납품발행) → `setstock/receive`(바코드입고) → 재고파생·사급소진
업체 `2048` · 도번 `AJR77225304` · sheet `901170` · 2개

| 재고점 | 변동 | 판정 |
|---|---:|---|
| 세트원장 `nx.set_stock_maint` | **+2.00** | 세트재고 증가 ✅ |
| 단품(자재창고) | **+18.00** | 자도번 9종 파생 ✅ |
| **웹 사급원장 `nx.sagub_maint`** | **−12.00 (5행)** | **사급재고 소진 ✅** |

→ 대표님이 말씀하신 **"세트재고 +, 단품재고 +, 사급재고 −"가 그대로 일어난다.**

### ⚠ 여기서 함정 2개를 밟았다(기록)

**① 사급 측정축이 두 개다.** 처음엔 `PU_T_SAGUB_STOCK`(레거시)만 재서 "사급이 안 줄어든다"고 FAIL 을 냈다.
바코드 입고 경로는 `_post_sagub_out`(setin.py:32)이 **`nx.sagub_maint`(웹 사급원장)** 에 쓴다.
수동입고(146) 경로만 `_apply_sagub` 가 `PU_T_SAGUB_STOCK` 을 MERGE 한다(setin.py:410).
→ **경로마다 사급 테이블이 다르다.** 축을 틀리면 정상을 결함으로 오판한다.

**② 시료가 사급품이 아니면 당연히 0이다.** 첫 시료 `AJR77282405` 는 `bom_sub.sagub_flag='1'` 자재가 **0종**이었다.
→ 시료 조건에 **그 API 가 실제로 소비하는 재료**를 넣어야 한다([[testbed-write-cases]] 규칙1 재확인).

## 4. 검사승인 경로 — 미검증(데이터 없음)

현재 발행대기(status 00) **1,198건이 전부 `insp_flag='0'`(무검사)** 다.
유검사 건이 하나도 없어 `/api/setinsp/complete` 를 태울 수가 없었다.

읽기 케이스 G3 도 같은 이유로 FAIL —
검사대기(status 30) 35건이 **전부 260831 하루**에 있고 그 날짜는 이미 일마감이라 `HTTP 400` 이 난다.
**마감잠금이 제대로 작동한 것**이지 코드 결함이 아니다.

⟹ **검사승인은 내일 대표님이 유검사품으로 실제 납품→입고를 만들어야 검증된다.**

---

## 5. 레거시 흐름 대조 — 같은 테이블·같은 태그·같은 부호

| 단계 | 레거시 화면 | 웹 API | 일치 |
|---|---|---|:--:|
| **생산준비등록** | `w_pr_input_460_new` | `/api/ready/commit` | ✅ |
| · 자재창고 − | tag `B` 음수 (1,165,171행) | tag `B` 음수 | ✅ |
| · 파트재고 + | `PR_T_MAT_STOCK_WH` | 동일 | ✅ |
| · 준비재고 + | `PU_T_READY_STOCK` | 동일 | ✅ |
| **생산 바코드 실적** | `w_pr_input_018` | `/api/gagong/barcode/register` | ✅ |
| · 원소재 −(중량) | `PU_T_MAT_STOCK_WH` | 동일 | ✅ |
| · 가공창고/생산재고 + | `PU_T_MAT_STOCK_WH`·`PR_T_MAT_STOCK_WH` | 동일 | ✅ |
| · 절단이력 | `PU_T_CUT_DTL` (12,490행) | 동일 | ✅ |
| **세트입고(사급)** | `w_pr_input_135`·`w_pu_stock_146` | `/api/setstock/receive` | ✅ |
| · 세트입고 tag `S` | 313,298행 / 22,214,038 | 동일 태그 | ✅ |

## 6. 재고 흐름 최신성 — 전 구간 살아 있다

| 구간 | 총건수 | 최종일자 | 최근 7일 |
|---|---:|---:|---:|
| ①계획 | 4,436 | 261006 | 4,436 |
| ③자재입고 | 1,780,541 | 260915 | 13,981 |
| ④준비(키팅) | 947,957 | 260904 | 7,511 |
| ⑤세트입고 | 4,491 | 260902 | 342 |
| ⑥생산파트수불 | 1,394,386 | 260904 | 8,505 |
| ⑦생산실적 | 372,209 | 260904 | 1,813 |
| ⑧영업수불 | 666,969 | 260904 | 2,691 |
| ⑨웹원장 | 179,690 | 260906 | 7,240 |

F10 전 구간 연결도 PASS —
`①계획 4,436 → ②소요 105,468 → ③입고 457,262 → ④준비 947,957 → ⑥이동 1,187,900 → ⑦생산 372,209 → ⑧차감 1,365,055 → ⑩출하 307,778`

---

## 7. 오늘 바꾼 재고화면 — 전부 정상

2026-09-06 작업 = 재고화면 7종 웹(nx) 기반 전환 + 원장 오프닝밸런스 6,289행.

| 케이스 | 결과 |
|---|---|
| G6 자재재고 / G7 생산파트 / G8 영업 / G9 세트 | 300 · 2,731 · 306 · 4,151행 ✅ |
| E14 재고 3곳 정합 | 원장 176,854 · 수불장 1,780,541 · 재고 7,771 ✅ |
| E14b 재고 버킷 | 중복 0 · CUST_CODE 전부 Z99990 ✅ |

## 8. 레거시 대사 — 둘 다 diff 0

**미래정밀(2096) 자재세트입고현황** · 기준 260906
- 레거시 `PU_T_SET_MAT_STOCK` 603행 / −539,991 ↔ 웹 미러 603행 / −539,991 → **일치**
- 웹 화면 1,148행

**그린산업(주)김해공장(2345) 자재입고진행현황** · 기준 260906
- 레거시 입고수불 2,555행 / 2,196,542 ↔ 웹 미러 2,555행 / 2,196,542 → **일치**
- INPUT 1,625행 / OUTPUT 1,285행 — 340행 차이는 정상(OUTPUT=순수 업로드일자, INPUT=당김계산 반영)

> ⚠ 대사 시 기준일 주의 — `nx.plan_dtl` PLAN_YMD 최대값은 **261006(미래 계획일)**.
> 이걸 기준일로 넣으면 조회가 0행이 된다. 화면 기본값 **260906** 을 쓸 것.

---

## 9. 남은 FAIL 8건 분류

| 성격 | 건수 | 내용 |
|---|---:|---|
| **실제 확인 필요** | 1 | **F2 바코드입고** — 수불이력만 0. 내일 대표님 실물 테스트 |
| 마감상태(코드 정상) | 2 | G3/G4 — 검사대기가 마감된 260831 에만 존재 |
| 하네스 락 타임아웃 | 4 | 실코드 직접호출은 정상(생산재고 2,731 · 영업재고 306행) |
| 기존 미해결 | 1 | G0b 명세 없는 4건(0.3%) — `coopplan.py:1358` `except: pass` |

SKIP 31건 = 협력사 계정 자격증명 미제공(`_migration/.flow_secrets.json` 없음). 결함 아님.

---

## 10. 내일 최종 테스트 체크리스트

- [x] 재고화면 7종 nx 전환 · 배포 (커밋 `fe1ca5b`)
- [x] 원장 5개 재고점 레거시와 diff 0 (조정행 6,289건 · 원복키 `MAINT_TAG='OB' AND INSERT_USER_ID='GOLIVE'`)
- [x] 생산준비등록 → BOM 재고이동 **실제 실행 검증**
- [x] 생산 바코드 실적 → 자재차감·완제품증가 **실제 실행 검증**
- [x] 사급 루프(세트+ / 단품+ / 사급−) **실제 실행 검증**
- [x] 레거시 흐름 대조 (같은 테이블·태그·부호)
- [ ] **바코드입고 실물 테스트** ← 대표님 (F2 판정)
- [ ] **유검사품 검사승인** ← 대표님 (데이터가 없어 자동검증 불가)
- [ ] 화면 눈으로 확인 (CLAUDE.md §6 — 나는 API 응답만 봤다)

**유의** — 웹에서만 등록하고 레거시엔 없는 건은 밤 동기화(~19:00, 미러를 라이브로 통째 재적재) 때
사라질 수 있다. 하루 안에 끝나는 테스트면 무관하다.
