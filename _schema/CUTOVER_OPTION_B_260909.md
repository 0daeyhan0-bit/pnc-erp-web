# 컷오버 방향 — B안 검토본 (2026-09-09 오전) ※★이후 A안으로 확정됨

> ## ⛔ 이 문서는 **낡았다**. 아래 내용대로 하면 안 된다.
>
> **현행 결정 = A안** — `_conn()` 은 **`PARTNER_ERP`(라이브)** 다.
> 근거: main 커밋 `a7ab68e` "fix(rollback): _conn 을 라이브 PARTNER_ERP 로 되돌림"
> (2026-09-09 main 병합 확인). 같은 방향으로 `e2963e3` "ITEM_SUB 레거시 통일(대표 '반대로')" 도 병합됐다.
>
> 아래는 **오전 시점의 검토 기록**으로만 남긴다.
> 특히 §3 의 "`origin/fix/rollback-conn-live` 는 병합하지 않는다" 는 **뒤집혔다** — 그 방향이 채택됐다.
> 경위 = `WORKLOG_260909_MIRROR_TO_CLEAN.md` §7-B.

---

> (당시 기록) **대표 확정: "b로 해야지 이제"** — `_conn()` 은 `PARTNER_ERP_TEST3` 유지, nx 를 정본으로 간다.

---

## 0. 배경 — 9/7 롤백 이후 두 갈래가 있었다

```
2026-09-07 야간  컷오버 시도 → 롤백(대표 판단, 컷오버 연기)
                 · DB명 PARTNER_ERP_ORG → PARTNER_ERP 원복
                 · cutover_mark --clear → delta_sync 재가동
                 · ★코드는 nx 읽기 유지 (option B)
                 경위 = CUTOVER_EXECUTION_LOG_260907.md §6

2026-09-08 16:33  ac21833  fix(rollback): 컷오버 코드 flip 되돌림
                 · _conn() DATABASE=PARTNER_ERP_TEST3 → PARTNER_ERP  (=A안)
                 · 증상: 영업예상매출현황 2026-09-08~09-30 전 기간 0원·0도번
                 · 브랜치 origin/fix/rollback-conn-live
2026-09-08 16:44  d7738cb  그 경위를 §7 로 기록
```

**그런데 그 브랜치가 main 에 병합되지 않았다.** 실측(2026-09-09):

```
git merge-base --is-ancestor ac21833 origin/main  →  ★main 에 없음
현재 main / 작업 브랜치의 _conn()  =  PARTNER_ERP_TEST3
```

⟹ A안 조치가 어디에도 반영되지 않은 채, 코드는 계속 B안 상태(nx 읽기)로 돌고 있었다.

---

## 1. 두 갈래의 차이

| | A안 (ac21833) | **B안 (확정)** |
|---|---|---|
| `_conn()` | `PARTNER_ERP`(라이브) | **`PARTNER_ERP_TEST3`** |
| 조회 원천 | 레거시 실시간 | **nx (delta_sync 미러 + 클린 정본)** |
| 컷오버 후 | 레거시 은퇴 시 **전부 죽음** | 그대로 동작 |
| 지금 상태 | 미병합 | **이미 적용돼 있음** |

B안이 §1-9-1(컷오버 후 단일 테이블) 원칙과도 맞다.

---

## 2. ★B안에서 반드시 지켜야 할 것 — 스키마 명시

`_conn()` 이 `PARTNER_ERP_TEST3` 에 붙으면 **기본 스키마는 `dbo`** 다(실측 `SCHEMA_NAME()='dbo'`).
그런데 `TEST3.dbo` 에는 **컷오버 때 만든 사본**이 있고, 롤백 후 아무도 갱신하지 않는다.

```
SA_T_PLAN_ITEM_DTL   TEST3.dbo 331,897행 (231016 ~ 260819)   ★멈춤
                     TEST3.nx  346,670행 (231016 ~ 261009)
                     라이브     346,390행 (231016 ~ 261009)
```

⟹ **스키마 접두어 없는 SQL 은 죽은 사본을 읽는다.**
이것이 어제 §7 에 기록된 "죽은 TEST3.dbo 조회" 장애의 정체이고,
오늘 영업예상매출현황 9월 조회가 0행이던 원인이다.

### 2-1. 오늘 교정한 것 (커밋 3ee5098)

무접두 참조 **18곳 · 9종**을 `nx.` 명시로 바꿨다. 전부 dbo 사본이 뒤처져 있던 것들이다.

```
soyo.py 11곳    영업예상매출현황·예상 LG사급금액   ★9월 0행 → 662행 복구
prodsheet.py 3곳  공정별 바코드생산실적            스티커 14,654행 누락 해소
sales.py 2곳     출하 사급플래그
delivedit.py 1곳  납품편집
```

바꾼 대상 — 클린 정본이 있으면 그것, 없으면 `nx.`
```
sa_t_plan_item_dtl → nx.sale_plan_item    (웹 정본 · 과거분 338,055행 이관 완료)
pr_t_plan_input    → nx.prod_plan_input   (웹 정본)
나머지 7종         → nx.<이름>
```

### 2-2. 현재 일관성 (실측 2026-09-09)

```
_conn()/_nx()        전부 PARTNER_ERP_TEST3   ✔
PARTNER_ERP.dbo 명시  0곳                      ✔
무접두 잔존           0곳                      ✔
```

**B안 기준으로 코드가 일관돼 있다.**

---

## 3. 조치

- [x] 무접두 18곳 → `nx.` 명시 (커밋 `3ee5098`)
- [x] 계획 과거분 338,055행 이관 + 편성 DELETE 조건 교정 (커밋 `067f532`)
- [ ] **`origin/fix/rollback-conn-live` 브랜치 처리** — A안 조치이므로 **병합하지 않는다.**
      다만 §7 장애기록(`d7738cb`)은 경위 문서라 살릴 가치가 있다 → 이 문서로 대체하거나
      문서만 따로 반영. **브랜치는 닫는다.**
- [ ] `TEST3.dbo` 사본 처리 — 아무도 안 읽게 됐으므로 **혼선 방지를 위해 정리 검토**
      (지금 드롭하면 되돌리기 어려우니 컷오버 확정 후)

---

## 4. ★교훈 — 내가 오늘 네 번 틀렸다

**스캔 결과보다 기존 결정 기록을 먼저 읽어야 한다.**

| # | 내 오판 | 실제 | 어디에 있었나 |
|---|---|---|---|
| 1 | 재고 2종을 전환 대상으로 | **웹 재고 정본** | `00_MASTER_INDEX.md` C26-3 |
| 2 | 원가 3종을 전환 대상으로 | 대표님 검토 영역 | 대표 지시 |
| 3 | 계획 2종 "웹 정본 없음" | `nx.sale_plan` 있음 | `planrev.py:900` |
| 4 | **"TEST3.dbo 장애 새로 발견"** | **어제 이미 진단·수정** | `CUTOVER_EXECUTION_LOG §6` · 커밋 `ac21833` |

특히 4번은 대표님이 **"관련 내용이 있는데도 왜 새로 볼려고 하는거지?"** 라고 지적할 때까지
같은 조사를 반복했다. 커밋 로그와 `_schema/*.md` 를 먼저 grep 했으면 5분에 끝났을 일이다.

⟹ **작업 착수 절차: ① 커밋 로그 · ② `_schema` 기록 · ③ 그 다음에 스캔.**
