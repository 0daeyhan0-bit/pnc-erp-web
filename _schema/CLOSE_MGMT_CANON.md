# 마감관리 — 우리 로직 정본 (CLOSE_MGMT_CANON)

> 작성 2026-08-27. 대상 = 웹 `SCREEN.close`(시스템관리 > 마감관리) · 백엔드 `routers/close.py`(`/api/close/*`) · 공용 게이트 `common._lock_msg`/`_assert_open`.
> 원칙: **마감 = 확정(freeze) + 잠금**. 레거시 버그는 복제하지 않는다(`CLAUDE.md §1-7`).
> 관련 정본: `STOCK_GATING_CLOSE_LOCK_RULES.md`(규칙 A 재고게이팅 / 규칙 B 마감잠금) · `NX_STOCK_LEDGER_DESIGN.md` · `SALEOUT_PROGRAM_CANON.md`.
> 기록 근거: [[nextgen-erp-close-settlement]](마감=잠금·일마감⊂월마감·해제는 권한자+로그·소급은 당월조정) · [[nextgen-erp-material-close]]("마감 시점에 스냅샷 생성 = 다음달 기초재고") · [[newerp-matclose-movavg]](일별잔량 빌더).

---

## 0. 착수 전 실태 (2026-08-27 실측)

| | 상태 |
|---|---|
| 마감관리 화면 | **가시화 전용** — `[마감 실행]`·`[취소]`가 전부 `alert` 플레이스홀더. 운영 배포본에도 동일 |
| 표시하던 "최종 마감" | `PU_T_MONTH_STOCK_WH_DAILY` 의 `MAX()` — 그런데 이 테이블은 **조회할 때마다 `TRUNCATE` 후 재생성되는 작업테이블**(`w_pu_stock_260`). 서로 다른 일자 **1개(260717)** 뿐 = 마감일이 아니라 *"누가 그때 마지막으로 조회한 흔적"*. **"지연"이라는 진단 자체가 근거 없음** |
| 잠금 상태 저장소 | 없음. `nx.stock_close`(ym 전역)만 있고 화면과 **연결 안 됨**(2607 `close_flag=False` = 아무것도 안 잠김) |
| 재고이동 쓰기 잠금 커버리지 | **23/39 = 58%** |

### 0-1. 레거시는 어떻게 하고 있나
- **현재고** `PU_T_MAT_STOCK_WH` = 전표 저장 시 **실시간** upsert(`f_pu_set_mat_stock_wh`). 실측 마지막 갱신 = 당일 07:39
- **월마감 스냅샷** `PU_T_MONTH_STOCK_WH` = 진짜 저장소(19개월 2412~2607)
- **일수불** `PU_T_MONTH_STOCK_WH_DAILY` = 조회용 임시테이블(TRUNCATE 재생성)
- **일마감/월마감은 "잠금"** — `CM_M_DAILY_MAGAM`(gubun '1', 현재 **260812**) 이전 일자는 재고변경 차단, 예외테이블 `CM_M_DAILY_MAGAM_EXCEPT`, MASTER 계정만 강행
→ 즉 **레거시도 "마감=잠금"이 맞고**, 스냅샷 생성은 월마감 산출물이 담당한다.

---

## 1. ★우리 로직

### 1-1. 마감의 정의
**마감 = ① 그 시점 잔량을 스냅샷으로 확정(freeze) + ② 그 기간 재고이동 전표 CRUD 잠금.**
해제(reopen)는 **확정 스냅샷 제거 + 잠금 해제 + 로그**.

### 1-2. 스키마 (`_migration/create_period_close.py`, 멱등)
```
nx.period_close    domain(MAT/PRD/SAL) · ptype(D일/M월) · period(YYMMDD | YYMM)
                   close_flag · close_user/close_dt · reopen_user/reopen_dt · note
                   PK(domain, ptype, period)
nx.stock_snapshot  domain · ptype · period · item_code · stock_qty/amt · avg_cost · in/out_qty · close_dt
                   PK(domain, ptype, period, item_code)
```

### 1-3. ★자재 스냅샷 소스 = `nx.mat_stock_daily` (검증으로 확정)
**검증(2026-08-27, 읽기전용 전수대조)** — 우리 일별잔량 월말값 vs 레거시 `PU_T_MONTH_STOCK_WH`

| 기준월 | 공통 품목 | 수량 일치 | 우리만 | 레거시만 |
|---|---:|---:|---:|---:|
| 2606 (260630) | 2,342 | **2,342 (100.00%)** | 0 | 1,409 |
| 2607 (260731) | 2,534 | **2,534 (100.00%)** | 10 | 1,195 |

"레거시만" 1,195품목은 **전부 재고 0·금액 0** → 커버리지 갭 무해.
∴ **마감 시 `mat_stock_daily` 의 해당 시점 잔량을 그대로 확정하면 된다.**

**역할 분리**: 빌더(`matclose_movavg_build.py`)=매일 일별잔량 전개 / 마감=그 시점 값을 **확정 + 잠금**. 빌더를 마감이 대체하지 않는다.

### 1-4. ★기존 `/api/stockclose/*`(Phase5, 원장기반)를 쓰지 않는 이유
같은 세션에서 실측 대조:

| 방식 | 레거시 2606과 일치 | 비고 |
|---|---:|---|
| 기존 `/api/stockclose/run`(nx.stock_ledger 기반) | **45.88%** | 기말합계 **−5,892,366**(음수) · `stock_close_snap` **0행**=한 번도 실사용 안 됨 |
| 신규 `/api/close/run`(mat_stock_daily 기반) | **100.00%** | |

충돌표 **C13**("nx.stock_ledger MAT은 미동기 45% 오차로 게이트 source 탈락")과 정확히 일치.
→ 기존 엔드포인트는 **삭제하지 않되 현행 정본 아님**. 원장이 정본이 되는 **컷오버 이후 재검토** 대상.

### 1-5. 가드 (연쇄 무결성)
- **미래 기간** 마감 불가
- **순서 강제**: 직전 기간이 마감돼 있어야 다음 기간 마감 가능(첫 마감은 예외)
- **역순 해제**: 후속 기간이 마감돼 있으면 해제 거부(기초 연쇄의존)
- **중복 마감** 거부
- **재마감 가능**: 해제 후 다시 마감 — `period_close` UPSERT
- **원자성**: 스냅샷 확정과 잠금은 한 트랜잭션(`_nx_tx`)

### 1-6. 공용 잠금 게이트 (전 부서 동일)
```
common._lock_msg(cur, ymd, domain="MAT")   # 사유메시지 or None (비발생형)
   ① 일마감 period_close(domain,'D',YYMMDD)
   ② 월마감 period_close(domain,'M',YYMM)      ← 일마감 ⊂ 월마감
   ③ 하위호환 nx.stock_close(ym)                ← 구 전역 월잠금(기존 동작 보존)

common._assert_open(cur, ymd, domain, what)  # 위가 잠겼으면 HTTP 400 으로 거부
```
**규칙(사용자 확정 2026-08-27): 재고가 조금이라도 움직이면 잠근다. 재고이동과 문서발행을 겸해 분리가 어려우면 → 막는 쪽으로 지정.**

### 1-7. 적용 범위 — 재고이동 쓰기 전수
"재고를 실제로 움직이는가"를 **재고테이블 DML + 원장 헬퍼 호출**로 판별.

| | 적용 전 | 적용 후 |
|---|---:|---:|
| 재고이동 쓰기 엔드포인트 | 39 | 38 |
| 마감잠금 있음 | 23 | **38** |
| **커버리지** | **58%** | **100%** |

신규 적용 16곳: 자재 입·출고(`/api/stock/save` — 구 인라인 잠금을 공용 게이트로 승격) · 사급출고(save/confirm) · 출하실적(save/delete) · 출하처리(sale040/confirm) · 세트입고 · 세트재고조정 · 준비재고(commit/register) · 도입(save_batch/delete) · 가공이동(issue/delete) · 생산재고조정(save/delete).

**대상 아님(재고이동 아님, 실측 확인)**: 사급출고 요청삭제(`sagub_output_req`) · LG송장 발행/취소(`lg_songjang_dtl`, 발행 4컬럼 마킹) · 공정실적(`proc_result`) · 세트 거래명세서 발행(`set_input_req`) · 준비 전표 강제발행(코드 주석에 *"재고이동을 거치지 않고 전표 데이터만"* 명시) · 스크랩(`scrap_raw`).

---

## 2. 검증 (4게이트 · nx 쓰기 후 전량 원복 · 라이브 무접촉)

### 게이트A — 마감 엔진
| 케이스 | 결과 |
|---|---|
| 미래 기간 마감 | **차단**(400) |
| 자재 월마감 2606 | 스냅샷 **2,342품목 확정** + 잠금 |
| **확정 스냅샷 정확성** | 레거시 `PU_T_MONTH_STOCK_WH` 와 **공통 2,342 / 일치 2,342 / 스냅만 0 = diff0** |
| 중복 마감 / 직전 미마감 | 둘 다 **409 거부** |
| 후속 기간 마감중 해제 | **409 거부** |
| 역순 해제 | 성공 · **확정 스냅샷 동반 제거** · 전량 원복 |

### 게이트B — 잠금이 실제 쓰기를 막는가
도입-수입 실제 저장으로 왕복: 마감 전 성공 → 월마감 후 **차단** → 해제 후 성공 → **일마감**(하루만) 후 **차단** → 안 잠근 다음날은 성공.

### 게이트C — 루트별 왕복 (12루트, 마감 전 → 마감 후 → 해제 후)
도입 저장/일괄/삭제 · 자재 입·출고 · 생산재고조정 · 가공이동 · 세트재고조정 · 출하실적 등록 · 출하처리(040) · 세트입고 · 준비등록 · 사급출고 요청 → **전부 PASS**(마감 중에는 마감 사유로 거부, 해제 후 정상).

### 게이트D — 화면 실클릭(playwright)
화면 로딩(영역 3 · 현황 6행 · 플레이스홀더 0) → **월마감 실행 버튼** → DB 잠금·스냅샷 2,342 → **캘린더 30/30일 "월마감" 표시** → **해제 버튼** → 잠금해제·스냅샷 제거 → 원복. JS 오류 없음.

### ★검증이 잡아낸 실제 버그 5건
| # | 버그 | 조치 |
|---|---|---|
| 1 | **라우터를 `app.py` 끝에 등록하면 404** — 382행 `app.mount("/", StaticFiles)` 가 먼저 매칭. openapi 에는 뜨는데 호출은 안 됨 | 라우터를 **mount 앞**으로 이동. ★`CLAUDE.md §2` 의 *"app.py 끝에 2줄 append"* 안내는 **틀렸다 → 문구 수정 필요** |
| 2 | **재마감 불가(500)** — 해제 후 재마감 시 `period_close` PK 중복. `_is_closed` 가 flag=0 이라 통과시킨 뒤 INSERT 에서 터짐 | **UPSERT** |
| 3 | **원자성 없음** — 스냅샷은 커밋되고 잠금 INSERT 실패 → **고아 스냅샷 2,342건** 잔존 | `_nx_tx()` 한 트랜잭션 |
| 4 | **`/api/stock/save` 가 구 잠금만 봄** — `nx.stock_close` 인라인 조회라 `period_close` 마감이 안 먹음 | 공용 게이트로 승격(구 소스 폴백 유지) |
| 5 | **화면 입력값 리셋** — `draw()` 전체 재렌더로 대상일·대상월·조회월이 오늘/이번달로 되돌아감 → 캘린더 조회 불가, 해제 시 엉뚱한 기간 대상 | 입력값을 **화면 상태로 보존** |

★교훈: 커버리지 조사에서 **`_closed()` 헬퍼만 grep 해 인라인 `nx.stock_close` 조회를 놓쳐 "미적용 29개"로 과대보고**했다(실제 16개). `STOCK_GATING_CLOSE_LOCK_RULES` 에 *"전역 `_closed`만 세어 도메인 전용 락을 놓친 오탐"* 이라고 **이미 적혀 있던 실수를 반복**. 정적 탐지는 헬퍼·인라인·거부메시지를 모두 포함해야 한다.

---

## 3. 남은 것

| # | 항목 | 상태 |
|---|---|---|
| **C1** | **자재 일마감의 전제 = 일별잔량 빌더** — `mat_stock_daily` 에 **260826~260830 이 비어 있어**(빌더 수동 실행, 8/25까지) 오늘자 일마감이 **구조적으로 불가**. 엔진은 명확한 사유로 거부한다. **일마감 실운영하려면 빌더 자동화가 선행 필수** | ☐ [[newerp-matclose-movavg]] "자동화는 사용자가 보류(2026-08-22)" |
| C2 | **생산(PRD)·영업(SAL) 스냅샷** — 현재 **잠금만** 가능. 레거시도 월마감이 2502에 멈췄고 일별 인프라 없음. tag→기초/입고/출고/이동 파생식 규명 후 2단계 | ☐ |
| C3 | 레거시 일마감(`CM_M_DAILY_MAGAM` 260812) 과의 관계 — 병행운영 중 레거시가 막는 날짜를 우리도 볼지 | ☐ 판단 필요 |
| C4 | 기존 `/api/stockclose/*` 정리 — 컷오버(원장 정본화) 이후 재검토 | ☐ |
| C5 | 권한 — 마감/해제를 권한자만 하도록(`PERM.canEdit('close')`) | ☐ |
| C6 | `CLAUDE.md §2` 라우터 등록 위치 문구 수정(mount 앞) | ☐ |
| C7 | **자재수불장이 확정 스냅샷을 읽게 전환** — 현재 레거시 임시테이블(`PU_T_MONTH_STOCK_WH_DAILY`, 260717 하나뿐) 조회. `nx.stock_snapshot` + 미마감분 즉석계산으로 전환하면 화면 하나로 정리됨 | ☐ 재설계와 한 묶음 |
| C8 | ★**마감 = f(원장) 재설계** — 현재 `_snap_mat` 은 `mat_stock_daily`(임시본)에서 **복사**. 원설계([[nextgen-erp-material-close]])는 **파생**. 이동평균 로직을 빌더→마감엔진으로 이사하면 `mat_stock_daily`·빌더·임시화면 모두 불필요 | ☐ **승인 대기** |

---

## 4. 로그
| 일자 | 내용 |
|---|---|
| 2026-08-27 | 실태 규명 · 스냅샷 소스 검증(100.00%) · 스키마 · 마감엔진 · 공용게이트 승격 · 재고이동 38루트 100% 적용 · 게이트A~D 전항 PASS · 버그 5건 교정 |
| 2026-08-27 | 공백일(주말·휴일) 이월 확정 로직 추가 → 7월말까지 일괄마감(일 32일·월 2회, 스냅샷 84,296행). 검증: 레거시 2606/2607 diff0(2607 '스냅만10'은 전부 재고0=무해) · 8월기초==7월기말 수량·금액 완전일치 · 7월 31/31일 연속 |
| 2026-08-27 | **자재수불장 무작동 규명** — `js/data.js`(.gitignore, 26MB) 가 worktree 에 없어 전역 `DB` 미정의 → `SCREEN.matledger` 가 `ReferenceError` 로 렌더 실패. API 는 정상(일 3,480행·월 3,729행). 파일 배치 후 정상 확인(3,481행·JS오류0). 온보딩 §1-4b 에 기록 |

## 6. ★단가 불일치 근본원인 — 레거시는 총평균법, 우리는 이동평균 (2026-08-27 규명)

### 6-1. 먼저 정정 — "빌더가 정답"이 아니었다
앞서 `260630` 한 지점만 보고 *"빌더 98.6% = 정답, 파생 63.5% = 틀림"* 이라고 판단했으나 **틀린 판단**이었다.
`260630`은 **빌더의 시드 지점**(레거시 2606을 그대로 받은 자리)이라 당연히 일치한다. 한 달을 굴린 뒤를 보면:

| 대조 | 수량일치 | **단가일치** |
|---|---|---|
| 빌더 `260630` vs 레거시 2606 (시드지점) | 100.00% | 98.63% |
| **빌더 `260731` vs 레거시 2607 (한 달 굴린 뒤)** | 100.00% | **78.00%** |
| 파생(기초=레거시2606) `260731` vs 레거시 2607 | 99.92% | **80.17%** |

→ **빌더도 단가는 레거시를 재현하지 못한다.** 파생본이 오히려 근소하게 낫다.
기존 기록의 "mat_stock_daily == 레거시 월마감 100%"는 **수량만**의 이야기였다(수량은 실제로 100.00%).

### 6-2. 진짜 원인 = 회계방식 자체가 다름
레거시 월마감 정본 = **`src_extracted/sa_stock_01/w_pu_stock_160.srw`** (`DELETE`+`INSERT INTO pu_t_month_stock_wh`).
읽어보면 레거시는 **이동평균이 아니라 총평균법(월별 가중평균, periodic weighted average)** 이다.

```
STOCK_COST = (BASIC_AMT + INPUT_AMT) / (BASIC_QTY + INPUT_QTY)      ← 월 1회 계산
BASIC = 전월 기말 / INPUT = 그 달 전체 입고 합계
```

우리(빌더·파생 **공통**) 규칙과의 차이 6가지 — 이게 78~80% 불일치의 정체:

| # | 항목 | 레거시 (w_pu_stock_160) | 우리 (빌더 = 파생) |
|---|---|---|---|
| 1 | **평균방식** | **총평균법** — 월 단위 1회 | **이동평균** — 입고 건건 재계산 |
| 2 | **입고 tag** | `3,9,C,G,H,S,P,R` | `9,S` 만 |
| 3 | **검사 미통과 제외** | `NOT (insp_flag IN ('S','F') AND insp_proc_flag<>'1')` | 조건 없음 |
| 4 | **수입 금액원천** | `PU_T_STOCK_MAINT_C.TAXPAYERS`(과세표준) | `MAINT_AMT × EXCHANGE_RATE` |
| 5 | **소모품 제외** | `ISNULL(ITEM_SGROUP,'') < '990'` | `ITEM_SGROUP LIKE '99%'` |
| 6 | **품목마스터 조인** | `JOIN PR_M_ITEM` — 마스터에 없으면 탈락 | 조인 없음 |

(참고: 출고 tag `1,4,5,6,8,A,B,J` · 반납 `T` · 조정 `2` 로 명시 분류. 우리는 "전 tag 부호합"으로 뭉뚱그림 — **수량은 결과가 같아 100% 맞았다**.)

### 6-3. 그래서 결정이 필요한 지점
**수량은 이미 해결**(99.9%~100%, 잔여는 아래 6-4). 남은 건 **단가 = 회계방식 선택**이고 이건 원가에 직결된다.
- (A) **레거시 총평균법 재현** — 원가 diff0 하드룰과 정합. 단, 월 1회 단가라 일마감 단가는 근사치가 됨.
- (B) **우리 이동평균 유지** — 주류 ERP(SAP moving average V) 방식·실시간 원가에 맞음. 단, 레거시 대비 78~80%로 벌어짐 = 원가 diff0 깨짐.
- (C) **분리** — 회계용 월단가 = 레거시 총평균(diff0) / 운영용 일단가 = 이동평균. 둘 다 저장.

→ **대표 결정 대기.** 결정 전까지 마감 배포 보류.

### 6-4. 처리 완료 — 잔량 0 제외 (대표 확정 2026-08-27)
`_snap_mat` 이 `abs(qty) < 1e-9` 품목을 스냅샷에 적재하지 않도록 수정(state 에는 남겨 이월).
음수는 실재고이므로 유지. 0에서 재입고 시 단가는 어차피 리셋(재고≤0 refill 규칙)이라 avg 유실 없음.

| 대조 | 반영 전 | 반영 후 |
|---|---|---|
| 파생 `260630` 행수 | 4,060 | **2,332** (빌더 2,342 · 레거시 잔량있음 2,329) |
| 공통 | 2,342 | 2,329 |
| 수량일치 | 2,340 (99.9%) | **2,327 / 2,329 (99.9%)** |

레거시 원본도 `HAVING SUM(...)<>0 ...` 으로 전부 0인 행을 배제하므로 **규칙 정합**.
(레거시 2606 에 0 잔량 1,422행이 남아있는 건 금액만 있는 행 등 — 잔량 기준 비교에선 제외가 맞다.)

## 7. ★레거시 총평균법 재현 성공 — 실질 diff0 도달 (2026-08-27)

§6에서 규명한 레거시 산식(`w_pu_stock_160.srw`)을 **원문 그대로** 재현했다.
검증 스크립트 = **`_migration/legacy_total_avg_verify.py`** (읽기전용, 쓰기 없음).

### 7-1. 결과

| 월 | 품목집합 | 수량 | 금액 | **단가** |
|---|---|---|---|---|
| 2606 (기초=레거시 2605) | **3,751 = 3,751 · 재현만 0 · 레거시만 0** | **100.00%** | 99.81% | **99.81%** |
| 2607 (기초=레거시 2606) | **3,729 = 3,729 · 재현만 0 · 레거시만 0** | **100.00%** | 99.95% | **99.79%** |

**이동평균 방식일 때 78%였던 단가가 99.8% 로 올라갔다.** 품목집합·수량은 완전 diff0.

### 7-2. 잔차 전수 규명 — 알고리즘 결함 0

| 구분 | 건수 | 성격 |
|---|---|---|
| **마감 후 원전표 수정** | 2606 **6건** | ★재현 불가가 정상. 아래 7-3 |
| ±1원 반올림 | 2607 **6건** | `ROUND`/`FLOOR` 경계 |
| 외화 `PR_M_ITEM_COST` 폴백 | 2607 **2건** | 레거시는 `ITEM_COST × 기준환율`(창 변수). 우리는 KRW만 |
| 레거시 단가 0 | 2606 **1건** | 음수재고(-259) 레거시가 0으로 둠 |

### 7-3. ★"마감 후 전표수정" 실증 — 마감(확정)이 필요한 이유
레거시 2606 마감 시각 = **2026-07-30 13:40:27**. 단가가 어긋난 6품목의 입고전표는 **전부 그 이후에 수정**됐다:

| 품목 | 전표 | 마감 당시 금액(레거시 역산) | 현재 `MAINT_AMT` | `UPDATE_DATETIME` |
|---|---|---|---|---|
| `5210A22977R` | 260630 #114 | **417,820** (=1,607×260) | **11,298,817** | 2026-08-05 09:49 |
| `AAA36923608` | 260626 #1094 · 260629 #1962 | — | — | 2026-08-04 08:05 |
| `AJR30008101-4-1` | 260626 #659 | 3,600 | 5,304 | 2026-07-31 09:36 |
| `EAD63785401` | 260626 #587 | 624,300 | 825,000 | 2026-07-31 10:23 |
| `AJR76442511-삼화` | 260630 #2·#3 | — | — | 2026-07-30 **14:41** (마감 1시간 후) |
| `AJR76442512` | 260630 #4 | — | — | 2026-07-30 14:41 |

→ **원장은 사후에 바뀌지만 확정 스냅샷은 그 시점을 굳혀야 한다**는 마감의 존재 이유가 실증됐다.
동시에 **"마감을 언제든 재계산해 대사"는 성립하지 않는다** — 재계산은 *현재* 전표로 하므로 과거 확정값과 달라지는 게 정상이다.
따라서 **확정 스냅샷은 보존이 정답이고, 재계산은 검증이 아니라 "소급 발생 감지"에 쓴다.**

### 7-4. ★재현 시 반드시 지킬 것 — 품목코드 대소문자
레거시 SQL Server 는 **CI 콜레이션**이라 `GROUP BY MAT_CODE` 가 `동Body-6.35(축관물)` 과 `동BODY-6.35(축관물)` 을
**한 품목으로 합친다.** 파이썬 dict 는 대소문자를 가르므로 그대로 두면 수량이 갈라진다(실제 2606 수량 4건 어긋남 → 키를 `strip().upper()` 로 정규화하니 **수량 100.00%**).
※ 같은 계열 사고 3회째. **레거시 재현 코드는 품목키를 항상 `UPPER(LTRIM(RTRIM()))` 로 정규화한다.**

### 7-5. 그래서 §6-3 선택지의 실현가능성
- **(A) 레거시 총평균 재현** — **가능함이 증명됨**(99.8%, 잔차는 전부 알고리즘 외 사유).
- **(C) 분리** — (A)가 되므로 **바로 실현 가능**. 회계용 월단가 = 총평균(레거시 diff0) / 운영용 일단가 = 이동평균.

**대표 결정만 남았다.** 결정되면 `_snap_mat` 에 총평균 경로를 추가하고 월마감을 그쪽으로 돌린다.

## 8. C2·C4·C5·C6 구현 완료 (2026-08-27) — 회계방식 결정과 무관한 항목

### 8-1. C2 — 생산(PRD)·영업(SAL) 스냅샷 확정

**소스 선택 근거(중요)**: `nx.stock_ledger` 는 **PRD/ASY 가 0행**(§4-C 실측) → 원장으로는 스냅샷을 만들 수 없다.
그리고 레거시 `PR_T_MONTH_STOCK_WH` · `SA_T_MONTH_STOCK` 은 **2502 한 달치뿐**(실측) —
**레거시가 생산·제품 월마감을 2502 이후 하지 않는다.** 그래서 두 화면(480/040)이 2502 스냅샷 + 전 이동으로 매번 재구성한다.
⟹ 확정 스냅샷의 소스 = 게이팅 캐논 §4-C 가 정본으로 지정한 **레거시 재현 recipe**:

| 도메인 | 소스 | 레거시 |
|---|---|---|
| PRD 생산 | `live_api._prodstock` | `w_pr_stock_480` (2026-08-19 diff0 검증) |
| SAL 영업 | `live_api.salesstock` | `w_pr_stock_040` (2026-08-19 diff0 검증) |

**스키마 변경**: 생산은 **2축(품목 × 라인)** 이라 `nx.stock_snapshot` 에 `loc` 추가 + PK 확장
(`_migration/alter_snapshot_loc.py`, 멱등). 가공창고(P0001)=`loc ''` · 용접=라인코드. MAT/SAL 은 `loc ''`.
기존 137,876행은 `loc=''` 로 보존.

**검증** — `_migration/verify_close_prd_sal.py` (2607 월마감)

| 게이트 | PRD | SAL |
|---|---|---|
| ①스냅샷 == 정본 recipe (전수) | 874=874, 수량일치 874, 스냅만0·recipe만0 → **diff0 PASS** | 215=215, 일치 215 → **diff0 PASS** |
| ②정본 단품함수 교차검증(별도 SQL 경로) | 표본 60 · 불일치 0 → **PASS** | 표본 60 · 불일치 0 → **PASS** |
| ③원장 불변식 | 3,834행 위반 0 → **PASS** | 2,162행 위반 0 → **PASS** |
| ④음수재고 | 24건(실재고이므로 유지) | 0건 |

**★함정 — 두 화면의 조정(adj) 부호 규약이 반대다**(레거시 원본이 그렇고 각각 diff0 검증본):
- 생산 480 : `qty = 기초 + 입 − 출 **+** 조정`
- 제품 040 : `qty = 기초 + 입 **−** 조정 − 출`

같은 식으로 검사하면 SAL 이 19건 위반으로 **오탐**된다(실제로 겪음). 불변식 검사는 화면별 규약을 따라야 한다.

**빈 품번 1건**(생산 recipe, 잔량 −407.72)은 키가 없어 확정 불가 → 스냅샷에서 제외.
레거시 `PR_T_STOCK_MAINT_MAT` 의 빈 `MAT_CODE` 183건에서 나온 데이터 흠.

### 8-2. C5 — 마감/해제 권한 게이트
**규칙(deny by default)**: ①`nx.web_user` 의 `시스템관리자` role → 허용 ②`nx.user_perm(user,'close',can_edit=1)` → 허용 ③그 외 **403**.
백엔드 `_assert_can_close()` 가 `close_run`·`close_cancel` 진입 즉시 판정.

**★정직한 한계**: 이 앱은 세션 인증이 없고 사용자 식별이 프론트 `localStorage` 다.
`payload.user` 는 위조 가능하므로 이 게이트는 **오조작 방지**지 보안 인증이 아니다. 진짜 인증은 로그인/세션 도입 시 별도 과제.

**같이 잡은 버그 2건**
1. `screens.etc.js` `who()` 가 `PERM.user` 를 읽었다 — 실제 필드는 **`PERM.userId`**. 이대로 뒀으면 항상 `'web'` 이 되어 **모든 마감이 새 게이트에 막혔을 것**.
2. UI 게이트에 `PERM.isAdmin()` 을 쓰면 안 된다 — `core.js currentUser()` 가 **미등록 사용자를 `시스템관리자` 로 기본 반환(fail-open)** 한다. 백엔드와 동일 규칙으로 다시 작성.

**검증(백엔드 403 + 브라우저 UI)**

| 사용자 | 백엔드 | UI 버튼 |
|---|---|---|
| 미등록/빈 사용자 | **403 차단** (실행·해제 모두) | 0/4 · "🔒 마감 권한 없음" |
| `admin`·`TEST1`(시스템관리자) | **200 허용** | 4/4 |

### 8-3. C4 — 구 `/api/stockclose/*` 정리
프론트 사용처 0건 확인. `/api/stockclose/run` 에서:
- `point IN (MAT, PRD, ASY)` → **410 사용중단**(마감관리 `/api/close/run` 안내). MAT 은 `stock_ledger` 기반이라 **45.88% 부정확**했고, `nx.stock_close` 를 세워 새 잠금 `nx.period_close` 와 **이중 잠금원**이 되던 것.
- `lock=true` → **400 금지**(잠금은 마감관리로 일원화).
- `RDY/SAG` 는 대체 스냅샷이 아직 없어 **스냅샷 산출만** 유지. `/api/stockclose/status`(조회)는 유지.

검증: MAT/PRD/ASY 410 · RDY 200 · RDY+lock 400 · status 200.

### 8-4. C6 — `CLAUDE.md` §2 문구 오류 수정
"app.py **끝에** 2줄 append" 는 **틀린 안내**였다. app.py 끝에는 `app.mount("/", StaticFiles(...))` 가 있어
그 뒤에 등록한 라우터는 `/openapi.json` 에는 보이지만 **호출하면 StaticFiles 가 먼저 잡아 404** 가 난다(이번에 실제로 겪음).
→ "**`app.mount` 보다 위, 기존 `include_router` 블록 안**" 으로 수정 + 사고 사유 명시.

### 8-5. 남은 것
- **회계방식 결정(§6-3 A/B/C)** — 대표 결정 대기. 이게 풀려야 MAT 월마감·8월 일마감·성능 최적화가 진행된다.
- C1 일마감 빌더 자동화 — 파생이 diff0 나면 **불필요**해짐. 방식 결정 후 판단.
- C7 자재수불장을 확정 스냅샷 기반으로 전환.
- PRD/SAL **일마감**(현재 월마감만 검증). 일 단위도 같은 recipe 로 동작하나 전수검증 미실시.

## 9. ~~대표 결정 — 회계방식 = 총평균법~~ ★**정정됨 → §12 참조**

> **⚠ 이 절의 결론은 틀렸다.** 세무사 확인 결과 **신고된 평가방법 = 이동평균법 + 저가법**이다(2026-08-27).
> 아래 '총평균 채택'과 §11 의 'A1 확정=총평균'은 **잘못된 전제**였다. 유효한 결정은 **§12**.
> 단, 이 절의 **실측 데이터(음수재고·소모품 내역)는 그대로 유효**하다.


| # | 결정 | 내용 |
|---|---|---|
| 1 | **회계방식 = (A) 레거시 총평균법** | 이동평균은 은퇴. `STOCK_COST=(BASIC_AMT+INPUT_AMT)/(BASIC_QTY+INPUT_QTY)` |
| 2 | **음수재고 = 0 으로 처리** | **컷오버 때** 일괄. 그 전까지는 레거시대로 두고 리포트로만 노출 |
| 3 | 소모품 | 내역 확인 완료(아래 9-2) |

**결정 근거(실측)**: 원가엔진 `_harness/nx_cost_engine.py` 는 `mat_stock_daily.avg_cost` 를 **참조 0건**
(재료비 단가 = `nx.price_metal.std_price` · `PR_M_ITEM_COST`). 게이팅은 **수량만** 사용.
⟹ 이동평균 단가의 실사용처는 자재 일마감 조회화면 1개뿐 → 두 벌 유지할 근거 없음.
그리고 총평균은 우리가 이동평균에서 고쳤던 결함 3개를 **구조적으로 안 겪는다**
(수입금액=`TAXPAYERS` 원화라 환율 무관 · 월단위라 마이너스 평균폭발 없음 · 입고 tag 에 `P` 포함).

※ 이전 기록에서 "자재 단가가 원가에 직결" 이라 한 것은 **오류**였다(원가엔진 미참조).

### 9-1. 음수 재고자산 실태 (레거시 `PU_T_MONTH_STOCK_WH`, Z99990)

**2607 = 11품목 · 수량 −8,727 · 금액 −37,871,213원** (자재 재고자산 44.7억의 0.85%)

| 품번 | 품명 | sg | 기말 | 금액 | 단가 | 최초 음수 |
|---|---|---|---|---|---|---|
| 7072AR9374X | 28*1.2*3000 (O) | 210 | −660 | −13,124,091 | 19,887 | 2507 (4개월) |
| 7072AR9374K | 31.75*1.25*3000(O) | 210 | −578 | −11,495,264 | 19,888 | **2412 (5개월)** |
| MJU66885912-3M | 28.00*1.00*3000 고강도관 | 210 | −277 | −5,330,069 | 19,214 | 2601 (3개월) |
| MJU64433003 | Return,Bending | 230 | −6,676 | −3,713,753 | 556 | 2502 (2개월) |
| 5224A20005A | Strainer(Ø28.0-2) | 230 | −322 | −2,179,699 | 6,769 | **2412 (3개월)** |
| MGE64190603 | Pipe | 130 | −100 | −1,320,000 | 13,200 | 2502 (4개월) |
| AJR30133707-A-S-1 | STS Sub | 120 | −67 | −602,297 | 8,989 | 2607 (신규) |
| 5224A20005L·5210A00039H·MJU66885911-3M·7072AR9374K-H | | | 소액 | −106,040 | | 2412~2602 |

**추이 — 개선 중이나 금액은 고단가로 집중**

| 월 | 품목 | 수량 | 금액 |
|---|---|---|---|
| 2603 | 63 | −49,063 | −195,408,674 |
| 2604 | 92 | −105,250 | −29,592,864 |
| 2605 | 68 | −55,865 | −19,888,758 |
| 2606 | 34 | −15,900 | −9,971,426 |
| 2607 | **11** | **−8,727** | −37,871,213 |

품목수·수량은 크게 줄었으나(63→11), **금액은 동파이프 원소재(19,887원/kg)에 몰려 오히려 증가**.
**만성**: 11개 중 7개가 2502 이전부터 음수 — 일시적 타이밍 문제가 아니라 **미기록 입고 또는 과대 출고의 누적**.

→ **처리 = 컷오버 시 0 으로 조정**(대표 결정). 그 전까지는 레거시 값 유지 + 리포트 노출.
→ 조정 시 **재고실사 근거 + 조정전표**가 있어야 한다(임의 0 처리는 감사 지적 대상).

### 9-2. 소모품(재고자산 제외) 실태 — `ITEM_SGROUP >= '990'`

**2607 = 33품목 · 54,587,914원** · 2026년 1~8월 누계 **273,702,732원**(연 환산 약 **4.1억원**)

| sgroup | 분류 | 품목 | 금액 |
|---|---|---|---|
| 993 | 수불예외 | 9 | 31,868,754 |
| 992 | 일반소모품 | 10 | 15,632,660 |
| 991 | 생산소모품 | 14 | 7,086,500 |

**★확인이 필요한 2가지 (금액순 상세에서 드러남)**

1. **sg 993 '수불예외' = 자재가 아니라 금액 조정 전표다.**
   `소급` **30,784,440원**(2607 소모품 금액의 56%) · `물류비` 231,000 · `파이프 수불정산금액` 164,695 ·
   `용접봉 수불정산금액` 1,019 · `잡자재` · `샘플` · `단품`.
   실물 자재가 아닌 **정산·조정 금액을 자재코드처럼 매입 전표로 넣은 것**.
   회계적으로는 자재매입이 아니라 별도 계정(물류비·정산차액 등)이어야 한다.

2. **제품 부속품이 소모품으로 분류돼 있다** — 재고자산이어야 할 수 있음(2607 합 171만원):
   `MBM65584007 Card,Technical` 1,120,000 · `MFL38272406 Manual,Installation` 260,000 ·
   `MFL68125209` 160,000 · `MFL72040601` 120,000 · `MEZ67612933 Label` 50,000.
   LG 품번 체계(MBM/MFL/MEZ)를 가진 **제품 동봉물**인데 sg 991(생산소모품)로 잡혀 즉시비용 처리됨.

**타당한 소모품**(즉시비용 문제없음): LPG 5,187,780 · 샘플 3,904,920 · LN2 3,624,960 · LO2 1,548,000 ·
스트래치랩 1,680,000 · 바코드롤/리본/테이프/장갑/용지 등.

→ **세무사 확인 항목**: (a) sg 993 '수불예외'(연 환산 2.3억 규모)를 자재매입으로 계상하는 것이 맞는가
(b) LG 품번 동봉물(Card/Manual/Label)을 재고자산에서 빼는 것이 맞는가.

## 10. 해야할 일 (2026-08-27 현재)

### 10-1. 진행중
| # | 항목 | 상태 |
|---|---|---|
| T1 | **총평균법 전수 검증** | 구현 완료, 검증 실행중(5게이트: 월마감 vs 레거시 전수 · 월연속성 · 7월 31일 일마감 · 일=월 수량 diff0 · 일단가=직전월단가) |

### 10-2. T1 통과 후 바로
| # | 항목 |
|---|---|
| T2 | 2606·2607 월마감 + 8월 일마감(8/1~8/26) 실행 |
| T3 | `_snap_mat_movavg`(구 이동평균) 및 `_mat_moves/_mat_step` 제거 판단 — 대조 끝나면 삭제 |
| T4 | 성능 — 마감 1건당 수 초(매번 기초부터 재전개). 총평균은 월 1회라 부담이 줄지만 일마감 31회는 측정 필요 |
| T5 | `feat/close-mgmt` → PR → main → 배포 (**대표 승인 후**) |

### 10-3. 마감 도메인 잔여
| # | 항목 | 비고 |
|---|---|---|
| C1 | 일마감 빌더 자동화 | 총평균 채택으로 **불필요해짐** → X3(컷오버)로 이관 |
| C7 | 자재수불장을 확정 스냅샷 기반으로 전환 | 지금 일자뷰가 레거시 TRUNCATE 임시테이블. **우선순위 높음** |
| C8 | PRD/SAL **일마감** 전수검증 | 현재 월마감만 검증. 같은 recipe 라 동작은 하나 미검증 |
| C9 | 마감 후 **소급 발생 감지 리포트** | 확정 후 원장이 바뀐 건을 잡아내는 리포트(레거시에선 6건 실측) |
| C10 | 음수재고 리포트 화면 | X1 정리 전까지 상시 노출 |

### 10-4. 다른 브랜치
| # | 항목 |
|---|---|
| S1 | **판매및출고등록**(`fix/saleout-cust-ac`, 10커밋) — 대표 육안 확인 → PR → 배포 |
| S2 | 미구현 G2~G7(출고증 다품목·사급플래그·행별현재고·인쇄·Z99990차단·중복경고) — 별도 승인 |
| S3 | 미검증 4건(동시저장 채번경합·매출마감 상세모달·집계표 출력방식별·부가세조정 토글) |

### 10-5. 대표 확인 대기
| # | 항목 |
|---|---|
| A1 | 세무사 확인 — 신고된 재고자산 평가방법이 **총평균법**이 맞는지 |
| A2 | 배포 승인 — T5 |

## 11. ~~A1 확정 + 총평균법 구현·검증~~ ★**전제 오류 → §12 참조**

> **⚠ '11-1 A1 확정 = 총평균법'은 오답이었다.** 실제 신고 = **이동평균법 + 저가법**.
> 단, **11-2 구현물과 11-3 검증결과는 버리지 않는다** — 총평균 엔진(`_ta_build`)이 규명한
> **tag 정본·금액원천·제외기준은 이동평균에도 그대로 유효**하고, 레거시가 총평균으로 돈다는
> 사실 자체가 §12 의 핵심 근거다.


### 11-1. ★A1 확정 — 신고된 재고자산 평가방법 = **총평균법** (대표 확인)
§6-3 의 미결이 닫혔다. **회사가 세무서에 신고한 평가방법이 총평균법**이므로,
§9 의 (A) 채택은 시스템 편의가 아니라 **세무 신고와 일치시키는 선택**이다.
⟹ 이동평균으로 갔다면 **신고 방법과 불일치**가 되어 변경신고 없이는 쓸 수 없었다. 결정이 확정 근거를 얻었다.

### 11-2. 구현
`routers/close.py` — 레거시 `w_pu_stock_160.srw` 산식 이식.

| 함수 | 역할 |
|---|---|
| `_ta_build(cur, d_from, d_to, basic)` | 기간형 총평균 엔진(UPDATE 순서 U1~U4 → V1~V4 → 기말 그대로 재현) |
| `_ta_basic(cur, yymm)` | 기초 = 직전월 기말. **우리 확정 월스냅샷 우선**, 없으면 레거시 시드 |
| `_snap_mat_month(cur, period)` | 월마감 = 총평균으로 수량·금액·단가 확정 |
| `_snap_mat_day(cur, period)` | 일마감 = **수량 확정 + 직전 월확정 단가로 평가** |
| `_snap_mat_movavg(...)` | ★구 이동평균 — 삭제하지 않고 **보존**(대조·롤백용, DEPRECATED) |

반영한 함정 2개: **T-SQL `ROUND` 반올림**(파이썬 `round()` 는 은행가반올림이라 불가) ·
**품목키 `UPPER+TRIM` 정규화**(레거시 CI 콜레이션이 `동Body`/`동BODY` 를 합침).

### 11-3. 검증 — 게이트1 (월마감 vs 레거시 전수)

| 월 | 품목집합 | 수량 | 금액 | 단가 |
|---|---|---|---|---|
| 2606 (기초=레거시 2605) | **2,329 = 2,329 · 스냅만 0 · 레거시만 0** | **100.00%** | 99.70% | **99.70%** |
| 2607 (기초=**우리 2606 확정**) | **2,446 = 2,446 · 스냅만 0 · 레거시만 0** | **100.00%** | 99.67% | **99.47%** |

**품목집합·수량 완전 diff0.** 이동평균이던 시절의 단가 78% → **99.5~99.7%**.

**잔차는 전부 §7-2 에서 이미 규명한 사유** — 새로운 결함 없음:
- `5210A22977R`(2,151 vs 267) · `AJR76442511-삼화` · `AJR76442512` = **마감 후 원전표 수정**
- `7072AR9374T-H`(음수재고) = 레거시가 단가 0 으로 둠 → X1(컷오버 정리) 대상

★2607 단가 일치율이 2606 보다 낮은 것(99.70→99.47)은 **기초를 우리 2606 확정본으로 썼기 때문**이다.
2606 의 잔차가 이월된 것 = **연쇄 구조가 정상 작동한다는 증거**이지 새 오차가 아니다.
(검증 스크립트가 레거시 기초를 쓰면 2607 단독은 99.79% — §7-1)

### 11-4. 남은 검증(실행중)
게이트2 월 연속성 · 게이트3~4 7월 31일 일마감 후 **일=월 수량 diff0** · 게이트5 일단가=직전 월단가.

## 12. ★확정 회계방식 (2026-08-27, 세무사 확인 후) — 이동평균법 + 저가법

### 12-1. 사실관계 — 레거시가 신고 방법과 다르다

| | 방식 |
|---|---|
| **신고된 평가방법**(세무사 확인) | **이동평균법 + 저가법** |
| **레거시 시스템 실제 계산**(`w_pu_stock_160.srw` 실측) | **총평균법** · 저가법 **없음** |
| 우리가 원래 만들었던 것 | 이동평균법 (레거시와 단가 78% 일치) |

★**"이동평균이 레거시와 78% 밖에 안 맞는다"고 문제로 봤던 것이, 사실은 신고 방법이 맞고 레거시가 어긋난 것**이었다.
⟹ **하드룰 충돌**: "레거시와 diff0" vs "신고 방법 준수" → **신고 방법이 우선**.
⟹ 자재 단가는 **레거시 diff0 대상에서 제외**하고, 차이를 리포트로 상시 노출한다.
※ 원가엔진(`nx_cost_engine`)의 레거시 diff0 하드룰은 **영향 없음** — 원가엔진은 `avg_cost` 를 참조하지 않는다(§9 실측).

**세무사께 추가 확인 필요**: 그동안 재무제표 재고금액이 레거시(총평균) 값으로 작성됐다면
**신고 방법과 다르게 평가해온 것**이 된다. 과거분 처리 방향.

### 12-2. 도메인별 확정 (대표 결정)

| 도메인 | 평가방법 | 단가 기반 |
|---|---|---|
| **자재(MAT)** | **이동평균법** | 매입원가 |
| **생산(PRD, 재공품)** | **이동평균법** | **매입가**(제조원가 아님 — 12-8) |
| **영업(SAL, 제품)** | **이동평균법** | **판가 기반**(대표 재확인 2026-08-27) |
| 전 도메인 | **저가법** 별도 레이어 | 원가 vs 시가(NRV) 중 낮은 값 |

**★영업(SAL) = 판가 기반 이동평균 (대표 재확인)**
"판가도 수시로 업데이트된다"는 지적이 있었으나 **이동평균이면 그 변동이 완화**된다 —
입고 시점 판가로 가중평균되므로 최신 판가 하나가 재고 전체를 흔들지 않는다.

**남는 회계 이슈(재무제표용은 추후 — 대표 지시)**: 판가 기반은 미실현이익이 재고자산에 섞이고
신고한 저가법(원가 vs 시가 중 낮은 값)과 상충한다. **재무제표용 원가 축 + 저가법은 별도 과제**로 미룬다.
지금 만드는 것은 **운영·관리용 평가**다.

### 12-3. 총평균 작업에서 그대로 가져갈 것 (낭비 아님)

평균 계산식만 바뀐다. **분류 축·데이터 원천·제외기준은 전부 유효**하며, 기존 우리 이동평균의 오류를 교정한다:

| 항목 | 기존 우리 이동평균 | **정본(총평균 작업에서 규명)** |
|---|---|---|
| 입고(평균 갱신) tag | `9, S` 만 ❌ | **`3, 9, C, G, H, S, P, R`** |
| 출고 tag | (전 tag 부호합) | `1,4,5,6,8,A,B,J` · 반납 `T` · 조정 `2` |
| 수입 금액 | `MAINT_AMT × 환율` ❌ | **`TAXPAYERS`(과세표준, 이미 원화)** |
| 검사 미통과 | 조건 없음 ❌ | `NOT(insp_flag IN('S','F') AND insp_proc_flag<>'1')` |
| 소모품 제외 | `LIKE '99%'` | **`ITEM_SGROUP >= '990'`** |
| 품목마스터 조인 | 없음 | `JOIN PR_M_ITEM`(미등록 탈락) |
| 품목키 | 원문 | **`UPPER(LTRIM(RTRIM()))`** (CI 콜레이션) |
| 잔량 0 | 적재 | **제외**(대표 확정) |

**★일 단위 묶음 = 건별과 동일**: 이동평균은 원칙상 입고 건건 재계산이지만,
`(q0·a0 + Σamt)/(q0 + Σqty)` 는 순차 적용과 결과가 같고, 출고는 평균을 바꾸지 않으므로
**같은 날 여러 전표를 묶어도 건별과 동일**하다. 일 단위 전개로 충분하다.

### 12-4. 저가법 — 신설 필요(현재 시스템에 전혀 없음)
```
평가액 = MIN(취득원가(이동평균), 순실현가능가치(NRV))
평가손실 = 취득원가 − NRV  (NRV < 원가 일 때)
```
**NRV 기준을 정해야 한다(미결)**:
- 원소재 = LME/시세(`nx.price_metal` 보유)
- 부품·제품 = 최근 매입단가 또는 판가 − 판매비용
- 장기체화·불용 = 무이동 기간별 감액률

→ **세무사께 NRV 산정 기준 확인 필요.** 금액을 크게 좌우한다.

### 12-5. 작업 순서
1. 자재 이동평균 **교정본**(12-3 표 반영) 구현 → 검증
2. 생산 이동평균(제조원가 기반)
3. 영업 이동평균(판가 기반)
4. 저가법 레이어(NRV 기준 확정 후)

### 12-6. ★레거시 생산재고 평가방식 규명 (2026-08-27)

정본 = `src_extracted/sa_stock_02/dw_pr_stock_480_t2.srd` (생산재고조회 480 데이터윈도).

```
생산재고 금액 = stock_qty × item_cost2
item_cost2 = pr_m_item_cost.item_cost
             WHERE cost_tag='1'                                  ← 매입단가
               AND cost_apply_ymd <= :as_from_ymd                ← ★조회 '시작일' 기준 최신
               AND cust_code = CASE WHEN m.work_code='P2' THEN '2228' ELSE m.in_cust_code END
             ROW_NUMBER() OVER(PARTITION BY item_code,cust_code ORDER BY cost_apply_ymd DESC) = 1
```

**⟹ 레거시 생산재고에는 평가방법이 없다.** 품목마스터의 **매입단가 스냅샷을 그대로 곱할 뿐**이다.
이동평균도 총평균도 아니고, 굳이 분류하면 **최종매입원가법에 가깝다**.

**문제 3가지**
1. **신고 방법(이동평균법) 미적용** — 평가방법 자체가 적용돼 있지 않다.
2. **가공비·노무비가 전혀 반영되지 않는다** — 재공품인데 **원재료 매입가로만** 평가한다.
   재공품은 투입된 제조원가(재료비+가공비)로 평가해야 한다.
3. **조회 기간을 바꾸면 금액이 바뀐다** — 단가 기준일이 `as_from_ymd`(조회 시작일)라
   같은 시점 재고인데 조회 조건에 따라 평가액이 달라진다. **재무제표 값이 될 수 없다.**

※ 레거시 `PR_T_MONTH_STOCK_WH`(생산 월마감)는 **2502 한 달치뿐** — 그 뒤로 생산 월마감을 하지 않는다.
   즉 생산재고에는 **확정된 기말 평가액이 존재하지 않는다.**

**⟹ 우리 방향(생산 = 제조원가 기반 이동평균)이 옳다는 강한 근거.**
레거시와의 diff0 은 애초에 성립하지 않는다(레거시에 확정값이 없다). 대조는 **수량 축**으로만 한다.

### 12-7. 자재 이동평균 교정본 1차 결과 (2026-08-27)

| 대조 | 2606 | 2607 |
|---|---|---|
| 품목집합 (vs 레거시) | **2,329 = 2,329 · 스냅만0·레거시만0** | **2,446 = 2,446 · 스냅만0·레거시만0** |
| **수량** | **100.00%** | **100.00%** |
| 단가 (vs 레거시 총평균) | 61.79% | 63.65% |

수량 100% = **분류 축(tag·제외기준)이 정확**하다는 증거. 단가 차이는 **방법이 다르므로 정상**이다.

**★회계 영향 — 방법 전환에 따른 재고금액 변동**

| 월 | 이동평균(신고방법) | 레거시 총평균 | 차이 |
|---|---|---|---|
| 2606 | 4,906,159,776 | 4,936,089,738 | **−29,929,962 (−0.61%)** |
| 2607 | 4,336,032,467 | 4,473,853,756 | **−137,821,289 (−3.08%)** |

→ 신고 방법으로 바로잡으면 재고자산이 **1.4억원 감소**(2607 기준). 세무사 보고 대상.

**잡은 결함 — 반올림 누적**: `avg_cost` 는 `decimal(18,4)` 반올림본인데 그걸 다음 기간 기초로
되읽어 오차가 누적됐다(금액 항등식 `stock_amt = stock_qty × avg_cost` 위반 2606 29건·2607 13건).
→ **금액을 정본으로 두고 기초 단가를 `stock_amt/stock_qty` 로 복원**하도록 `_mv_base` 수정.

### 12-8. ★생산재고 = **매입가 기반** 이동평균 (대표 결정 2026-08-27)

제조원가(재료비+가공비) 기반으로 하려다 **매입가 기반**으로 확정했다.

**채택 근거 — 실측 3가지**

| 근거 | 실측 |
|---|---|
| ① **가공비를 전 품목에 일관 적용할 수 없다** | 생산재고 **795품목 중 라우팅 보유 557 (70.1%)**. 나머지 30%는 UPH 가 없어 가공비 산출 불가 → 같은 재고에 **70% 제조원가 + 30% 매입가**가 섞여 **일관성이 깨진다**. 반쪽 제조원가보다 **일관된 매입가**가 낫다 |
| ② **지금 목적은 운영·관리용** | 재무제표용 제조원가 평가 + 저가법은 **별도 과제로 보류**(대표 지시) |
| ③ **구현·성능** | 자재와 **같은 엔진**(`_mv_step`) 재사용. 원가엔진 호출이 없어져 마감 성능도 개선 |

**★"생산 저평가 + 영업 고평가가 상쇄된다"는 근거로 삼지 않는다 — 기록에 남긴다**
- 규모가 다르다: 2607 생산 **457,999,957** vs 영업 **562,181,388** = **1.23배** → 정확한 상쇄가 아니다
- **재무제표는 계정별로 표시**된다. 총액이 우연히 맞아도 **재공품·제품 각 계정은 여전히 틀린다**
- 감사·세무에서 "상쇄되니 괜찮다"는 설명은 통하지 않는다
⟹ 채택 사유는 **①②③**이다. 나중에 재무제표용을 만들 때 이 근거가 그대로 재사용되지 않도록 명시해 둔다.

**단가 규칙**
```
생산창고 입고 단가 = 그 품목의 자재(MAT) 확정 스냅샷 avg_cost
                    없으면 pr_m_item_cost(cost_tag='1') 최신  ← 레거시와 동일 폴백
생산창고 자체의 이동평균을 (품목 × 재고위치) 축으로 전개
```
※ MAT avg_cost 는 월말값이므로 월중 입고에 쓰는 것은 **근사**다. 운영·관리용이라 허용하고,
   재무제표용을 만들 때 일단위 단가로 정밀화한다.

**대조 기준**: 레거시 생산재고는 확정 기말평가액이 없고 평가방법도 없다(12-6)
→ **금액 대조 불가. 수량 축으로만 diff0 한다.**

## 13. 생산재고 단가 — 가공/용접 분석과 단가 결정 체인 (2026-08-27)

### 13-1. ★가공과 용접은 단가 원천이 다르다 (실측 확인)

레거시 480 은 거래처를 분기해 단가를 찾는다:
`cost_tag='1' AND cust_code = CASE WHEN work_code='P2' THEN '2228' ELSE in_cust END`

**2607 생산재고 874품목 실측 — 이 분기가 데이터 분포와 정확히 일치한다**

| work_code | 품목 | 2228 단가 보유 | 매입처 단가 보유 | 단가 아무거나 |
|---|---|---|---|---|
| **P2 (가공)** | 452 | **390 (86%)** | 4 | 414 |
| (없음) | 354 | 26 | **332 (94%)** | 337 |
| **P1 (용접)** | **68** | **0** | **0** | **8 (12%)** |

⟹ 가공품은 `2228` 거래처로, 그 외는 품목 매입처로 단가가 등록돼 있다.
⟹ **★P1(용접) 68품목은 단가원이 사실상 없다** — 레거시 분기를 그대로 쓰면 통째로 단가0 이 된다.

### 13-2. 단가0 174건 원인 분해 (874품목 중 19.9%)

| 원인 | 건수 | 해결 |
|---|---|---|
| **BOM 있음** | **69** | **부품 매입가 합산**(SUB) — 대표 지적사항 |
| `PR_M_ITEM_COST` 에 단가는 있는데 못 씀 | **59** | **거래처 조건 불일치** → 폴백 완화로 회수 |
| 자재 스냅샷에 있는데 단가0 | 57 | 자재 쪽 단가도 0 (별건) |
| 진짜 단가원 없음 | **41** | 품목마스터 정비 대상 |

### 13-3. ★가공과 용접은 단가0의 성격이 정반대다

| 구분 | 품목 | 금액 | 단가0 | 그중 BOM | 자재스냅 | COST존재 |
|---|---|---|---|---|---|---|
| **가공**(loc='') | 421 | 221,929,568 | 69 (16.4%) | **2** | 38 | 39 |
| **용접**(loc≠'') | 453 | 217,679,388 | 105 (23.2%) | **67** | 19 | 20 |

- **가공 단가0** = 거래처 조건·자재단가 문제 (BOM 2건뿐)
- **용접 단가0 = SUB 문제** (BOM 67건) → **부품 매입가 합산으로 해결**

용접 재고는 **라인별 11개**(S9·S5·Q1000·RAC·S11·S8·S6·S5-2·S4·P0002·S10)로 분리된다.

### 13-4. 용접링(BCUP) — 마스터 정비 이슈가 섞여 있다
생산재고 BCUP **10품목 중 7건 단가0**인데 `PR_M_ITEM_COST` 보유는 **9건** → **거래처 조건 때문에 못 가져온다.**
게다가 마스터가 정리되지 않았다:
```
BCUP 1S-2.4*28      lg=E sg=910 make=3 매입처=2204
BCUP-1.0*9.5        lg=E sg=230 make=3 매입처=(없음)
BCUP1-1.6-9.6       lg=I sg=230 make=3 매입처=(없음)
BCUP-1S 1.6*12(OD)  lg=E sg=240 make=3 매입처=2204
```
같은 용접링인데 **sgroup 910/230/240 혼재 · 매입처 공백**. → 품목 분류정리 과제와 연결.

### 13-5. ★확정 — 생산재고 단가 결정 체인

레거시처럼 거래처를 하드 분기하지 않는다(P1 68품목이 통째로 0 이 된다).
**가공/용접 구분은 재고 축으로 유지**하고, **단가 원천만 아래 체인으로 통일**한다.

```
① 자재(MAT) 확정 스냅샷 avg_cost                    ← 자재로 관리되는 품목
② BOM 있으면 부품 매입가 합산(원가엔진 material_u)   ← SUB (용접 67건)
③ PR_M_ITEM_COST — 거래처 조건 완화                  ← 59건 회수
      2228 → 품목 매입처 → 아무 거래처(COST_APPLY_YMD 최신)
④ 그래도 없으면 0 + 리포트 노출                      ← 41건, 마스터 정비 대상
```

**검증 게이트**: (a) 수량 diff0 유지(480 recipe 대비) (b) 단가0 감소 실측 (c) 금액 항등식.

## 14. ★생산재고 단가 원칙 확정 + 검증 (2026-08-27)

### 14-1. 원칙 — **자재가 먼저 단가를 갖고, 생산은 그것을 받는다** (대표 지적)
생산재고 단가의 원천은 **자재**다. 생산 단계에서 단가를 따로 만들어내는 것이 아니라,
자재가 확정한 단가를 승계하고 생산창고에서의 혼합만 이동평균으로 반영한다.

**실증 — 제 진단이 한 번 틀렸다(정정 기록)**
"자재 스냅샷에 있는데 avg_cost 가 0" 이라고 판단했으나 **오독이었다.** 자재는 정상이었다:

| 품번 | **자재(MAT) 단가** | 기초 시드(레거시 2605) | 전개구간(2606~2607) 매입 |
|---|---|---|---|
| `BCUP1S-2.4*20.2(OD)` | **158.51** | 9,550개 / 1,513,800원 | **0건** |
| `BCUP1S-1.6*8.0(OD)` | **18.50** | 10,000개 / 185,000원 | **0건** |
| `BCUP-1S 2.4*32` | **313.00** | 3,000개 / 939,000원 | **0건** |
| `BCUP30-1.0*28.0*2` | **1,271.00** | 1,680개 / 2,135,280원 | **0건** |

**진짜 원인**: 생산 기초가 **레거시 2502 생산 월마감 시드**인데 그 시점엔 자재 마감이 없어 단가를 못 채웠고,
이 품목들은 **2502 이후 생산창고 입고가 0건**이라 기초 단가 0 이 그대로 굳었다.
코드가 **입고가 있을 때만** 단가를 갱신하니 영원히 0. `_prd_price` 가 현재 자재단가를 이미 갖고 있었는데도 **쓰이지 않는 경로**였다.

→ **수정**: `_snap_prd` 전개 후 단가 0 인 품목은 **자재 단가를 그대로 승계**한다.

### 14-2. 단가 결정 체인 (최종)
```
① 자재(MAT) 확정 스냅샷 avg_cost                       ← 원천
②' 실매입 전표 가중평균(Σ매입금액/Σ매입수량, as-of)     ← 자재 잔량0 이라 스냅샷에 없는 품목용
②  BOM 부품 매입가 합산(원가엔진 material_u)           ← SUB
③  PR_M_ITEM_COST 거래처 완화(2228→매입처→임의 최신)
④  전개 후에도 0 이면 **자재 단가 승계(보정)**          ← 14-1
⑤  그래도 0 이면 리포트
```
★②' 근거: 용접링은 **신성소재(2204)·성보스프링(2274)** 매입이며(창암스프링 아님),
`BCUP1S-2.0*19`·`BCUP1S-2.0*38(ID)` 처럼 **자재 잔량 0 이라 자재 스냅샷에 아예 없는** 품목은
실매입 전표가 유일한 단가원이다.

### 14-3. 검증 5게이트 결과

| 게이트 | 결과 |
|---|---|
| ① 자재 품목집합·수량 vs 레거시 | 2606 **2,329=2,329 전수 diff0** · 2607 **2,446=2,446 전수 diff0** |
| ② 생산 수량 vs 480 recipe | **874/874 diff0** |
| ③ 단가0 (PRD 2607) | **174(19.9%) → 46(5.3%) → 19(2.2%)** |
| ④ 금액 항등식 (MAT·PRD·SAL) | **허용초과 0** (반올림 허용 = 수량×0.00005) |
| ⑤ **생산 단가 == 자재 단가** | 347품목 중 **277 일치(79.8%)** |

**게이트⑤의 79.8% 는 정상**이다 — 나머지 20% 는 생산창고에서 여러 시점 입고가 섞여
가중평균이 자재 단가와 달라진 것으로, **이동평균의 정상 동작**이다.
마감 로그에 단가 승계 실적이 찍힌다: PRD 2606 `단가보정 201` · PRD 2607 `단가보정 15`.

### 14-4. 남은 것
- **잔여 단가0 19건**(PRD 2607): `BCUP 1S-2.4*28`(3,222개, **매입전표 0건**) · `MJU008558xx` 계열(S5 라인).
  단가 원천 자체가 없다 → 어떻게 들어온 재고인지 추적 필요(사급품/품번통합 전 이력 가능성).
- **자재 단가0 146~170건(6~7%)** — 미분석. 별도 원인 규명 필요.
- **T4 성능** — §15.

## 15. T4 성능 — 마감 속도 최적화 (2026-08-27) · **값 diff0 보존**

§12-8 에서 "성능 개선"을 근거로 적었으나 **측정도 최적화도 하지 않은 상태**였다. 실측하고 잡았다.

### 15-1. 결과

| 항목 | 최적화 전 | 최적화 후 | 개선 |
|---|---|---|---|
| **자재 일마감** | **58.38초/일** | **3.98초/일** | **14.7배** |
| 자재 31일 | 1,810초 (30분) | **123초 (2분)** | |
| **생산 일마감** | 21.36초/일 | **3.16초/일**(warm) | **6.8배** |
| 생산 31일 | 662초 | **138초** (1회차 43초 + warm 3.16×30) | |

**값 보존 검증**: 자재 2607 재마감 **2,446/2,446 diff0 PASS** · 생산 2607 재마감 **874/874 diff0 PASS**.
최적화는 **속도만 바꾸고 값은 바꾸지 않는다**를 매 단계 확인했다.

### 15-2. 병목 규명 — 구간별 측정 (생산 260805 기준)

| 구간 | 소요 | 판정 |
|---|---|---|
| `_prd_base` | 0.09초 | — |
| `_prd_price` | 3.28초 | 2차 대상 |
| `_prd_moves` | 0.55초 | — |
| **`_prd_price_bom`** | **34.16초** | **★병목 90%** |

### 15-3. 적용한 최적화 3가지

**① 배치 INSERT (자재 58.4 → 3.7초, 15.7배)**
행 단위 `cur.execute` 로 넣어 **품목당 1회 왕복**이 발생했다(자재 2,441행 = 2,441왕복).
→ `pyodbc fast_executemany` + `executemany` 로 한 번에 전송. 공용 헬퍼 **`_snap_bulk()`** 신설,
   MAT·PRD·SAL 적재부를 모두 위임.

**② 원가엔진 싱글턴 + BOM 단가 (품목,연월) 캐시 (생산 21.4 → 6.8초)**
마감마다 `NxCostEngine()` 을 새로 만들어 내부 캐시(`_hasbom`/`_hdr`/단가)가 **매번 콜드**였고,
같은 품목을 일마감 31회 동안 31번 재전개했다.
→ 엔진 싱글턴(`_bom_engine()`, 죽으면 재생성) + `_BOM_PX_CACHE[(item, yymm)]`.

**③ 단가 체인 월 캐시 (생산 6.8 → 3.16초)**
`_prd_price` 가 마감마다 `PR_M_ITEM_COST` 전체 + 실매입 전표 as-of 누계 + `nx.item` 을 **전체 스캔**했다.
구성요소가 전부 월 단위로 사실상 불변이라 → `_PRD_PX_CACHE[yymm]`.

### 15-4. ★캐시 정합성 — 무효화 규칙
**자재 마감이 갱신되면 생산 단가 캐시는 낡는다.** `close_run` 에서 `domain='MAT'` 마감 직전에
`_PRD_PX_CACHE.clear()` · `_BOM_PX_CACHE.clear()` 로 무효화한다.
(생산 단가의 원천이 자재이므로 — §14-1)

**※근사 명시(의도적)**: 두 캐시 모두 **연월 단위**다. 월 내 단가 변동은 반영되지 않는다.
이 값들은 "다른 경로로 단가를 못 구한 품목"의 폴백이므로 월 단위로 충분하다고 판단했다.
**정밀이 필요해지면 캐시 키를 일자로 낮추면 된다**(코드 주석에도 명기).

### 15-5. 남은 성능 여지
- **첫 마감(콜드) 43초** — 엔진 워밍업. 연속 마감에선 1회만 발생하므로 우선순위 낮음.
- 월마감 재실행 64초 — 월 전체를 다시 전개하므로 일마감보다 느린 것이 정상.

## 16. 단계1 — 자재 단가0 보정 (2026-08-27) · **완료**

### 16-1. 원인 분해 (MAT 2607 단가0 170건)

| 원인 | 건수 | 처리 |
|---|---|---|
| **매입 있고 금액도 있음** | **73** | **★실매입 단가로 회수** |
| 매입 있으나 금액0(무상입고) | 76 | 단가원 없음 — 잔존 |
| 기초에 수량만 있고 금액0 | 16 | 일부 회수 |
| 기초에 없음 + 매입도 없음 | 5 | 잔존 |

생산에서 겪은 것과 **같은 패턴** — 전개구간에 매입이 없어 **레거시 기초의 금액0이 그대로 굳은 것**.

### 16-2. 구현 — `_mv_buyprice()`
전개 후 단가가 0 인 품목을 **실매입 전표 가중평균**(Σ매입금액/Σ매입수량, as-of)으로 보정.
`_MAT_BUY_CACHE[yymm]` 월 캐시(T4 교훈 적용).

**★판단 근거(회계)**: 이동평균법 자체를 바꾸는 것이 아니라 **결함 기초를 보정**하는 것이다.
재고자산을 0 으로 누락시키는 것보다 **실제 지불가로 계상**하는 편이 정확하다.

### 16-3. 검증 결과

| 게이트 | 결과 |
|---|---|
| ① 자재 수량 vs 레거시 (보정이 수량을 안 건드려야) | 2606 **2,329 diff0** · 2607 **2,446 diff0 PASS** |
| ② 단가0 | **MAT 2607 170(7.0%) → 99(4.0%)** · MAT 2606 → 75(3.2%) |
| ③ 금액 항등식 | MAT·PRD·SAL 전부 **허용초과 0 PASS** |
| ④ 잔여 원인 | 99건 = **무상입고 76 · 매입이력 없음 21 · 기타 2** |

**재고자산 변화**: MAT 2607 4,336,032,505 → **4,343,601,245원** (**+7,568,740원**).
단가0 이던 73품목이 실제 매입가로 계상된 결과. 누락돼 있던 재고자산이 드러난 것이다.
마감 로그에 보정 실적: MAT 2606 `단가보정 71` · MAT 2607 `단가보정 33`.

### 16-4. ★그 과정에서 낸 사고 1건 — 캐시 무효화 과잉
`close_run` 에서 자재 마감 시 `_BOM_PX_CACHE` 까지 함께 비웠다.
**BOM 부품합산은 원가엔진이 단가마스터를 읽는 것이라 자재 마감과 무관**한데도 지운 탓에
자재 마감마다 생산이 콜드 재계산(**241초**)됐고, 그 사이 **트랜잭션 커넥션이 유휴로 끊겨 500** 이 났다.

→ 무효화 범위를 **자재 단가를 참조하는 캐시(`_PRD_PX_CACHE`·`_MAT_BUY_CACHE`)로만** 축소.
**교훈: 캐시 무효화는 "그 캐시가 실제로 무엇에 의존하는가"로 범위를 정해야 한다.
넓게 지우면 안전한 것이 아니라 성능·안정성 사고가 된다.**

### 16-5. 잔여 99건 = 단가원이 실제로 없는 것
- **무상입고 76건** — 매입 전표는 있는데 금액이 0. 사급/무상 반입으로 보인다.
- **매입이력 없음 21건** · 기타 2건.
→ 이들은 **마스터 정비 또는 사급 축 처리**가 필요하다(별도 과제). 리포트로 노출한다.

## 17. 단계2 — ★중대 결함: 금액 0 입고가 단가를 희석시켰다 (2026-08-28)

### 17-1. 결함 — 추측이 아니라 전개 추적으로 확인
가중평균 `(q0×a0 + pamt) / (q0 + pq)` 에서 **`pamt=0` 인 입고가 분모만 키워** 기존 단가를 희석시켰다.
반복되면 0 이 된다. **돈을 내지 않은 입고가 기존 재고의 평가를 깎았다.**

**실증 `MJU63057401` (기초 235.71원)**

| 일자 | 입고 | 금액 | 단가 |
|---|---|---|---|
| 260701 | 4 | **0** | 235.71 → **47.14** |
| 260702 | 5 | **0** | 47.14 → **23.57** |
| 260713 | 18 | **0** | 11.79 → **0.62** |
| 260720 | 20 | **0** | 0.03 → **0.00** ← 소멸 |

`MJU63156803` 도 동일: **757.55 → 0**.

### 17-2. 수정 — 대표 확정 "0원이 재고가격에 영향을 주면 안 된다"

**자재 `_mv_step` 규칙2 신설**
```python
if pq > 0 and pamt > 0:    # ★금액이 있는 입고만 평균 갱신
    avg = ((q0*a0 + pamt)/(q0+pq)) if q0 > 0 else (pamt/pq)
else:                      # 입고 없음 · 금액0(무상) → 단가 불변
    avg = a0
```
**생산 `_snap_prd` 도 동일 결함** — 참조 단가 `c` 가 0 이면 같은 희석이 일어났다. `pq > 0 and c > 0` 으로 조임.

**근거**: 무상입고(사급 반입 등)는 취득원가가 없으므로 기존 재고의 평가를 바꿀 근거가 없다.

### 17-3. 검증 5게이트 — 전부 통과

| 게이트 | 결과 |
|---|---|
| ① 자재 수량 vs 레거시 | 2606 **2,329 diff0** · 2607 **2,446 diff0 PASS** (단가 규칙이 수량 미변경 확인) |
| ② 단가0 | MAT 2607 99 → **97** · 2606 75 → **74** |
| ③ **실증 2건 단가 유지** | `MJU63057401` **235.71** · `MJU63156803` **757.55** ✅ |
| ④ 금액 항등식 | MAT·PRD·SAL 전부 **허용초과 0** |
| ⑤ 잔여 원인 | MAT 97 = 무상입고 75 · 매입이력없음 20 · 회수가능 2 |

### 17-4. ★회계 영향 — 재고자산 회복

| 스냅샷 | 수정 전 | 수정 후 | 차이 |
|---|---|---|---|
| MAT 2606 | 4,912,759,689 | **5,019,111,676** | **+106,351,987** |
| MAT 2607 | 4,343,601,245 | **4,372,967,357** | **+29,366,112** |
| PRD 2606 | 226,743,277 | **245,311,042** | +18,567,765 |
| PRD 2607 | 439,228,460 | **442,905,975** | +3,677,515 |

**단가0 까지 떨어진 건 2건뿐이었지만 재고자산은 1억원 이상 회복**됐다.
= **0 까지 가지 않았어도 부당하게 희석된 품목이 훨씬 많았다**는 뜻이다.

### 17-5. 내 예상이 빗나간 점 (기록)
"무상입고 75건은 이 결함의 산물일 것"이라고 봤으나 **아니었다.**
결함을 고친 뒤에도 75건은 그대로다 = **실제로 단가원이 없는 것이 맞았다.**
결함(희석)과 단가원 부재는 **별개 문제**였다.

### 17-6. 이상치 리포트 신설 — `/api/close/anomaly`
마감을 막지 않고 **사람이 봐야 할 것만 드러낸다**. 도메인·기간별로:
- **단가0** + 원인 분류(회수가능 / 무상입고 / 매입이력 없음)
- **음수재고** + 컷오버 정리대상(X1) 표시

실측: MAT 2607 단가0 97·음수 11(**−37,891,533원**) / PRD 2607 단가0 19·음수 24(−1,413,255원) /
SAL 2607 단가0 2·음수 0.

## 18. ★단계2 정정 — 내가 지시 범위를 넘어 계산식을 바꾼 사고 (2026-08-28)

### 18-1. 무엇을 잘못했나 (경위 기록)
**지시**: "단가 0인 것만 재고 금액에 반영시키지 말라" — **결과에서 빼라**는 것.

**내가 한 것**:
1. tag C(34,953건)·P(10,261건)가 **100% 금액 0** 인 것을 보고 → **"레거시 잔재"라고 단정**.
   ★tag C 는 비고조차 없어 **성격을 모르는 상태**였는데도 일반화했다.
2. 그 근거로 **`TA_PRICE_TAGS` 를 만들어 계산식(`_mv_step`)을 바꿨다** — 지시하지 않은 것.
3. 결과: **왕복·생산 물량의 금액이 편도로 증발**하는 구조를 만들었다.
   `자재창고 → 키팅 → 생산창고` 는 100원이 빠지는데 **복귀는 0원**으로 들어온다.
   자체 SUB 생산도 부품 300원을 넣고 **0원짜리 완성품**이 된다.
4. 그걸 **"재고자산 1억원 회복"** 이라고 성과처럼 보고했다. 실제로는 **증상(단가 희석)만 가리고
   원인(금액 미복원)은 그대로 둔 것**이었다.

**어긴 규칙**: 성급한 일반화 금지 · 일반화 전 전수검증 · **지시 범위 준수**.

**되돌린 것**: `TA_PRICE_TAGS` 제거 · `_mv_step`·`_snap_prd` 계산식 원복(`if pq > 0:`).
**남긴 것**: `_snap_bulk` 의 **결과 제외 규칙만**.

### 18-2. 확정 규칙 (대표 확정 2026-08-28)

| 대상 | 처리 |
|---|---|
| **음수 재고** | 확정 스냅샷에서 **수량·금액 모두 제외**. 다음 달 이월 안 함. **컷오버 미이관** |
| **단가 0** | 동일 제외 — "단가 원천이 없는 것은 **잘못된 재고일 확률이 높다**" |
| **당월 거래** | **전부 반영**. 계산은 온전히 하고 그 **결과**가 음수·단가0 이면 확정에서 뺀다 |
| **제외분** | **`nx.stock_snapshot_drop` 보존** → 리포트로 드러남 ("잘못된 것이 보여지게 하는 것이 맞다") |

**근거**: 우리 시스템은 `_neg_stock_msg` 로 **음수를 애초에 차단**한다. 지금 보이는 음수는 전부
**레거시가 게이트 없이 쌓은 과거 잔재**이므로 물려받지 않는 것이 설계와 일치한다.

**★판정은 반올림 후 값으로**: `avg_cost` 는 `decimal(18,4)` 로 저장되므로 반올림 **전** 값으로
걸르면 `0.00003` 같은 미세단가가 통과해 단가0 이 남는다(실측 1~2건).

### 18-3. 검증 5게이트 — 전부 통과

| 게이트 | 결과 |
|---|---|
| ① 확정 스냅샷 단가0·음수 | **전 도메인 0건** |
| ② 제외분 보존 | `stock_snapshot_drop` 에 전량 |
| ③ 리포트 노출 | 인식분/제외분 + 원인 분류 |
| ④ 금액 항등식 | 전 도메인 **허용초과 0** |
| ⑤ 인식 + 제외 = 전체 | **누락 없음** |

**제외 실적 (= 컷오버 미이관 대상)**

| 도메인 | 기간 | 단가0 | 음수수량 | 음수 금액 |
|---|---|---|---|---|
| MAT | 2606 | 74 | 34 | **−20,605,698** |
| MAT | 2607 | 56 | 31 | **−37,900,495** |
| PRD | 2606 | 16 | 25 | −1,422,345 |
| PRD | 2607 | 7 | 9 | −213 |
| SAL | 2607 | 2 | 0 | 0 |

**MAT 2607 인식분 = 2,338품목 · 수량 6,805,250 · 금액 4,389,960,191원**

**원인 분류(MAT 2607)**: 음수재고 31 · 단가0 금액0입고만 51 · 단가0 회수가능 3 · 단가0 매입이력없음 2

### 18-4. ★레거시 대조로 제외 규칙 정당성 확인 (2026-08-28) — 종결

제외분이 **레거시에는 금액이 있는지** 전수 대조했다.

| 제외분 | 건수 | 레거시에 단가 있음 | **레거시도 단가 0** | 레거시에 품목 없음 |
|---|---|---|---|---|
| **단가0** | 56 | **0** | **56** | 0 |
| **음수수량** | 31 | 11 | 20 | 0 |

**★단가0 56건은 레거시도 전부 0.** 우리가 못 채운 게 아니라 **애초에 단가원이 없다** → **제외 정당**.
⟹ 앞서 "회수가능(실매입 단가 있음) 3건" 은 **라벨 오류**였다. `buy` 는 **전 기간** 매입 이력을 보는데
   마감 시점(as-of) 단가는 없고 레거시도 0 이다. 라벨을 **"과거 매입이력은 있으나 마감시점 단가 없음"**
   으로 정정했다. **살릴 것 없음.**

**음수 31건 중 11건은 레거시에 단가가 있다** = **단가는 정상인데 수량이 음수**인 것.
단가 문제가 아니라 **수량이 잘못된 것**이므로 제외가 맞다(우리는 음수를 허용하지 않는다).

**∴ 단계2 종결.** 제외 규칙이 데이터로 정당화됐다.

### 18-5. 레거시 대조 기준 변경
음수를 이월하지 않으므로 **레거시와 수량이 벌어진다**(2607 실측 31건, 차이가 정확히 레거시 음수분과 일치).
⟹ 대조 게이트를 "레거시 diff0" 로 두면 안 된다. **"차이 = 레거시 음수분"** 을 검증한다.


---

## §19. ★결함수정 — 제외분이 **다음 기간 기초에서도 빠지던** 문제 (2026-08-28)

### 무슨 일이 있었나
확정 스냅샷에서 **단가0·음수수량·잔량0** 을 제외(§18)하고 `nx.stock_snapshot_drop` 에 보존했다.
그런데 다음 기간의 **기초를 `nx.stock_snapshot` 에서만 읽었다.**
⟹ 제외된 품목은 다음 기간이 **0에서 출발**한다. 음수가 소리 없이 사라지고 재고가 늘어난 것처럼 보인다.

**실측 (`11588O-1`)**

| | 값 |
|---|---|
| 2606 기말 | **−190** (음수 → 스냅샷 제외, drop 에 보존) |
| 2607 마감이 읽은 기초 | **0** (스냅샷에 없으니) |
| 그래서 나온 2607 기말 | **2042** |
| 실제(연속성 유지 시) | **1852** ← −190 + 7월 이동 |

이 한 품목만이 아니다. 수불장 파생과 확정 스냅샷을 대조하니 **수량차 34건 · 금액차 419건**.

### 왜 놓쳤나
"제외"의 의미를 **평가에서 빼기**로 정의했는데, 구현이 **기초 연쇄에서도 빼기**가 되어 있었다.
대표가 미리 물었던 지점이다 — *"당월에 당연히 반영이 되었고… 그럼 다음달에도 반영이 안되는거 아니야?"*
그때 답을 코드에 반영하지 못했다.

### 규칙 (확정)
> **제외는 표시·평가에서만 한다. 수량·금액의 연속성은 어떤 경우에도 끊지 않는다.**
> 기초 복원은 **`nx.stock_snapshot` ∪ `nx.stock_snapshot_drop`**.

구현 = `routers/close.py` `_snapshot_rows(cur, domain, ptype, period, with_loc)`.
`_mv_base`(자재) · `_prd_base`(생산) 둘 다 이 헬퍼를 쓴다. **SAL 도 스냅샷 기초를 쓰게 되면 같이 적용할 것.**

### 남은 조치
- 이미 저장된 **2607 확정 스냅샷은 옛 로직 산물**이다(위 2042 등). **재마감 필요** — 대표 승인 후.
- 재마감 후 `/api/close/ledger` 와 확정 스냅샷이 **수량 diff0** 이 되는지 재대조한다.

---

## §20. C7 — 수불장을 확정 스냅샷 기반으로 전환 (2026-08-28)

`GET /api/close/ledger?domain=MAT&d_from=&d_to=&zero=&q=`

**설계**(정본 `STOCK_CLOSE_HANDOFF.md` §7-1): 수불장은 저장하지 않는 **파생 뷰**다.
```
기말 = 기초(직전 확정 스냅샷 ∪ 제외분) + 입고 − 출고 ± 조정
```
- **마감과 같은 엔진**(`_mv_base`/`_mv_moves`/`_mv_step`)을 호출한다. 화면이 따로 계산하면 값이 갈린다.
- 기초 스냅샷 시점과 조회 시작일 사이는 **전표로 이월**해 붙인다(이중계상 금지 — §7-7 #2).
- **불변식 `기초+입−출±조정=기말` 을 서버가 검산**해 `invariant_breaks` 로 돌려준다. 숨기지 않는다.

**실측 (2026-08-01~08-28)**: 품목 3,646 · 9.3초 · **불변식 위반 0건**
기초 6,805,250 / 43.9억 → 입 3,078,068 / 68.3억 → 출 3,944,654 → 조정 28,106 → 기말 5,966,771 / 42.5억

**이걸로 대체되는 것**: `live_api.matledger` 는 레거시 임시테이블을 읽는다 —
월 `PU_T_MONTH_STOCK_WH`, 일 `PU_T_MONTH_STOCK_WH_DAILY`(레거시 `w_pu_stock_260` 이 **조회할 때마다 TRUNCATE**).
즉 일자 수불장이 "누가 언제 조회했느냐"로 바뀐다. **신규 화면은 이 두 테이블을 참조 금지.**


---

## §21. 재마감 실행 + 수불장 diff0 달성 (2026-08-28, 대표 승인)

### 실행
1. 백업 `nx.stock_snapshot_bak_260828` (6,674행)
2. 2607 마감해제 — MAT 2,338 · PRD 837 · SAL 212 품목 제거
3. 2607 재마감 (수정본) — MAT **2,336품목** 확정

### 재대조에서 추가로 드러난 결함 2건
§19 를 고치고도 diff 가 남아 끝까지 추적한 결과, **같은 기간인데 마감과 수불장이 다르게 계산**하고 있었다.

| # | 결함 | 증상 | 수정 |
|---|---|---|---|
| A | **기초 후보를 `TOP 1` 로 뽑고 나서 조건 검사** — 최신 월마감이 target 보다 뒤면 그 아래 쓸 수 있는 마감이 있는데도 **레거시 시드로 추락** | 마감은 2606 스냅샷 기초, 수불장은 레거시 2606 시드 기초 → **금액차 394건** | target 이전 마감 중 **최신**을 훑어 선택. `_mv_base`·`_prd_base` 둘 다 |
| B | **단가0 보정(`_mv_buyprice`)을 마감만 적용** | 전개 후 단가0 인 품목이 수불장에선 금액 0 → **금액차 33건** | 수불장도 기초·기말에 동일 보정 |

### 최종 검증 — `/api/close/ledger` vs 확정 스냅샷(∪ 제외분) 2607
```
수불장 품목 3,743 · 불변식 위반 0 · 기초 260630 확정 스냅샷(월마감 2606)
★대조 2,446품목 — 수량차 0 · 금액차 0 · 누락 0     ⟹ diff0
```
`11588O-1` = 1852 (수정 전 2042) — 음수 기초 −190 이 정상 승계됨.

### 교훈 (재발 방지)
> **"같은 값을 두 곳에서 계산하면 반드시 갈린다."**
> 화면과 마감이 같은 숫자를 보여야 한다면 **같은 함수를 호출**해야 한다.
> 이번에 갈린 3가지 — 기초 소스(스냅샷 ∪ 제외분) · 기초 선택 규칙 · 단가0 보정 —
> 는 전부 "한쪽에만 있던 처리"였다. 새 화면을 붙일 때 **엔진 호출로 끝나는지** 먼저 확인할 것.


---

## §22. 자재수불장 화면 전환 완료 (C7, 2026-08-28)

`SCREEN.matledger` → `GET /api/close/ledger`. **레거시 임시테이블 참조 0건.**

### 화면이 바뀐 점
| | 전 | 후 |
|---|---|---|
| 소스 | 월 `PU_T_MONTH_STOCK_WH` / 일 `PU_T_MONTH_STOCK_WH_DAILY`(조회 시 TRUNCATE) | 확정 스냅샷 + 전표 파생 |
| 조회 | 일자 1개 또는 마감월 1개 | **기간**(from~to) — 아무 구간이나 |
| 단가 | 금액/수량(레거시 총평균 잔재) | **이동평균**(신고 평가방법) |
| 검산 | 없음 | 불변식 위반·평가조정을 **화면 상단에 노출** |
| 컬럼 | 23 | 24 (**평가조정** 추가) |

### ★평가조정(va) 열을 새로 둔 이유
이동평균에는 금액 항등식을 **의도적으로** 깨는 규칙이 둘 있다.
① 단가0 보정(전개 후에도 0이면 실매입 가중평균 주입 §13-5) ② 재고≤0 매입 refill 시 단가 리셋.
둘 다 "이동"이 아니라 **단가를 고쳐 끼우는** 행위라 `기초+입−출±조정` 으로 설명되지 않는다.
잔차를 버리면 화면 합계가 안 맞고, 오차로 숨기면 원인을 못 찾는다 ⟹ **명시 열로 드러낸다.**
```
기초금액 + 입고 − 출고 + 조정 + 평가조정 = 기말금액      (항상 성립)
```

### 실측 (2026-08-01~08-28)
```
품목 3,670 · 기초 260731 확정 스냅샷(월마감 2607)
수량  기초 7,502,521 + 입 3,078,068 − 출 3,944,654 + 조정 28,106 = 기말 6,664,042   차 0
금액  4,343,601,245 + 6,833,113,649 − 6,965,804,717 + (−5,105,541) + (−2,810,338)
      = 4,202,994,298 = 기말                                                        차 0
불변식 위반 0건 · 평가조정 92건(−2,810,338원)
```
**화면 실구동(playwright)**: 3,670행 렌더 · 컬럼 24 · JS 오류 **0건** · `close/ledger` 호출 확인.

### 남은 것
- 출고금액 계산 시 **그날 평균 갱신 후** 단가를 쓴다(갱신 전 단가를 쓰면 1.46억 어긋남 — 실측).
- 생산(PRD)·영업(SAL) 수불장은 미구현. `domain=MAT` 외에는 400 으로 막아 뒀다.


---

## §23. 생산(PRD) 수불장 신설 (2026-08-28)

`GET /api/close/ledger?domain=PRD` · 화면 = 자재수불장 상단 **자재/생산 토글**.
축 = **(품목 × 재고위치)** — 가공창고 P0001 = loc `''` · 용접은 라인코드. 화면에 "재고위치" 열 추가.

**마감(`_snap_prd`)과 같은 순서로 돈다** — 기초 → 단가 → 일자 전개 → 단가0 보정.
새로 짜지 않고 `_prd_base` / `_prd_price` / `_prd_price_bom` / `_prd_moves` 를 그대로 호출한다.

### ★실측으로 드러난 함정 — 단가 as-of 시점
`_prd_price(cur, target)` 는 **as-of 일자**로 실매입 누계를 계산하는데, 성능 캐시는 **월(YYMM) 단위**다.
처음엔 기간 시작일로 먼저 호출했더니 **'월초 as-of' 단가가 캐시에 박혀** 기간 전체에 쓰였고,
같은 2607 을 마감은 월말 단가로 전개해 값이 갈렸다.

| | 수불장(수정 전) | 마감 |
|---|---|---|
| `AAA31179503` 단가 | 2,906.41 | 2,698.69 |
| 스냅샷 대조 | 금액차 **216건** | — |

⟹ **단가는 조회 종료일 기준 하나로** 전 구간을 전개한다(마감과 동일). 수정 후 금액차 0.
(같은 뿌리의 세 번째 사례 — §21 "같은 값을 두 곳에서 계산하면 갈린다". 이번엔 *언제* 계산하느냐였다.)

### 검증 (2607)
```
품목×위치 3,834 · 기초 260630 확정 스냅샷(월마감 2606)
불변식 위반 0건 · 평가조정 14건
수량 차 0.00 · 금액 차 0
★스냅샷 대조 874건 — 수량차 0 · 금액차 0 · 누락 0        ⟹ diff0
```

### 미지원
영업(SAL)은 040 recipe 기반이라 별도(§7-5). `domain` 은 MAT·PRD 만 허용, 그 외 400.


---

## §24. 수불장 ↔ 자재입출고현황 대조 — **차이 75건 발견, 원인 미규명** (2026-08-28)

### 대조 (2026-08-01~08-28)
| | 값 |
|---|---|
| 수불장(`/api/close/ledger`) 품목 | 3,670 |
| 자재입출고현황(`/api/live/matinout`) 품목 | 4,109 |
| 공통 | 3,670 |
| **현재고 불일치** | **75건 (2.04%)** |
| 수불장에만 | 0 |
| 입출고현황에만 | 439 — 대부분 sgroup 130 가공품(284). 자재수불장 scope(소모품·미등록 제외) 밖이라 **정상** |

### ★버킷 범위 차이라는 가설은 **틀렸다**(실측으로 기각)
"입출고현황은 `Z99990/IS0001` 버킷만 보고 수불장은 전 버킷을 본다"고 추정했으나,
문제 품목은 **버킷이 하나뿐**인데도 **세 소스가 전부 다르다**.

| 품목 | 수불장 | 입출고현황 | `PU_T_MAT_STOCK_WH` 잔액 |
|---|---|---|---|
| `4A00742F` | 4,158 | 1,308 | 1,959 |
| `MJH63775702` | 1,402 | 2,402 | 2,412 |
| `4930A20053B` | 201,534 | 202,282 | 204,671 |

즉 **세 값이 서로 다른 3개의 진실**을 말하고 있다. 어느 하나가 맞다고 단정할 근거가 아직 없다.

### 다음에 확인할 것 (추정 금지 — 전표로 검산할 것)
1. `_matinout` 의 `stock` 이 무엇을 재는지 정확히 규명(잔액 직독 / 기초+이동 재계산 / 시점 기준).
2. 세 소스를 **같은 품목·같은 기간의 전표 원본**으로 각각 검산해 어느 것이 전표와 맞는지 확정.
3. 확정된 것을 정본으로 삼고 나머지를 그쪽으로 수렴(§21 원칙: 같은 값은 한 곳에서 계산).

★이 차이는 **컷오버 전에 반드시 규명**해야 한다. 화면마다 재고가 다르게 보이는 상태로 넘어갈 수 없다.


---

## §25. ★§24 불일치 75건 — **원인 규명 완료** (2026-08-28)

### 결론 : 수불장이 맞고, 자재입출고현황이 틀렸다
75건을 **전표(원천 `PU_T_STOCK_MAINT`)로 전수 검산**했다.

| 소스 | 전표와 일치 |
|---|---|
| **수불장** `/api/close/ledger` | **72/75 (96.0%)** |
| 자재입출고현황 `/api/live/matinout` | **0/75 (0.0%)** |

```
4A00742F      기초 1,150 + 8월이동 +3,008 = 4,158   ← 수불장 4,158  · 입출고 1,308
5410A20111T   기초 10,685 + 8월이동 −8,296 = 2,389  ← 수불장 2,389  · 입출고 3,135
EAP65285905   기초 811 + 8월이동 −797 = 14          ← 수불장 14     · 입출고 387
```

### 원인 : 입출고현황은 **전표로 계산하지 않고 잔액 테이블을 읽는다**
`matinout` 의 `stock` 은 `PU_T_MAT_STOCK_WH`(자재창고 잔액 미러)를 읽는다.
75건 중 **49건(65.3%)에서 입출고현황 값 = 미러 잔액과 정확히 일치**한다(3,135 / 7,922 / 556 / 400 / 387 …).
그 미러 잔액이 전표와 어긋나 있다 = **미러 드리프트**.
예) `EAP65285905` 는 전표상 797개가 나가 14개인데, 잔액 테이블엔 **387개가 그대로 남아** 있다.

★기각된 가설 (실측으로 확인) — 전부 원인이 아니다
  · 버킷 범위 차이(`Z99990`/`IS0001`) — 전 버킷과 버킷한정의 이동이 **완전히 동일**
  · 창고재고이동(`PU_T_STOCK_MOVE`) — 대상 품목 전부 **0**
  · 절단입고(`pu_t_cut_dtl`) — 전부 **0**

### 나머지 3건 = **라이브 ↔ nx 미러 전표 자체가 다름**
| 품목 | 라이브 전표 | nx 미러 전표 | 차 |
|---|---|---|---|
| `AJR30073602-F&T-1` | 23건 −956 | 22건 −756 | **1건 −200 누락** |
| `AJR30073602-F&T-2` | 23건 −956 | 22건 −756 | **1건 −200 누락** |
| `7072AR9374D` | 30건 −832.1 | 30건 −826.1 | 건수 같고 **수량 −6 차이** |

수불장은 기초를 확정 스냅샷에서, 이동을 **라이브 전표**에서 읽는다.
반면 미러(`nx.PU_T_STOCK_MAINT`)에 그 전표가 덜 실려 있다 ⟹ **동기화 누락**.
`r_delta_sync` 는 최근 30일 DELETE+재복사인데 이 3건이 빠졌다 — 재싱크로 해소되는지 확인 필요.

### 조치
1. **`matinout` 을 전표 파생으로 전환** — 잔액 직독을 끊는다(수불장과 같은 엔진 호출).
   §21 원칙: 같은 값을 두 곳에서 계산하면 갈린다.
2. 미러 드리프트 3건 — `r_delta_sync` 재실행 후 재대조.
3. 컷오버 후에는 `nx.stock_ledger` 단일 원장이 정본이 되므로 이 문제는 구조적으로 사라진다(§7-8).

★**"화면마다 재고가 다르게 보이는" 상태의 정체 = 계산본(전표) vs 잔액본(미러) 혼용.**
  신규 ERP 는 **전표 파생 하나**로 간다.


---

## §26. ★§24·§25 정정 — 불일치 75건은 **결함이 아니다** (2026-08-28 확정)

> §25 에 "입출고현황이 틀렸다 / 동기화 누락 결함" 이라고 적었으나 **둘 다 틀렸다.** 아래가 최종.

### 사실관계
| 기간 | 라이브 vs 미러 전표 |
|---|---|
| 상반기 (~2606) | **차 0건** |
| 2607 (마감 완료) | **차 0건** · 2606 월마감 시드도 차 0 |
| 2608 (진행 중) | 90건 — 미러 최종 sync **14:04** 이후 라이브 활동 |

**마감된 과거는 완전히 동일하다 ⟹ 확정 재고 금액에 영향 없음.**

### 두 화면이 다른 이유 (설계상 정상)
| 화면 | 원천 | 성격 |
|---|---|---|
| 자재수불장 · 마감 (`close.py`) | **라이브** `PARTNER_ERP.dbo` | 실제 데이터만 — 재고 금액이 정확해야 하므로 |
| 자재입출고현황 (`live_api.py`) | **nx 미러** | 라이브 복제 + **직원 테스트 입력 포함** |

차이 = ① 미러 sync 시차 ② **미러에 섞인 테스트 입력분**.

### ★수불장을 미러로 전환하려던 계획 — **철회**
정본 `CUTOVER_MUST_AND_DAILY_MIGRATION.md` §10 이 "조회=nx미러∪웹" 이라 해서
수불장·마감도 미러로 옮기려 했으나, **지금 nx 미러에는 직원 테스트 입력이 들어간다**(대표 확인).
옮기면 **테스트 데이터가 재고 금액에 잡힌다.** 하면 안 된다.

> **마감·재고금액은 라이브 전표만 읽는다. 병행 테스트 기간에는 이 분리를 유지한다.**
> 컷오버로 테스트 입력이 실데이터가 되는 시점에 한 번에 전환한다(그때 미러=정본).

### 남은 확인거리 (결함 아님, 인지만)
- `260825` 미러 전용 2건 (`AJR33295152`/`AJR33295154` 각 5개, 우윤택, 8/26 입력) = 테스트 입력.
  `r_delta_sync` 가 최근 30일 DELETE+재복사인데 살아 있다 → sync 범위/대상 확인 필요.
- 병행기간 동안 두 화면 값이 다른 것은 **정상**이다. 사용자에게 혼동을 주므로
  화면에 원천을 표기하거나(“테스트 입력 포함”) 컷오버로 해소한다.


---

## §27. 영업(SAL) 수불장 신설 + **마감 결함수정** (2026-08-28)

`GET /api/close/ledger?domain=SAL` · 화면 토글 **[자재 | 생산 | 영업]** 완성.
축 = 품목(위치 없음). 이동 원천 = 레거시 제품재고조회 **040 분기 그대로**를 일자별로 편 것.
단가 = **판가 기반 이동평균**(§7-4 확정) · 판가 = `pr_m_item_cost(cost_tag IN ('S','E'))` as-of.

### ★발견한 결함 — 영업 마감이 이동평균이 아니었다
`_snap_sal` 이 040 recipe 를 그대로 확정해 **as-of 판가 × 수량** 으로 굳히고 있었다.
판가가 오르면 **기초 재고까지 전량 신규 판가로 재평가**된다 = 이동평균이 아니다.

실측(2607) — 7/9 판가 인상 품목 **33건**에서 수불장과 금액이 갈렸다.
| 품목 | 2606 기초단가 | 7/9 판가 | 마감(구) | 수불장(이동평균) |
|---|---|---|---|---|
| `6851A20037L` | 36,786 | 37,597 | 940×37,597 = **35,341,180** | **34,578,840** |
| `5211A20459K` | 9,119 | 9,167 | 806,696 | 802,472 |

⟹ `_snap_sal` 을 **`_sal_ledger` 호출로 교체**했다(§21: 같은 값은 한 곳에서 계산).
   구 recipe 는 `_snap_sal_recipe` 로 보존(수량 대조용, DEPRECATED).

### 검증 (2607 재마감 후)
```
수불장 품목 801 · 기초 260630 확정 스냅샷(월마감 2606)
불변식 위반 0 · 평가조정 2
수량 차 0 · 금액 차 0
★스냅샷 대조 214건 — 수량차 0 · 금액차 0 · 누락 0        ⟹ diff0
재마감 3.5초 · 212품목 확정
```

### 수불장 3도메인 완료
| 도메인 | 단가 | 축 | 스냅샷 대조 |
|---|---|---|---|
| 자재 MAT | 이동평균(매입원가) | 품목 | **diff0** |
| 생산 PRD | 이동평균(매입가 기반) | 품목 × 재고위치 | **diff0** |
| 영업 SAL | 이동평균(**판가 기반**) | 품목 | **diff0** |


---

## §28. 수불장 3도메인 — **검증 완료** (2026-08-28)

### 최종 상태
| 도메인 | 화면 | 불변식 | 확정 스냅샷 대조 | 단가 |
|---|---|---|---|---|
| 자재 MAT | 3,670행 · 24컬럼 | 위반 0 | **수량·금액 diff0** | 이동평균(매입원가) |
| 생산 PRD | 3,539행 · 25컬럼 | 위반 0 | **수량·금액 diff0** | 이동평균(매입가 기반) · 축=품목×재고위치 |
| 영업 SAL | 678행 · 24컬럼 | 위반 0 | **수량·금액 diff0** | 이동평균(**판가**, `nx.price_item` 단일소스) |

화면 실구동(playwright) — 3토글 전부 렌더 · **JS 오류 0**.

### 성능 (생산)
**110초 → 20초.** 두 가지가 원인이었다.
1. **BOM 없는 품목까지 원가엔진 호출** — 310품목 42초 소요·보강 **0건**(전부 헛돌았다).
   `nx.bom` parent 존재 여부로 선필터 → 42초 → 20초·보강 53건.
2. 조회마다 전 단계 재계산 → `_LEDGER_CACHE`(도메인·기간별) 도입. 2회차 10초.
   ★캐시는 조회 전용 — 마감 실행/해제 시 `_ledger_cache_clear()` 로 버린다(stale 금지).

### 이 작업에서 찾아 고친 결함 5건
| # | 결함 | 뿌리 |
|---|---|---|
| 1 | 제외분(단가0·음수)이 **다음 달 기초에서도 빠져** 음수가 사라짐 (§19) | 기초 소스 불일치 |
| 2 | 기초 후보를 `TOP 1` 로 뽑고 조건 검사 → **레거시 시드로 추락** (§21-A) | 기초 선택 규칙 불일치 |
| 3 | 단가0 보정을 **마감만** 적용 (§21-B) | 처리 단계 불일치 |
| 4 | 영업 마감이 이동평균이 아니라 **as-of 판가 × 수량** (§27) | recipe 직확정 |
| 5 | 영업 단가가 **미러(`pr_m_item_cost`)** — 컷오버 후 얼어붙을 코드 | 단일소스 하드룰 위반 |

1~4 = **"같은 값을 두 곳에서 계산하면 반드시 갈린다"** 한 뿌리(§21).
5 = `CLAUDE.md` §1-9-1 · `DO_NOT_USE_FIELDS` §18 (컷오버 후 단일 테이블·폴백 금지).

### 남은 것 (수불장 밖)
- 미러 단가 직독 **69건** 정리 — coopquote/coopquote2/dtrade/price/pricemgmt/sourcing/`_prd_price`.
  전부 컷오버에 죽는 코드. 건별 성격 확인 후 클린 이관.
- `_prd_price` ③단계가 `PR_M_ITEM_COST`(매입단가 as-of)를 본다 — 판가와 성격이 다르므로 별도 판정 필요.


---

## §29. 운영 방침 — **월마감만 운영. 일마감은 상시 운영하지 않는다** (대표 확정 2026-08-28)

> 대표: **"일 스냅샷은 의미가 없네"**

### 왜 의미가 없나
수불장은 **기말을 저장하지 않고 매번 계산**한다(§7-1 파생 뷰).
```
기말 = 기초(확정 스냅샷) + 그 기간 전표
```
따라서 일 스냅샷이 있든 없든 **화면 값이 달라지지 않는다.** 전개 구간이 짧아져 조금 빨라질 뿐인데,
결과 캐시(§28)로 재조회가 이미 0.3초라 그 이득도 사라졌다.

반면 비용은 남는다 — 매일 마감을 돌려야 하고, **소급 전표가 생기면 그 뒤 일 스냅샷을 전부 다시** 만들어야 한다.

### 방침
- **운영은 월마감만 한다.** 월말에 `close_run(domain,'M',YYMM)` 로 확정 → 다음 달 기초.
- **일마감 기능은 제거하지 않는다.** 코드가 일·월을 같은 경로로 처리해 유지 비용이 0이고,
  특정 시점 재고를 확정해야 할 일(실사·감사·분쟁)이 생기면 그때 쓴다.
- `_mv_base`/`_prd_base`/`_sal_base` 의 기초 선택 순서는 그대로 둔다:
  **① 일마감(있으면) → ② 월마감 → ③ 레거시 시드.** 일 스냅샷을 만들면 자동으로 그게 기초가 된다.

### 참고 — 일마감을 돌릴 때 걸리는 것
`mat_stock_daily` 최신이 **미래 날짜(2026/09/06)** 다(이월 전표 때문). 일마감은 이 데이터가 있는 날만
가능하므로, 실제로 돌릴 일이 생기면 **미래분이 어떻게 처리되는지 먼저 확인**할 것.
