# 협력사 포털 + 모바일 웹앱 설계

> 착수 2026-08-29 · 브랜치 `feat/partner-portal`
> 대표 요구(원문 요약):
> - 협력사마다 **자기 계획만** 보게 하고 그 위에서 작업하게
> - **핸드폰 앱으로 송장 발행**, 입고 시점에 폰으로 정리
> - 송장 **바코드/QR 을 폰 또는 리더기로 읽어 자동 입고**
> - **IQC 검사품 / 무검사품** 구분
> - 생산계획이 **새로 올라오거나 수정되면 앱에서 변동 알림**

---

## 0. 먼저 — 이미 있는 것 (2026-08-29 실측)

**생각보다 많이 되어 있다. 새로 만들 것은 셋뿐이다.**

| 요구 | 현재 | 근거 |
|---|---|---|
| 송장 발행 | ✅ `POST /api/setin/issue` — `barcode_no` 채번 | `routers/setin.py` |
| 바코드 스캔 조회 | ✅ `GET /api/setstock/scan?barcode=` | 협력사·도번·자도번수 반환 |
| 바코드 입고 | ✅ `POST /api/setstock/receive` | `tag 2`=바코드 / `3`=장부 |
| **IQC 분기** | ✅ `insp_flag='1'` → 상태 **30(입고대기)** · 일반 → **90(입고완료)** | `setstock_receive` |
| 자도번 자동 재고반영 | ✅ 입고완료 시 `stock_ledger` 파생(`MAINT_TAG='S'`) | 세트→자도번 분해 |
| 마감잠금 | ✅ `_assert_open(입고일,"MAT","세트입고")` | |
| 협력사별 계획 화면 | ✅ `SCREEN.partnerplan` (유형별 묶기 6/7/8) | `screens.etc.js` |
| 상태 라이프사이클 | ✅ 필드 준비 `10발행→20출발→30입고대기→40검사중→90완료→99반품` | `nx.set_input_req.status` |
| 협력사 역할·소속 | ✅ `roles:['협력사']` · `partner:'미래정밀'` | `nx.web_user` |

**실데이터도 있다**: `set_input_req` 1,203 · `set_input_req_dtl` 2,517 · `set_stock_maint` 4,149.
상태 분포 = `00` 1,198 · `10` 4 · `90` 1.

> 설계 근거 = [[newerp-coop-setin-programs]] (대표 확정 워크플로우·세트 재고모델),
> [[newerp-coop-plan-delivery-formulas]] (당김·최대발행일·완료수량),
> [[newerp-coopplan-grouping-livesync]] (유형별 묶기),
> [[nextgen-erp-goal-scope]] (Partner ERP 목표), [[newerp-partner-external-access]] (외부접속).

---

## 1. ★★가장 급한 결함 — 백엔드에 인증이 없다

### 실측
- 로그인·사용자·비밀번호가 **프론트 JavaScript 안에** 있다(`core.js` `SEED_USERS`, `nx.web_user.udata` JSON).
  ```javascript
  {id:'miraero', pw:'1234', nm:'미래정밀', roles:['협력사'], partner:'미래정밀'}
  ```
- **API 는 누구든 부르면 응답한다.** `Depends`·토큰·세션 검사가 없다.
- 협력사 API 도 **`cust` 를 쿼리 파라미터로 받는다**:
  `GET /api/partner/deliv420?cust=...` — 값만 바꾸면 남의 데이터가 나온다.

### 위험
협력사에게 접근을 열면 **미래정밀이 대원 계획을 그대로 볼 수 있다.** URL 파라미터 하나 바꾸면 된다.
비밀번호도 브라우저에서 그대로 보인다.

### ⟹ 원칙
> **협력사에게 열기 전에 백엔드 인증·소속 강제가 반드시 선행한다.**
> 화면에서 숨기는 것은 보안이 아니다. **서버가 거부해야 한다.**

---

## 2. 1단계 — 인증 · 소속 강제 (선행 필수)

### 2-1. 저장
`nx.web_user` 를 **행 단위 실테이블로 승격**한다(지금은 `__ALL__` 한 행에 JSON 통째).
```
nx.app_user      user_id · pw_hash · salt · name · utype(내부/협력사) · partner_code · status
                 last_login · fail_cnt · locked_until
nx.app_session   token · user_id · issued_at · expires_at · ip · ua · revoked
```
- **비밀번호는 해시로만 저장**(평문 금지). 기존 평문은 최초 로그인 시 해시로 승격.
- `partner_code` = **거래처코드**(`CM_M_CUST.CUST_CODE`). 지금 `partner:'미래정밀'` 은 **이름**이라 위험하다
  (동명·개명·공백). **코드로 바꾼다.**

### 2-2. 검사
```
POST /api/auth/login    → 토큰 발급(만료 있음)
GET  /api/auth/me       → 현재 사용자·역할·partner_code
POST /api/auth/logout   → 토큰 폐기
```
FastAPI `Depends(current_user)` 로 **전 엔드포인트에 부착**. 무토큰 = 401.

### 2-3. ★소속 강제 (핵심)
협력사 계정이 부르는 API 는 **`cust` 파라미터를 신뢰하지 않는다.**
```python
def scope_cust(user, req_cust=None):
    """협력사면 자기 코드로 **고정**. 내부 사용자만 req_cust 를 쓴다."""
    if user.utype == '협력사':
        return user.partner_code          # 파라미터 무시
    return req_cust
```
- 협력사가 남의 코드를 넣어도 **자기 것만** 나온다.
- 대상: `partner/planstatus` · `partner/deliv420` · `setin/*` · `setstock/*` 등 협력사 노출 API 전부.

### 2-4. 검증 (필수)
- 협력사 토큰으로 **남의 `cust` 요청** → 자기 데이터만 나오는지
- 무토큰 호출 → **401**
- 내부 사용자 → 종전대로 전 협력사 조회 가능(회귀 없음)
- TestBed 에 `[R]` 케이스로 등록

---

## 3. 2단계 — 협력사 포털 (모바일 웹앱 / PWA)

**네이티브 앱이 아니라 웹앱으로 간다**(대표 동의). 스토어 심사·배포가 없고, 링크만 열면 되고,
지금 웹 코드를 그대로 쓴다. 폰 카메라 QR 스캔도 브라우저에서 된다.

### 화면 4개
| 화면 | 하는 일 | 쓰는 API |
|---|---|---|
| **① 내 계획** | 자기 계획만 · 일자축 · 요청수량/완료수량 | `partner/planstatus`(소속 강제) |
| **② 송장 발행** | 계획에서 납품분 선택 → 송장 생성 → **QR 표시/인쇄** | `setin/issue` — ★`setinreq` 재구현 |
| **③ 내 송장** | 발행분 목록 · 상태 추적 · **출발 처리(20)** | `setin/list` + 상태전이 API(신규) |
| **④ 입고 스캔**(우리 담당자용) | QR 스캔 → 확인 → 입고 | `setstock/scan` → `receive` |

### QR 코드
- 값 = **`barcode_no`**(이미 채번됨). 새 체계를 만들지 않는다.
- 발행 화면에서 QR 렌더 → 협력사가 **인쇄해 상자에 부착**.
- 우리 입고: **폰 카메라** 또는 **기존 바코드 리더기**(리더기는 키보드 입력이라 그대로 동작).

### 신규 필요 API
```
POST /api/partner/depart     송장 출발 처리 (status 10 → 20)   ← 협력사 앱
GET  /api/partner/my         내 계획·송장 요약 (홈 화면)
```

### IQC 분기 — 코드는 이미 있고, 지금은 잠자는 상태
```
insp_flag='1' → 입고 시 status 30(입고대기) → [IQC 승인] → 90(입고완료) → 재고 파생
insp_flag='0' → 입고 즉시 90 → 재고 파생        ← ★지금은 전부 이쪽
```
지금 `insp_flag` 는 1,203건 **전부 `'0'`** 이라 검사 경로가 실행되지 않는다.
**분기 자체는 `setstock_receive` 에 이미 있으므로 건드리지 않는다**(§4-1).
나중에 `insp_flag` 를 채우면 그대로 살아난다.

---

## 4. 3단계 — 계획 변동 알림

### 감지
계획은 **매일 재편성**된다([[newerp-plan-program-master]]). 그래서 "바뀌었다"를 알려면 **직전 편성과 비교**해야 한다.
```
nx.plan_snapshot_partner(partner_code, plan_ymd, item_code, qty, snap_at)
편성 후 → 직전 스냅샷과 diff → 협력사별 변동(신규/증가/감소/삭제) 산출
```
> ★주의: [[newerp-nxledger-cutover-diagnosis]] 교훈 — 계획 비교는 **같은 기준일끼리** 해야 한다.
> 기준일이 하루만 달라도 80%/100%/77% 로 출렁인다. 스냅샷에 **기준일을 함께 저장**한다.

### 전달
| 방식 | 장단 |
|---|---|
| **웹 푸시(PWA)** | 앱 설치 없이 폰 알림. iOS 는 홈화면 추가 필요 |
| **문자/알림톡** | 확실히 도달. 건당 비용 |
| **포털 내 배지** | 무료·확실. 다만 열어봐야 안다 |

**권고: 포털 배지 + 웹 푸시**로 시작하고, 중요 변동만 알림톡을 얹는다.

---

## 5. 순서와 이유

```
1단계  인증 · 소속 강제        ← 이게 없으면 2·3단계가 위험하다
2단계  포털 4화면 + QR
3단계  변동 감지 + 알림
```

**1단계를 건너뛰면 안 된다.** 협력사에게 URL 을 주는 순간 남의 데이터가 열린다.

---

## 6. 외부 접속 (기록 있음 — 재확인 필요)

[[newerp-partner-external-access]] 에 방식이 정해져 있다:
**Cloudflare Tunnel + Access**(무료, 상시 PC 를 커넥터로, ERP 서버는 계속 사내 격리).
2026-07-27 결정이므로 **현재 구축 여부를 확인**해야 한다.

---

## 7. 대표 확정 (2026-08-29)

| # | 물음 | **확정** |
|---|---|---|
| 1 | 계정 단위 | **협력사별 1개** (업체 = 계정 1:1) |
| 2 | 송장 발행 주체 | **협력사가 직접 발행.** 단 **계획서에 지정된 것만** |
| 3 | 포털 대상 구분 | **거래처에 필드를 등록해 관리** |
| 4 | IQC | **지금은 넣지 않는다. 추후 추가 가능하게 자리만 남긴다**(4-1) |
| 5 | 알림 채널 | **절삭 협력사 = 알림톡 · 나머지 = 웹** |

### 2번 확정의 함의 — 발행 화면은 이미 있고 계획 제한도 걸려 있다
`POST /api/setin/issue` 가 **이미 계획분만 발행**하도록 막고 있다:
```sql
WHERE sheet_no=? AND remarks='PLAN_COMPOSE' AND status IN ('00','10')
```
`PLAN_COMPOSE` = **계획에서 자동 편성된 송장**. 협력사가 임의로 만들 수 없다.
⟹ 대표 요구("계획서에서 지정된 것만 발행")가 **그대로 구현돼 있다.**

**★`setinreq`(거래명세서 발행-바코드)를 내린 사유 = 구현이 잘 안 됐다**
(대표 확인 2026-08-29). 커밋 `088be2d` 에는 "요청"만 적혀 있었다.
⟹ **이번 작업의 핵심 대상이 바로 이 화면이다.** 되살리는 게 아니라 **다시 만든다.**

무엇이 부족한지는 착수 전에 실행해 보고 확정한다(추측 금지). 지금까지 코드로 확인한 것:
- 화면 골격·상태 뱃지(`00요청 10발행 20출발 30입고대기 40검사중 90완료 99반품`)는 있다.
- `setin/list` → 체크 → `setin/issue`(배치 QR 채번) 흐름도 있다.
- ☐ **QR 렌더·인쇄가 없다**(발행만 하고 코드를 눈에 보여주지 않는다).
- ☐ 협력사 관점 화면이 아니다(우리 담당자용 그리드).

| 화면 | 상태 | 성격 | 평가 |
|---|---|---|---|
| **`deliv420` 거래명세서 발행** | 메뉴 있음 | 레거시 `w_pr_outside_420` 재현 | ✅ **대표 확인: "작동이 정확하다 · 레거시를 잘 구현했다"**(2026-08-29) |
| **`setinreq` 발행(바코드)** | **내려놓음**(`core.js:161` 주석) | QR/바코드 | ★**구현이 잘 안 돼서 내림**(대표) — **이번에 다시 만들 대상** |
| `delivedit` 거래명세표 수정 | 신규(Dyun) | 수량수정·재발행 | |

> ※대표 화면에는 이 메뉴가 보였으나 `feat/partner-portal`·`zt/main` 코드 모두 주석 처리 상태다
> (실측 2026-08-29). 브라우저 캐시 또는 다른 세션 미푸시분. 손대기 전에 확인할 것.

### 3번 — 포털 대상 필드 (거래처에 등록·관리)
레거시 `CM_M_CUST` 는 컷오버에 죽는다. ⟹ **클린 쪽에 둔다.**
```
nx.partner_portal   cust_code(PK) · portal_use('1'/'0')   포털 개방 여부
                    noti_kind('T' 알림톡 / 'W' 웹)        알림 채널
                    noti_phone · noti_name                 알림톡 수신처
                    upd_user · upd_dt
```
**관리 화면** = 기준정보에 「협력사 포털 관리」 신설.
목록에서 **체크로 켜고 끈다**(대표가 직접 지정). 자동 판별 없음.

> 왜 별도 테이블인가 — `CM_M_CUST` 는 레거시 미러라 **매일 sync 가 통째로 덮는다**
> (`r_delta_sync` do_full = TRUNCATE + 재적재). 거기에 우리 필드를 넣으면 **매일 지워진다.**
> 단가 마스터·재고 잔량에서 이미 겪은 문제와 같은 구조다.

### 5번 확정 — 알림 채널 2원화 · **대상은 대표가 직접 지정**
| 대상 | 채널 |
|---|---|
| 대표가 지정한 협력사(주로 절삭) | **알림톡** |
| 나머지 | **웹**(포털 배지 + 웹푸시) |

> **★자동 판별하지 않는다**(대표 지시 2026-08-29: *"그 협력사들은 내가 지정해줄게"*).
> `CUST_TYPE='6'`(절삭-협력사)이 **29곳**으로 잡히긴 하지만, 그걸 조건으로 쓰지 않는다.
> **분류를 알림 조건에 겹쳐 쓰면 분류를 바꿀 때 알림이 딸려 바뀐다.**
> ⟹ 거래처마다 **켜고 끄는 전용 필드**로 관리하고, **화면에서 대표가 직접 지정**한다.
> (참고로 6번에 `가공반(사내)` 같은 내부 조직도 섞여 있어 자동 판별은 위험하다.)

**웹푸시 실제 동작**(대표 질문 "핸드폰에 실제로 떠?"):
- **안드로이드** — 브라우저를 닫아도 알림이 뜬다. 앱과 동일.
- **아이폰** — **홈 화면에 추가**해야 알림이 온다. 사파리로만 보면 안 온다.
  ⟹ 아이폰 협력사에는 "홈 화면에 추가" 1회 안내가 필요하다.

### 4-1. IQC (수입검사) — **지금은 넣지 않고, 나중에 붙일 자리만 남긴다** (대표 확정 2026-08-29)

**실측 — 지금 안 쓰고 있다**
```
nx.QA_T_CUST_IQC_HEAD   3,232행 · 기간 210616 ~ 220317   ← 2022년 3월에 멈춤
nx.set_input_req.insp_flag   1,203건 **전부 '0'(무검사)**
nx.item 검사 여부 컬럼        없음
```
판정 컬럼은 `QA_T_CUST_IQC_HEAD.RESULT_OK`('1' 합격 3,230 / '0' 2).
웹 `qc.py` 는 **조회 전용**(`/api/qc/iqc/list`·`detail`) — 판정 쓰기 없음.

**대표 지시**: *"수입검사 부분은 추후에 추가될 수 있도록 해줘."*

#### ⟹ 설계 원칙 — 나중에 붙일 수 있게 **자리를 비워 둔다**
지금 IQC 를 구현하지 않는다. 그러나 **나중에 넣을 때 기존 코드를 뜯지 않아도 되게** 짠다.

| 남겨 둘 것 | 지금 | 나중에 |
|---|---|---|
| `set_input_req.insp_flag` | **필드 유지**(전부 `'0'`) | 값만 `'1'` 로 채우면 검사 대상이 된다 |
| 상태 `30 입고대기` · `40 검사중` | **상태값 유지**(안 쓰지만 지우지 않음) | 그대로 되살아난다 |
| `setstock_receive` 의 분기 | **코드 유지** — `insp=='1'` 이면 30, 아니면 90 | 조건이 이미 있으므로 **수정 없이 동작** |
| IQC 승인 API | 없음 | `POST /api/qc/iqc/approve` 신설 → 30/40 → 90 → 재고 파생 |

**핵심**: `setstock_receive` 에 **이미 분기가 들어 있다.**
```python
newstat = "30" if insp == "1" else "90"   # 검사품=입고대기, 일반=입고완료
```
⟹ **지금은 `insp_flag` 가 전부 `'0'` 이라 모두 즉시 입고완료(90)** 로 흐른다.
   나중에 검사를 도입하면 **`insp_flag` 를 `'1'` 로 채우는 것만으로** 이 분기가 살아난다.
   **입고 로직을 다시 건드릴 필요가 없다.**

#### 나중에 도입할 때 정해야 할 것 (지금 결정하지 않음)
1. **검사 대상 판별 기준** — 품목별 / 협력사별 / 건별 중 무엇으로 `insp_flag` 를 채울지
2. **검사 결과 기록처** — 레거시 `QA_T_CUST_IQC_*` 를 계속 쓸지, 클린(`nx.qc_*`)으로 갈지
   > ★컷오버 후 레거시는 죽는다(`CLAUDE.md` §1-9-1). **새로 만들면 클린으로 간다.**
3. **승인 화면** — 품질 메뉴(`수입검사(IQC)조회`)를 조회 전용에서 **판정 가능**으로 승격할지
4. **40(검사중) 을 쓸지** — 30(입고대기) → 90 으로 바로 갈지, 40 을 거칠지

#### 지금 지켜야 할 것 (중요)
- **`insp_flag`·`status 30/40` 을 "안 쓰니까" 지우지 말 것.** 지우면 나중에 다시 만들어야 한다.
- 새로 만드는 화면·API 는 **상태 코드 전체(00·10·20·30·40·90·99)를 그대로 다룬다.**
  지금 안 나오는 상태라도 **표시·필터에서 빼지 않는다.**
- 입고 흐름을 단순화한다고 **`setstock_receive` 의 `insp` 분기를 없애지 말 것.**

---

## 8. `setinreq` 진단 — "구현이 잘 안 됐다"의 실체 (2026-08-29 실측)

로컬 백엔드(8012)에 **읽기 API 만** 호출해 확인했다(쓰기 없음).

### 백엔드는 정상이다
| API | 결과 |
|---|---|
| `GET /api/setin/list` | **200** · rows 50 · 협력사 11곳(FONE THAI 174 · 이젠터 167 · AUDY 162 · 대원산업 147 …) |
| `GET /api/setin/list?cust=2096` | **200** · rows 50 (협력사 지정 조회 정상) |
| `GET /api/setin/detail?sheet=900254` | **200** · 자도번 명세 정상(`use_qty`·`mat_qty`) |
| `GET /api/setstock/list` | **200** · 입고 실적 정상 |
| `GET /api/setin/invoice?barcode=500001` | 404 "해당 SET바코드 송장 없음" — **발행분이 없어서**(정상 동작) |

⟹ **API 는 문제가 없다. 문제는 화면이다.**

### ★결함 1 — QR 이 없다. Code39 1차원 바코드다
```javascript
function _barcodeDataURL(text){        // core.js:1082
  const data="*"+text+"*", ...          // ← Code39 (1차원 바코드)
```
대표 요구는 **"바코드(또는 QR코드)를 핸드폰 또는 바코드 리더기로"** 인데,
현재 구현은 **Code39 하나뿐**이고 QR 이 없다.

**왜 문제인가**: Code39 는 **전용 리더기용**이다. 폰 카메라로는 인식률이 낮고,
좁고 긴 형태라 상자에 붙였을 때 각도·조명에 취약하다.
**QR 은 폰 카메라로 잘 읽히고 방향에 강하다.**
⟹ **QR 을 추가**한다. Code39 는 기존 리더기 호환을 위해 **함께 유지**(둘 다 인쇄).

### ★결함 2 — 화면이 협력사 관점이 아니다
`setinreq` 는 **129줄**짜리 우리 담당자용 그리드다.
협력사가 폰으로 쓰기엔 컬럼이 많고 터치 대상이 작다.

### ★결함 3 — `deliv420` 에 비해 인쇄 기능이 빈약하다
| | `setinreq`(내려놓음) | `deliv420`(정확히 작동) |
|---|---|---|
| 코드량 | 129줄 | 훨씬 큼 |
| 인쇄물 | 거래명세표 1종 | **거래명세표 + 스티커** 2종 |
| 라벨 규격 | 없음 | `100×50mm` · 매수 · 프린터 설정(localStorage) |
| 재출력 | 없음 | **발행번호로 재출력** 가능 |

`deliv420` 은 **스티커 라벨 규격까지** 갖췄다. `setinreq` 에는 그게 없다.

### ⟹ 재구현 방향
1. **QR 추가**(Code39 병기) — 폰 스캔용
2. **협력사용 모바일 화면 신설** — 기존 `setinreq` 그리드는 우리 담당자용으로 남김
3. **인쇄는 `deliv420` 의 스티커 구조를 재사용** — 이미 검증된 것을 다시 만들지 않는다
4. `setin/invoice` 는 그대로 쓴다(정상 동작 확인)

> **원칙**: `deliv420` 은 대표가 "정확히 작동한다"고 확인했다. **손대지 않는다.**
> 필요한 부분은 **가져다 쓰되 복사가 아니라 공용 헬퍼로 뽑는다**(두 곳이 갈라지면 또 드리프트).

---

## 9. 폰 스캔 입고 — 업무 프로세스 검증 (2026-08-29 실측)

대표 질문: *"우리 담당이 핸드폰으로 찍어서 송장을 입력하는 게 설계·업무 프로세스상 문제가 없는지."*

### 결론 — **문제 없다. 이미 그렇게 하고 있다.**
```
입고 건수   하루 90~190건 (8월)          담당자   17명
입고 수단   바코드 20,211건 (94%)         자도번   평균 2.1개 · 최대 14개
            장부수정 1,209건 (6%)         한 송장  도번 평균 7종 · 최대 35종
시간대      11시 · 14시 집중
```
**이미 94% 를 바코드로 처리한다.** 폰으로 바꾸는 것은 *방식*의 변화가 아니라 **도구**의 변화다.

### ★대표 설명으로 바로잡은 것 — 내 오독
> *"현장에서는 협력사 송장을 보통은 믿고 진행하고, 그게 만약 달라지면 반품처리를 하는 구조다.
>  하나하나 세어서 받기에는 품목이 너무 많다."*

나는 처음에 **"같은 송장이 2번 이상 입고된 건 2,378건 = 분할입고"** 라고 진단했다. **틀렸다.**
```
다회입고 2,378건 중
   도번이 여러 개라 행이 여러 개(분할 아님)   2,290건 = 96%
   ★같은 도번을 나눠 받음(진짜 분할)             88건 =  4%
```
`SHEET_NO 156035` → 34행이지만 **서로 다른 도번 34종**이다. 분할이 아니다.
⟹ **한 송장을 통째로 받는 구조가 맞다.** 대표 설명과 데이터가 일치한다.

**반품도 별개 경로다**
```
반품 193건 · 합 −10,733  → 전부 tag='3'(장부수정) · SHEET_NO=0
+입고와 −반품이 같은 SHEET_NO 에 함께 있는 송장: **0건**
```
반품은 송장에 연결되지 않는다. 나중에 **별도 장부수정으로 뺀다.**
⟹ "일단 믿고 받고, 다르면 나중에 뺀다" 는 구조가 **데이터로 확인된다.**

### ⟹ 설계가 단순해진다
| 내 오해 | 실제 |
|---|---|
| 스캔 → 수량 입력 → 부분입고 | **스캔 → 송장 전량 입고**(수량 입력 불필요) |
| 반품을 송장에 연결 | **반품은 별도 화면**(장부수정 tag='3') |

**지금 코드가 이미 맞다.**
```python
WHERE h.barcode_no=? AND h.status IN ('10','30')   # 그 QR 의 송장 전부 전량 입고
```
⟹ `setstock_receive` 의 전량 입고 방식을 **바꾸지 않는다.**

### ★그래도 지켜야 할 것

**① 스캔 후 확인 단계는 필수 (한 번에 최대 35종)**
스캔 1회로 도번 **평균 7종·최대 35종** + 자도번까지 재고에 들어간다.
잘못 스캔하면 **35종이 통째로 잘못 입고**된다.
⟹ 스캔 → **확인 화면**(협력사·도번수·총수량) → 승인. 곧바로 입고하지 않는다.
   `GET /api/setstock/scan` 이 이미 그 정보를 준다(협력사·도번·자도번수).
   **믿고 받는 구조일수록 "무엇을 받는지" 확인은 더 중요하다** — 세지 않는 대신 화면으로 본다.

**② 진짜 분할 88건 (4%) 은 지금 막힌다**
```python
raise HTTPException(404, "발행 상태의 송장이 없습니다(이미 입고완료?)")
```
두 번째 스캔이 404 로 거부된다.
☐ **대표 확인 필요**: 이 88건을 지금 어떻게 처리하는가?
   (a) 장부수정(tag='3')으로 따로 넣는다 → 지금 방식 유지, 폰에서는 장부수정 화면으로 안내
   (b) 나눠 스캔한다 → 부분 입고를 지원해야 함

**③ 담당자 17명 → 인증이 여기서도 필요**
누가 입고했는지 남으려면 **각자 로그인**해야 한다.
지금은 `INSERT_USER_ID` 를 클라이언트가 보내는 값에 의존한다(위조 가능).
⟹ **1단계 인증**이 협력사뿐 아니라 **우리 담당자 쪽에서도 전제**다.

**④ 오스캔 되돌리기**
믿고 받는 구조에서는 **잘못 스캔했을 때 즉시 되돌리는 길**이 있어야 한다.
`setin/issue` 에 `cancel` 은 있으나 **입고 취소(90 → 되돌림)** 는 없다.
☐ 입고 취소 API 필요 — 재고 파생분(`stock_ledger`)까지 함께 되돌려야 한다.
   ※ **마감된 기간이면 취소도 막아야 한다**(`_assert_open`).

---

## 10. ★중복 스캔 입고 차단 (구현 완료 2026-08-29)

> **대표 확정**: *"같은 송장이 2번 찍혀서 들어오는 건 치명적 오류인데 이걸 막을 방법은? 경고 메시지도 띄우고"*

### 왜 치명적인가
현장은 **세지 않고 송장을 믿고 받는다**(대표 설명). 그래서 중복 스캔은
**재고를 그대로 두 배**로 만든다. 세어서 받는 구조라면 다음 단계에서 걸리지만, 믿고 받으면 **아무도 모른다.**

### 실측 — 레거시에서 실제로 일어나고 있었다
```
같은 (송장, 도번) 2회 이상 = 99건 · 200행
   전부 같은 날 · 전부 바코드(tag '2') · 장부수정(tag '3') 0건
```
**레거시는 상태를 보지 않고 찍을 때마다 한 줄씩 넣는다** → 막지 못한다.
`CLAUDE.md §1-7`("레거시는 정답이 아님 — 검증·버그 플래그") 에 해당한다. **복제하지 않는다.**

### 세 겹으로 막는다
| 겹 | 시점 | 동작 |
|---|---|---|
| **1** | **스캔하는 순간** | `GET /api/setstock/scan` 이 `warn` 을 실어 보낸다 — **입고 버튼을 누르기 전에** 뜬다 |
| **2** | 입고 시도 | `POST /api/setstock/receive` 가 **409 로 거부** + 언제·누가·얼마 받았는지 |
| **3** | 발행 전(00) | `barcode_no` 가 없어 **스캔 자체가 불가** — 구조적으로 안전 |

### 실제 메시지 (검증 결과, 2026-08-29)
**① 스캔 경고**
```
★이미 입고된 SET바코드입니다 — 260827 · web · 20개 (1건).
  중복 입고하면 재고가 실제보다 늘어납니다.
```
**② 입고 차단 (409)**
```
중복 입고 차단 — SET바코드 700004 는 이미 입고되었습니다. (260827 · web · 20개 · 1건)
다시 넣으면 재고가 실제보다 늘어납니다.
추가 납품분이면 [장부수정]으로, 잘못 입고했으면 [입고취소]로 처리하세요.
```

### 애매한 메시지도 없앴다
| 전 | 후 |
|---|---|
| `404 발행 상태의 송장이 없습니다(이미 입고완료?)` | `404 SET바코드 … 송장을 찾을 수 없습니다. 바코드를 확인하세요.` |
| (같은 메시지로 뭉뚱그림) | `409 입고할 수 없는 상태입니다 — 입고완료 3건. 발행(10)/입고대기(30)만 입고됩니다.` |
> **"?" 로 추측하게 두지 않는다.** 상태를 세어서 그대로 알려준다.

### 예외 통로
`force: true` 를 주면 넘어간다. **관리자가 사유를 알고 추가할 때만** 쓰며,
일반 화면(폰 앱)에는 **노출하지 않는다.**

### 검증 (2026-08-29)
```
scan  700004(이미 입고) → 200 · already=1 · warn 나옴          ✅
recv  700004            → 409 차단 + 일시·담당자·수량 고지      ✅
recv  999999999(없음)   → 404 "바코드를 확인하세요"             ✅
발행 전(00)             → barcode_no 없음 = 스캔 불가           ✅
```

---

## 11. 입고취소 API (구현·검증 완료 2026-08-29)

중복 차단 메시지가 *"잘못 입고했으면 [입고취소]로"* 라고 안내하므로 **그 길을 실제로 만들었다.**
믿고 받는 구조(세지 않고 송장대로 입고)에서는 **되돌리는 길이 없으면 차단이 곧 막다른 길**이 된다.

### 엔드포인트
| API | 용도 |
|---|---|
| `GET  /api/setstock/cancel_preview?barcode=` | 되돌릴 것을 **미리 보여준다**(입고 n건 · 원장 n행/수량) |
| `POST /api/setstock/cancel` | 실제 취소. `{barcode, user, reason}` |

### 되돌리는 것 — 입고가 만든 것 **셋 전부**, 한 트랜잭션
| # | 대상 | 동작 |
|---|---|---|
| ① | `nx.set_stock_maint` | 입고 거래행 삭제 (`sheet_no=바코드 AND in_tag='1'`) |
| ② | `nx.set_input_req.status` | `90/40/30 → 10`(발행) 복귀 — **다시 스캔할 수 있게** |
| ③ | `nx.stock_ledger` | 자도번 재고 파생분 삭제 (`SHEET_NO=바코드 AND MAINT_TAG='S'`) |

**★셋 중 하나만 지우면 장부가 어긋난다.** 그래서 `_nx_tx()` 한 트랜잭션으로 묶고,
실패 시 `rollback()` 한다. 성공 후 `stock_changed()` 로 캐시를 버린다(캐시 stale 금지).

> **삭제 키가 맞는지 확인함** — `receive` 의 원장 INSERT 는 `SHEET_NO` 에 **바코드**(`int(bc)`)를
> 넣는다(송장번호가 아니다). 취소의 DELETE 키와 **일치**한다. 어긋났다면 재고 파생분이 남았을 것이다.

### ★마감 잠금
`_assert_open(cur, 입고일, "MAT", "세트입고 취소")` — **마감된 달의 입고는 취소도 막는다.**
재고가 움직이는 작업이므로 `STOCK_GATING_CLOSE_LOCK_RULES.md` 규칙B 대상이다.

### 검증 — TestBed(no-commit, 오염 0) 왕복 21/21 PASS

**A. 전 과정 왕복** (`SET700005`, 발행→입고→중복→취소→재입고)
```
입고 전 scan 경고 없음                                     PASS
입고 성공                                                  PASS
중복 입고 409 차단 / scan 사전 경고                        PASS
cancel_preview 미리보기                                    PASS
취소 → ①입고거래 0 ②송장 '10' 복귀 ③원장 0                PASS
취소 후 경고 사라짐 · 재입고 성공                          PASS   ← 되돌림이 진짜임을 증명
없는 바코드 취소 → 404                                     PASS
마감된 2607 차단 / 열린 2608 통과                          PASS
롤백 오염 0 (diff={})                                      PASS
                                                    합계 15/15
```

**B. ★재고 파생분 실증** (`SET700006`, 자도번 **14종** 짜리 송장)
A 의 표본은 자도번이 없어 원장이 0행이었다 — **0→0 은 검증이 아니다.**
그래서 `set_input_req_dtl` 이 14행 있는 송장(`900863`)을 발행해 다시 돌렸다.
```
입고     → ledger_posted=14 · 원장 실측 14행 / 140개        PASS
preview  → 원장 14행 · 140개 로 보여줌                      PASS
★취소    → 원장 14행 **전부 제거**(남은 0)                  PASS
           입고거래 0 · 송장 '10' 복귀                       PASS
롤백 오염 0                                                 PASS
                                                     합계 6/6
```

### TestBed 보강 — 오염 0 검증 대상 확대
`flow_server.py` 의 `ROLLBACK_TABS` 에 세트입고 3테이블을 넣었다.
```python
"nx.set_input_req", "nx.set_input_req_dtl", "nx.set_stock_maint"
```
> 입고취소는 이 테이블들을 지운다. **목록에 없으면 "깨끗하다"가 거짓말이 된다.**

### 남은 확인거리
- 취소는 원장 행을 **`MAINT_YMD` 무관하게** 바코드로 지운다. 입고와 원장은 같은 트랜잭션·같은 날
  기록되므로 지금은 어긋날 수 없다. 다만 **입고일과 원장일이 갈라지는 경로가 생기면**
  `_assert_open` 이 원장 쪽 마감을 못 본다 — 그때 재점검한다.
- 화면(폰 앱) 연결은 **2단계**에서. `force:true`(중복 무시)와 마찬가지로
  **취소 버튼도 일반 협력사 화면에는 노출하지 않는다** — 우리 담당자 화면 전용.

---

## 12. ★1단계 완료 — 인증 · 소속 강제 (2026-08-29)

§1 이 지목한 "가장 급한 결함"을 닫았다. **협력사에게 열기 전에 서버가 거부한다.**

### 무엇이 문제였나 (실측)
| # | 결함 | 실제로 무슨 일이 |
|---|---|---|
| 1 | 로그인이 브라우저 안에서 끝남 | `String(u.pw)!==String(pw)` — 개발자도구로 우회 가능 |
| 2 | `GET /api/perm/users` 가 **무인증** | 누구든 부르면 **평문 비밀번호 12개**를 그대로 받았다 |
| 3 | `SEED_USERS` 에 평문 비밀번호 | **JS 파일째** 배포 — 소스 보기로 전부 보였다 |
| 4 | 협력사 API 가 `cust` 파라미터를 신뢰 | URL 값만 바꾸면 **남의 계획이 그대로** |

### 정본
```
nx.app_user     계정 행 단위 · PBKDF2-SHA256(120k) 해시 · partner_code=거래처코드
nx.app_session  토큰(12h) · ip/ua/last_seen · revoked
```
> `nx.web_user`(JSON 한 행) 는 **은퇴**했다. 코드에서 읽는 곳 0 (주석만 남음).
> 계정 정본을 두 곳에 두면 드리프트가 난다 — 하드룰 §1-9-1.

### A. 이관 (`_migration/auth_bootstrap.py`)
```
12명 이관 · 평문 -> 해시 · 평문 잔존 0
partner '미래정밀'(이름) -> '2096'(거래처코드, nx.cust 정확일치)
```
**이름은 동명·개명·공백에 깨진다.** 모호하면 코드를 넣지 않는다 —
틀린 코드를 넣는 것은 **남의 데이터를 여는 길**이다.
멱등: 이미 있는 계정의 비밀번호는 건드리지 않는다(재실행이 남의 비번을 되돌리면 안 된다).

### B. 인증 (`routers/auth.py`)
`POST /api/auth/login` · `GET /api/auth/me` · `POST /api/auth/logout` · `POST /api/auth/password`
- 없는 계정과 틀린 비밀번호를 **같은 문구**로 답한다(계정 존재 여부를 흘리지 않는다)
- 5회 실패 -> 10분 잠금 · `compare_digest`(타이밍) · 비번 변경 시 **다른 기기 세션 전부 해제**

### C. 프론트 — `window.fetch` 를 한 곳에서 감쌌다
fetch 호출이 **524곳**이다. 전부 고치면 하나는 반드시 빠진다.
```javascript
window.fetch = function(input, init){ ... 토큰 자동 부착 ... }
```
지금 있는 것도, 앞으로 만들 것도 자동으로 붙는다.
401 이 오면 토큰을 버리고 **로그인 화면으로 되돌린다**(만료를 조용히 무시하지 않는다).
`SEED_USERS`/`SUPER_USER` 의 **평문 비밀번호는 전부 제거**했다. DEV 자동로그인은 은퇴.

### D. 계정 API — 비밀번호가 나가지 않는다
| | 전 | 후 |
|---|---|---|
| GET | 무인증 · **평문 pw 포함** | 로그인 필수 · **pw 없음**, `pw_set`(설정여부)만 |
| GET 범위 | 전체 | 관리자=전체 / 그 외=**본인 1건** |
| POST | 누구나 전체 덮어쓰기 | **시스템관리자만** · 빈 pw = 기존 유지 · 목록에서 빠져도 삭제 안 함 |

`close.py` 의 시스템관리자 판정도 `web_user` -> `app_user` 로 옮겼다.

### E. ★소속 강제
```python
def scope_cust(user, req_cust):
    if user.utype == '협력사':
        return user.partner_code or '__NONE__'   # 파라미터 무시
    return req_cust
```
- **거래처코드 없는 협력사는 `__NONE__`** — 빈 값이 전체 조회로 새면 안 된다.
- 바코드만 받는 API(파라미터에 걸 자리가 없는 것)는 `assert_own_barcode` 로 **주인**을 확인한다.
- **입고 처리는 `staff_only`** — 협력사가 자기 송장을 스스로 입고하면
  아무도 물건을 확인하지 않은 채 재고가 는다.

| API | 적용 |
|---|---|
| `setin/list` · `setstock/list` · `partner/deliv420` | `scope_cust` (cust 고정) |
| `partner/planstatus` | `scope_cust` — 이 화면의 협력사 축은 **wc**(작업처코드) |
| `setin/detail` · `setin/invoice` · `deliv420/invoice` · `deliv420/cancel` | 바코드/송장 **주인 확인** |
| `setin/issue` | **자기 계획으로 만들어진 송장만** 발행 (대표 확정 §7-2) |
| `setstock/scan` · `receive` · `cancel` · `cancel_preview` | **담당자 전용**(협력사 403) |

### 검증 — 실호출 48건 PASS · FAIL 0
**B 인증 20/20** — 로그인·틀린비번401·없는계정 동일문구·me/무토큰/가짜토큰·
협력사 `partner_code=2096`·소속강제 5종·비번변경 후 다른기기 401·로그아웃·5회실패 423

**E 소속 강제 22/22**
```
무토큰 5개 API -> 전부 401
협력사가 cust=2148(남의 코드) 요청
   setin/list      130행 전부 2096      setstock/list   600행 전부 2096
   planstatus      124행 전부 2096      deliv420        124행
내부 사용자는 종전대로(2148 조회 147행 · 전체 800행)   <- 회귀 없음
협력사 scan/receive/cancel/cancel_preview -> 전부 403
남의 송장 명세 403 · ★남의 송장 발행 403 · 내부는 열림
perm/users : 12명 · pw 포함 0명 · 협력사는 본인 1건 · 협력사 저장 403
```

**E 엄격 대조 6/7** — 약한 검사(빈 결과도 통과)를 보강해 다시 돌렸다
```
협력사가 2148 요청한 결과  ==  협력사가 2096 요청한 결과   (요청값이 무시됨)
그 결과            !=  내부가 2148 요청한 결과(66행)  (남의 데이터가 아님)
남의 거래명세표 403 · 남의 발행 취소 403 · 자기 것은 200
```
> 1건 FAIL 은 **표본 선택 오류**였다 — 고른 바코드 700001 이 `status='99'`(취소된 발행)라
> `status<>'99'` 필터에 걸려 404 가 났다. 자기 것(700006)도 취소분이면 똑같이 404 이고,
> 정상 발행(700003)은 내부·협력사 모두 200 이다. ⟹ **회귀 아님**(원래 동작).

**회귀 — flow TestBed 41/41 PASS · 오염 0**
마감 권한 게이트 3건이 `app_user` 이관 후에도 그대로 동작한다:
무권한 차단 · 조회전용 차단 · 관리자는 게이트 통과(중복이라 차단).

### F. 사용자관리 화면도 정본에 맞췄다
| 전 | 후 |
|---|---|
| 목록을 **localStorage** 에서 읽음 | **서버(nx.app_user)** 에서 읽음 |
| 안내문 "비번은 프로토타입 평문" | **PBKDF2 해시 · 화면에 나오지 않음** (사실과 맞춤) |
| 비번칸에 값 표시 | `설정됨/미설정` 만 · 입력칸은 **"비우면 유지"** |
| 삭제 = 목록에서 제거 | **상태를 '정지'** 로 (서버는 목록에서 빠졌다고 지우지 않는다) |

### 실행 확인 — 브라우저 부팅 순서 그대로 14/14 PASS
```
index.html 새 ?v · core.js AUTH+fetch래퍼 · ★평문 비밀번호 0
boot.js 서버로그인(옛 String(u.pw)!== 제거) · 사용자관리가 서버를 읽음
로그인 전 perm/users -> 401
로그인 후 화면이 쓰는 필드 12명 누락 0 · pw 필드 없음 · 협력사 partner=2096
★빈 pw 로 전원 저장 -> 비번변경 0건 · TEST3 기존 비번으로 그대로 로그인 · 계정 12명 유지
```

### ★검증 중 두 번 발목 잡힌 함정 (기록)
**다른 세션 서버가 `0.0.0.0:8012~8015` 에 이미 떠 있다.** 내가 `127.0.0.1:8014` 로 띄우면
윈도우는 **둘 다 바인딩을 허용**하고, `curl 127.0.0.1:8014` 가 **남의 서버로 갈 수 있다.**
⟹ 고친 코드를 띄웠는데 **옛 응답**이 온다. 두 번 겪었다(입고취소 "엔드포인트 없음", 인증 "라우트 없음").
**대책** — 검증용 포트는 `netstat -ano | grep ":<포트> "` 로 **비어 있는지 먼저 확인**하고 쓴다.
(이번엔 8031 을 썼다.)

### 남은 것
- **협력사 계정이 지금 1개**(`miraero`)뿐이다. 나머지 협력사 계정은 **개설 필요**
  (거래처코드를 반드시 넣는다 — 없으면 `__NONE__` 이라 아무것도 못 본다).
- 내부 담당자 17명도 각자 계정이 필요하다(§9-③ — 누가 입고했는지 남으려면).
- 협력사에 열려 있지 않은 **내부 전용 API 는 아직 무인증**이다. 협력사 노출 API 부터 막았다.
  전면 부착은 2단계에서 화면과 함께.

---

## 13. ★내부 API 전면 인증 게이트 (2026-08-29)

§12 는 **협력사에게 노출되는 API** 만 막았다. 그 뒤 협력사 토큰으로 내부 API 를 찔러 보니
**그대로 열려 있었다.**

### 실측 — 무엇이 열려 있었나
```
협력사 토큰(miraero) 으로 호출
  ★열림  /api/cust/list            전 거래처 목록
  ★열림  /api/item/list            품목 마스터
  ★열림  /api/close/ledger         자재 수불장
  ★열림  /api/partner/workcenters  전 협력사 작업처 목록
  ★열림  /api/perm/all             권한 설정

전수: 엔드포인트 530개 중 **인증 결선 21개뿐 · 509개 무방비**
```
화면 메뉴는 `ROLE_MOD` 에 '협력사' 항목이 없어 거의 안 보인다. 그러나
**메뉴에서 숨기는 것은 보안이 아니다** — URL 을 직접 치면 열렸다.

### 왜 미들웨어인가 (라우터마다 붙이지 않은 이유)
| | 라우터 44개에 Depends | **미들웨어 한 곳** |
|---|---|---|
| 누락 | 반드시 하나는 빠진다 | 구조적으로 불가 |
| 충돌 | 공유파일을 44번 만짐 | 1파일 |
| 신규 | 새 엔드포인트마다 기억해야 | **자동 보호** |

```python
@app.middleware("http")
async def _auth_gate(request, call_next):
    is_open, path = path_policy(request.url.path)
    if is_open: return await call_next(request)
    u = current_user(request)
    if not u: return 401
    if u["utype"] == "협력사" and not coop_allowed(path): return 403
```

### 정책 — deny by default
**무인증 허용**(이것만)
```
/ · /js/* · /css/*  …   정적자원(프론트)
/openapi.json · /       ★deploy_pull.ps1 헬스체크가 친다 — 막으면 배포가 깨진다
/api/auth/login         로그인 자체
/api/_flow/*            TestBed 제어(롤백서버에만 존재)
```
**협력사 화이트리스트**(`COOP_ALLOW`) — 여기 없으면 403
```
auth/me · auth/logout · auth/password
perm/users                     GET=본인 1건 / POST 는 라우터가 403
partner/planstatus             내 계획
partner/deliv420 (+issue/cancel/invoice)
setin/list · detail · issue · invoice
setstock/list                  내 납품이 입고됐는지(읽기전용·소속강제됨)
   ※ setstock/scan·receive·cancel 은 staff_only — 입고는 우리가 받는 행위다
```
> **새 협력사 화면을 만들면 여기 한 줄을 추가한다.** 자동으로 열리지 않는 것이 요점이다.

### 성능 — 인증을 모든 요청에 걸면 느려진다
그대로 두면 요청마다 `SELECT×2 + UPDATE + commit` 이다. **60초 토큰 캐시**로 흡수했다.
```
/api/auth/me 평균 23ms/건 (20회 실측)
```
로그아웃 · 비밀번호 변경 · 계정 저장 시 **즉시 무효화**한다 —
안 버리면 **끊은 세션이 캐시 안에서 계속 산다.**

### 검증 24/24 PASS
```
① 무토큰 5종                      → 401
② ★협력사가 내부 API 5종          → 403   (위에서 전부 ★열림이던 것)
③ 협력사 허용 5종                 → 200
④ 내부 사용자 7종                 → 200   회귀 없음
⑤ 토큰캐시 23ms/건  ⑥ 로그아웃 직후 401
헬스체크 /openapi.json · / · /js/core.js → 200 (배포 안 깨짐)
```

### ★게이트가 드러낸 것 — 하네스도 무인증이었다
게이트를 걸자 flow TestBed 의 `[F]`·`[R]` 30여 건이 **전부 401** 이 났다.
하네스가 토큰 없이 화면 API 를 부르고 있었던 것이다(게이트가 작동한다는 증거이기도 하다).
```
flow_cases.DEFAULT_AS = "super"        [F]/[R] 기본 로그인 계정
flow_scenarios.tok_of(c)               as_ 를 **명시**하면 그것(None=무토큰), 없으면 기본계정
```
> `c.get("as_")` 로 짜면 안 된다 — **'미지정'과 '명시적 무토큰'이 구분되지 않아**
> "무토큰 401" 케이스가 거짓 통과한다. `"as_" in c` 로 구분한다.

러너 말미의 **직접 호출 3구간**(차단 사유 고지 · 조회 리포트 · 캐시 정합)은 케이스 루프를
안 거쳐서 케이스만 고쳤을 때 그대로 401 이 남았다. 같은 일이 두 갈래로 갈려 있으면 이런 게
남는다 — 총 8곳을 결선했다.

**최종: PASS 83 · FAIL 0 · SKIP 1 · 오염 0**

### 남은 것
- `ROLE_MOD` 에 '협력사' 항목이 없어 프론트 메뉴가 사실상 빈 화면이다.
  2단계에서 **협력사 전용 화면 4개**를 붙이면서 함께 정리한다.
- 내부 사용자끼리의 세분 권한(부서별)은 종전 `PERM.can` 그대로다 — 이번 게이트는
  **로그인 여부 + 협력사 차단**까지만이다.

---

## 14. 2단계 착수 — 협력사 모바일 웹앱 (2026-08-29)

### 왜 별도 페이지인가 (`partner.html`)
내부 ERP(`index.html`) 안의 화면으로 만들지 않았다.
- 협력사에게 내부 메뉴 트리를 보여줄 이유가 없다 (`ROLE_MOD` 에 '협력사'가 없어 어차피 빈 화면)
- 폰에서 쓰는 화면은 레이아웃이 근본적으로 다르다 — 한 손·세로·큰 터치영역
- 백엔드는 `COOP_ALLOW` 로 이미 막혀 있다 — 이 페이지는 **그 안에서만 논다**

### 화면 (1차)
| 탭 | 내용 |
|---|---|
| 홈 | 2주 계획수량·품목수·발행할 송장 · 가까운 납기 6건 · 송장 상태별 현황 |
| 내 계획 | 일자별 그룹 · 자도번 · 수량 |
| 내 송장 | 발행 대기 목록 · 상태별 현황 |
> 송장 발행(QR)·입고 스캔은 다음 단계. 지금은 "준비 중" 안내를 띄운다.

### 신규 API
```
GET  /api/partner/my?days=14    홈 요약 (계획·송장상태·발행대기)
POST /api/partner/depart        송장 출발 처리 10발행 → 20출발
```
둘 다 `COOP_ALLOW` 에 등록했다 — **등록하지 않으면 협력사가 못 부른다**(deny by default).

`depart` 를 분리한 이유 — 발행만 하고 며칠 뒤 오는 경우가 있어,
우리 담당자가 **오늘 뭐가 오는지** 알아야 입고를 준비한다.

### ★두 번 틀렸고 두 번 다 '정본을 안 쓴 탓'이었다
**① `plan_part_mat.PLAN_QTY`** — 그런 컬럼은 없다(`part_plan_qty`).
**② 더 중요한 것 — `MAT_CODE` 로 그룹했다.** 그건 **원소재**다.
   협력사가 만드는 것은 **자도번(`ASSY_ITEM_CODE`)** 이다. 정본 `partner_planstatus` 는
   자도번 축이고 수량은 `plan_item_dtl` 조인이다.
⟹ SQL 을 새로 짜는 대신 **`partner_planstatus` 를 그대로 호출**하도록 고쳤다.
   새로 짜면 계획 화면과 숫자가 갈린다(하드룰 §1-10 의 정신 — 계산은 정본으로만).

**③ 라우트 함수를 직접 부를 때는 모든 파라미터를 명시**해야 한다.
   빠뜨리면 FastAPI 의 `Query(...)` 기본값 **객체**가 그대로 들어와 `.strip()` 에서 터진다.

### 검증 8/8 PASS (실계정)
```
무토큰 → 401
절삭-협력사 미래정밀   계획 404행/21,714개 · 발행대기 100건 · 상태 요청130·발행4·입고완료1
이지링크 온세화학      계획 4행/264,463개
절삭/설치-부자재       계획 0행 (그 업체는 2주 창에 계획이 없다 — 정상)
★협력사가 cust=2148   → 자기 것(2096)만
계획수량 = 정본 부분집합  my(2주) 21,714 ≤ planstatus 전체 36,789
내부는 지정 거래처 조회   대원산업 316행
```

### 실측이 말해주는 것 — 화면을 유형별로 갈라야 한다
| 매입유형 | 업체 | 계획 있음 | 세트입고 송장 있음 |
|---|---:|---:|---:|
| 절삭-협력사 | 11 | 11 | **9** |
| 절삭-부자재 | 53 | 40 | 1 |
| 설치-부자재 | 20 | 16 | **0** |
| 이지링크 | 3 | 3 | **0** |

**송장 발행·입고 스캔은 사실상 절삭-협력사 11곳의 기능**이다.
나머지 76곳에 같은 화면을 주면 빈 송장 화면을 본다.
계획 조회는 70곳이 쓸 수 있고, **17곳은 계획도 송장도 없어** 발주 조회가 없으면 쓸 게 없다.
⟹ 다음 단계에서 **유형별 화면 분기**를 넣는다.

---

## 15. 송장 발행(QR) · 입고 스캔 · 유형별 분기 (2026-08-29)

### 흐름 (전 구간 구현)
```
협력사 : 발행대기 목록 체크 → 수량 확인 → 발행 → ★QR 표시/인쇄 → 출발 처리(10→20)
담당자 : QR/리더기 스캔 → ★확인 화면 → 입고 → 재고 파생
```
**스캔 즉시 입고하지 않는다.** 도번이 최대 35종 들어가므로 잘못 찍으면 통째로 잘못 입고된다.

### QR — 왜 서버에서 만드나
- 내부망이라 **CDN 을 못 쓴다**(브라우저 QR 라이브러리를 불러올 수 없다)
- 직접 인코더를 짜면 **현장에서 안 찍혀도 확인할 방법이 없다**(디코더가 없다)
⟹ 검증된 `segno`(순수 파이썬·의존성 0)를 **repo `backend/vendor/` 에 넣고** 서버에서 SVG 생성.
   **서버에 `pip install` 을 요구하지 않는다** — 배포는 `git pull` 뿐이라(§6) 의존성을 추가하면
   운영에서 500 이 난다.

값 = `'SET' + 발행번호` — **레거시 스티커(Code39)와 같은 값**이다.
`setstock/scan` 이 숫자만 뽑아 쓰므로 **리더기·폰 어느 쪽으로 찍어도 같다.**

### ★검증하다 잡은 결함 2건 (둘 다 배포 전)
**① 출발(20)을 입고가 몰랐다** — 방금 추가한 상태인데 `receive` 가 `IN ('10','30')` 만 받아
   **출발 처리하면 입고가 막혔다.** `('10','20','30')` 으로 고쳤다.
   *새 상태를 만들면 그 상태를 소비하는 곳을 전부 찾아야 한다.*

**② segno 가 Micro QR 을 골랐다** — 짧은 값이면 자동으로 M3(19×19)를 선택하는데
   **마이크로 QR 은 폰 카메라·리더기 상당수가 못 읽는다.**
   `make_qr()` 로 **표준 QR 강제**(버전1 · 29×29). 현장에서 안 찍히면 치명적인 대목이다.

### 유형별 탭 분기
세트입고를 안 쓰는 협력사(76곳)에는 **송장 탭을 숨긴다.**
```
탭 = 홈 · 내 계획 [+ 내 송장(세트입고 쓰는 곳만)] [+ 입고(담당자만)]
```
실측 근거: 세트입고 송장이 있는 곳 = 절삭-협력사 9/11 · 나머지 1/76.
같은 화면을 전원에게 주면 **대부분이 빈 송장 화면**을 본다.

### 검증 — TestBed 왕복 13/13 · 오염 0
```
협력사 홈           계획 404행 · 발행대기 100건 · 발행한송장 2건
협력사가 발행        SET700006 · 3건
QR SVG              628바이트   ★남의 바코드 QR → 403
출발(10→20)         성공        재처리 → 409
담당자 스캔          미래정밀 · 도번 3종     ★협력사는 스캔 403
입고                 3건 · 재고파생 8행      중복 → 409
협력사 홈 반영       요청127 · 발행4 · 입고완료4
롤백                 오염 0 (diff={})
```
**전체 TestBed 회귀: PASS 83 · FAIL 0 · SKIP 1 · 오염 0**

### 정직하게 남기는 한계
- **QR 이 실제로 디코딩되는지는 폰으로만 확인 가능하다** — 이 환경에 디코더가 없다.
  다만 값이 레거시 스티커와 같아 **폰이 안 되면 기존 Code39 리더기로 대체**된다.
- 폰 카메라 스캔은 `BarcodeDetector` API — **안드로이드 크롬은 되고 아이폰 사파리는 안 된다.**
  아이폰은 리더기·수동 입력으로 안내한다.
- 외부(협력사 사무실)에서 접속하려면 **별도 경로가 필요**하다(지금은 사내망·ZeroTier 안에서만).

---

# ☐ 다음에 이어서 할 일 (2026-08-29 기준 · 여기부터 읽으면 된다)

> **왜 여기서 멈췄나** — 지금까지 만든 것은 **사내망(200.200.200.184) 안에서만 접속된다.**
> 대표가 사외에 있어 **폰으로 실물 확인을 못 했다.** 코드·검증은 끝났고 배포도 됐다.
> ⟹ **①번(폰 실물 확인)부터 시작한다.**

## 지금 상태 — 운영 배포 완료 (엔드포인트 526)

| 항목 | 상태 |
|---|---|
| 인증(`nx.app_user`/`app_session`·PBKDF2·토큰12h·5회잠금) | ✅ 배포 |
| 소속 강제(협력사는 `cust` 무시·자기 것만 · 바코드 주인확인) | ✅ 배포 |
| 내부 API 전면 게이트(무방비 509 차단 · 협력사 화이트리스트) | ✅ 배포 |
| 협력사 계정 **87개** 개설·활성화 | ✅ 완료 |
| `partner.html` 모바일 웹앱(홈/내계획/내송장/입고) + PWA | ✅ 배포 |
| 송장 발행 → **QR** → 인쇄 / 출발(10→20) | ✅ 배포 |
| 입고 스캔(리더기·카메라) → 확인 화면 → 입고 | ✅ 배포 |
| 유형별 탭 분기(세트입고 안 쓰는 76곳은 송장 탭 숨김) | ✅ 배포 |
| 중복 입고 409 차단 · 입고취소(3곳 되돌림) | ✅ 배포 |
| TestBed 포털 15케이스 상설 (`PASS 98 · FAIL 0 · 오염 0`) | ✅ 완료 |

---

## ☐ ① 폰 실물 확인 — **가장 먼저 · 사내망에서**

```
http://200.200.200.184:8010/partner.html      ← 사내 WiFi 폰
http://192.168.194.90:8010/partner.html       ← ZeroTier 폰
```
| 계정 | 확인할 것 |
|---|---|
| `p2096` / `GMvebkzETr` (미래정밀) | 홈 카드 · 내 계획 · **내 송장 → 체크 → 발행 → QR** |
| `TEST1` / `pnc1!` (담당자) | **[입고] 탭** — QR 스캔 → 확인 화면 → 입고 |
| `p2136` (동주금속·부자재) | **송장 탭이 안 보이는 게 정상** |

**★반드시 확인할 것 = QR 이 실제로 읽히는가.**
이 환경에는 QR 디코더가 없어 **폰으로만 확인 가능**하다.
- 안 읽히면 → 값이 `'SET'+번호` 로 **레거시 스티커(Code39)와 같으므로 기존 리더기로 대체** 가능.
- 아이폰 사파리는 `BarcodeDetector` 미지원이라 **카메라 스캔이 안 된다**(리더기·수동입력 안내 중).
  안드로이드 크롬은 된다.

같이 볼 것: 글자 크기 · 무엇이 먼저 보여야 하는지 · 빠진 정보.

---

## ☐ ② 계정 비밀번호 배포 — ①이 끝난 뒤

`D:\피앤씨인더스트리\100_AI_AGENT\협력사포털_계정_260829.csv` (87건 · 평문은 여기에만)
- ID = `p` + 거래처코드 (예: 미래정밀 `p2096`)
- 계정은 이미 **활성(사용)** 상태다. 비번만 아직 안 나갔다.
- **★배포 전에 시드 비밀번호를 바꿔야 한다** — `super/super`, `TEST*/pnc*!` 는 컷오버 전 변경 대상.
  하네스 계정 `flowcoop` 비번은 repo 밖 `_migration/.flow_secrets.json`(gitignore).

---

## ☐ ③ 외부 접속 경로 — 협력사가 자기 사무실에서 쓰려면

지금은 **사내망·ZeroTier 안에서만** 열린다. 협력사에게 열려면 별도 경로가 필요하다.
참고 = `[[newerp-partner-external-access]]` (Cloudflare Tunnel + 인증보강).
**인증·소속강제·API 게이트가 이미 서 있으므로 터널만 얹으면 된다.**

---

## ☐ ④ 3단계 — 계획 변동 알림  ★대표 지정 대기

**막혀 있다: 알림톡 받을 협력사 목록을 대표가 지정해야 한다**(자동 판별 금지 — §7-5).
- `nx.partner_portal` 테이블 신설(`cust_code` PK · `portal_use` · `noti_kind` T/W · `noti_phone` · `noti_name`)
  ★`CM_M_CUST` 에 넣으면 **매일 sync(do_full=TRUNCATE)가 지운다.**
- 기준정보에 「협력사 포털 관리」 화면 — 목록에서 **체크로 켜고 끈다**.
- 절삭 협력사 = 알림톡 / 나머지 = 웹푸시(아이폰은 **홈 화면에 추가**해야 알림이 온다 — PWA 준비됨).

---

## ☐ ⑤ 남은 다듬기

- **아이폰 카메라 스캔** 대안 — 사파리가 `BarcodeDetector` 미지원. 리더기 안내로 버티는 중.
- **`ROLE_MOD` 에 '협력사' 항목이 없다** — 내부 ERP(`index.html`) 로 들어오면 `기준정보` 메뉴만 보이고
  누르면 403 이 난다(백엔드는 막혀 있어 **유출은 아니다**). 협력사는 `partner.html` 로만 안내한다.
- **17곳은 계획도 송장도 없다** — 발주서 기반 업체다. 포털에 **발주 조회**가 없으면 쓸 게 없다.
  (실측: 계획 있음 70/87 · 세트입고 송장 있음 10/87)
- `setinreq`(구 바코드 발행 화면) — `partner.html` 이 대체했다. 정리 여부 판단 필요.

---

## ☐ ⑥ 이 작업에서 발견했으나 **범위 밖이라 남긴 것**

**`procreg_save`(생산실적 등록) 커서 중첩** — 고쳐서 배포했다(같은 부류의 운영 간헐 오류).
다만 **`backflush._avail_bulk`·`_prod_shortages` 를 커넥션 공유로 부르는 패턴 자체**는 남아 있다.
생산 도메인 담당이 전수로 볼 것. TestBed 가 `HY000` 으로 잡아준다.

**컷오버 관련(별건)** — `feat/cutover-live-to-mirror` 브랜치에 **자기-UNION 행 2배 결함**(의심 32곳).
컷오버 절차서 7번(일괄 flip)이 그 브랜치를 쓴다. **컷오버 전 반드시 수정.**
`fix/saleout-cust-ac` 는 판매출고 재병합 대상(미배포라 현재 운영 영향 없음).
