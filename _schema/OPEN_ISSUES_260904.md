# 미해결 쟁점 — 2026-09-04 편성 대사

> 계획업로드 19:12 → 편성 19:24 기준. 검증 = `_schema/VERIFY2_260904_*.txt`
> ★DB 는 nx 만 변경했고 라이브는 읽기만 했다(CLAUDE.md §1-1).

## 오늘 처리한 것 (완료)

| 항목 | 결과 |
|---|---|
| 마스터 동기화 5종 | 추가계획 신규14·갱신4·삭제1 / 소요량 5행 / 나머지 이미 동기 |
| BOM 링크 누락 | **52 → 0행** (상위품목 17개, 추가58 / 제거41) |
| 백업 | `nx.bk_bomline_260904_1920` · `nx.bk_item_260904_1920` |
| 경로 단절 점검 | 전부 OK (지워도 못 닿는 하위 없음) |
| 계획 원본 / 추가계획 | **완전 일치** (4,054행·163,495 / 15,232) |

---

## ★쟁점 1 — 파트별계획 −1,390 (사급품이 필터에서 탈락)

**증상**: [4] ④파트별계획에서 레거시에만 155키, 수량 −1,390.
차이 상위가 전부 `MJU00892201` 한 품목이다.

**추적 결과 — 링크 문제가 아니었다**

```
nx.bom_line        5행  OK  (링크 52행 채운 뒤)
v_pr_bom           5행  OK
plan_part_temp     5행  OK  (BOM 전개 통과)
plan_part_gagong   0행  ← 여기서 탈락
plan_part_dtl      0행
```

**원인** = `backend/routers/planrev.py:192`

```sql
WHERE a.vir_item_flag='0' AND ISNULL(a.in_cust_code,'') IN ('','2228')
```

`MJU00892201` 은 **공정마스터 5건을 갖고 있는데도** `in_cust_code='2201'`
(에프원 공조 · 사급)이라 이 WHERE 에서 떨어진다.
⟹ **링크를 채워도 결과가 안 바뀐다**(실측: 재편성 후 수치 변화 0).

**낮에 본 `-20-1` 건과 같은 필터·같은 계열이다.**
그쪽은 사급 중간도번이 하위로 가는 **리드타임 누적 경로**를 끊는 문제였고,
이쪽은 사급 자재 자체가 **파트별계획에서 빠지는** 문제다. 뿌리는 같다.

**판단 필요**: 레거시는 사급품도 파트별계획에 넣는다. 웹이 빼는 게 맞는지,
아니면 필터를 넓혀야 하는지 = 업무 규칙 확인이 먼저다. 코드부터 고치면 안 된다.

---

## ★쟁점 2 — 품목별계획 +62,679 (원인 미규명)

**증상**: [3] ④품목별계획에서 **웹에만 359키**, 수량 +62,679. 한 방향 쏠림.
양쪽 있는 키는 수량 차이 0건 — 즉 **웹이 없는 키를 만들어내고 있다**.

```
WO1093875KR|5006AR7236A     레거 0 / 웹 20,000
WO1093882KR|5006AR7236D     레거 0 / 웹 10,000
WO1092054KS|5006AR4091D     레거 0 / 웹  4,200
WO1089046AA|PQ060905R01.AKOR 레거 0 / 웹  2,000  (AA 4건 동일)
```

**단서**: 제번 접미사가 `KR`·`KS`·`AA`·`GR` 로 섞여 있다.
`PQ060905R01.AKOR` 같은 품번은 일반 도번 형태가 아니다(설치품 계열로 보임).
축(일자/제번) 오류일 가능성 — 검증 스크립트도 "축 오류 먼저 의심" 을 띄운다.

**아직 안 본 것**: 이 359키가 어느 STEP 에서 생기는지(M/H/K 중),
레거시 `PR_T_PLAN_ITEM_DTL` 쪽 필터가 무엇을 빼는지.

---

## 쟁점 3 — 당김일자 17개 일자 차이

[6b] `part_plan_ymd` 기준 17개 일자에서 수량이 다르고,
**같은 키인데 당김일이 다른 건 8건**. 총량이 같아도 화면 일자칸이 어긋난다.
쟁점 1·2 를 정리한 뒤 다시 봐야 한다(상류가 바뀌면 이것도 바뀐다).

---

## 참고 — 점검 도구의 사각지대 (2026-09-04 실측)

- `sync_before_compose.py` 는 **값(qty·플래그)만** 맞춘다. 링크 추가/삭제는
  의도적으로 제외돼 있다(스크립트 주석에 명시).
- `precheck_compose.py` [1] 은 링크 누락 52행을 **"이상 없음"으로 통과**시켰다.
  → 점검을 통과해도 링크는 따로 봐야 한다:
  ```sql
  SELECT COUNT(*) FROM PARTNER_ERP.dbo.PR_M_ITEM_BOM m
   WHERE ISNULL(m.EXCEPT_FLAG,'0')<>'1'
     AND NOT EXISTS (SELECT 1 FROM nx.bom_line l JOIN nx.bom_header h ON h.bom_id=l.bom_id
                      WHERE h.item_code=m.ITEM_CODE AND l.child_item=m.MAT_CODE)
  ```
- 편성 API `/api/planrev/compose_all` 은 **인증이 걸려 있다**(401).
  편성은 화면에서 돌려야 한다.

---

## 다음 순서

1. 쟁점 1·2 업무 규칙 확인 (사급품을 파트별계획에 넣는가 / 359키의 정체)
2. TestBed 전구간 — `powershell -File _migration\run_testbed.ps1` (롤백 모드, 오염 0)
3. 레거시 대사 3건
   - 파트별 생산계획
   - 협력사 계획현황 — **2096** 미래정밀 · **2148** 대원산업
   - 자재입고현황 — **2345** 그린산업(주)김해공장
