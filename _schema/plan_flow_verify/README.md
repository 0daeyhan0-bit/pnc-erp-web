# 계획 흐름 검증 스크립트 (2026-09-03)

분석 결과 문서 = `_schema/PLAN_FLOW_ANALYSIS_260903.md`

**전부 읽기 전용.** DB 를 바꾸지 않는다. 라이브 `PARTNER_ERP` 는 SELECT 만 한다.
sibling `../New_ERP/db_client.py` 를 쓰므로 저장소 밖 자격증명이 필요하다.

## 실행

```powershell
python _schema\plan_flow_verify\<파일>.py
```

## 목록

| 파일 | 무엇을 보나 |
|---|---|
| `flow_census.py` | **계획 테이블 계보 전수** — 업로드→H→I→파트별→소요를 라이브·웹 나란히 |
| `vf_all9.py` | ① 업로드원본 · ② 파트별410 |
| `vf_all9b.py` | ③ 키팅 · ④ 580 · ⑤ 420 |
| `vf_all9c.py` | ③ 키팅(재시도) · ⑥ 130 · ⑦ 010 · ⑧ 050 · ⑨ 040 |
| `vf_all9d.py` | 키팅 + ⑤420 · ⑨040 · ⑧050 **차이 상세** (어느 제번·도번인지) |
| `vf_detail.py` | 040 제번나눔 · 420 원천 대조 |
| `chk_as_dup2.py` | **A/S 이중계상 확증** — 웹 STEP5 에 A/S 가 섞였고 레거시는 0건 |
| `chk_mirror.py` | ★**nx 미러 신선도** — 계획 8테이블이 라이브와 얼마나 다른지 + 갱신시각 |
| `chk_420src.py` | 420 계획원천 3종(라이브/미러/웹뷰) 비교 — 미러만 낡았음을 보임 |
| `chk_partplandtl.py` | ★`partplandtl.py` 가 −53% 미러를 읽어 화면이 반토막임을 실측 |
| `chk_setexc4.py` | 세트제외 판별축 규명 (상위 part_plan_ymd) |
| `chk_setexc5.py` | 세트제외 조건 **교차검증 — 단독으로는 못 쓴다는 결론** |

## 주의

- **`src=nx` 는 레거시가 아니다.** 낡은 미러다. 레거시 대조는 `src=live` 또는 라이브 직독.
  (이걸 몰라서 420 을 "웹이 틀렸다"고 오판했다 — 실제로는 웹이 맞았다)
- 대사 기준일은 스크립트 상단 `F`/`T`/`YMD` 상수. 계획이 새로 돌면 값이 달라진다.
- `vf_all9*.py` 는 웹 API 를 직접 import 해 호출하므로 백엔드 기동이 불필요하다.
