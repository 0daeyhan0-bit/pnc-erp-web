# 품목별 원가분석 V2 — 계산로직 클린 재구현 · 검증 (durable)

> 최초 2026-08-22. 진행중. 이 문서는 **살아있는 설계·검증 기록**(사용자 지시: "이건 중요한 과정이니 기록을 꼭 남겨주고").

## 0. 배경 / 목표

- **V1**(`SCREEN.costanalysis`, `nx_cost_engine.py`)은 레거시 SP(`SP_CS_견적서_실원가용`)와 **diff0(레거시 재현) 우선**으로 만들어짐 → **레거시 버그·잔재까지 그대로 안고 있음**(사용자 지적).
- **V2** = **우리 시스템에 맞는 올바른 계산로직**. 레거시 버그 비복제, 잔재 제거. "레거시 diff0"가 목표가 아니라 **"우리식 정답"**이 목표.
- **★북극성 = 실제 손익 정확도**(사용자 2026-08-22: "실제 손익이 얼마인지에 대한 정확도가 중요한 만큼 이 모든 것을 감안해서 원가 프로그램을 개선"). 손익=LG판가−실원가. **재료비가 1순위**(모든 시변 가격소스를 정확한 as-of 유효일자로).
- 원칙: 원본(V1) **무수정**. 옆에 짓고(V2) 오라클로 증명 후 전환([[newerp-bom-mirror-legacy-debt]] BOM_MIRROR_DEBT_AND_DIFF0_PRINCIPLE).
- **시작 = 계산로직 검증부터**(사용자: "계산로직부터", "우선 그런 것들부터 검증"). 통합 대상: **R02(조달경로 선택업체 원가)**, **except_flag 제거 + 관련 flag 재검토**.

## 1. 현재 계산 경로(V1)

- 엔진: `_harness/nx_cost_engine.py` (963줄). 산식정본 = [[newerp-legacy-cost-algorithm]] (`SP_실원가용_250910`): 재료비/가공비/LME/일반이윤.
- 백엔드: `/api/cost/nx`(bulk)·`/api/esti`. 화면: `SCREEN.costanalysis`(screens.dev.js). V2 화면=`SCREEN.costanalysis_v2`(복제 완료·독립).
- 오라클/게이트: `_harness/cost_oracle.py`(레거시 SP) · `engine_rebaseline.py`. 게이트 기준일 **260630**(월말 stable).
- 설계차 등록부: `_harness/COST_DESIGN_DIFFS.md`(레거시와 diff≠0가 정상인 의도적 차이).

## 2. 레거시 버그·잔재 카탈로그 — 계산로직 (★현재 엔진 상태 실측 확인)

> 근거: `_legacy_analysis/LEGACY_BUG_CANDIDATES.md` + 엔진 실측. **등록부가 stale한 항목 있음**(아래 A2).
> V2결정: **유지**=우리식 이미 맞음 / **교정**=레거시버그 고침 / **판단대기**=사업지식 필요.

| # | 항목 | 레거시 SP 동작 | 현재 V1 엔진 (실측) | V2 결정 |
|---|---|---|---|---|
| A1 | **lgroup 필터 → 타그룹 제작부품 가공비 누락** | 가공비를 `CS_M_PROC.ITEM_LGROUP IN(상위품목lgroup,'J')` 공정만 계상. 타그룹 부품 실제공정 0처리 | **재현**(lgroup-fix, `_valid_procs` L185-194) | **판단대기** — BOX 등 타그룹 제작부품 가공비를 제품원가에 포함? (예 AGF30058407⊃AGF30058505 868원) |
| A2 | **임율 GETDATE(오늘) 적용** | `CS_M_LABOR_COST_RATE APPLY<=GETDATE()` 최신 임율(원가일 무관) | **★이미 교정됨** — `labor_rate` L182 `r[0]<=ym`(as-of 원가월). 등록부 "GETDATE 재현중"은 **stale** | **유지**(우리식 정답) |
| A3 | **용접봉 재료비 흡수(성분분해 부정확)** | 용접봉(RAC)을 가공비 아닌 재료비 흡수. 총액 상쇄로 맞으나 재료/가공 분해 틀림 | 용접봉=공정종속 분리(COST_DESIGN_DIFFS **D1**) | **★교정 확정(사용자 2026-08-22)** — "용접공수도 우리는 공정으로 뺐어". 용접행위=공정 가공비, 용접봉=공정종속 재료비 분리 [[newerp-weld-cost-split]] |
| A4 | pur_price 계열(asof/fx/vendorstrict/zeroprice) | SP 정합용 픽스 다수 | 재현(as-of≤ymd·통화환산·거래처일치·0원포함) | 재검토 — 대부분 우리식과 일치할 것 |

## 3. Flag 인벤토리 — except_flag 제거 연동 재검토 (사용자 지시)

> "except_flag를 제거하면서 관련 flag들을 검토를 다시해야할 필요도 있어."

| flag | 의미 | 원가 영향 | 이관/상태 |
|---|---|---|---|
| `CS_CALC_EXCEPT_FLAG` | **원가 제외**(전개는 하되 원가 스킵) | 재료비/가공비 계상 스킵 | routing_edge STEP7로 이관중 [[newerp-routing-edge-flag-retire]] |
| `except_flag`(전개제외) | 상위 SUB 거래처 귀속(발주 주체) | 전개 자체 스킵 → 원가 트리 변동 | 상위SUB 거래처 귀속규칙 [[newerp-except-flag-vendor-rule]], 재싱크 [[newerp-bom-flag-sync-cutover]] |
| `make_type` | 1=사내제작·2=유상사급·3=매입 | 제작=원가전개, 매입=구매단가 | B3 오분류(이젠터 3→2) |
| `INNER_PROD` / `SAGUB_FLAG` | 사내생산 여부 / 유상사급 여부 | 사급비·매입 판정 | B3(SAGUB_FLAG 누락 국내유상사급) [[newerp-purchase-vendor-rules]] |

→ **V2 과제**: except_flag 제거 시 위 flag들이 **원가 계상(전개제외·원가제외·사급·매입 판정)**에 미치는 영향을 재검증. 특히 CS_CALC_EXCEPT_FLAG↔routing_edge, except_flag↔거래처귀속.

## 4. R02(조달경로 선택업체 원가) 통합

- 품목 BOM관리 조달경로 **R02(route_order)** 선택업체 매입단가로 실원가 재계산 = 앞서 dev구현·**미배포**(sourcing.py `sourcing_route_cost`, [[newerp-sourceprofile-route1-select]]).
- V2 계산로직에서 **정식 통합** — 부품별 선택 매입처(sourcing_profile) 단가를 `pur_price`가 우선 반영.

## 5. 검증 스코프 & 방법

### 5.1 스코프 = 리시빙 매출 상위 25% (사용자 확정 2026-08-22)
- **근거**: 원가 개선 임팩트는 매출 큰 품목에 집중 → "매출액 대비 비교가 중요".
- **정의**: `PARTNER_ERP.dbo.SA_T_LG_RECEIVING_DTL` **2601~2608**(올해 1월~현재), 품번별 `SUM(recv_amt)` 랭킹 **상위 25%**.
- **실측(2026-08-22, dbo 라이브·전량 KRW)**:
  - 전체 **1,010품번 / 매출 ₩53,317,874,579**
  - **상위 25% = 253품번 / 매출 ₩50,557,892,591 = 94.8% 커버** (컷=₩22.9M)
  - 파레토: 매출 80%=상위 103품번(10%). → 253품번이면 거의 전 매출 포괄.
  - Top: PQ061208C41.AKOR(3.80B)·ADM74790619(2.33B)·AJR30077403(2.21B)·PQ060905R01.AKOR(1.70B)·AJR75563503(1.46B)…
  - nx 미러 리시빙 품번수=1,010(=dbo) → **원가엔진(nx) 실행 가능**. 단 최근월 qty/amt는 미러 stale 위험 → 스코프 랭킹은 dbo 기준.
  - 목록: `scratchpad/costv2_top25_scope.csv` (rank,item_code,recv_amt,recv_qty)

### 5.2 방법
- 오라클=`cost_oracle.py`(레거시), 엔진=`engine_rebaseline.py`. 단, **V2 목표는 diff0 아님** → 각 우리식 교정은 COST_DESIGN_DIFFS 방식으로 **설계차 등록**(레거시와 diff≠0=정상).
- 253품번을 원가 요소별(원자재·부자재·용접봉/용접공수·LG사급·가공비·LME·이윤)로 **현재 계산 분해** → ①레거시 재현 ②우리식 이미 맞음 ③계산방식 변경 필요(용접 공정화·flag·R02) 분류. 기준일 260630(월말 stable).

## 5-스코프. ★분석 초점 좁힘 (사용자 확정 2026-08-22)
> "판가, 사급부품 가격 변동으로 인한 분석만 하면 될 거 같아."
- V2 재료비/손익 분석의 **핵심 2축 = ①판가(LG 판매가) 변동 ②사급부품 가격 변동**. 이 둘이 매월 변하는 실제 손익 변동 드라이버.
- 판가 = 원소재 소요량(중량) × LME 인정가(Cost Table) 변경분으로 인상/인하(개별품 가격분리 불가, 위 계산로직 정본).
- 나머지(R-1 사급전환·R-3 8/6·R-5 협력사가·R-6 사급차액 등)는 배경 조건으로 두되, **분석 산출물은 "판가·사급부품가 변동이 253품목 손익에 미친 영향"**에 집중.

## 5A. ★재료비 계산 — V2 최우선 (사용자 확정 2026-08-22)

> "용접도 중요하지만 **재료비 계산이 가장 중요**." 재료비는 아래 3가지로 **시변·품목조건 분기**한다. 이게 V2 계산로직의 1순위.

### R-1. 절삭가공품 원소재 사급전환 (2602~)
- **사실**: 2026년 **2월부터 LG전자가 절삭가공품 원소재를 사급전환**. 단 **전환된 품목 / 안 된 품목이 혼재**.
- **재료비 영향**: 전환품 = 원소재를 LG가 사급 → 재료비 원재료비 = **LG사급가** 기준([[newerp-metal-unit-price-source]] "절삭재료비 원재료비=LG사급가"). 비전환품 = 우리가 **직매입** → 직매입가 기준.
- **검증 필요**: ①품목별 사급전환 여부를 무엇으로 판정하나(flag/유효일자?) ②전환 시점(2602) 전후로 재료비 소스가 바뀌나 ③현재 엔진이 이 분기를 하나(as-of ym으로).

### R-2. 직거래품 — 월별 변동  → ★해결방향: 실제 마감가격(확정입고 마감) (사용자 2026-08-22)
- **★사용자 확정**: 직거래 원소재는 **추정단가(price_metal/Cost Table) 대신 실제 마감가격으로 정리**. = 우리가 실제 매입·확정한 값.
- **소스 = 확정입고 마감** (레거시 `w_pu_input_120` 확정입고집계표). 거래처별 매입 확정입고·마감기준 월별·거래처분류(1유상사급/3절삭-원자재[LS메탈 등]/3절삭-협력사/7절삭-부자재). nx 대응 = 확정입고(PU_T_STOCK_MAINT 9/S/C/G/H + _C tagP) 월마감.
- **장점**: R-7(동가↑시 우리 매입가 즉시↑)이 실제값에 그대로 반영 → 판가지연 squeeze가 손익에 정확히 드러남. 추정단가 스테일 문제도 소멸.
- **★소스 후보 2개 비교(사용자 검토 2026-08-22)**:
  | | 확정입고 마감(w_pu_input_120) | 이동평균 마감(nx.mat_stock_daily) |
  |---|---|---|
  | 값 성격 | 실제 매입가(lot 단가) | 실제 소비원가(재고 이동평균) |
  | 회계정합 | 매입시점 | **재료비=소비원가 표준** |
  | 가용성 | 매입 있는 달만 | **일단위 항상(재고 이월)** |
  | R-7 반영 | 매입 즉시 full shock | 소비 따라 반영(정확 매칭) |
  | 구축 | 레거시 집계 | **이미 nx 구축·8/22 검증** |
- **★제안(대사 추천)**: **직거래 원소재 소재단가 = mat_stock_daily 이동평균 평균단가**(price_metal 추정 대체). 회계상 재료비=소비원가라 이동평균이 정답, 일단위 항상 가용, R-7도 소비매칭으로 정확 반영, 이미 구축·검증. **확정입고 마감**은 "매입 시점 shock" 별도 분석용 보조. **사용자 최종결정 대기.**
- **고려사항**: ①단위(원단위=원/kg vs mat_stock_daily 평균단가 단위) 정합 확인 ②당월 미마감 시점 처리 ③원소재 품번↔소요 중량 매핑.
- (구) 직거래품은 매월 LG인증가(판가)+직매입가 변동 — 판가는 R-2 손익 상단(LG판가), 원가(직매입)는 실제 마감가로.
- **사실**: 직거래품은 **매월 LG인증가(판가)가 바뀌고**, 우리가 **직매입한 금액(원가)도 바뀐다**.
- **재료비/손익 영향**: 판가·원가 모두 월별 as-of 필요. LME 판가연동 [[newerp-dtradeprice]].
- **검증 필요**: 현재 엔진이 직거래품 LG인증가·직매입가를 **원가월 as-of**로 잡나, 최신 스냅샷으로 잡나(price_metal 옛스냅샷 결함 [[newerp-metal-unit-price-source]]).

### R-3. 사급 원소재 단가 변경 (260806)
- **사실**: 사급 원소재 단가가 **2026-08-06에 변경**됨.
- **영향**: as-of 유효일자 처리 필수 — 8/6 이전/이후 원가월에 다른 단가. 8월 원가는 6일 경계로 갈림.
- **검증 필요**: 사급 원소재 단가의 유효일자 테이블(price_metal/원소재마스터 [[newerp-cutmatcost-db]])에 260806 변경이 반영됐나, 엔진이 as-of로 집나.

### R-4. LG 사급부품 가격 수시 변동
- **사실**: **LG 사급부품(LG가 사급하는 부품) 가격도 수시로 바뀐다**.
- **영향**: 사급비(SA_JAI_AMT) 재료비 성분이 시변 → 원가월 as-of 필요. 옛 스냅샷 쓰면 손익 왜곡.
- **검증 필요**: LG 사급부품 단가 소스·유효일자, 엔진이 원가월 as-of로 잡나(price_metal 옛스냅샷 결함 [[newerp-metal-unit-price-source]]).

### R-5. 협력사 사급 원소재 인상 (2601~) — ★데이터 확정
- **사실**: 2026년 1월부터 **협력사에 내보내는(사급) 원소재가를 CU 20,000 / 고강도 22,000으로 인상**.
- **실측 확정(2026-08-22)**: `nx.price_metal.partner_price` = **CU 20,000 · 고강도 22,000** (202601·202605·202608 동일). = 협력사가(우리가 협력사에 사급하는 단가).
- **std_price = LG사급가**(우리가 LG로부터 받는 단가): CU 18,457~19,888(202601)→22,841(202608), 고강도 →22,889(202608). **LME 사급차액 = std − partner**(_metal_sub). 예 CU 202601: std~19,000 < partner 20,000 → 차액 **음수**(협력사에 손해보고 사급) → 202608 std>partner로 양전.

### R-6. 매입 비싸게 / 사급 싸게 품목 — 사급차액 손익반영
- **사실**: 어떤 품목은 **우리가 비싸게 매입하고 싸게 사급**(내보냄) → 그 차액만큼 손해. V1에서 **신규로 다룸**.
- **현재 V1**: 사급차액(실출고가−실입고가, 음수=손해) 컬럼 r[17]·실사급금액 r[24]. 손익=LG−실원가 **+ 사급차액** [[newerp-sagub-diff-reflection]].
- **V2**: 이 처리 **유지·검증**(재료비 정확도의 일부).

### R-8. 매입 이력 있는 부품 = 불출(사급) 기준도 존재 (사용자 예시 2026-08-22)
- **사실(예시 4H00049A, make_type=2)**: 우리가 **매입**(두진산업 2012, 절삭-협력사, 2,284개/879,340원≈385/개) → 협력사(미래정밀 2096)로 **사급 불출**(4H00049A-1, 205개/단가207=42,435). **매입가·불출가 둘 다 실적 존재**.
- 소스: 매입=자재입고집계표(PU_T_STOCK_MAINT 9/S/C/G/H + _C tagP)·불출=자재불출집계표(PU/SA_T_STOCK_MAINT+_C, 실제 이동일).
- **손익 함의**: 이런 품목은 **실제 매입가(우리가 산 값)**와 **불출가(사급 출고값)**를 실적에서 직접 잡을 수 있음 → R-6(매입비싸게/사급싸게 차액)·R-7(매입가 즉시인상)의 실측 근거. V2 재료비/손익을 마스터 단가 대신 **실적 매입·불출 기준**으로도 대조 가능.

### make_type='2' 안의 2유형 (사용자 예시 2026-08-22) — Q1 판단 근거
> make_type='2'(레거시 생산구분 2:외주)는 실제로 **두 흐름이 섞임**. 사용자 판단 진행중.
1. **매입→사급 pass-through**: 4H00049A — 우리가 두진산업 매입 → 미래정밀로 사급 불출. (원자재를 우리가 사서 협력사에 사급)
2. **외주완성 매입**: AAA36923606(설치자재, in_cust=토탈솔루션) — 토탈솔루션에서 **완제품으로 사옴**. 레거시 견적원가조회=생산구분 2:외주, 구성부품 전부 **2:구매단가**, 재료비 704. (그냥 사오는 것)
3. **유상사급 가공**: AJJ30041801(서포터, in_cust=대원산업) — 대원에서 사지만(2:외주) BOM **완전전개**. 우리가 소재·사급부품을 대원에 사급→대원 조립→우리가 가공비 지급. 재료비 37,785.
   - **SUB(AJJ30041801-SUB 체결-SUB)=생산구분 1:사내** = **우리가 사내에서 체결 작업 후 대원에 사급**. SUB 안=구매부품(screw/supporter 2:구매단가)+사급부품(MJX valve 1:사급단가). 즉 make_type='2' 외주품 밑에 사내제작 SUB가 들어가는 다계층.

### ★단가구분 = 부품별 재료비 산정 기준 (레거시 견적원가조회 실측, 핵심)
> AJJ30041801 BOM에서 구성부품마다 **단가구분**이 다름 = 재료비를 어느 단가로 잡느냐의 정본:
> - **3:소재단가** = 원소재(동/고강도 Tube). 예 5210A23089D(CU) 18,458 · MJU00777404(고강도) 22,485 · MJU00777405 22,889. ← price_metal.std(=LG사급가)
> - **1:사급단가** = LG 사급부품. 예 MJX65110201/401/117203(Valve,Service) 4,371/7,440/15,233. ← R-4 사급품 가격
> - **2:구매단가** = 매입부품·용접봉. 예 Screw·Supporter·RAC30599301-1(용접봉) 60,000. ← pur_price
> **→ V2 재료비 = Σ(부품별 단가구분에 맞는 단가 × 소요량).** 엔진의 INNER_PROD/cost_gubun 판정이 이 단가구분(1/2/3)과 일치해야 정확.
- ★레거시 정본 원가화면 = **견적원가조회(bom) = `w_cs_esti_010`**(단가기준일·원가구분 내부용/공정·업체/공정/용접/체결 보기, 하단 공정별 임율·LME차액 그리드). 원가 검증 시 이 화면과 대조.

### R-7. ★판가 인상 지연 vs 매입가 즉시 인상 = 시차 손익 squeeze (사용자 확정 2026-08-22)
- **사실**: LG가 동(구리) 가격 인상 시 **판가 인상을 최대한 미루는** 경우가 있음. 반면 **우리는 직거래 부품/원소재를 인상된 가격으로 즉시 매입**.
- **손익 영향**: 매입가↑(즉시) vs 판가↑(지연) → **시차 기간 동안 마진 압박(실제 손해)**. 이게 "실제 손익" 왜곡의 핵심.
- **V2 필수**: 매입가(직매입 as-of 일단위)와 판가(LG TAGE/TAGS as-of)를 **각자 실제 유효일자로** 잡아야 시차 squeeze가 손익에 드러남. 판가·매입가 둘 다 옛/평균 스냅샷 쓰면 이 손해가 안 보임.
- **연결**: R-2(직거래 월별 판가·매입가)·5-스코프(판가 변동 분석)의 구체 시나리오. 분석 산출물에 "판가지연 구간 손익" 표기.

### ★검증 발견 (2026-08-22, 실측)
- **price_metal = 월(apply_ym) granularity** → **R-3(8/6 사급단가 변경) 표현 불가**: 8월 전체가 202608 단가. 8/1~8/5 원가가 옛 단가여야 하나 반영 안 됨. **V2 재설계=원소재 단가 일단위 유효일자 필요**.
- 2026 price_metal = **202601·202605·202608만**(매월 아님). as-of로 2602~04=202601, 2606~07=202605 사용(월 갭은 as-of로 커버됨). 202608=8/6 인상분.
- **apply_ym 포맷 = YYYYMM(6자리)**. 엔진 std_metal_price `apply_ym<=ymcut` 비교 시 ymcut 포맷 일치 여부 **확인 필요**(YYMM이면 문자열비교 오류 위험).
- 직매입(pur_price)=price_item **apply_ymd(일) as-of** — 일단위 OK.

### ★분류 판정 소스 확정 (2026-08-22, 사용자 지정 + 실측)
- **사급/직거래 판정 = `nx.lg_settle_unit`(동정산 원단위) 컬럼 `gubun1`**(값 '사급'/'직거래'). 화면=원소재 마스터›동정산 원단위. **컴포넌트(assy_pn×sub_pn) 단위** — 한 Assy가 사급·직거래 부품 혼재 가능(예 3A00965M: 5210AP4026A=사급, 5424AP3074F=직거래).
- **"설치품(cut_gubun=설치)은 전부 직거래"**(사용자 확정).
- 실측(원단위 최신월 **2606**): 전체 사급 13,064 / 직거래 4,337.
- **스코프 253 Assy 분류(2606 원단위)**: 순사급 65 · 순직거래 26 · **혼합 147** · 원단위없음 15.
- ⚠️ **불일치 2건(사용자 확인 필요)**:
  1. "설치=전부 직거래"인데 원단위에 **설치 Assy 3개가 사급 컴포넌트** 보유 → 데이터 오류 후보.
  2. `lg_settle_unit`은 **ym=2606 한 달만** 적재(2601~2608 아님) → 다월 재료비에 원단위 시변 부재. 원단위 다월 적재 or 2606 고정 여부 확인 필요.
- make_type/routing·협력사 seeding = **기록 전수 확인 중**(추측 금지, 사용자 지시). make_type 엔진 사용=in_cust/make_type/cost_gubun→INNER_PROD(사내전개 vs 매입) 판정.

### ★가격 소스 맵 (2026-08-22 실측) — 월변동은 영업이 관리
> Q4 메커니즘(사용자): **원단위(소요·규격)는 안정(전월복사)**, **가격은 영업이 매월 변동** — ①LG 판가=매월 직거래 인정가 반영 ②사급품가도 매월 변동.

| 성분 | 소스 테이블 | 유효일자 | 엔진 사용 | 비고 |
|---|---|---|---|---|
| **LG 판가**(손익 상단) | `price_item` vendor∈(1010/1020/1030 LG)·type TAGE/TAGS | **apply_ymd(일) as-of** ✅ | `lg_cost()` L412 | 월별 판가변동 캡처됨 |
| **매입단가**(직매입) | `price_item` type '매입' | apply_ymd(일) as-of ✅ | `pur_price()` L284 | |
| **원소재 소재단가**(사급가/협력사가) | `price_metal` std/partner | **apply_ym(월)** ⚠ | `std_metal_price()` L259 | R-3 8/6 mid-month 미표현 |
| **직거래 LME 인정가 Cost Table** | `nx.lg_lme_costtable`(재료비=f(LME,환율))·`lg_lme_header` | apply_ym(월) | ❓ **엔진 미참조 후보** | lglme.py. **R-2 직거래 재료비를 엔진이 이걸 쓰는지 미확인=갭 후보** |
| **사급품 가격**(R-4) | ❓ 소스 특정 필요(lgsagub/price_item 사급type?) | ❓ | ❓ | LG 사급부품 단가 월변동 |

★**검증 포인트**: (a) 직거래품 재료비를 엔진이 price_metal로 계산하나, lg_lme_costtable(직거래 인정가)로 계산하나 — 후자면 엔진이 안 읽어 **R-2 갭**. (b) 사급품가 소스·as-of 확인(R-4). (c) 원단위 다월 적재(사용자 확인 예정).

### ★계산로직 정본 = `26.06월_동정산_원단위(4개사 통합본).xlsx` (사용자 지정 2026-08-22)
> **정본 탭 = `1.2단(피앤씨)`**. (관련 탭: `0.Cost Table(직거래)`·`0.Cost Table(사급)`·`0.황동 원단위`·`0.Cable 원단위`·`동정산(직거래)`·`동정산(사급)`·`1.x단(대원/명진/썬성/한국G&S)`·`3.공정(Assy)`). 파일 위치=repo 루트. **수식 상세는 추후(사용자: "그 수식은 추후에 다뤄야")** — 지금은 기록만.

**2단(피앤씨) 탭 컬럼**: A변경(시방) B업데이트일정 C No D AssyP/N E Desc F협력사 G P/N(하위1) H Desc I수량 **J구분1(직거래/사급)** K구분2(황동/L/W/CAPI) L외경 M T N길이 **O중량 P소재비변경후 Q소재비변경전 R재료비변경후 S재료비변경전 T재료비차액**.

**핵심 수식(실측)**:
- `O중량 = VLOOKUP(P/N, '0.황동 원단위', 5, 0) × I수량` → **원단위(개당중량)×수량**
- `P소재단가(변경후) = '0.Cost Table(직거래)'!I44` · `Q(변경전) = !W44` (직거래는 Cost Table 직거래에서, 사급은 Cost Table 사급에서 조회 추정)
- `R재료비(변경후) = O중량 × P소재단가` · `S(변경전) = O × Q` · `T차액 = R − S`

**→ 판가 변동 메커니즘(사용자 확정)**: **개별 품목 가격분리 불가**. **원소재 소요량(중량) × LME 인정가(Cost Table) 변경분**으로 판가 인상/인하. T(재료비차액)이 그 근거.

**★V2 연결 검증포인트**: 이 정본은 재료비를 **중량 × Cost Table(직거래/사급) 소재단가**로 계산. 현재 엔진은 **price_metal**로 계산 → 소스가 다름(Cost Table=lg_lme_costtable vs price_metal). **직거래 재료비가 엔진↔정본 일치하는지 = R-2 핵심 검증**(추후). 원단위(중량)=황동/Cable 원단위 탭 = nx.lg_settle_unit 대응.

### 재료비 검증 순서(스코프=상위25% 253품번)
1. 253품번을 **사급전환/비전환/직거래**로 분류 → 각 그룹 재료비 소스 확인
2. 현재 엔진의 재료비 산식이 그룹별로 맞는지(사급가 vs 직매입가, as-of 유효일자) 실측
3. 260806 사급단가 변경·직거래 월변동이 원가월별로 정확히 반영되는지
4. 갭 = V2 계산방식 재설계 항목으로 등록

## 5C. 기록 전수 확인 결과 (2026-08-22, 서브에이전트 + 실측)

### 재료비 계산 체인 (엔진 실측, 정본)
- **원소재 소재단가** = `nx.price_metal.std_price`(=절삭재료비 CS_M_METERIAL_COST.TOT_COST=LG사급가). 실원가=협력사가(partner=CU20,000/고강도22,000)+LG사급가+LME차액(std−partner). ([[newerp-metal-unit-price-source]], METAL_UNIT_PRICE_SAGUB_SOURCE.md)
- **price_metal 8/21 전체정렬 수정됨**(라이브 CS_M∪웹, 8월 22,387 반영, 재료비 diff0 스윕 9/11 PASS). 단 **R-3(8/6 mid-month)은 월단위라 여전히 미표현**.
- **make_type→INNER_PROD→재료비 갈림**(nx_cost_engine.py:245-373): make_type='1'→INNER=1(사내전개, 소재단가×중량); ''→조건부; 그외(2/3/4/5)→INNER=0(매입가 pur_price leaf, 하위전개 중단). ★엔진은 **4/5를 구별 안 함**(전부 INNER=0). cost_gubun='3' 저장이어도 INNER=0이면 SP가 동적 '2'(구매단가) — INNER_PROD 우선.
- ⚠ **make_type '2' 정의 문서 불일치**: "외주가공"(BOM_EXPLOSION_RULES) vs "유상사급"(V2·vendor-rules). **사용자 확정 필요.**
- **routing 개선=except_flag→routing_edge(생산처 work_center) 이관**이지 make_type 재정의 아님. make_type B3 오분류(이젠터 3→2·SAGUB_FLAG=0 누락)는 corrections.json 등록·승인대기.

### flag 관계
- **except_flag(생산/전개) ≠ cs_calc_except(원가)** — 한 bom_line에 둘 다. 엔진은 **cs_calc_except만 읽음**(except_flag 안 읽음). ([[newerp-bom-flag-sync-cutover]])
- **except_flag ↔ SAGUB_FLAG 상호배타**: 안보냄=EXCEPT(상위SUB 거래처 귀속)/보냄=SAGUB(사급). ([[newerp-except-flag-vendor-rule]])
- **except_flag 재싱크→LME 과다** 위험(외주완성 SUB 전개정지 안 함, CS 2계층 복구 필요 [[newerp-lme-overcount-rootcause]]). cs_calc_except 재싱크=**보류 결정**.

### R02 협력사 seeding
- R02 sourcing_profile 매입처 시드(R01에서). **배포완료 PR#25**. 원가 pur_price 반영은 **미배포·후속과제**(route/cost 원가 미반영 결정=diff0 보호). V2 통합 예정.
- ⚠ **사용자 "최근 협력사 seeding"이 R02 sourcing_profile인지, 별도 협력사매핑([[newerp-coop-2026-mapping]])인지 확인 필요.**

### 샘플 재료비 분해 (esti, ymd=260630) — 손익=LG판가−실원가
| 품번 | 그룹 | 재료계 | 가공 | LME차액 | 실원가 | LG판가 | 손익 |
|---|---|---|---|---|---|---|---|
| AJR30117902 | 절삭(CU) | 21,993 | 267 | 0 | 22,327 | 23,325 | +998 |
| AJR30125601 | 절삭(SS) | 88,349 | 4,192 | −840 | 94,503 | 132,282 | +37,779 |
| PQ061208C41.AKOR | 설치(직거래) | 56,121 | 1,150 | 0 | 62,118 | 58,872 | **−3,246 적자** |
| AJR30077403 | 절삭 | 26,437 | **1,463 ★stale** | 0 | 28,175 | 27,557 | −618(가공 정상335이면 흑자권) |
- ★esti sil.agg는 원자재/부자재/LG사급 분해가 0으로 안 채워짐(JAI_COST 합만) → 성분분해는 /api/cost/nx(cst.won/bu/sa) 사용 필요.

## 5D. ★V2 재료비 계산모델 — 종합 (2026-08-22, 기록 전수분석)
> **V2 재료비 = Σ(부품별 [단가구분 단가] × 소요량) + Σ(구매 동부품 LME차액) + 용접봉(공정종속 재료비)**. 재료비=base(최말단 leaf 단가합)+LME(전서브트리 별도순회). BOM=CS_M_ITEM_BOM(=nx.bom_line 미러). 중량=(외경−두께)×두께×π×길이×비중/1e6.

| 부품 유형 | 판정(make_type/INNER_PROD/cost_gubun/gubun1) | 재료비 단가 | 소스 | 유효일자 | 상태 |
|---|---|---|---|---|---|
| **원소재·사급전환(3)** | INNER=1 & cg='3', gubun1=사급 | 소재단가=**LG사급가**×중량 | price_metal.std | 월⚠ | ✅정확·8/6미표현 |
| **원소재·직거래(3)** | INNER=1 & cg='3', gubun1=직거래 | ❓직거래 인정가 vs price_metal | **lg_lme_costtable(정본) vs price_metal(현엔진)** | 월 | ⚠**R-2 갭** |
| **LG 사급부품(1)** | **ITEM_SGROUP='310'**(519품목, leaf일때만·비-leaf 310은 전개) | **사급가=COSP**, 통째 Σ소요×COSP | `cosp_price()`·`sagub_whole()`; 등급별=`lg_settle_unit.mat_cost`(L/W 18,458·직관P/C 19,888·고강도 18,687~) | ❓ | ✅**R-4 해소**(sgroup310/COSP) |
| **매입부품(2)** | INNER=0, mk∈(2/3) | 구매단가×수량 | price_item('매입') | 일✅ | ✅as-of·통화·거래처 |
| **용접봉(2)** | sgroup=910, RAC | 용접봉단가×[Σ관경 use_qty×1.5] | proc_weld(←CS use_qty) | — | ✅공정종속 43/43 |
| **사내제작 SUB** | INNER=1, mk='1'(또는 ''+has_gagong) | 하위 재귀전개 | CS_M_ITEM_BOM | — | ✅cg3fix |
| **LME 사급차액** | INNER=0 & 동부품(중량>0·metal·in_cust)·lme_except≠1·루트/자식 cg≠'5' | (std−partner)×중량×qty | price_metal std/partner | 월 | ✅13/13 diff0 |

**핵심 규칙**: cost_gubun='3' 저장이어도 **INNER_PROD=0이면 동적 '2'(구매단가)** — INNER_PROD 우선. 전개경계=INNER=0 or cg='5' 정지 / INNER=1 전개(mk='1'이면 cg무관).
**★단가구분1(사급단가)은 현재 엔진에 독립 분기가 없음** = R-4 핵심 갭.
**직거래 재료비 소스 불일치**(엔진 price_metal vs 정본 Cost Table=lg_lme_costtable) = R-2 핵심 갭.

### V2 남은 검증과제 (사용자 확인 대기)
1. **R-4** 단가구분1(LG 사급부품) 소스 테이블·유효일자 특정
2. **R-2** 직거래 재료비 = price_metal vs lg_lme_costtable(Cost Table 정본) 일치 검증
3. **R-3** 원소재 단가 일단위 유효일자 재설계(8/6 mid-month)
4. lg_settle_unit 다월(2601~2608) 적재 여부(현 2606만)
5. make_type='2' 정의 확정(외주가공 vs 유상사급) + 3유형 처리규칙
6. 설치 Assy 3개 사급 컴포넌트 데이터 오류 여부

### 기록보정
- ⚠ `_schema/METAL_UNIT_PRICE_SAGUB_SOURCE.md` durable 파일 **실재하지 않음**(메모리 slug만 존재). 필요시 재생성.
- 산식정본 = `_legacy_analysis/SP_CS_견적서_실원가용_250910.sql`(987줄)·`LEGACY_COST_ALGORITHM.md`.

## 5E. flag·사급/직거래 판정 종합 (2026-08-22, 기록 전수분석)

### 판정 필드 정본
| 판정 | 정본 필드/소스 | 값→의미 | 원가처리 |
|---|---|---|---|
| 생산/전개 제외 | `bom_line.except_flag`(←PR EXCEPT_FLAG) | 1=개별전개금지·상위SUB 거래처귀속 | **원가 무영향**(생산·조달만) |
| 원가 계산 제외 | `bom_line.cs_calc_except`(←CS) | 1=엔진 노드 미생성 | 이중계상 방지 핵심 |
| 사급 여부 | `SAGUB_FLAG`(→sagub_default) | 1=우리가 사서 공급 | 조달/계획 포함 |
| except↔sagub | 상호배타 | **안보냄=EXCEPT(거래처귀속) / 보냄=SAGUB(사급조달)** | 공급여부로 갈림 |
| 제작구분 | `MAKE_TYPE` | 1=자체 / 3=매입 / 2·4·5=외주사급 | INNER_PROD 분기 |
| 소재 vs 구매단가 | `INNER_PROD_FLAG`(MAKE_TYPE 우선) | 0=구매단가·전개중단 / 1&cg3=소재단가 | 저장 cost_gubun 맹신금지 |
| 직납 | `cost_gubun='5'` | 직납가 고정·전개정지·내부LME없음 | lme_total 루트가드 |
| 사급 vs 직거래(LG원소재) | **`lg_settle_unit.gubun1`** | 사급/직거래(설치·LG미인정) | OSP·품목코드로 구분불가 |
| 유상/무상/자체 사급 | `MAKE_TYPE`+lgroup | 절삭=E·mt2(유상)/설치=G·mt1(자체)/무상=문영 | 유상만 LME |
| LG 사급부품 분류 | **`ITEM_SGROUP='310'`**(519) | 사급단가=COSP 통째, 비-leaf 전개 | R-4 |
| 사급차액 반영 | tag5출고−tag(9SCGH)입고 | **(B)묻힘만 가산·(A)직접leaf 제외** | 손익=(LG판가−실원가)+사급차액합 |

### make_type='2' 3유형 flag 조합 (사용자 예시 + 기록)
- **A 매입→사급 pass-through**(두진→미래): 매입(구매단가)+SAGUB_FLAG=1 사급불출. ★기록엔 이 pass-through 직접연결 없음=사용자 확인.
- **B 외주완성 매입**(토탈/태국F&T): MAKE_TYPE 2/3·INNER=0→구매단가·전개중단.
- **C 유상사급 가공**(대원): MAKE_TYPE 2/4·SAGUB_FLAG=1, BOM 완전전개(소재단가+사급단가+구매단가+사내SUB).
- 실측 plan_mat_source 분류: **매입 52.8%·유상사급 21%·외주가공 17.8%·자체 5.7%·외주완성 2.6%**.

### except_flag 제거 → 원가 영향 (재검토)
- **엔진은 cs_calc_except만 읽음**(except_flag 안 읽음) → except_flag 은퇴는 생산/조달축, 원가는 구조상 무영향(단 실값 실측 권장=C4 미검증).
- ⚠ **except_flag만 재싱크→LME 과다**(외주완성 SUB 전개정지 안 함, AJR75563402 +345). cs_calc_except 재싱크=**보류 결정**("원가는 옆세션 유지").
- ⚠ **`/api/bom/save` 전체DELETE+재INSERT** → payload에 cs_calc_except 빠지면 0 리셋→이중계상(MJC 사례 +14,304). BOM 쓰기 시 숨은플래그 보존 필수([[CS_CALC_EXCEPT_HANDOFF]]).
- **routing_edge seeding = wc(생산처/작업장) 시드**(라이브 PR_M_ITEM). ★"실제 제작 업체명=vendor인지 wc인지" 불명=사용자 확인.

### 추가 불명 (사용자 확인)
- **[[newerp-dtradeprice]] = wikilink만·메모리 미작성** → R-2 직거래 월별 LG인증가/직매입가 소스 미확정.
- 사급차액 수불정산: 월별 소요vs입고 1:1 부적절(재고시차). recvcompare_ledger=기초+입고−소요=기말, "매월 3억차이"=손실아닌 재고잔량.
- 무상사급(문영 2026)=LME 없음(우리가 판게 아님).

## 5F. 이동평균 마감가 검토 — 재료비 기준 적합성 (2026-08-22, 실측)
> 사용자: "네 생각을 묻고 싶어. 우리 이동평균 계산법이 문제 없는지 검토." + "우리 원소재 처리방식에 문제가 좀 있어."

### 검증 결과
- **방법론 = 정답**: 이동평균 = 재고 소비원가 회계표준. **7월 입고가 vs 마감가: 원소재 24품목 중 22개(92%) ±1% 일치**. 불일치 2건(7072AR9374N −799·7072AR9374W +1,645)=재고 이월 blend(정상 MA 동작, 버그 아님).
- **★데이터 품질 문제(원소재 처리방식, 사용자 인정)**: mat_stock_daily 최신(260822) 전체 3,960품목 중 —
  - **수량>0 & 평균단가 0 = 94품목** → 이대로 재료비 쓰면 **원가 0** (MAF66426701 442,938개 등). 원인=무가격 입고(tag P)·리셋.
  - **음수재고금액 115 · 음수재고 111** → 마이너스재고(무입고 과다출고) 잔재.
  - 정상(수량>0 & 단가>0)=2,282.
- **결론**: 이동평균 로직은 정확(매입 추종 92%). 그러나 **원소재 재고/무가격 데이터가 오염**돼 있어 **그대로 재료비 기준으로 쓰면 94+ 품목이 0/음수 원가로 틀어짐**.

### 대사 의견 (권고)
1. **이동평균 방향은 옳음**(소비원가·일단위·매입추종). 단 **원소재 재고 처리 데이터 정리가 선행**돼야 신뢰 가능.
2. 정리 필요: ①**음수재고 근본원인**(무입고 과다출고=재고 정합/백플러시) ②**무가격 0단가**(tag P 입고에 소재단가/직전유효 마감가 fallback) ③**직거래/사급 원소재 분리**(사급 원소재는 LG사급가, 직거래만 이동평균).
3. **fallback 규칙 필수**: avg=0 또는 음수면 → 소재단가(price_metal std)/직전 유효 마감가/매입가로 대체. 정리 전 raw 사용 금지.
4. 이 기회에 **원소재 재고 처리방식 자체 재검토**(사용자 지적) — 재료비 기준으로 승격하려면 원장 정합이 전제.

## 5G. ★원소재 재료비 기준 = usage(사급/직거래)별 분리 (2026-08-22, 사용자 지적 + 실측)
> 사용자: "품목별로 LG 사급으로 들어온거랑 직구매한거랑 분리가 안 됨" + "사급이랑 실매입가격이랑 차이가 있어."

### 실측
- **원소재(sgroup210) 입고 tag별(2601~)**: tag9 매입(직구매) qty 530,856 단가 **19,841** · tag5 58,584 단가 19,287 · **tagB 무가격(단가0) · tagC 무가격(단가0)** · SA_T_LG_RECEIVING 원소재=**0품목**.
- **원소재 매입(9,S) 48품목 전부 직구매**(LG거래처 1010/1020/1030 = 0건). → **LG 사급 원소재는 물리 매입으로 안 들어옴**(사급가=price_metal로 계상).
- **∴ 이동평균 avg = 직구매 실매입가(+무가격 오염). LG사급가(≈22,387) ≠ 실매입가(≈19,841).**

### ★사급/직구매 분리 = 거래처 코드 (2026-08-22, 사용자 지적 LS메탈 실측)
- **같은 사업자를 거래처 코드로 나눠 사급/직구매/설치 구분**. 예 LS메탈(사업자 120-87-52157):
  - **2151 LS메탈**(cust_type4) = 절삭-원자재 **직구매**
  - **2187 LS메탈(설치)**(cust_type5) = 설치-원자재 **직거래**
  - **2237 LS메탈-사급**(cust_type4) = **유상사급**
- **∴ 분리는 거래처(코드) 레벨엔 존재. 이동평균(mat_stock_daily)은 mat_code로 뭉쳐 그 분리를 상실** = "품목별 사급/직구매 분리 안됨"의 실체.
- **V2 처리**: 직거래 실매입가 = **직구매 거래처(원자재 분류) 매입만 집계**(사급 거래처 2237 제외). 이동평균 raw 그대로 금지. 사급 거래처 매입(2237)은 사급차액(R-6) 계산용(우리 매입가 vs LG사급가).
- ★확인필요: 확정입고 화면 "거래처분류"(1유상사급/3원자재/7부자재)와 CM_M_CUST.cust_type(1/4/5/6)이 불일치 — 사급 판정 정본 필드 확정 필요(거래처명 '-사급' vs cust_type vs 별도 매입유형).

### ★조달구분 축 설계 — 거래처 중복코드 대체 (2026-08-22, 대사 추천)
> 레거시: 같은 공급사를 거래처코드로 쪼갬 — LS메탈/LS메탈-사급, Hailiang/Hailiang-사급, **삼화코리아 2111(유상사급)/2316(직거래)**(사업자동일, [[newerp-partner-identity-rationalize]] 기존기록), **부광금속/부광금속(설치)**·LS메탈(설치 2187). 조달구분 = **사급/직구매/설치** 3종을 거래처코드로 표현. 사급/직구매/설치가 거래처명·코드에 숨음 → 마스터 지저분·통합조회 난.
> **신규 ERP 추천(= [[newerp-partner-identity-rationalize]] 방향)**:
1. **거래처=1 identity**(사업자번호 통합), `-사급` 중복코드 폐지.
2. **조달구분(사급/직구매/설치)=거래(매입·입고)의 속성**(거래처코드 아님, transaction dimension).
3. **재고·이동평균=(품목×조달구분)별 별도 pool** → 사급 재고풀(LG사급가)/직구매 재고풀(실매입가) 분리.
4. 원가=usage(gubun1)에 맞는 pool 단가. 보고=거래처(통합)+조달구분 양축.
- 효과: 마스터 정리 + 사급/직구매 명시분리(이동평균 blend 해소) + 사급차액(R-6) 두 pool 차이로 자동 + 공급사 통합정산.
- **마이그**: 레거시 `-사급` 거래처 → (통합거래처+조달구분=사급) 매핑. 음수재고 정리와 같은 단계.

### 핵심 결론 (V2 재료비 설계 원칙)
- **같은 원소재 품번이라도 재료비 단가는 usage(BOM edge)의 gubun1에 따라 갈린다**:
  - **사급 usage**(gubun1=사급) → **LG사급가**(price_metal std)
  - **직거래 usage**(gubun1=직거래) → **실매입가**(이동평균 마감가, fallback 규칙 적용)
- **이동평균 마감가는 "직거래 usage에만" 적용.** 품번 단위 blanket 금지 → **BOM edge gubun1별로 단가 소스 분기**.
- **사급가 vs 실매입가 차이 = 손익의 핵심**(R-6). 우리가 싸게(19,841) 사서 사급가(22,387)로 계상되면 그 차이가 마진/손익에 반영돼야.
- ★무가격(tagB/C 0단가) 입고는 이동평균을 오염 → 직거래 재료비 fallback(소재단가/직전유효/실매입) 필수(5F).

## 5H. ★V2 재료비 목표 설계 (Target Design) — 조달구분 축 (2026-08-22 확정, "설계 바꾸고 시작")
> 사용자 결정: "둘 다 단계적" + "설계를 바꾸고 시작". 아래가 바뀐 설계. 원장은 마이그 소유(무변경), 계산·마스터 축만 개선.

### 데이터 모델 (변경)
1. **거래처 = 1 identity**(사업자 통합). `-사급`/`(직거래)`/`(설치)` 중복코드 **폐지**([[newerp-partner-identity-rationalize]]).
2. **조달구분(procure_type) = 거래(매입·입고)의 속성**: **사급 / 직구매 / 설치**. 레거시 거래처코드 → 조달구분 매핑(예 LS메탈-사급→사급, LS메탈→직구매, LS메탈(설치)→설치).
3. **재고·원가 pool = (품목 × 조달구분)**. 사급 pool / 직구매 pool 분리 → 각 pool이 정확한 단가.

### 재료비 단가 소스 (usage별 = BOM edge gubun1)
- **사급 usage**(gubun1=사급) → **LG사급가 = 절삭재료비 "원재료비"**(`CS_M_METERIAL_COST`=화면 원소재마스터›절삭재료비 탭, =price_metal.std). 예 19.05×1 CU=21,993. **원재료비(협력)=20,000=partner**(R-5). LME사급차액=원재료비−원재료비(협력). **★사급 쪽은 현행 엔진이 이미 정확**(변경 대상 아님).
- **직거래 usage**(gubun1=직거래) → **직구매 pool 실매입가**(이동평균 마감가, **직구매 조달구분 매입만** 집계; 사급 거래처 매입 제외)
- **fallback(읽기 시점)**: avg=0/음수/무가격 → 소재단가(price_metal std)/직전 유효 마감가/실매입가. 원장 무변경.

### 판가·손익
- **LG 판가** = price_item TAGE/TAGS(vendor 1010/1020/1030) **as-of**(월별 직거래 인정가 변동 반영).
- **손익 = LG판가 − 실원가 + 사급차액**. **사급차액 = LG사급가 − 실매입가**(우리가 싸게 사서 사급가 계상 시 마진, R-6).
- **R-7 squeeze**: 매입가 즉시↑(실매입 pool) vs 판가 지연(TAGE as-of) → 실제값으로 손익에 드러남.

### 단계 (단계적)
- **Phase 1 (즉시·안전, 지금 시작)**: V2 계산이 **usage별 단가 분리(사급=LG사급가 / 직거래=직구매pool 실매입가) + fallback**. **원장·마스터 무변경**. → 253품목 손익 즉시 정확화.
- **Phase 2 (설계·승인 후)**: **거래처 identity 통합 + 조달구분 축 신설**(매입·입고 속성). 레거시 -사급/(직거래)/(설치) 거래처 매핑. 마이그 조율.
- **Phase 3 (마이그)**: 음수재고·단계게이팅([[newerp-stock-gating-close-lock]]).

## 5I. ★이동평균 검증 = 사급가로 평가됨(직구매 실매입 아님) (2026-08-22, 실측 결정적)
> 사용자: "사용되는 품목별 직거래 원소재 이동평균 가격 보여줘, 금액 맞는지 확인." → 검증 결과 **불일치 발견**.

- **실측(사용 원소재 소비상위, 기준 260822)**: **이동평균 마감가 ≈ 사급가(원재료비)**, 직구매 실적단가는 ~2,000~3,000 낮음.
  | 품번 | 이동평균마감가 | 직구매실적 | 사급가 |
  |---|---|---|---|
  | 7072AR9374L(22.22×1.2 CU) | 22,387 | **19,782** | 22,387 |
  | 7072AR9374N(15.88×1 CU) | 22,017 | 19,136 | ~22,017 |
  | 7072AR9374M(12.7×0.8 CU) | 22,028 | 18,902 | 22,028 |
  | MJU66885911-3M(25.4×1 고강도) | 22,889 | 20,205 | 22,889 |
- **결론**: 현재 이동평균은 **재고를 사급가로 평가**(기초 2606 스냅샷이 사급가 기준). **직거래 실매입가 미반영** → 이대로 직거래 재료비로 쓰면 **~13% 과대계상**.
- ★**조달구분 pool 분리의 결정적 근거**: 직거래 pool=실매입가(19,782)·사급 pool=사급가(22,387) 분리 필수. 현행 blend된 mat_stock_daily 그대로는 직거래에 부적합.
- 이상치: 37.5*1.9*3000·58*3.3*3000=이동평균0(무가격), 9.52*0.65*5000=15,287인데 직구매0.

## 5J. ★정정: 직구매 > 사급 (거래처별 실측) — 조달구분=거래처 -사급 여부 (2026-08-22)
> 사용자: "직구매 실적 단가가 더 높아야 하는데?" → 맞음. 5I의 "직구매 19,782"는 **사급 매입(물량 dominant) 혼입 오류**.

- **거래처별 실측(2601~, 원소재 매입)** — 같은 품번을 사급 거래처 / 직구매 거래처가 다른 단가로:
  | 품번 | 사급 매입(-사급 거래처) | 직구매(비-사급 거래처) |
  |---|---|---|
  | 7072AR9374N | HAILIANG-사급 19,221·LS메탈-사급 18,709 | **LS메탈(2151) 20,316** |
  | 7072AR9374M | HAILIANG-사급 18,659 | **LS메탈 19,601** |
  | MJU66885911-3M | LS메탈-사급 20,162 | **LS메탈 21,564** |
  | 3H00627L | 대진동관 19,938 | **LS메탈(설치) 22,563** |
- **∴ 직구매 > 사급 매입**(사업 상식대로). 사급 원소재는 LG-협력 저가로 우리에게 옴, 직구매는 시장가.
- **조달구분 판정 = 거래처 "-사급" 여부**로 실용 가능(LS메탈-사급/HAILIANG-사급=사급 / LS메탈·부광금속·대동신관·대진동관=직구매 / LS메탈(설치)=설치). ★단 정본 필드(거래처명 suffix vs cust_type vs 매입유형)는 확정 필요.
- **V2 확정**: **직거래 원소재 실매입가 = "직구매 거래처(비-사급) 매입만"의 이동평균**(사급 거래처 제외). 현행 blend된 mat_stock_daily 그대로 금지.

## 5K. ★거래처 조달구분 필드 설계 — 마이그 무영향 (2026-08-22, 사용자 지시 "거래처부터 정리")
> 사용자: "마이그 때 영향 안 되도록 거래처 필드를 수정하는 설계. 자재·협력사·BOM 등 영향 다 받을 수 있으니 기록하면서." → **레거시 거래처 코드 무변경 + 조달구분 필드 추가**.

### 분석 (원소재/부자재 공급 14거래처·사업자11·중복3)
- **★-사급 거래처는 사업자번호 없음**(가상 라우팅 코드): LS메탈-사급(2237)·HAILIANG-사급(2238).
- 직구매/설치는 사업자 보유: LS메탈(2151, 사업자1208752157)/LS메탈(설치)(2187), 부광금속(주)(2097, 1368101457)/부광금속(설치)(2179).
- **cust_type**: 4=원자재(사급·직구매 **공용**)·5=설치·6=협력사·7=부자재 → **cust_type만으론 사급/직구매 구분 불가. 이름 '-사급'이 판정자.**

### 설계 (마이그 안전)
- **레거시 CM_M_CUST 코드/필드 무변경**(마이그 매핑 보존). → 별도 nx 필드로만.
- **nx.cust_procure 신설**(cust_code PK · **procure_type**[사급/직구매/설치] · **identity_biz**[사업자통합키] · seed_val · user_val · procure=COALESCE(user_val,seed_val)). ★routing_edge 편집보존 패턴([[newerp-routing-edge-flag-retire]]) 재사용: 시드+사용자편집+멱등 재싱크.
- **시드 규칙**: 이름 '-사급'→**사급** / 이름'(설치)' or cust_type5→**설치** / 그외→**직구매**. (사업자 없음+원소재+사급명=사급 확실)
- **identity_biz**: 사업자번호로 중복거래처를 1 identity로 묶음(**코드 병합 아님**, 보고/rollup·정산 통합용).

### 영향 (자재·협력사·BOM — 기록)
- **자재(매입/재고)**: 매입 거래처 procure_type로 **이동평균 pool 분리**(사급 pool/직구매 pool, 5H·5J).
- **협력사**: cust_type6 = 사급 출고/가공 대상(별도 축, 사급차액 R-6).
- **BOM**: edge gubun1(사급/직거래) ↔ 거래처 procure_type **연결**(usage별 단가 소스, 5G).
- **원가(V2)**: 직거래=직구매 pool 실매입 이동평균 / 사급=사급가(원재료비). 조달구분 필드가 이 분기의 정본.

### ★거래처 분류 (사용자 직접 분류 2026-08-22) — 다축
> 단순 사급/직구매/설치 아님. **4축**: 조달(사급/직구매)·용도(절삭/설치)·자재종류(원소재/부자재)·역할(원자재/부자재/협력사가공).

| 코드 | 거래처 | 조달 | 용도 | 자재종류 |
|---|---|---|---|---|
| 2237 | LS메탈-사급 | **사급** | 절삭 | 원소재 |
| 2238 | HAILIANG-사급 | **사급** | 절삭 | 원소재 |
| 2151 | LS메탈 | 직구매 | 절삭 | 원소재 |
| 2097 | 부광금속(주) | 직구매 | 절삭 | 원소재 |
| 2301 | 대동신관 | 직구매 | 절삭 | 원소재 |
| 2340 | 아이엠아이 | 직구매 | 절삭 | 원소재 |
| 2355 | 대진동관 | 직구매 | 절삭 | 원소재 |
| 2187 | LS메탈(설치) | 직구매 | **설치** | 원소재 |
| 2179 | 부광금속(설치) | 직구매 | **설치** | 원소재 |
| 2266 | 케이비 | — | 절삭 | (**협력사=가공외주**, 조달대상 아님) |
| 2280 | 에스씨티 | 직구매 | 절삭 | **부자재** |
| 2016 | 삼원동관 | 직구매 | 절삭 | **부자재** |
| 2339 | 경성 | 직구매 | 절삭 | **부자재** |
| 2293 | 윤산 | 직구매 | 절삭 | **부자재** |

- **사급 원소재(지금은)= LS메탈-사급(2237)·HAILIANG-사급(2238) 2곳뿐**("지금은"=시변 가능 → 유효일자 관리).
- **★R-1 사급전환(2602)과 연결(실측)**: HAILIANG-사급(2238) 매입 **2602부터 시작**(사급전환 시점 일치), LS메탈 직구매(2151) 2601 17,875→2607 2,789 **감소**(사급이 직구매 대체). 거래처 분류 자체는 안정, 시변=**새 사급 거래처 추가**(apply_ym로 커버). 2601 원가=직구매 위주 / 2602+ 원가=사급 확대.
- LS메탈은 사급코드(2237)/직구매코드(2151) 별도. 케이비=협력사(가공외주)라 조달구분 없음.

### ★정정(2026-08-22, durable 정독 후) — 새 테이블 불필요, 기존 인프라로 파생
> 정독 발견: 조달구분은 **이미 존재하는 필드로 파생 가능** → `nx.cust_procure`(내가 만들었던 것) **폐기·DROP**. 정독 안 했으면 중복 테이블 만들 뻔.
- **조달구분 파생 = 기존 필드 조합**(전부 nx 안전, 신규 마스터 변경 0):
  - **사급** = `nx.mgmt_vendor_gubun.override_gubun`='유상사급-원재료'(이미 2235 JINTIAN·2236 심양금속·2237 LS메탈·2238 HAILIANG 등록됨, 2026-08-18) OR `CUST_TYPE`='1'(유상사급)
  - **직구매(절삭원자재)** = `CUST_TYPE`='4' & 사급 override 없음 · **직구매(설치원자재)** = `CUST_TYPE`='5'
  - **부자재** = `CUST_TYPE`='7'(절삭)/'8'(설치) · **협력사(가공외주)** = `CUST_TYPE`='6' · **소모품**='9' · **이지링크**='A'
  - **용도(절삭/설치)** = `nx.item.cut_gubun`(nx 네이티브·미러아님·싱크안전, 절삭3622/설치273/분지관114) — ★거래처 아니라 **품목 레벨**(경성 혼재 사례가 증명)
  - **무상사급** = `nx.sagub_free_vendor`(문영2014·경성정밀2350). 유상만 LME.
- **검증(2026-08-22)**: 사장님 수기분류 14건 = 위 파생과 100% 일치 확인.
- 원가 연결: **사급→사급가(price_metal std)** · **직구매 원소재→직구매 pool 실매입가** · **부자재→매입 구매단가** · **협력사→가공외주(별도)**.

### ★조달구분 = 거래 필수 구분자 (사용자 지시 2026-08-22) — 과거 vs 신규 분리
> 사용자: "매입 입고/불출 할 때 이 구분자를 무조건 선택하도록. 입고/출고 프로그램 전체 수정해야 하는 거 아니야?" → **맞음(going forward), 단 과거는 거래처 유도로 충분.**

| | 과거(지금 원가분석 2601~2608) | 신규(going forward) |
|---|---|---|
| 조달구분 소스 | **거래처 유도**(nx.cust_procure, 레거시가 -사급 등으로 이미 구분) | **입고/불출 거래에 필수 구분자** |
| write 프로그램 | **수정 불필요** | **~10개+ 전부 수정** |
| 거래처 | 분리 유지(레거시) | **통합**(구분자로 대체) |

- **영향받는 write 프로그램(~10+)**: 자재입고(matrecv/receive·gagong_receive)·자재출고(matissue/save)·사급출고(sagub/output/save)·매입마감(purmagam/save)·도입수입(dopip)·세트출고(prodsheet/issue)·kanban/label-issue·재고조정(partstockadj) 등.

### 단계 (확정)
- **Phase 1 (지금·안전)**: nx.cust_procure(거래처 조달구분 14건 로드, 다축) → 원가분석이 사급/직구매 분기. **write·마스터·원장·레거시 전부 무변경 = 마이그 무영향.** 즉시 원가 정확화.
- **Phase 2 (큰 프로젝트, 설계·승인 후)**: 거래처 통합 + **입고/불출 write 프로그램 전체에 조달구분 필수 필드**(going forward). 신규 거래는 거래처 코드가 아니라 명시 구분자로.
- **Phase 3 (마이그)**: 음수재고·게이팅.

## 5L. ★durable 80개 정독 종합 — V2 반영 핵심규칙 (2026-08-22, 4갈래 서브에이전트)
> "정확한 분석하고 시작" 지시로 durable 전수 정독. 아래는 새로 확정·주의해야 할 규칙(기존 §5A~5K 외).

### 재고·물량 (원가 물량 산정)
- **재고 물량 정본 = `nx.mat_stock_daily`**(이동평균·QTY 99.85% 검증). **`nx.stock_ledger` MAT는 자재 현재고 정본 아님**(병행 미동기화 45% 불일치, 금지). 현재고=기초(`stock_close_snap` 직전마감월 end_qty)+Σ원장(마감월+1~).
- **재료비=마스터 단가 as-of 재계산**(재고원장 금액 아님): 소재단가=price_metal, 매입=`PR_M_ITEM_COST[COST_TAG='1']×환율`, 임율=labor_rate.
- **소비량=실사용BOM(`nx.bom` real=1) use_qty×실생산량, 회수율(PROD_RATE) 제외 확정.**
- **원소재 재고 이월**로 "월별 입고≠소비" 발생(소요는 정확). 월별 대사 시 재고시차 고려.

### ★DO_NOT_USE (원가에서 피할 필드)
- `PR_M_ITEM_COST.MAT_COST/PROC_COST/OTHER_COST`(0, 분해아님) · `ITEM_COST`는 **판가(lg_cost)로만**(재료비 금지). price_type: COST_TAG '1'=매입·'S'=TAGS·'E'=TAGE.
- `PR_M_ITEM_BOM`(재료전개 금지)→**`CS_M_ITEM_BOM`** · `EXCEPT_FLAG`(필터 금지)→**`CS_CALC_EXCEPT_FLAG≠'1'`** · 용접봉 `CS_T_ITEM_WELD`그리드/`PR_M_ITEM_BOM.use_qty` 금지→`CS_M_ITEM_BOM.USE_QTY`(×1.5).
- 용접봉(RAC*)=`nx.proc_weld`(bom_line 자식 금지) · sgroup='910' 자재소요 방출 금지.

### 마이그 무영향 원칙 (V2 지킬 것)
- 새 필드는 **nx 네이티브 재구축 테이블에 ADD만**(미러 nx.PR_M_*/nx.CM_M_CUST엔 금지 = r_bulk_copy DROP+재복사 소실). sync_*.py 멱등 반영. 라이브 PARTNER_ERP 쓰기 금지.
- 마스터 repoint는 바이트동일 검증 후 완료(출력불변). in_cust 소실 회귀 감시(과거 재료비 0 사고).

### 재료비 성분·정합
- **원/부/사급 재료비 분해 = ITEM_SGROUP**(원자재 110/120/130/220·부자재 230/910·**사급 310**) — 현재 nx 미구현(esti 0). V2 구현대상.
- **원소재 두께 정산기준 = 협력사 0.65**(파일), 개발BOM/견적 LG 0.70 → 0.70 계산분 재계산 필요.
- 중량=계산값(net_weight 껍데기 1.0 금지), **비중 CU 8.94 vs 8.96 문서 불일치=확인요**.
- 협력사 동관/사급 판정 = **판매단가(COST_TAG='S') 유무**(metal=CU 자동판정 금지, ~270 오분류 이력).
- make_type 정본: **1자체·2외주가공·3매입·4사급가공·5외주완성**(BOM_EXPLOSION_RULES).
- 검증게이트=260630 월말 diff0(82.5% PASS, FAIL 0.011%).

## 5M. ★사급/제작 판정 = 불출 이력 (COOP_QUOTE_V2, durable 정독 2026-08-22) — make_type '2' 답
> "답이 있다니깐"(사용자) → COOP_QUOTE_V2_RULES.md가 정본. **마스터(cg/mk/inc) 못 믿음, 불출 이력(tag5)으로 "지금 실제 조달방식" 판정**(2026-08-09 진화).

### 재료비 결정트리 (CQV2:384-393, 정본)
```
cg=3(소재단가) & metal∈{CU,고강도}:
   당월 불출(PU_T_STOCK_MAINT tag='5', 마감기준 window) 있음 → 【사급부품】 = 판매단가(COST_TAG='S') × 소요
   불출 없음                                              → 【제작동관/수불】 = 중량 × 사급가(20,000/22,000)
cg=2 & sg=910 → 【용접봉】 = 사급가 21,100 × 소요(견적서 c14)
cg=1 / cg=2 나머지 → 【사급부품】 = 판매단가(S), 없으면 매입이력
cg=5 직납 → 제외
```
### ★핵심 정정 (앞선 이해 대비)
- **make_type '2' 애매함 무의미**: 사급/제작 판정 = **불출 이력(tag5)** 우선(make_type 아님). '2'=외주지만 실동작은 불출로.
  - `-6-1`류 판매이력 끊김=업체 자체생산 전환→불출 없음→제작동관. **불출 이력이 조달전환 자동반영.**
- **★사급받는 부품 단가 = 판매단가(COST_TAG='S'), 매입가(pur_price) 아님**(CQR 08-07 매입가→CQV2 08-09 판매단가 정정). 인하도 정상.
- **가공비 = 견적서 c39**(입고가−재료비 아님) — nx.routing 엔진 가공비와 **별개 소스**(엔진은 스테일 이슈).
- **용접봉 소요 정본 = CS_M_ITEM_BOM.USE_QTY = Σ관경 ITEM_USE_QTY × 1.5**(WELD), 단 CQV2는 견적서 c14 → 소스 이원화(재고정산 vs 견적손익).
- 거래처(업체) 필터 필수(판매단가는 처리중 업체로, 미필터=가짜 역마진).
- **사급출고 = 매출**(tag5, PU_T_STOCK_MAINT, 사급단가 S, 부호−, 마감기준). 유상사급은 매출마감 직결.
- 견적원가관리(ESTI): 엔진=NxCostEngine, nx.esti_bom에 **사급여부·신규여부 필드** 보유, 확정 시 nx.bom 승격.

### ★make_type 코드 확정 (레거시 원가 SP 직독 2026-08-22) — 원가에선 '1'만 의미
> 레거시 원가 정본 = **w_cs_esti_020(견적원가손익금액) → `SP_CS_견적서(실원가용)_250910`**. MAKE_TYPE 쓰임 = **딱 1곳**(L140-141·184-185):
> `INNER_PROD_FLAG = IIF(MAKE_TYPE='', IIF(IN_CUST='' OR 공정있음,'1','0'), IIF(MAKE_TYPE='1','1','0'))`
- **∴ 원가에서 make_type 2·3·4·5는 전부 동일(INNER_PROD='0'=매입가 leaf·전개정지). 4/5뿐 아니라 2/3도 원가상 무의미 flag.** MAKE_TYPE='1'만 사내생산(전개).
- **원가 핵심 축 = INNER_PROD_FLAG('1'사내/'0'매입)**, make_type 아님. 이후 SP 전체가 INNER_PROD로 분기(가공비 L359-362=INNER='1'만·매입 L241=INNER='0').
- 은납/용접(SILVER_SOLDER) override: INNER_PROD='1' 강제(L352).
- → V2도 make_type 코드값(2/4/5) 구별 불필요. **INNER_PROD 판정(make_type='1' or 빈값+공정/in_cust빈값)** 만 재현. 사급/제작 실동작은 §5M 불출이력으로.

### 정독 잔여 불명(전량 정독 후 확정 필요)
- make_type 4/5 nx 매핑 · 용접봉 소요 이원화(BOM×1.5 vs 견적 c14) · 사급 관리플래그(SAGUB_STOCK_FLAG vs item_class='J') · lg_settle_unit 다월 · nx.routing 가공비 잔여 하드갭.

## 5N. ★레거시 원가 SP 재료비 단가 직독 — R-2/R-4 확정 (2026-08-22)
> `SP_CS_견적서(실원가용)_250910.sql` 직독. **재료비 단가에 직거래/사급 분기 없음.**

- **① 원소재(cg='3')** (L214·323): `WON_MAT_COST = CS_M_METERIAL_COST.tot_cost`(=LG 사급가/원재료비), `JAI = WON_MAT_COST × 중량 × USE_QTY`. → **모든 원소재를 LG 사급가로. 직거래/사급 구분 없음.**
- **② 구매품(cg≠3, INNER_PROD='0')** (L296-306): `MAT_COST = PR_M_ITEM_COST.ITEM_COST × 환율 WHERE cust_code=IN_CUST_CODE AND cost_tag='1' AND cost_apply_ymd≤기준일 ORDER BY DESC`. → 매입가 as-of, 거래처=품목 IN_CUST_CODE.
- **③ 입고이력 기반 거래처 선택 = 전부 주석처리(비활성)** (L256-292). 그냥 품목 IN_CUST_CODE 사용.
- **LME**(L329): `JAI_SUB = WON_MAT_COST_SUB × 중량 × qty`, `WON_MAT_COST_SUB = tot_cost − tot_cost_sub`(LG−협력사).

### ★결론 (R-2/R-4 확정)
- **레거시 원가 = 원소재를 무조건 사급가로 계상(직거래도 사급가).** → 이동평균이 사급가와 일치한 이유 설명됨(레거시 자체가 사급가).
- **V2 "직거래 원소재 = 실매입가"는 레거시에 없는 우리식 개선** = 실제 손익 정확화의 핵심. 레거시 한계(직거래도 사급가→손익왜곡)를 V2가 바로잡음.
- **R-2 소스 확정**: 레거시=CS_M_METERIAL_COST(사급가) / 구매품=PR_M_ITEM_COST[cost_tag='1']. Cost Table(lg_lme_costtable)은 별개(직거래 인정가 화면용). 엔진 price_metal=CS_M_METERIAL_COST 미러 → 레거시와 동일(사급가).
- **R-4 사급부품**: 실원가 SP=매입가(cost_tag='1'). 단 CQV2 협력사견적은 판매단가(cost_tag='S') — 맥락 다름(실원가 vs 견적손익). V2 원가분석은 실원가 SP 계보.

## 5O. lg_settle_unit 의존 제거 (사용자 확정 2026-08-22)
> "lg settle unit은 지금 큰 의미 없다. LG 판가를 읽어오면 돼."
- **판가 = LG 판가 직독**(`price_item` vendor 1010/1020/1030, TAGE/TAGS, apply_ymd as-of). 원단위로 판가 계산 안 함 → **lg_settle_unit 다월 적재 불필요(무의미)**.
- **원소재 사급/직거래 판정 = 거래처 조달구분**(사급 거래처 매입=2237/2238 vs 직구매 거래처=CUST_TYPE 4/5) **+ 불출이력(§5M tag5)**. **lg_settle_unit.gubun1 의존 제거.**
- **중량(소요) = BOM(CS_M_ITEM_BOM.USE_QTY)**, lg_settle_unit 원단위 아님.
- ∴ 앞선 "설치 3개 사급 컴포넌트"·"lg_settle_unit 다월" 불명은 **폐기**(lg_settle_unit 안 씀).

## 5P. ★영업 업로드 = 가격 mapping 정본 (백엔드 직독 2026-08-22)
> "영업에서 업로드하는 사급가·판가를 확인하면 mapping이 될 것 같은데?" → 맞음. 확인됨:
| 업로드 | 엔드포인트(price.py) | 저장(mapping) |
|---|---|---|
| **LG 판가**(PO Price) | `/api/price/lgprice_upload` L311 | **price_item** vendor **1010(SAC)/1020(RAC)** · type **TAGE(수출)/TAGS(내수)** · Start Date=apply_ymd. 헤더=Material·MKT(1→TAGS/2→TAGE)·Unit Price·Start Date·Curr |
| **사급부품가**(COSP) | `/api/price/sagub_upload` L222 | **price_item** vendor **'LG'** · **COSP** · Start Date |
| 원소재 사급가 | 절삭재료비 탭 DND | CS_M_METERIAL_COST / price_metal.std |

### ★가격 소스 맵 (완성, 전부 영업 업로드/매입 실적)
- **LG 판가**(손익 상단) = `price_item` 1010/1020 TAGE/TAGS **as-of**(엔진 lg_cost가 이미 읽음). "LG 판가 읽어오면 됨"=이것.
- **사급부품가**(R-4) = `price_item` 'LG' COSP ← sgroup310 COSP와 일치(R-4 확정).
- **원소재 사급가** = CS_M_METERIAL_COST(=price_metal.std).
- **직거래 원소재 실매입가**(V2 신규) = 직구매 거래처(비-사급) 매입(PU_T_STOCK_MAINT).
- **구매품 매입가** = PR_M_ITEM_COST cost_tag='1'(품목 IN_CUST_CODE).
- 참고: price.py에 **"실입고가 > 사급출고가(판가역전)" 특이단가 리포트** 이미 존재(L422) = R-6/R-7 실측 도구.

## 5Q-정정. ★직거래 원소재 손익왜곡 = V2 가치 (2026-08-22, 정확본)
> ★앞선 5Q의 "+1.15억, 전부 사급가"는 **오류**(원소재가 make_type별로 사급가/매입단가로 갈리는 걸 놓침). 정정:
- **직거래 원소재 40품목 중**: 사급가 계상 **2품목**(make=1&cg3) / 매입단가 마스터 계상 **38품목**(make 2/3). "전부 사급가" 아님.
- **현재계상단가(make=1&cg3→price_metal 사급가, else→price_item 매입단가) vs 실매입 gap**:
  | 재질 | 품목 | gap(실매입−현재계상) |
  |---|---|---|
  | **고강도** | 12품목 89t | **+0.61억**(현재 과소→손익 과대) |
  | CU | 28품목 59t | +0.10억 |
  | **총** | 40품목 | **+0.71억 손익 과대** |
- **핵심 = 고강도 직거래 원소재**: 현재 계상단가(사급가 or 매입단가 마스터)가 실매입보다 낮음(예 MJU66885910 마스터 19,216 vs 실매입 22,248). 3H00627Q/M(2품목)은 사급가 계상.
- **V2 = 직거래 원소재를 실매입가로 계상 → 손익 0.71억 하향(정확화).** 매입단가 마스터도 실매입과 item별 불일치라 실매입 기준이 정답.
- 8개월·직구매 매입분 기준. 실제 제품손익 반영은 소비량 기준 재계산 필요(엔진).

## 5Q. (오류·정정됨 → 5Q-정정 참조) 직거래 원소재 손익왜곡 실측
> 사용자 확인: "직거래 원소재 쓰는 부분이 전부 문제 / 전부 사급가격으로 정리됨. 실물가격이 사급가보다 높은데 보정 안 함."
- **현재 시스템 = 원소재 전부 사급가(price_metal.std)로 계상**(직거래/사급 무관, 레거시 SP §5N). **실매입가와의 차이 보정 없음**(LME 차액은 사급가−협력사가로 별개 개념·직거래엔 무의미).
- **실측 왜곡(8개월, 직구매 매입분 기준)**:
  | 재질 | 품목 | 방향 | 손익 |
  |---|---|---|---|
  | **고강도** | 12품목 89t | 실매입>사급가→재료비 과소 | **+1.84억 과대** |
  | CU | 25품목 56t | 실매입<사급가→재료비 과대 | −0.68억 과소 |
  | 합계 | 37품목(미매칭3제외) | | **순 +1.15억 과대** |
- 예 3H00627L(설치 고강도): 사급가 18,686 계상 vs 실매입 22,563 → kg당 3,877 과소. LME=0(보정없음).
- **∴ V2 = 직거래 원소재를 실매입가로 계상 → 손익 정확화. 이게 이번 이니셔티브의 정량적 가치.**
- ★주의: 규격(diam×thick) price_metal 정확매칭 실패 시 사급가 오조회(3품목 제외). 엔진의 _classify_mat/_snap_od 통해야 정밀. 위 수치는 근사(방향·규모 확정, 소수점 정밀은 엔진 필요).

## 6. 결정 로그 (담당 판단 — 하나씩 확정)

| 일자 | 항목 | 결정 | 사유 |
|---|---|---|---|
| 2026-08-22 | (문서 생성·카탈로그 정리) | — | 계산로직 검증 착수 |
| 2026-08-22 | A2 임율 GETDATE | **유지(우리식 as-of ym)** | 엔진 이미 교정됨(L182), 등록부 stale |
| 2026-08-22 | A3 용접봉/용접공수 | **교정(공정 분리)** | 사용자: "용접공수도 우리는 공정으로 뺐어" → **계산방식 자체가 달라져야** (재료흡수 X, 공정 가공비 계상) |
| 2026-08-22 | A1 lgroup 가공비 | 판단대기(검증 먼저) | 타그룹 제작부품 가공비 포함 여부 = 실측+사업지식 |
| 2026-08-22 | Q2 협력사 seeding 정의 | ★사용자 정정 | R02 sourcing_profile 아님. **except_flag 제외하면서 "우리가 실제 제작하는 업체명"을 입력한 것**(routing_edge/sourcing vendor). 정확한 이벤트 기록은 사용자 확인 대기 |
| 2026-08-22 | Q4 lg_settle_unit(동정산 원단위) | ★사용자: **매월 갱신** | 단 실측상 nx.lg_settle_unit엔 ym=2606만 적재됨 → 다월(2601~2608) 적재 갭. 사용자 확인 예정 |
| 2026-08-22 | Q1 make_type '2' | 사용자 판단중 | 종류별 샘플 제공(make_type2_by_group.txt): 외주/협력 in_cust(MTS·두진·중앙정밀·태영·대원·미래·이젠터·둔안·토탈솔루션 등) 전 prod_group 분포. cg 2/3/1/빈 혼재 |
| 2026-08-22 | Q3 설치 3개 사급 | 사용자 확인중 | — |
| 2026-08-22 | R-2 직거래 원소재 재료비 소스 | **이동평균 마감가(mat_stock_daily) 방향**(대사 추천, 사용자 검토중) | 소비원가 회계정합·매입추종 92%·일단위·이미구축. vs 확정입고(매입shock 보조) |
| 2026-08-22 | §5R 협력사 routing 분리 원가반영 | **(B) 매입가 거래처 오버라이드(소스=routing_edge.wc)** but **실효≈0** | 계획서 생산 기록=soyo STEP7 routing_edge.wc override. 검증축=wc(생산처)≠vendor_resolved. 실측: 협력사노드 5,490 중 in_cust≠wc 단1건 → 이미정합. 래퍼는 wc_user편집·in_cust공백만 재지정(안전장치·미래추종), 나머지 in_cust 유지 |
| 2026-08-22 | §5S R01/R02 조달경로 원가반영 | **route-aware 설계(활성=route_alloc.is_active)** — R01 현행/엔진, R02 활성시 sourcing_route_line 구분+vendor로 재판정. **오늘 실효=0(R02 미활성 전량)** | 사용자: "routing 1,2도 설계 반영". R02 정의 1건(AJR75563402)·미활성. 래퍼가 route_alloc 조회해 R02 노드만 재판정 |
| 2026-08-22 | ★음수재고/무가격 정리 시점 | **지금 원장 정리 금지 → 마이그가 소유** | 음수=레거시실적 재현(우리버그 아님). 지금 원장 손대면 마이그 재고 true-up과 이중조정·드리프트. 신규ERP 단계게이팅은 컷오버때 적용 [[newerp-stock-gating-close-lock]]. **V2는 읽기시점 fallback(avg=0/음수→소재단가/직전유효/매입가)으로 원장무변경·마이그무영향·재료비정확** |

## §5R 협력사 BOM분리→routing 이관의 원가 반영 (2026-08-22 실측)

사용자 지시: "우리는 협력사를 BOM에서 분리를 해서 routing으로 넣은 부분도 잘 반영해야해."

### 실측 사실
- **협력사 조달분리물 = `nx.routing_edge`** (42,625행). 컬럼: parent_item·child_item·seq·`gubun`·`vendor_seed`·`vendor_resolved`·`src_except`·`src_sagub`·`wc`. except_flag 은퇴 → 여기로 이관 [[newerp-routing-edge-flag-retire]].
- **`vendor_resolved` 채움 = 7,170엣지 = src_except=1 전량**. 즉 "실제 누가 조달/제작하나"의 클린 정답이 전개제외 엣지에 담김(명진-type 상위 SUB 귀속).
- **★원가엔진은 routing_edge를 안 읽음.** 재료비=`nx.bom_line`(외주완성 전개정지), 가공비=`nx.routing`(공정, 사내노드만). routing_edge는 **조달/계획 레이어**.

### 협력사가 원가에 반영되는 실제 경로 (현행)
- 협력사 제작 SUB = **INNER_PROD=0(외주완성)** → `_leaf_val`에서 **매입가(pur_price, cost_tag='1', cust=in_cust)로 계상**([nx_cost_engine.py:356-361]). 협력사 가공비는 그 매입가 안에 포함 → **구조상 이미 반영**(PROCUREMENT_BOM_WORKPLAN §3-C4 "무영향"과 일치).
- 즉 routing_edge를 안 읽어도 원가 총액은 협력사 몫을 매입가로 흡수 중.

### ★정확도 리스크 = 매입가의 **거래처 불일치**
- 매입가 거래처 = **품목마스터 `in_cust`**(품목당 1개만 등록 → EXCEPT_FLAG_VENDOR_RULE의 "미래정밀로 뜸" 문제).
- routing_edge `vendor_resolved` = 실제 협력사(명진 등). **둘이 어긋나면 매입가 단가가 틀어짐.**
- 단 전개제외 부품은 개별 전개 안 되고 상위 명진 SUB가 통째 매입되므로, SUB의 in_cust만 맞으면 총액은 정합. **리스크는 "SUB의 in_cust가 실제 협력사와 다르게 등록된 경우"에 국한.**

### V2 반영 방향 (사용자 결정: (B) 매입가 거래처 오버라이드, 2026-08-22)
- 사용자 근거: "생산계획·협력사계획에 어느 정도 검증이 됐다고 봐서" → routing_edge 판정을 원가가 차용해도 안전.

### ★계획서를 routing_edge로 생산한 기록 (soyo.py 실측)
- **[soyo.py:478-489] `_step7_sql`**: 생산계획 자재소요(compose_mat→nx.plan_part_mat) 생성 시 각 부품의 **생산처(work_center)를 품목마스터 대신 `routing_edge.wc`에서 오버라이드**. `ov_wc=ISNULL(routing_edge.wc, 마스터default)`.
- **[soyo.py:525-553] `_routing_edge_sync`**(편집보존): `wc_live`=라이브 PR_M_ITEM 시드, `wc_user`=사용자편집(NULL=미편집), **유효 wc=COALESCE(wc_user,wc_live)**. gubun=EXCEPT→전개제외/SAGUB→사급.
- ➡️ 검증된 축은 `vendor_resolved`가 아니라 **`wc`(생산처)**. 협력사 부품이면 wc=협력사코드. **(B)의 정확한 소스 = routing_edge.wc**(계획이 실제 검증·사용한 필드).

### ★실측 규모 (2026-08-22) — 이미 정합, 오버라이드 실효 ≈ 0
- routing_edge.wc가 협력사(거래처)코드인 노드 = **5,490**.
- 그중 **품목마스터 in_cust ≠ routing.wc = 단 1건**(`MJU66998702-1`: in_cust 공백→wc=2280).
- ∴ **협력사 routing 분리는 원가에 이미 정확 반영**(wc_live가 in_cust와 동일 마스터에서 시드 → 5,489/5,490 일치). 원가엔진 in_cust ≡ 계획 wc.
- **(B)의 현재 실효 = 1건**(in_cust 공백 노드 매입가 vendor 확보). **전방위 가치 = 앞으로 wc_user 편집(협력사 재배정)이 생기면 원가가 자동 추종**하는 얇은 안전장치.
- **결정**: V2 래퍼에 `wc_user≠''`(사용자편집) OR `in_cust 공백` 노드만 매입가 vendor를 routing_edge.wc로 재지정. 나머지는 in_cust 그대로(=현행 정합 유지). 엔진 원본 무변경.

## §5S R01/R02 조달경로(routing 1,2)의 원가 반영 (2026-08-22 실측·설계)

사용자 지시: "routing 1, 2에 대한 부분도 설계에 반영을 해야 할 것 같아." (R01=현행경로, R02=대안경로)

### R01/R02 데이터 모델 (실측)
- **`nx.sourcing_route`**(헤더): route_id·item_code·route_no(1=R01/2=R02)·route_name·vendor_code·gubun·`current_flag`·`approve_flag`·apply_from·reject_flag. 예: AJR75563402 route_no=2(R02) approve=1·current=0.
- **`nx.sourcing_route_line`**(경로별 부품): route_id·child_item·`gubun`(**제작/매입/외주(유상사급)**)·`vendor_code`·is_rawmat·node_kind(PART/SUB)·parent_line. (R02 1건=11line: 매입7·외주1·제작3)
- **`nx.route_alloc`**(활성판정): item_code·route_id·`is_active`·alloc_ratio. **route_id=0=R01 base(현행), else=특정 R02 route_id.** 예: AJR75563402 → R01(0) is_active=True / R02(1536) is_active=False.
- **`nx.route_seq`**: 품목별 마지막 route_no.

### 현재 상태 (실측)
- **전 품목 R01(base) 활성.** R02 정의는 `AJR75563402` 1건뿐이며 **미활성**(is_active=False). ∴ **오늘 원가=R01=정합**, R02 전환 품목 0건.
- routing_edge.route_id는 전부 1(자체 numbering, route_alloc과 별개). **원가엔진은 route 시스템(route_alloc/sourcing_route)을 안 읽음.**

### 원가 반영 설계 (route-aware)
**원가의 조달 축(제작/매입/사급 + vendor)은 "활성경로"에서 온다:**
1. **활성경로 조회** = `route_alloc.is_active=True`의 route_id (품목별).
2. **R01(route_id=0) 활성** → 원가 = 현행 bom_line + routing_edge.wc 축(=엔진 그대로). ✓ 오늘 전량 이 경로.
3. **R02(특정 route_id) 활성** → 그 route의 `sourcing_route_line`으로 **노드 조달축 재판정**:
   - `gubun=제작` → 사내 전개(INNER_PROD=1): 재료+가공 전개.
   - `gubun=매입` → 외주완성(INNER_PROD=0): `vendor_code` 매입가.
   - `gubun=외주(유상사급)` → 유상사급: 원소재 사급가 + LME 차액.
4. **V2 래퍼**: route_alloc 조회 → 활성=R02인 노드만 위 규칙으로 재판정, 나머지는 엔진 그대로. **오늘 실효=0건**(R02 미활성)이나 R02 전환 시 원가 자동 정합.

### ★단일 시스템 통합 (2026-08-22, 사용자 승인 "한개 시스템")
**실측: 조달경로 운영 시스템은 이미 1개.** routing_edge는 조달경로가 아님.
- **조달경로 정본 = 조달 프로파일**(sourcing_route + route_alloc + sourcing_profile). 하류 전량 소비: plan_mat_source[soyo:103,115]·수동발주[manorder:52]·협력사배분[common:422].
- **routing_edge 실소비 = `wc`(생산처) 1개뿐**[soyo:488]. `vendor_resolved` 소비처 **0건**, `gubun`·`vendor_seed`·`src_except`·`src_sagub` = INSERT만·미소비 = **except_flag 은퇴 마이그 잔재**. 이 죽은 컬럼이 조달프로파일과 중복돼 보여 "2개 시스템" 오인 유발.

**통합 2단계:**
- **STEP-U1 (죽은 조달컬럼 제거)**: routing_edge에서 gubun·vendor_seed·vendor_resolved·src_except·src_sagub 제거 → 조달 정본 1개 명확화. **생산계획/협력사계획 검증 불필요**(wc 소스 무변경, 안 읽히는 컬럼만 제거).
- **STEP-U2 (wc를 조달프로파일로 흡수 → routing_edge 은퇴)**: 생산처(wc)를 route/profile 모델 라인속성으로 이관, 생산계획 STEP7이 조달프로파일에서 생산처 읽게. **★생산계획·협력사계획 diff0 회귀검증 필수**(생산처 소스 교체 지점).
  - **핵심 리스크**: routing_edge=42,625엣지 전량+wc_user 편집보존. 조달프로파일 R01 baseline은 라인 미저장(실사용BOM 파생, sourcing_route_line=11행/R02 1건뿐). → wc 담을 커버리지·편집보존을 먼저 맞춰야 흡수 후 생산처가 마스터폴백으로 안 떨어짐.
  - **검증 게이트**: 흡수 前 compose→plan_part_mat 스냅샷(생산처 포함) vs 흡수 後 = **행별 생산처 diff0**. 협력사계획(410/명세서420)도 前後 동일. 하네스 재사용 [[newerp-plan-soyo-verify]](자재소요 100% 재현)·[[newerp-coopplan-grouping-livesync]](93협력사 불일치0).

### V2 원가와의 관계
- 원가는 통합 후 **단일 정본(route_alloc 활성경로)** 만 참조. STEP-U1/U2는 조달 축 정리 → 원가는 §5S route-aware 설계 그대로 얹힘.

## 관련
[[newerp-legacy-cost-algorithm]] [[newerp-legacy-bug-candidates]] [[newerp-bom-mirror-legacy-debt]] [[newerp-realcost-bom-expansion]] [[newerp-weld-cost-split]] [[newerp-routing-edge-flag-retire]] [[newerp-sourceprofile-route1-select]] [[newerp-except-flag-vendor-rule]]
