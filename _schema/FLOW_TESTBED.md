# 흐름 TestBed — 화면 프로그램 실구동 검증 (정본)

> 최종 검증 2026-08-28 · 브랜치 `feat/close-mgmt`
> 코드 = `_migration/flow_server.py`(롤백 백엔드) · `flow_scenarios.py`(실행기) · **`flow_cases.py`(케이스 — 여기만 고친다)**
> 관련: `STOCK_GATING_CLOSE_LOCK_RULES.md`(게이팅 캐논) · `CLOSE_MGMT_CANON.md`(마감) · `NX_STOCK_LEDGER_DESIGN.md`(원장)

---

## 1. 무엇을 하는가

**우리가 개발한 프로그램(화면)이 부르는 실제 API 를 그대로 호출해서**
① 값이 원장·수불장·재고 **3곳에 같은 값으로** 적히는지,
② 우리가 세운 **시스템 규칙(음수·마감·권한·유효성)이 실제로 막는지**
를 한 번에 검증한다. 단순 흐름만 보지 않는다 — 규칙이 같이 돌아야 시스템이다.

정적 분석(grep)으로 결론내지 않는다. **구동 결과가 근거다.**
(2026-08-28 실측: grep 으로 "미러 미반영"이라 보고했던 결함 F1·F2 는 실제 구동에서 전부 정상이었다.)

## 2. 오염 0 원리 — 왜 실DB 에 대고 돌려도 안전한가

`flow_server.py` 는 **일반 백엔드와 같은 app** 을 띄우되, 기동 직전에
`common._nx` / `_nx_tx` 를 **no-commit 공유 커넥션**으로 몽키패치한다(실측 102곳).

| | 동작 |
|---|---|
| 화면 → HTTP → 라우터 → DB쓰기 | **전 경로가 실제로 실행된다**(가드·채번·미러반영 포함) |
| `commit()` / `close()` | **no-op** — 어떤 호출도 확정되지 않는다 |
| 종료 시 | `/api/_flow/rollback` → 기동시점 행수와 대조해 **불변 증명** |

즉 "진짜로 돌리되 아무것도 남지 않는다". 매 실행마다 오염 0 을 **증명**하고 끝난다.

---

## 3. 쓰는 법 (다른 세션·다른 개발자)

```bash
# 창 1 — 롤백 모드 백엔드 (기본 8099. 다른 사람과 겹치면 --port 로 바꾼다)
python _migration/flow_server.py --port 8099

# 창 2 — 검증
python _migration/flow_scenarios.py              # 전체
python _migration/flow_scenarios.py --list       # 케이스 목록만(실행 안 함)
python _migration/flow_scenarios.py --kind R     # 규칙만
python _migration/flow_scenarios.py --only 키팅   # 이름에 '키팅' 들어간 것만
python _migration/flow_scenarios.py --port 8098
```
종료코드 = FAIL·미구현이 있으면 1, 전부 통과면 0 (CI 에 걸 수 있다).

**동시에 여러 세션이 돌릴 때는 포트를 나눈다** — 8099/8098/8097 …
서버 하나에 공유 트랜잭션 하나이므로 **한 서버를 둘이 같이 쓰면 서로의 미커밋을 본다.**

### 3-0. 브랜치마다 결과가 다를 수 있다 (정상)
케이스는 **전 도메인**을 담고 있는데, 브랜치마다 있는 기능이 다르다.
없는 엔드포인트는 404 → **SKIP** 으로 보고한다(‘차단’으로 세지 않는다 — 막지도 않았는데 PASS 가 되면 안 된다).

**실측 기준선(2026-08-28, 같은 케이스 28건을 두 브랜치에서 실행)**

| 브랜치 | PASS | FAIL | 미구현 | SKIP |
|---|---|---|---|---|
| `main` | 21 | 1 | **3** | 3 |
| `feat/close-mgmt` (PR #97) | **28** | 0 | 0 | 0 |

main 에서 뜨는 미구현·FAIL 은 하네스 오류가 아니라 **main 에 아직 그 규칙이 없다는 사실**이다.
PR #97 이 정확히 이것들을 채운다:

| main 판정 | 내용 |
|---|---|
| ☐미구현 | 자재 쓰기 **마감기간 잠금 없음** — 마감월 전표가 그냥 저장된다 |
| ☐미구현 | 키팅 **마감기간 잠금 없음** |
| ☐미구현 | 공정별생산실적 **자재부족 차단 없음** — 자재 없이 실적이 잡힌다 |
| ★FAIL | 백플러시 차단 사유에 **축별 보유가 없다**(`가용 -240 < 소요 …` 만) — 규칙 A-0-1 미충족 |

★즉 이 TestBed 는 **"우리 규칙이 이 브랜치에 실제로 들어와 있는가"를 재는 자**이기도 하다.
  main 에 병합될 때마다 돌려서 숫자가 내려가지 않는지 본다.

### 3-1. 내 프로그램을 검증에 추가하는 법 — `flow_cases.py` 에 dict 하나

```python
# 흐름: 값이 제대로 적히는가
dict(kind="F", name="자재입고 (자재입고관리)", method="POST", path="/api/stock/save",
     body=lambda ctx: {"screen":"receipt","user":"flowverify",
                       "rows":[{"MAINT_YMD":YMD,"MAT_CODE":ctx["mat"],"qty":100}]},
     probe="원장MAT", delta=+100, mirror=True),

# 규칙: 제대로 막는가 (거부 + DB 무기록 이어야 PASS)
dict(kind="R", name="자재출고 — 가용 초과 차단", method="POST", path="/api/stock/save",
     keyword="재고부족",
     body=lambda ctx: {...}),
```

| 키 | 뜻 |
|---|---|
| `kind` | `"F"` 흐름 / `"R"` 규칙 |
| `path` | **화면이 실제로 부르는 그 경로** (js 에서 `grep "/api/"` 로 찾는다) |
| `body` | dict 또는 `lambda ctx: dict` — `ctx` 는 픽스처 결과 |
| `probe` | 관측 이름(`flow_server.PROBES` 키). 없으면 `PROBES` 에 추가 |
| `delta` | 기대 증감 |
| `mirror` | `True` 면 수불장·재고까지 **3곳 일치** 요구(자재 계열) |
| `keyword` | (규칙) 거부 사유에 들어갈 말. 달라도 거부되면 PASS — 참고용 |
| `skip_if` | `lambda ctx: bool` — 픽스처가 없으면 SKIP |

**참조할 실데이터가 필요하면** `FIXTURES` 에 `(키, SQL, 반영함수)` 를 추가한다.
★픽스처는 **쓰기 시작 전에** 전부 읽고 커서를 닫는다(§6-2 이유).

**새 관측 지점이 필요하면** `flow_server.py` 의 `PROBES`(일자 스코프) 또는
`PROBES_MAT`(품번 스코프)에 한 줄 추가한다. 전 테이블 집계는 금지 — 느려서 못 돈다(§6-5).

### 3-2. 규칙을 만들면 케이스도 같이 만든다 (하드룰)
게이팅 캐논 §0-★ = **예외 없음 + 사유 고지**. 새 규칙을 코드에 넣었으면
`flow_cases.py` 에 `[R]` 케이스를 **같은 커밋에** 추가한다. 규칙은 검증과 함께 산다.

### 3-3. 판정 읽는 법
| 표시 | 뜻 |
|---|---|
| `✅ PASS` | 흐름=값 일치 / 규칙=거부 + DB 무기록 |
| `★FAIL` | 흐름=값 불일치 / 규칙=거부했는데 **DB 에 기록됨** |
| `☐미구현` | 규칙이 **막지 못했다** — 막아야 할 것이 통과 |
| `– SKIP` | 픽스처 부족(참조할 실데이터 없음) |

---

## 4. 결과 (2026-08-28) — **PASS 전건 · FAIL 0 · 오염 0**

### [F] 흐름 — 자재입고 → 조정 → 출고 → 반품 → 수정 → 삭제 → 키팅 → 생산실적 → 판매출고

| 단계 | 화면 | API | 결과 |
|---|---|---|---|
| 자재입고 | 자재입고관리 | `POST /api/stock/save` (receipt) | ✅ 3곳 일치 +100 |
| 자재조정 | 자재재고조정 | 〃 (adjust) | ✅ 3곳 일치 +50 |
| 자재출고 | 자재출고관리 | 〃 (issue) | ✅ 3곳 일치 −30 |
| 자재반품 | 〃 | 〃 (return) | ✅ 3곳 일치 −15 |
| 자재수정 | 〃 | `POST /api/stock/update` | ✅ 3곳 일치 +50 (100→150) |
| 자재삭제 | 〃 | `POST /api/stock/delete` | ✅ 3곳 일치 −150 |
| 키팅확인 | 준비실적처리 | `POST /api/kitting/cell-confirm` | ✅ 원장 RDY +10 (flag-only — 설계대로) |
| 생산실적 | 공정별생산실적 | `POST /api/procreg/save` | ✅ `nx.proc_result` +7 |
| 판매출고 | 판매및출고등록 | `POST /api/saleout/save` | ✅ `nx.saleout_maint` −5 |

**3곳 = `nx.stock_ledger`(원장) · `nx.PU_T_STOCK_MAINT`(수불장) · `nx.PU_T_MAT_STOCK_WH`(자재재고).**
자재 6종 쓰기 전부에서 세 값이 같이 움직인다 ⟹ 화면마다 값이 갈리는 문제 없음.

### [R] 규칙 — 실제로 막는지

| 규칙 | 결과 |
|---|---|
| 음수재고 차단 — 출고/반품/조정 3종 | ✅ `재고부족 (… 가용 2050 < 출고 …)` · `음수재고 유발 (결과재고 < 0)` |
| 마감기간 잠금 — 자재/키팅 | ✅ `2607 마감된 월입니다 — 생성/수정/삭제 불가` |
| 마감 권한 게이트 — 무권한/조회전용 | ✅ `마감 권한이 없습니다(flowverify)` |
| 마감 중복 실행 차단 | ✅ `자재 2607 는 이미 마감되었습니다.` |
| **생산실적 게이트 — 백플러시/공정별실적** | ✅ `자재부족으로 생산실적 불가 — …` |
| **차단 사유 고지**(A-0-1) | ✅ 품목·소요·축별 보유·생산창고 위치까지 포함 |
| 입력 유효성 4종(미등록·수량0·일자누락·screen) | ✅ 전부 차단 |
| 단가0·음수 제외 리포트 · 자재/생산 수불장 | ✅ 조회 정상 |

★**차단은 전부 "거부 + DB 무기록"** 으로 확인한다(거부했는데 기록되면 FAIL).

---

## 5. 생산실적 게이트 — 예외 제거 완료 (2026-08-28)

대표 지시: **"생산실적도 막아라. 우리 시스템은 예외가 없다. 대신 왜 안 되는지 알려줘라."**
정본 규칙 = `STOCK_GATING_CLOSE_LOCK_RULES.md` **§0-★**.

| 경로 | 전 | 후 |
|---|---|---|
| `_backflush_core` | `_tracked()` 예외로 **소비대상 자식의 70.2%(6,988/9,955) 무판정 통과** | 예외 제거·전수 판정 |
| `/api/procreg/save` 공정별생산실적 | 게이트 **없음**(INSERT 만) | 마감잠금 + 자재가용 + 사유 |
| `_is_inner_prod` 대상 필터 | 라이브 커넥션에서 `nx.item` 조회 실패 → **예외를 삼키고 False** → 게이트 스킵 | 필터 제거. BOM 전개 결과로만 판정 |

판정축 = **실제 소비축**(RDY 준비재고 → MAT 자재재고). 생산창고는 소비축이 아니므로
가용에 넣지 않되 **사유에 실어** 어디 있는지 알린다.
성능: 자식마다 3쿼리 → `_avail_bulk` 로 **축당 1쿼리**(500건 청크).

★설계상 "해당 없음"(예외 아님): 키팅은 flag-only 로 **차감이 없다** → 게이트 대상이 아니다.
  "차감이 없어서 해당 없음"과 "차감이 있는데 안 막음"은 다르다. **후자는 금지.**

---

## 6. 하네스를 만들며 실측한 함정 (재발 방지 — 그대로 밟지 말 것)

1. **`app.mount("/", StaticFiles)` 뒤에 등록한 라우트는 404** — `/openapi.json` 에는 보여도 호출이 안 된다. 제어 라우트를 `app.router.routes` **맨 앞으로** 옮겨야 한다(CLAUDE.md §2 와 같은 함정).
2. **쓰기 시작 뒤 별도 커넥션으로 `nx.stock_ledger` 를 읽으면 무한 대기** — 자기 자신의 미커밋 잠금에 걸린다. 하네스가 2분·10분 타임아웃 나던 진짜 원인. → **픽스처는 전부 쓰기 이전에 읽고 커서를 닫는다.** 자기가 쓴 행을 봐야 하면 `/api/_flow/sql`(공유 커넥션)로 읽는다.
3. **롤백 검증도 같은 함정** — 롤백 *전에* 별도 커넥션으로 COUNT 하면 멈춘다. → 기준 행수는 **서버 기동 시점**에 잡는다.
4. **`io.TextIOWrapper(sys.stdout.buffer,…)` 재래핑은 `python -u` 를 무력화**한다(블록 버퍼 → 진행상황이 안 보임). `line_buffering=True` 필수.
5. **프로브를 전 테이블 집계로 두면 못 돈다** — 스텝마다 170만 행 COUNT. 테스트 스코프(일자·품번)로 좁히면 빠르고 오히려 정확하다.
6. **`INSERT_USER_ID` 는 `'web'` 하드코딩** — payload 의 `user` 는 원장에 안 남는다. 테스트 행을 사용자로 못 거른다(수량으로 특정할 것).
7. **`_migration/*` 는 gitignore** 였다 → `!_migration/flow_*.py` 예외 등록(과거 scratchpad 하네스 소실 사고와 같은 조건이었다).
8. **화면을 playwright 로 열려면 로그인 오버레이를 통과해야 한다** — `#lg-id`/`#lg-pw`/`#lg-login` (admin/1234).

---

## 7. 다음 확장

- 영업(SAL) 수불장 — 040 recipe 기반이라 별도(`close/ledger` 는 현재 MAT·PRD 만)
- 수불장 ↔ 자재입출고현황 **차이 75건(2.04%)** 규명 — `CLOSE_MGMT_CANON.md` §24. 컷오버 전 필수
- 화면(JS) 레벨 구동을 케이스로 편입(현재는 API 레벨 + 별도 playwright 확인)

---

## 8. [S] 인증 · 소속 강제 케이스 신설 (2026-08-29) — **PASS 83 · FAIL 0 · 오염 0**

### 왜 새 종류가 필요했나
기존 `[R]` 은 **"막혔는가"** 만 본다. 소속 강제는 그것으로 판정할 수 없다 —
협력사가 남의 코드를 넣으면 **200 이 나오되 자기 데이터만** 나와야 하기 때문이다.
`[R]` 로 짜면 "차단되지 않음 = 미구현" 으로 잘못 보고된다.

⟹ **`[S]` 종류**를 만들었다. 기대 상태코드 + **실제 응답값 검사**를 함께 본다.
```python
dict(kind="S", name="...", method="GET", path="...", as_="miraero",
     expect=200,                                   # 기대 HTTP 상태
     check=lambda res, ctx: (True, "실제 관측값"))  # 화면에 그대로 찍힌다
```
`as_` = 로그인할 계정(None 이면 무토큰). 하네스가 `/api/auth/login` 으로 실제 로그인해
토큰을 붙인다 — **인증을 붙인 뒤로는 하네스도 로그인해야 검증이 된다.**

### 실행 결과 — 사용자가 실제로 겪는 그대로

**① 로그인 전에는 아무것도 안 보인다**
```
무토큰 협력사 계획조회 / 송장목록 / 입고실적 / 입고스캔  →  401 로그인이 필요합니다
★무토큰 계정목록  →  401        (예전에는 평문 비밀번호 12개가 그대로 나왔다)
```

**② 로그인**
```
옳은 비밀번호        200  슈퍼관리자 · 유효 12시간
틀린 비밀번호        401  아이디 또는 비밀번호가 올바르지 않습니다. (남은 시도 4회)
★없는 계정          401  아이디 또는 비밀번호가 올바르지 않습니다.   ← 같은 문구(존재 여부를 흘리지 않는다)
협력사 로그인        200  미래정밀 · 유형 협력사 · 거래처코드 2096
```

**③ ★협력사가 남의 코드(2148=대원산업)를 넣으면**
```
setin/list?cust=2148        200   130행 · 거래처 ['2096']
setstock/list?cust=2148     200   600행 · 거래처 ['2096']
planstatus?wc=2148          200   199행 · 거래처 ['2096']
deliv420?cust=2148          200   167행 · 자기코드(2096) 결과와 **동일** = 요청값이 무시됐다
   같은 요청을 내부가 하면   200    96행                        = 남의 데이터가 아니었다
```
> 마지막 두 줄이 핵심이다. **"자기 것과 같고, 남의 것과 다르다"** 를 둘 다 확인해야
> 소속 강제가 걸린 것이다. 빈 결과가 나와도 통과해버리는 약한 검사는 증명이 아니다.

**④ 내부 사용자는 종전대로 (회귀 없음)**
```
내부가 cust=2148  →  200  147행 · 거래처 ['2148']
내부가 전체       →  200  800행 · 거래처 8곳
```

**⑤ 바코드·송장번호만 알면 되는 API — 주인을 확인한다**
```
협력사가 남의 송장 명세      403  다른 협력사의 문서입니다.
협력사가 남의 송장 발행      403  다른 협력사의 송장입니다(900003).
협력사가 남의 거래명세표     403  다른 협력사의 문서입니다.
협력사가 남의 발행 취소      403  다른 협력사의 문서입니다.
자기 거래명세표             200  품목 6종                    ← 자기 것은 열린다
내부는 그 송장이 열린다      200  자도번 1종                  ← 회귀 없음
```

**⑥ 입고 처리 = 우리가 받는 행위 (협력사 거부)**
```
협력사 입고스캔/입고/취소/미리보기  →  전부 403  "…은(는) 담당자만 할 수 있습니다."
```

**⑦ 계정 API**
```
계정목록(관리자)   200  12명 · **pw 필드 0개** · 비번설정 12명
협력사가 계정목록  200  1명 = ['miraero']        ← 본인 것만
협력사가 계정저장  403  계정 관리는 시스템관리자만 할 수 있습니다.
조회전용이 계정저장 403  (내부 사용자여도 관리자가 아니면 거부)
```

**⑧ 세트입고 왕복 — 발행 → 입고 → 중복차단 → 취소 → 재입고**
```
① 송장 발행          200  SET바코드 700006 채번 · 1건
② 스캔(입고 전)      200  협력사 명진산업 · 도번 1종 · 이미입고 0건 · 경고 없음
③ 입고               200  입고 1건 · 자도번 재고파생 14행
④ ★다시 스캔        200  경고 = ★이미 입고된 SET바코드입니다 — 260829 · web · 10개 (1건).
                          중복 입고하면 재고가 실제보다 늘어납니다.
⑤ ★중복 입고        409  중복 입고 차단 — SET바코드 700006 는 이미 입고되었습니다…
⑥ 취소 미리보기      200  입고 1건 · 재고파생 14행 / 140개
⑦ ★입고취소         200  입고 1건 · 재고파생 14행 되돌림
                     **DB 확인 → 입고거래 0건 · 재고파생 0행 · 송장상태 [('10',1)]**
⑧ 취소 후 스캔       200  이미입고 0건 · 경고 없음
⑨ 재입고             200  재입고 1건 · 재고파생 14행
⑩ 없는 바코드 취소   404  SET바코드 999999999 의 입고 내역이 없습니다.
```
> ⑦ 의 마지막 줄이 중요하다 — **응답만 믿지 않고 DB 를 다시 조회**해 3곳이
> 실제로 되돌아갔는지 확인한다. `ctx["_sql"]` 로 **미커밋 상태**를 그대로 본다.

### 하네스 자체도 손봤다
| 무엇 | 왜 |
|---|---|
| `ROLLBACK_TABS` 에 `app_user`·`app_session` 추가 | 하네스가 로그인하면 세션이 생기고 실패하면 잠금이 걸린다. 없으면 "오염 0" 이 계정 테이블을 **안 본 채로** 통과한다 |
| `delta()` 방어 신설 | `flow_server._probe` 는 쿼리가 실패하면 `None` 을 넣는다. 러너가 `None - None` 으로 죽어 **멀쩡한 케이스를 거짓 FAIL** 로 만들었다 |
| `call(..., token=)` · `token_for()` | 인증을 붙인 뒤로는 하네스도 로그인해야 한다 |

### ★실행 중 겪은 함정 (§6 에 추가)
**서버 하나에 실행은 한 번.** `flow_server` 는 `0.0.0.0` 에 바인딩한다.
`netstat | grep 127.0.0.1:8098` 로 죽이면 **안 죽는다.** 그래서 앞 서버가 살아남고,
롤백이 끝나지 않은 **더러운 트랜잭션 위에서 두 번째 실행**이 돌아
`close/anomaly`·`close/ledger` 가 500, 프로브가 `None` 을 뱉었다.
**코드 문제가 아니었다** — 깨끗한 서버로 다시 돌리니 전부 PASS.
⟹ 죽일 때는 `grep ":8098 "`(인터페이스 무관)로 잡고, **바인드 오류가 0인지 로그로 확인**한 뒤 돌린다.

### 하네스 계정 (★운영 전 반드시 바꿀 것)
`flow_cases.ACCOUNTS` 에 개발 시드 계정이 있다(환경변수 `FLOW_PW_*` 로 덮어쓸 수 있다).
화면(브라우저)에는 비밀번호가 더 이상 실려 나가지 않지만, **하네스는 로그인해야 하므로**
자격증명이 필요하다. **컷오버 전에 시드 비밀번호를 바꾸고 환경변수로 넘긴다.**

### 총계
```
[F] 흐름 11 · [R] 규칙 30 · [S] 인증·소속 42 = PASS 83 · FAIL 0 · SKIP 1(픽스처 부족)
오염 0 PASS — 기동시점 행수와 불변(app_user·app_session 포함, diff {})
```

---

## 9. 협력사 포털 전 구간 상설 등록 (2026-08-29) — **PASS 98 · FAIL 0 · 오염 0**

지금까지 포털은 ad-hoc 스크립트로 한 번 돌려보고 끝냈다. 그러면 **다음에 누가 고쳤을 때 안 잡힌다.**
15케이스를 상설로 넣었다 — "화면이 부르는 그 API 를, 실제 계정으로, 순서대로".

### ★응답만 믿지 않는다
200 이 와도 행이 안 생겼을 수 있다. 단계마다 `ctx["_sql"]` 로 **미커밋 DB 를 직접 조회**한다.
```
② 발행   → set_input_req  채번 + 00→10          (요청 잔여 0건 확인)
⑤ 출발   → set_input_req  10→20
⑨ 입고   → set_stock_maint 2건 · stock_ledger(S) 7행 · status 90
⑫ 취소   → 위 3곳이 **전부** 0 / status 10 복귀
```

### 잡아낸 결함 5건
| # | 무엇 | 성격 |
|---|---|---|
| 1 | **출발(20)을 `receive` 가 몰랐다** — `IN ('10','30')` | 기능 결함(출발 처리하면 입고 불가) |
| 2 | **마이크로 QR** — 폰·리더기 상당수가 못 읽는다 | 현장 치명 |
| 3 | `partner_my` 커서 중첩 → `HY000` | 잠재 결함 |
| 4 | **`procreg_save` 커서 중첩** — 커서를 쥔 채 `_prod_shortages(nx,…)` 호출 | **운영 간헐 오류**(데이터에 따라 남) |
| 5 | 협력사 홈 과부하 | 0.59s → 0.24s (숫자는 정본과 동일) |
1·2 는 배포됐다면 **현장에서 터졌을** 것들이다.

### ★★케이스 순서 규칙 — 읽기 먼저, 쓰기 나중
하네스는 트랜잭션 하나를 **끝까지 열어 둔다**(no-commit). 쓰기가 쌓일수록
`plan_part_mat`·`deliv_issue` 같은 큰 테이블 스캔이 급격히 느려진다.
```
운영(깨끗한 커넥션)   0.24s
쓰기 누적 후          45s+ → 타임아웃
```
**제품 결함이 아니라 하네스 특성이다.** 순서를 이렇게 고정한다:
```
AUTH(조회 위주) → PORTAL(홈 조회 + 왕복) → SETIN(왕복) → [F]/[R](쓰기)
```
> 처음에 포털을 **맨 앞**으로 옮겼더니 이번엔 뒤의 `deliv420` 조회가 타임아웃했다.
> **문제를 옮긴 것이지 푼 게 아니었다.** 무거운 조회는 전부 앞에 모아야 한다.

### 하네스 보강
| 무엇 | 왜 |
|---|---|
| `call()` 이 **비-JSON 응답**(QR=SVG)을 그대로 반환 | 파싱 실패를 오류로 세면 멀쩡한 케이스가 거짓 FAIL |
| `status 0` 이면 **예외 사유를 먼저** 출력 | check 의 note 로 덮이면 `HTTP 0 · 값 None` 만 남아 원인을 못 찾는다 |
| QR 검사가 **마이크로 QR 을 크기로 판별** | 실측: 표준 v1 = 21모듈 → **150px** / 마이크로 M3 = 15모듈 → **114px** (임계 140px) |

### ★실행 규칙 (밟은 함정)
**실행마다 `flow_server` 를 새로 띄운다.** 앞 실행을 중간에 죽이면 **미커밋 잠금**이 남아
다음 실행의 픽스처가 무한 대기한다(§6 에 이미 적힌 함정인데 다시 밟았다).

---

## 10. 마감 멱등성 상설 케이스 + **워밍 스레드 HY000 근본수정** (2026-08-30)

### 10-1. 왜 케이스를 넣었나
같은 기간을 두 번 마감했더니 값이 달랐다 — SAL D 260828 이 **677,272,841 → 703,546,042**.
원인은 캐시 키가 **연월**인데 담긴 값은 **as-of 일자** 단가였다(`CLOSE_MGMT_CANON` §30).
**마감은 그 시점을 확정하는 것**이므로, 돌릴 때마다 달라지면 어느 쪽이 맞는지 알 수 없다.

정작 문제는 **이걸 잡는 검증이 하나도 없었다**는 것이다. 그래서 상설로 남긴다.
`flow_cases.py` → `IDEM_CASES` 8건 (`[마감] ①~⑧`).

| # | 무엇을 보나 |
|---|---|
| ①②③ | 생산 — 기준값 확보 → **짧은 기간을 먼저 조회(오염 유발)** → 전체 재조회가 **같은가** |
| ④⑤⑥ | 영업 — 같은 3단 |
| ⑦ | **생산재고조회 = 생산 수불장** (대표 확정 '가': 값이 같아야 비교가 된다) |
| ⑧ | **영업재고조회 = 영업 수불장** |

⑦⑧ 은 **수불장 단가를 받은 행만**(`cost_src='수불장(이동평균)'`) 센다.
화면에는 수불장에 없는 품목(단가0·음수라 `_snap_bulk` 제외규칙에 걸린 것)이 남아 있어,
전체 합계로 비교하면 **축이 달라 항상 어긋난다**. 없는 값을 만들지 않는다는 설계 그대로다.

### 10-2. ★근본원인 — 워밍 스레드가 공유커넥션을 물었다
케이스는 `--only "[마감]"` 단독으로는 **8/8 통과**인데 전체 스위트에서는
`하네스 로그인 실패(super) — 500` 으로 40여 케이스가 통째로 SKIP 됐다.
서버 로그는 `auth.py auth_login` 에서 `pyodbc HY000 다른 hstmt의 결과에 연결이 사용 중`.

`flow_server.py` 에 `_raw_ok(tag)` 진단을 넣어 결정적 증거를 잡았다.

```
[raw] app 임포트 전: OK
[raw] app 임포트 후: ★Error ('HY000', '...다른 hstmt의 결과에 연결이 사용 중입니다...')
```

⟹ **연결이 첫 요청 전에 이미 오염돼 있었다.** 범인은 커서 누수가 아니라 **동시 사용**이다.

| 어디 | 무엇 |
|---|---|
| `routers/rawmatledger.py` | **모듈 임포트 중** `_warm_lme` 스레드 기동 |
| `routers/sagubledger.py` | **모듈 임포트 중** `_warm_bg` 스레드 기동 |
| `app.py` startup | `_run` 스레드 — 원가엔진 예열(`_get_cost_engine`) |

하네스는 오염 0 을 보장하려고 **커넥션 하나**(`NoCommitConn(RAW)`)를 전 라우터에 물린다.
이 스레드들이 `_nx()` 를 부르면 곧 RAW 다. 워밍이 결과셋을 물고 있는 동안 본 스레드가
쿼리하면 **MARS 가 꺼진 ODBC 드라이버(`SQL Server`)에서 정확히 HY000** 이 난다.
단독 실행이 통과한 건 **워밍이 먼저 끝났을 뿐**인 타이밍 문제였다.

### 10-3. 고침 — 끄는 게 아니라 **동기로**
처음엔 `FLOW_TESTBED=1` 이면 워밍을 **건너뛰게** 했는데, 그러자 원가엔진이 차가운 채로
생산재고조회에 들어가 **600초를 넘겨 타임아웃**했다(⑦). 게다가 클라이언트가 타임아웃해도
서버 요청은 공유커넥션에서 계속 돌아 **뒤 케이스까지 물린다**.

문제는 워밍 자체가 아니라 **스레드**였다. 그래서 `FLOW_TESTBED=1` 이면 세 곳 모두
**요청을 받기 전에 같은 워밍을 동기로 끝낸다**(`_warm_lme()` / `_warm_bg()` / `_run()` 직접 호출).
동시 사용이 없어 HY000 이 안 나고, 운영과 같은 따뜻한 상태로 시험한다.
운영에는 이 환경변수가 없으므로 기존 스레드 워밍 그대로다.
`flow_server.py` 는 `import app` **직전**에 `os.environ["FLOW_TESTBED"]="1"` 을 세운다(임포트 시점 스레드라 그 전이어야 한다).

검증
| | 가드 전 | 끄기 | **동기(채택)** |
|---|---|---|---|
| `[raw] app 임포트 후` | ★HY000 | OK | **OK** |
| 생산재고조회 | — | **600s 초과 FAIL** | **29.4s / 3,546행** |
| 영업재고조회 | — | — | **4.7s / 2,192행** |
| 기동시간 | — | — | 26초(동기워밍 포함) |

### 10-4. 함정 추가 (§6 연장)
| 함정 | 실체 |
|---|---|
| **공유커넥션 + 백그라운드 스레드 = HY000** | 커서 누수처럼 보이지만 **동시 사용**이다. 드라이버가 `SQL Server`(MARS 없음)라 결과셋 하나만 열린다. |
| 임포트 시점에 뜨는 스레드가 있다 | `@app.on_event("startup")` 만 볼 게 아니라 **모듈 최상단 `Thread(...).start()`** 를 함께 봐야 한다. |

---

## 11. 품목별계획(STEP5) 레거시 100% 일치 — 모델BOM 함정 2종 (2026-09-01)

파트별계획이 레거시와 215건 어긋난 것을 파고들어 **원인 3개**를 분리했다.
최종 = 품목별계획 **공통 2,802 제번 도번집합 불일치 0**.

### 11-1. 원인 ① 미러 낡음 → `_schema/sync_model_bom.py` 신설
편성 STEP5 는 `nx.PR_M_MODEL_BOM`(미러)으로 **모델 → ASSY 도번**을 정한다.
미러가 낡으면 같은 제번에 **웹만 옛 도번**이 붙는다.
실측 09:19 라이브 100건 신규 + 38건 적용일자 수정을 미러가 못 따라가 **215건**이 어긋났다.
`--apply` 로 96 갱신 + 100 신규 → 63,011 = 63,011 (백업 `nx.bk_model_bom_260901_1057`).
⟹ **215 → 52건**. 이때도 **수량차는 0**이었다(도번만 다름).

★INSERT 만 하는 동기화는 틀린다 — **적용일자 UPDATE 를 못 따라간다**(이번 사고의 핵심).

### 11-2. 원인 ② 유령 ASSY — 모델BOM 에 있으나 품목마스터에 없는 도번
`AJR30133610` 은 라이브 모델BOM 에 21개 모델로 등록됐지만
**품목마스터 미등록 · 주문 0건 · BOM 0건** = 실체가 없는 행이었다.

| | 레거시 | 웹(수정 전) |
|---|---|---|
| 품목별계획 등장 | **0행** | 6행 |
| 전개 방식 | 품목마스터 **조인** → 자동 탈락 | 모델BOM **단독 읽기** → 그대로 유입 |

모델BOM 전체에 이런 유령이 **179종** 있다(대부분 2023-10 등록분).
BOM 이 없어 파트별·자재소요로는 못 내려가 **수량 영향은 0**이지만, 도번 집합이 어긋난다.

**고침** `planrev.py _step5_item` — `prate`(이미 `PR_M_ITEM` 전건을 담고 있다)의 키를
`_known` 으로 삼아 후보에서 제외. ⟹ 8 → 2건.

★필터는 **모델BOM 분기 안에서만** 건다. 밖에서 걸러 `assys` 가 비면
주문(`recvmap`) 폴백으로 새어 나가 **엉뚱한 답을 만들어낸다**.

### 11-3. 원인 ③ ★모델BOM 유효기간은 **당김 전 원본일자**로 판정한다
계획일자는 라인당김이 적용된 값이지만, **어느 ASSY 도번을 쓸지는 원본일자가 정한다.**
당김이 유효기간 경계를 넘으면 둘이 갈린다.

```
6JMGM03B  원본 260918 → 당김후 260917   (레거시 ORG/PLAN 과 동일 = 당김 자체는 일치)
모델 ZRUM140LTE6.EWGBLEU 경계 …260917 | 260918…
  웹  260917 로 판정 → 구도번 AJR30027702·AJR77224517·AJR77263007
  레거 260918 로 판정 → 신도번 AJR30125601·AJR30133302·AJR30133701
```

전수검증 (2,802 제번)

| 판정 축 | 레거시와 불일치 |
|---|---|
| 당김 후(`pulled`) | 2 |
| **원본(`plan_dtl.PLAN_YMD`)** | **0** ← 채택 (깨짐 0) |

**고침** 소스쿼리에 `MIN(d.PLAN_YMD)` 를 한 컬럼 더해 `mymd` 로 받고,
**유효기간 비교에만** 쓴다. 계획일자·시각(`ymd`·`ohm`)은 당김 값 그대로 — 당김을 되돌린 게 아니다.

### 11-4. 검증 방법 — 재현 말고 **실제 함수를 부른다**
초반에 선택로직을 파이썬으로 베껴 검증하다 `recvmap` 폴백을 빠뜨려 한 번 헛짚었다.
최종 검증은 `from routers import planrev; planrev._step5_item(cur)` 로 **실코드를 실행**하고
레거시 `PR_T_PLAN_ITEM_DTL` 과 전수 대사했다 → **불일치 0 · 유령 0**.
(⑤ 이후 단계는 전역 인증게이트(app.py:202) 때문에 API 로는 못 돌린다 — 화면에서 ④ 재편성.)

### 11-5. ★내가 밟은 오진 (재발 방지)
| 오진 | 실체 |
|---|---|
| "레거시가 `ORG_PLAN_YMD` 축으로 판정한다"를 레거시 ORG/PLAN 만으로 검증 | 그 제번들은 **둘 다 260918** 이라 축을 구분 못 한다. 판별되는 건 **웹 당김이 경계를 넘은** 케이스뿐 — 대조군을 잘못 골랐다. |
| `6JMGM03B` 가 구도번인 걸 보고 "축 문제"라 단정 | 그 행은 **모델BOM 동기화 이전 편성분**이었다. 10:59 재편성분으로 다시 보니 8건만 남았다. **편성 시각과 마스터 변경 시각을 먼저 대조할 것.** |

### 11-6. 최종 대사 결과 (2026-09-01 11:55 재편성)

```
③ 품목별 plan_item_dtl    공통 2,802 제번 · 도번집합 불일치 0        ← 100%
④ 파트별 plan_part_dtl    총량 288,257 = 288,257  차 0              ← 100%
                          남은 4건 = 레벨차만(수량 동일)
⑤ 자재소요 plan_part_mat  불일치 11쌍(직전 111) · 수량차 2(직전 100)
```

경과 — ④ (제번,도번,lv) 불일치: **215 → 52 → 5 → 4건**, 총량차 **0**.

### 11-7. ⑤ 잔여 — ★`-19-1` 은 유령이 아니라 **명진 사급 SUB 발주단위**다 (판정 정정)

**★★집계 그레인에 속지 말 것.** 처음엔 (작업처,자재) 로 **합산해서** 11쌍만 보고
"190+3=193 수량 일치 → 웹이 더 정확" 이라고 판정했다. **틀렸다.**
(제번,자재) 그레인으로 내리면 상쇄가 풀리고 진짜 규모가 나온다.

```
(작업처,자재) 합산 :  11쌍          ← 상쇄돼 작아 보인다
(제번,자재)  그레인 :  웹에만 72 · 레거시에만 11 · 수량차 2   (용접봉 제외)
제번별 총량        :  6I2M03K2 +240 · 6I2M03VG +300 · 6J2M01UA +168 …  13제번 전부 웹 과다
```

**실체 — `-19-1` 접미사 = 명진(2306) 사급 SUB 발주단위**

| 근거 | 실측 |
|---|---|
| 품목마스터 등재 | `AJR33796526-19-1` = `명진 SUB`(in_cust 2306) · `AJR77142503-19-1` = `[-19-1] 명진 SUB`(2306) |
| 거래 실적 | `PU_T_STOCK_MAINT` 4,365건 · `PU_T_SET_INPUT_REQ_DTL` 1,113건 · 사급/재고 테이블 다수 |
| 종수 | 자재소요에 14종 — **패턴이지 예외가 아니다** |
| **다른 `-19-1` 은 웹도 레거시와 동일** | `AJR77142503-19-1` 390=390 · `AJR77202902-19-1` 230=230 · `AJR75645201-19-1` 234=234 · `AJR33796512-19-1` 209=209 |

⟹ **`AJR30100102-19-1` 한 종만 품목마스터에 등록이 빠져 있다.**
웹은 그 코드를 못 찾아 **건너뛰고 하위 `MJU3907432x` 7종으로 전개**한다 → 자재 7배.
레거시는 마스터를 안 보고 그대로 쓴다. **이 건은 레거시가 맞고 웹이 틀렸다.**
`3H01582A` +3 도 같은 건(제번 `6J1M04UJ`, 계획원본은 양쪽 동일).

**조치 = 라이브 품목마스터에 `AJR30100102-19-1` 등록**(형제 코드처럼 `명진 SUB`·in_cust 2306).
등록되면 다른 `-19-1` 과 같이 자동 일치한다. 코드 수정 불요.

**해소 (2026-09-01 12:17~12:20)** — 라이브에 이미 있는 것을 미러·클린에 반영했을 뿐이다.

| 단계 | 스크립트 | 내용 |
|---|---|---|
| 미러 | `_schema/sync_item_bom_delta.py --apply` | 품목 +1 · BOM +12 |
| **클린**(편성이 읽는 곳) | `_schema/sync_clean_item_bom_delta.py --item AJR30100102 --apply` | 품목 +1 · BOM +11/−9 |
| 클린 | `… --item AJR33796526 --apply` | BOM +1/−1 (`AJR33796526-19-1`→`AJR30100102-19-1` 교체) |

```
전: AJR30100102 → 부품 9개 직접                       (옛 구조)
후: AJR30100102 → AJR30100102-19-1 → 부품 10개        (라이브와 동일)
```
검증 = `nx.v_pr_bom` 뷰 자식수가 라이브와 일치(8/8 · 10/10 · 9/9).
클린 전용 컬럼(`item_type`·`unit`·`status`·`apply_from`)만 형제 `-19-1` 값을 본떴다 — 지어낸 값 없음.

★**미러(`nx.PR_M_ITEM_BOM`)를 채워도 편성은 안 바뀐다.** 편성 STEP6/7 이 읽는 것은
`nx.v_pr_bom` = **`nx.bom_header`+`nx.bom_line`(클린)** 이다(`planrev._ensure_bom_snap`).
두 스크립트가 짝이며, **클린 쪽이 실제로 계획을 바꾸는 쪽**이다.

| 갈래 | 쌍 | 판정 |
|---|:--:|---|
| **`-19-1` 클린 BOM 미반영** | 9 | ★**웹이 틀림** — 클린 동기화로 해소(데이터) |
| **가상품목 평탄화** | 1<br>(④ 4건과 동일) | `MHT54371603` −18 = `WO1064179SVC` 13 + `WO1082876SVC` 5.<br>웹 `ADM74930507`→lv1 / 레거 `AJR76462726`→lv2, **수량 동일**. **웹 설계**([[plan-virtual-item-flatten]]).<br>★④의 4건과 **별개가 아니라 같은 건** — 대사 키에 `upper_item_code` 가 들어가 ⑤ 에서 한 번 더 잡혔다. |

### 11-7-1. ★내가 밟은 오진 (세 번째)
| 오진 | 실체 |
|---|---|
| **합산 그레인으로 판정** | (작업처,자재) 합산은 서로 다른 제번의 +/− 를 **상쇄**한다. 11쌍으로 보였지만 제번 단위로는 83건·최대 +300. **차이를 판정할 땐 반드시 최소 그레인(제번)까지 내려갈 것.** |
| **"품목마스터에 없다 = 유령 = 레거시가 틀렸다"** | 마스터 미등록은 **레거시가 틀렸다는 증거가 아니라 마스터가 빠졌다는 증거**일 수 있다. 판정 전에 **형제 코드**(같은 접미사 패턴)와 **거래 실적**을 볼 것 — `-19-1` 13/14종은 멀쩡히 등재돼 있었다. |
| §1-7 을 근거로 "웹이 더 정확" | "레거시는 정답이 아님"은 **검증한 뒤에** 쓰는 말이다. 검증 없이 갖다 붙이면 반대로 웹 버그를 정당화하게 된다. |
| **"유일경로 = 단절위험" 으로 보류** | `AJR33796526-19-1`(상위 1·하위 11)을 지우면 끊긴다고 판단해 보류했다. 실은 **교체**였다 — 같은 부모에 새로 붙는 `AJR30100102-19-1` 이 하위 10종을 그대로 커버하고, 실제로 잃는 건 `3H01582A` 1종뿐(그게 바로 `3H01582A` 웹+3 의 정체였다). 옛 코드는 **라이브 상위연결 0 · 레거시 자재소요 0 = 이미 은퇴**. ⟹ 단절 판정은 상위/하위 **개수**가 아니라 **새 노드가 하위를 커버하는지(차집합)** 로 해야 한다. 스크립트에 반영함. |
| 미러만 채우고 끝낼 뻔함 | 편성이 읽는 건 **클린**(`v_pr_bom`)이다. 미러 동기화 후 "됐다" 고 했으면 계획은 그대로였다. **고치기 전에 그 코드가 어느 테이블을 읽는지 먼저 확인할 것.** |

**총량 −590 의 정체 = 용접봉**
```
전체 차       −590
RAC*(용접봉)  웹 0 / 레거 1,715      ← 공정 nx.proc_weld 로 분리, BOM 전개에서 제외(§1-10)
```
용접봉을 빼면 실질 차이는 **−15**(위 소액 2건). 설계대로다.

★교훈 — **"레거시와 차이 = 웹 버그" 가 아니다.** 이번 11쌍 중 8쌍은 레거시가 틀렸다.
차이를 볼 때 **양쪽 도번이 품목마스터·BOM 에 실재하는지**를 먼저 본다(유령이면 그쪽이 범인).

### 11-7-2. ★클린 BOM 드리프트 5,154 는 **거의 전부 무해**하다 (겁먹지 말 것)

```
클린 nx.bom_line 37,638 / 라이브 PR_M_ITEM_BOM 42,461   ← 4,823 차이, 커 보인다
```
플래그로 분해하면 실질이 없다.

| 라이브에만 있는 5,154 | 건수 | 판정 |
|---|---:|---|
| `EXCEPT_FLAG=1` (전개제외) | **5,148** | 레거시도 안 쓴다. **클린이 안 담는 게 정상** |
| except=0·유효 | 6 | 그중 계획 사용 품목 **3** |
| └ `AJR30167201-A-S-1` | 1 | 레거 1 / 웹 1 — **이미 일치** |
| └ `RAC30599301-1` ×2 | 2 | 용접봉 — `nx.proc_weld` 분리, **설계대로** |

`클린에만 318` 도 양쪽 소요가 같다(273/273 · 103/103 · 19/19).
⟹ **실질 영향 0건. 일괄 동기화는 불필요하고 위험하다**(5,154건을 한 번에 건드릴 이유가 없다).

★그러면 오늘 3건은 왜 틀렸나 = **낡아서가 아니라 "당일 변경분" 이라서**다.
`AJR30100102-19-1`(09-01 08:40) · `AJR33796526`(08-31) · `ADM74930507-STS` 전부 최근 변경분이다.
⟹ 필요한 것은 전체 동기화가 아니라 **당일 변경분 따라잡기**(품목 단위 델타).

### 11-8. 남은 것
- **`soyo.py`(현행)에도 같은 구멍 2개가 있다**(`soyo.py:95-101`). 검토본 무변경 원칙(§계획)에 따라
  이번엔 손대지 않았다 — 편성은 검토본으로 돌고 있다. 현행 승격 시 함께 이식할 것.
- 유령 179종은 **모델BOM 쪽 데이터 문제**다. 편성에서 막았을 뿐 원천은 남아 있다(라이브 정리 대상).
  자재소요 쪽 유령(`AJR30100102-19-1`·`AJR33796526-19-1`)도 같은 성격 — **레거시가 남긴 것**이라
  웹은 이미 올바르게 전개한다. 정리는 라이브 마스터 쪽 일이다.
- ★**추가계획(`nx.prod_plan_input`)은 편성 전에 동기화해야 한다** — 라이브에 새 A/S 가 들어오면
  웹만 빠진다. 실측 `WO1094482AR`(260901·AJR30117902·92) 1건이 빠져 ④ 총량 −92,
  ⑤ 7쌍이 각 −92 로 어긋났다. `_schema/sync_prod_plan_input_refresh.py` 로 동기화 후 재편성하니 0.
| 단독 통과 ≠ 정상 | 타이밍으로 가려진다. **전체 스위트로 확인**해야 한다. |
| 케이스 순서를 바꿔 넘기는 건 해결이 아니다 | 앞서 포털을 앞으로 옮겼더니 `deliv420` 이 타임아웃 — **문제를 옮긴 것**이었다. |
| **워밍을 끄면 다른 데가 터진다** | 엔진이 차가워져 무거운 조회가 타임아웃한다. 끄지 말고 **동기로** 돌린다. |
| 한 요청이 타임아웃하면 뒤가 다 물린다 | 클라이언트만 끊길 뿐 서버는 **공유커넥션에서 계속 돈다**. 느린 케이스 하나가 스위트를 무너뜨린다. |

---

## 11. [E] 전 구간 프로세스 — 계획업로드부터 마감까지 (2026-09-01 신설)

### 11-1. 왜 만들었나 — 조각은 다 통과하는데 줄이 끊긴다

기존 케이스(§4~§10)는 **도메인별 조각**이다 — 재고·마감·포털·인증. 전부 PASS 여도

> **"계획을 올려 → 소요를 뽑아 → 발주하고 → 받아서 → 만들고 → 팔고 → 마감한다"**

는 한 줄이 실제로 이어지는지는 아무도 안 봤다.
2026-08-31 에 실사용 버그 3건(자재입고 500 · 삭제버튼 무반응 · 요청수량 이중차감)이
한꺼번에 터진 곳이 **정확히 이 구간**이다. 조각검증은 통과했는데 줄이 끊겨 있었다.

`flow_cases.E_CASES` 16건. **CASES 앞**에 놓는다(§9 순서 규칙 — 무거운 조회를 먼저).

### 11-2. 판정 원칙 — "다음 단계가 쓸 수 있는 값이 나왔는가"

200 이 왔는지가 아니라 **앞 단계 산출물이 뒷 단계의 입력으로 연결되는지**를 본다.
끊긴 지점이 정확히 드러난다.

| 케이스 | 무엇을 잇는가 |
|---|---|
| E1~E3 | 계획원본 · 편성 단계로그 · ④ 실행이력 |
| **E4** | ④파트별 → ⑤자재소요 제번 연결 |
| E5·E6 | ⑤ → 협력사(업체별로 갈라지는가 · 화면이 읽는가) |
| **E7·E8** | 거래명세서 요청수량 이중차감 · 세트제외 배치 |
| E9 | 자재입고진행현황 |
| **E10·E11** | ★업체별 자재계획 레거시 대사 + 차이 큰 업체 TOP10 |
| **E12** | ★작업처 오버라이드(routing_edge) 신선도 |
| E13 | 협력사 세트입고 → 재고 파생 |
| **E14·E14b** | 재고 3곳 정합 · 버킷 중복 |
| E15 | 마감(수불장)이 값을 내는가 |

### 11-3. ★편성 단계(①④⑤)를 여기서 실행하지 않는 이유

STEP6/STEP7 은 `DROP TABLE` 후 재생성이고 수백초가 걸린다. 공유 트랜잭션 하나에
그걸 태우면 뒤 케이스가 전부 잠금 대기에 걸린다(§6-2 와 같은 함정).
⟹ **직전 편성 결과를 읽어 연결을 검증**한다. 편성 자체의 정합은 별도 대사 몫.

### 11-4. 첫 실행이 잡아낸 것 (2026-09-01)

**[진짜 결함 1건 — 아무도 모르고 있었다]**

`E12` ★**작업처 오버라이드가 낡았다 (39행)**

발견 경로가 이 TestBed 의 설계의도를 그대로 보여준다:
```
E10  업체별 대사 → 미래정밀(2096) −96,768 / 케이비(2266) +96,768
                    ↑ 수량 합계는 정확히 일치, 업체 배정만 뒤바뀜
추적  소요의 작업처는 BOM 이 아니라 nx.routing_edge.wc 에서 온다
        (planrev._step7_sql: ov_wc = ISNULL(routing_edge.wc, 마스터))
        wc = COALESCE(wc_user, wc_live), wc_live 는 nx.item 에서 시드
실측  nx.item 은 라이브와 불일치 0(최신)인데 wc_live 는 39행이 stale
      wc_user(사용자 편집)는 0건 — 즉 아무도 손대지 않았는데 옛 업체가 남아 있다
조치  POST /api/routing/sync 한 번이면 해소
```
> **"계획이 틀렸다"로 보이지만 실제로는 마스터 변경이 반영 안 된 것**이다.
> 업체가 바뀌면 **발주가 엉뚱한 협력사로 나간다.** 조용히 두면 안 된다.

**[진짜 버그 1건 — 코드를 고쳤다]**

`E14 → E14b` ★**자재재고 유령 버킷**

```
E14   음수재고 2행 감지
분류  5210A22409A(−17,279) = 레거시에도 있음(담당자 백플러시 소비, 기존 데이터)
      AJR77144307-STS(−4)  = ★웹에서만 생김
추적  같은 (자재,창고)에 2행이 있다
        [Z99990 · IS0001 =  92]  ← 정상(라이브와 동일)
        [2005   · IS0001 =  −4]  ← 유령행 (stockdelete, 08-31 22:49)
원인  자재재고 버킷키의 CUST_CODE 는 **창고 소유주 'Z99990' 고정**인데
      (라이브 7,762행 전부 Z99990) stock_update/delete 가 원장의 거래처(매입처)를
      버킷키로 써서 UPDATE 가 기존 행을 못 찾고 **새 행을 INSERT** 했다.
      save 경로는 이미 같은 이유로 issue 에서 Z99990 을 고정하고 있었다(stock.py:415)
      — update/delete 에만 그 처리가 빠져 있었다.
수정  stock.py `_mat_mirror_edit` 버킷키를 Z99990 으로 일원화(수불장 전표엔 원 거래처 유지)
정리  `_schema/fix_matstock_ghost_bucket.py` (기본 dry-run, --apply 로 실행)
```
> 재고조회는 창고 **합**을 보므로 값이 조용히 틀어지고, 음수차단 규칙도 이 행은 못 막는다.

**[하네스 자체 오류 3건 — 고쳤다]**

| 무엇 | 실체 |
|---|---|
| E9 파라미터명 | `matinput/list` 는 `base_ymd`+`days` 다(`ymd_from/to` 아님) → 400 |
| E4 판정식 | `plan_part_mat` 에는 파트별을 안 거치는 계열(WO=수주·직납품)이 **설계상 정상**으로 있다. 절대값 0 을 요구해 "고아 4,478행"으로 오판 — 레거시에도 4,484행 있었다. ⟹ **레거시 대비**로 바꿈 |
| **E11 거짓 PASS** | SQL 가드에 막혀 `rows` 가 비었는데 "전 업체 일치"로 PASS 가 났다. **거짓 PASS 는 진짜 실패보다 나쁘다** — `_refused()` 로 거부와 빈 결과를 구분 |

**[서버 가드 1건]**
`/api/_flow/sql` 이 `startswith("select")` 만 봐서 **CTE(`WITH ... SELECT`)를 막았다.**
막아야 할 것은 쓰기이므로 시작토큰(select|with) + DML 키워드로 판정하도록 교정.

### 11-5. ★E10/E11 — 업체별 자재계획 대사 (대표 지시 2026-09-01)

> "자재계획쪽은 업체별로 계획을 분석해보면 될 거 같네"

자재 단위로만 보면 어느 협력사가 틀어졌는지 안 보인다. 업체별로 먼저 갈라
문제 협력사를 특정하고 **그 안에서만** 자재를 판다. 용접봉(`RAC*`)은 공정 분리라
설계상 차이 — 제외하고 센다(CLAUDE.md §1-10).

실측(2026-09-01): 업체 112곳 · 업체×자재 3,169쌍 · **불일치 15쌍(0.47%)**
그중 14쌍이 위 routing_edge 건, 1쌍이 `MHT54371603`(BOM 계층 −18, 기존 미해결).

